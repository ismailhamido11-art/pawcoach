---
phase: 03-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/functions/vaccineReminders.ts
  - pawcoach/functions/medicationReminders.ts
autonomous: false
requirements:
  - NOTIF-03
  - NOTIF-01

must_haves:
  truths:
    - "Un utilisateur free recoit les rappels vaccins (pas de filtre premium)"
    - "Un medicament avec next_date J+3 declenche un email de rappel au proprietaire"
    - "medicationReminders.ts est enregistre comme CRON dans Base44 et se declenche quotidiennement"
  artifacts:
    - path: "pawcoach/functions/vaccineReminders.ts"
      provides: "Rappels vaccins sans filtre premium"
      contains: "isPremium check removed"
    - path: "pawcoach/functions/medicationReminders.ts"
      provides: "Rappels medicaments CRON"
      exports: ["Deno.serve handler"]
  key_links:
    - from: "medicationReminders.ts"
      to: "base44.asServiceRole.entities.HealthRecord"
      via: "filter({ type: 'medication' })"
      pattern: "type.*medication"
    - from: "medicationReminders.ts"
      to: "base44.asServiceRole.integrations.Core.SendEmail"
      via: "email loop"
      pattern: "SendEmail"
---

<objective>
Etendre les rappels email aux medicaments et retirer le filtre premium des rappels vaccins.

Purpose: NOTIF-03 assure que les free users recoivent leurs rappels vaccins. NOTIF-01 cree le meme systeme de rappel pour les medicaments avec next_date.
Output: vaccineReminders.ts modifie (filtre premium retire), medicationReminders.ts cree (nouvelle fonction CRON) — puis CRON registration manuelle dans Base44 UI.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Pattern exact de vaccineReminders.ts a reproduire pour medicationReminders.ts -->

```typescript
// Structure de base commune a toutes les fonctions CRON reminder
import { createClientFromRequest } from 'npm:@base44/sdk@0.8.20';

Deno.serve(async (req) => {
  const base44 = createClientFromRequest(req);
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  // Fetch all data upfront (eviter N+1)
  const records = await base44.asServiceRole.entities.HealthRecord.filter({ type: "TYPE_ICI" });
  const allDogs = await base44.asServiceRole.entities.Dog.list();
  const allUsers = await base44.asServiceRole.entities.User.list();

  const dogMap = new Map((allDogs || []).map(d => [d.id, d]));
  const userMap = new Map((allUsers || []).map(u => [u.email, u]));

  const REMINDER_DAYS = [14, 7, 3, 1, 0];
  const todayStr = today.toISOString().slice(0, 10);

  const upcoming = (records || []).filter(r => {
    if (!r.next_date) return false;
    const due = new Date(r.next_date);
    due.setHours(0, 0, 0, 0);
    const diffDays = Math.round((due.getTime() - today.getTime()) / (1000 * 60 * 60 * 24));
    return diffDays >= 0 && REMINDER_DAYS.includes(diffDays);
  });

  let sent = 0;
  for (const record of upcoming) {
    if (record.reminder_sent_date === todayStr) continue;
    const dog = dogMap.get(record.dog_id);
    if (!dog) continue;
    const user = userMap.get(dog.owner);
    if (!user) continue;

    // ... SendEmail + update reminder_sent_date
    sent++;
  }
  return Response.json({ ok: true, checked: upcoming.length, sent });
});
```

// Champ HealthRecord pertinent pour les medicaments :
// - type: "medication"
// - title: nom du medicament (ex: "Frontline", "Bravecto")
// - next_date: prochaine prise / renouvellement
// - reminder_sent_date: dedup (existe sur l'entite, utilise dans vaccineReminders)
// - dog_id: lien vers le chien

// User fields : is_premium, trial_expires_at, email
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retirer le filtre premium de vaccineReminders.ts (NOTIF-03)</name>
  <files>pawcoach/functions/vaccineReminders.ts</files>
  <action>
Supprimer les lignes 72-73 de vaccineReminders.ts qui filtrent les free users :

```typescript
// SUPPRIMER ces 2 lignes :
const isPremium = user.is_premium || (user.trial_expires_at && new Date(user.trial_expires_at) > new Date());
if (!isPremium) continue;
```

Apres suppression, la boucle passe directement de `if (!user) continue;` a la construction de la date et l'envoi de l'email. Tous les utilisateurs (free, premium, trial) recoivent leurs rappels vaccins.

Ne rien changer d'autre — conserver la logique de dedup (reminder_sent_date), les REMINDER_DAYS, la fonction resolveVaccineName, et le format de l'email.
  </action>
  <verify>
    <automated>grep -n "isPremium" C:/Users/smalt/Desktop/app-chien-ia/pawcoach/functions/vaccineReminders.ts | wc -l</automated>
  </verify>
  <done>Le fichier vaccineReminders.ts ne contient plus aucune reference a isPremium. La boucle continue directement apres `if (!user) continue;`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Creer medicationReminders.ts (NOTIF-01)</name>
  <files>pawcoach/functions/medicationReminders.ts</files>
  <action>
Creer le fichier `pawcoach/functions/medicationReminders.ts` en reproduisant exactement le pattern de vaccineReminders.ts, adapte aux medicaments :

- Fetch `HealthRecord.filter({ type: "medication" })` au lieu de `type: "vaccine"`
- Meme REMINDER_DAYS = [14, 7, 3, 1, 0]
- Meme logique dedup via `reminder_sent_date`
- PAS de filtre premium — tous les utilisateurs recoivent les rappels medicaments
- Subject email : `Rappel médicament pour ${dog.name}`
- Body email :
  ```
  Bonjour !

  Rappel médicament pour ${dog.name} : "${record.title}" est prévu le ${formattedDate} (dans ${diffDays} jour${diffDays > 1 ? "s" : ""}).

  Pense à renouveler ou administrer ce médicament. 💊

  — PawCoach
  ```
- Apres envoi, update `reminder_sent_date` sur le HealthRecord (meme try/catch que vaccineReminders)
- Retourner `Response.json({ ok: true, checked: upcoming.length, sent })`

Pas de resolveVaccineName ici — les titres de medicaments sont utilises tels quels (Frontline, Bravecto, etc.).

Utiliser `.getTime()` dans le calcul de diffDays pour eviter l'erreur TypeScript de soustraction de Date objects :
```typescript
const diffDays = Math.round((due.getTime() - today.getTime()) / (1000 * 60 * 60 * 24));
```
  </action>
  <verify>
    <automated>grep -c "SendEmail\|reminder_sent_date\|medication" C:/Users/smalt/Desktop/app-chien-ia/pawcoach/functions/medicationReminders.ts</automated>
  </verify>
  <done>Le fichier medicationReminders.ts existe, contient les trois elements cles (SendEmail, reminder_sent_date, filter medication), et suit exactement le pattern de vaccineReminders.ts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enregistrer medicationReminders comme CRON dans Base44 UI (NOTIF-01)</name>
  <what-built>medicationReminders.ts est pousse sur Git et detecte automatiquement par Base44. Mais le declencheur CRON (frequence, heure) doit etre configure manuellement dans l'UI Base44 — cette configuration ne peut pas etre faite via Git push.</what-built>
  <how-to-verify>
1. Ouvrir l'app PawCoach dans Base44 Developer Mode
2. Aller dans l'onglet "Backend" ou "Functions"
3. Verifier que `medicationReminders` apparait dans la liste des fonctions detectees (Base44 auto-detecte depuis Git)
4. Cliquer sur `medicationReminders` → configurer comme CRON :
   - Frequence : Daily (meme config que vaccineReminders)
   - Heure : meme heure que vaccineReminders (typiquement 8h00 ou 9h00 matin)
5. Sauvegarder la configuration
6. Verifier que le CRON apparait dans la liste des CRON actifs aux cotes de vaccineReminders
  </how-to-verify>
  <resume-signal>Tape "ok cron configure" quand medicationReminders est enregistre comme CRON dans Base44 UI avec la meme frequence que vaccineReminders</resume-signal>
</task>

</tasks>

<verification>
Apres les trois taches :
1. `grep -n "isPremium" pawcoach/functions/vaccineReminders.ts` retourne 0 resultats
2. Le fichier `pawcoach/functions/medicationReminders.ts` existe et contient `type: "medication"`, `SendEmail`, `reminder_sent_date`
3. `git diff pawcoach/functions/vaccineReminders.ts` montre uniquement la suppression des 2 lignes isPremium
4. medicationReminders est visible dans Base44 UI comme CRON actif avec frequence Daily
</verification>

<success_criteria>
- vaccineReminders.ts : filtre premium retire, tous les users recoivent les rappels vaccins
- medicationReminders.ts : cree, pattern identique a vaccineReminders.ts, filtre `type: "medication"`, pas de filtre premium
- medicationReminders CRON enregistre dans Base44 UI — sans cette etape, les emails ne sont jamais envoyes
</success_criteria>

<output>
After completion, create `.planning/phases/03-notifications/03-01-SUMMARY.md`
</output>
