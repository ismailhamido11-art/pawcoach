---
phase: 02-ai-enrichment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/functions/monthlySummary.ts
autonomous: true
requirements:
  - AI-03
must_haves:
  truths:
    - "L'email mensuel affiche le mood moyen du mois calcule sur les DailyCheckins (ex: 2.8/4)"
    - "L'email mensuel affiche l'energie moyenne du mois calcule sur les DailyCheckins (ex: 2.1/3)"
    - "L'email mensuel liste les symptoms recurrents (signales >= 2 fois dans le mois)"
    - "L'email mensuel mentionne le streak du mois (jours de check-in consecutifs max ou total check-ins)"
    - "Si aucun check-in n'existe pour le mois, l'email ne s'envoie pas (ou mentionne l'absence de donnees)"
  artifacts:
    - path: "pawcoach/functions/monthlySummary.ts"
      provides: "Monthly summary enrichi avec DailyCheckins (mood, energy, symptoms, streak)"
      contains: "monthCheckins|avgMood|avgEnergy|recurringSymptoms"
  key_links:
    - from: "pawcoach/functions/monthlySummary.ts"
      to: "entities.DailyCheckin.filter"
      via: "requete des check-ins du mois pour chaque chien"
      pattern: "monthCheckins|DailyCheckin"
---

<objective>
Enrichir monthlySummary.ts pour que le resume mensuel inclut les donnees DailyCheckin : mood moyen, energie moyenne, symptoms recurrents et statistiques de check-in du mois — et pas seulement les HealthRecord (vetVisits + poids + notes).

Purpose: Aujourd'hui le monthly summary est un email passe-partout avec 3 stats HealthRecord. Il ignore totalement les check-ins quotidiens qui contiennent le vrai signal de sante (humeur, energie, symptoms). C'est le rapport mensuel le plus utile et il est actuellement vide de sens.

Output: monthlySummary.ts modifie — fetch DailyCheckins par chien, calcul des stats mensuelles, email enrichi avec les vraies donnees
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pawcoach/.planning/PROJECT.md
@pawcoach/.planning/ROADMAP.md
@pawcoach/.planning/STATE.md

<interfaces>
<!-- Architecture actuelle de monthlySummary.ts -->
// Charge : Dog.list(), User.list(), HealthRecord.list() (global, filtre en memoire)
// Pour chaque chien premium : calcule vetVisits, notes, weightChange
// Envoie un email simple avec ces 3 donnees

<!-- Entite DailyCheckin -->
DailyCheckin: {
  dog_id: string,
  date: string,        // "YYYY-MM-DD"
  mood: number,        // 1-4
  energy: number,      // 1-3
  appetite: number,    // 1-3
  symptoms: string[]   // optionnel
}

<!-- Variables deja disponibles dans la boucle -->
// monthStr : "YYYY-MM" (mois precedent)
// lastMonthEnd : Date du dernier jour du mois
// dog : entite Dog courante
// user : entite User du proprietaire

<!-- Pattern de l'email existant (ligne 54-62) -->
await base44.asServiceRole.integrations.Core.SendEmail({
  to: user.email,
  from_name: "PawCoach",
  subject: `🐾 Résumé de ${monthName} pour ${dog.name}`,
  body: `Bonjour !\n\nVoici le résumé du mois de ${monthName} pour ${dog.name} :\n\n• Visites vétérinaire : ${vetVisits}\n${weightLine}\n• Notes ajoutées : ${notes}\n\nBravo pour le suivi de ${dog.name} ! 🌟\n\n— PawCoach`,
});

<!-- ATTENTION : le fichier charge TOUT en global (allRecords = HealthRecord.list())
     mais NE charge PAS les DailyCheckins globalement.
     Ajouter un fetch par chien (filter par dog_id) dans la boucle.
     Pas de liste globale car trop lourde a charger pour tous les chiens. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Charger les DailyCheckins du mois et enrichir l'email mensuel</name>
  <files>pawcoach/functions/monthlySummary.ts</files>
  <action>
Lire le fichier complet AVANT toute modification.

**Etape 1 : Dans la boucle `for (const dog of (dogs || []))`, apres le calcul de `weightChange` (avant le bloc `try { await SendEmail }`)**, ajouter :

```typescript
// Fetch DailyCheckins for this dog this month
const allDogCheckins = await base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dog.id }).catch(() => []);
const monthCheckins = (allDogCheckins || []).filter(c => c.date && c.date.startsWith(monthStr));

const checkinCount = monthCheckins.length;

// Calculate averages (only if check-ins exist)
let checkinStatsLines = "• Check-ins : aucun ce mois-ci";
if (checkinCount > 0) {
  const avgMood = (monthCheckins.reduce((s, c) => s + (c.mood || 0), 0) / checkinCount).toFixed(1);
  const avgEnergy = (monthCheckins.reduce((s, c) => s + (c.energy || 0), 0) / checkinCount).toFixed(1);
  const avgAppetite = (monthCheckins.reduce((s, c) => s + (c.appetite || 0), 0) / checkinCount).toFixed(1);

  // Recurring symptoms (>= 2 occurrences)
  const symptomCounts: Record<string, number> = {};
  monthCheckins.forEach(c => {
    if (c.symptoms?.length) c.symptoms.forEach((s: string) => { symptomCounts[s] = (symptomCounts[s] || 0) + 1; });
  });
  const recurringSymptoms = Object.entries(symptomCounts)
    .filter(([_, n]) => n >= 2)
    .sort((a, b) => b[1] - a[1])
    .slice(0, 5)
    .map(([s, n]) => `${s} (${n}x)`)
    .join(", ");

  checkinStatsLines = [
    `• Check-ins : ${checkinCount} jours sur le mois`,
    `• Humeur moyenne : ${avgMood}/4`,
    `• Energie moyenne : ${avgEnergy}/3`,
    `• Appetit moyen : ${avgAppetite}/3`,
    recurringSymptoms ? `• Symptomes recurrents : ${recurringSymptoms}` : null,
  ].filter(Boolean).join("\n");
}
```

**Etape 2 : Modifier le body de l'email** pour inclure `checkinStatsLines` entre les stats existantes.

Remplacer le `body` existant :
```typescript
body: `Bonjour !\n\nVoici le résumé du mois de ${monthName} pour ${dog.name} :\n\n• Visites vétérinaire : ${vetVisits}\n${weightLine}\n• Notes ajoutées : ${notes}\n\nBravo pour le suivi de ${dog.name} ! 🌟\n\n— PawCoach`,
```
Par :
```typescript
body: `Bonjour !\n\nVoici le résumé du mois de ${monthName} pour ${dog.name} :\n\n${checkinStatsLines}\n${weightLine}\n• Visites vétérinaire : ${vetVisits}\n• Notes de santé ajoutées : ${notes}\n\nBravo pour le suivi de ${dog.name} ! 🌟\n\n— PawCoach`,
```

Aucune autre modification. La gestion des erreurs email (try/catch existant), le calcul du poids, et les autres variables existantes restent identiques.

ATTENTION REGRESSION : le fichier utilise `new Date(a.date) - new Date(b.date)` pour trier les poids (ligne ~36). TypeScript peut signaler une erreur de type. Ne pas modifier ce tri existant — il fonctionne en runtime Deno.
  </action>
  <verify>
    <automated>cd C:/Users/smalt/Desktop/app-chien-ia && git diff HEAD pawcoach/functions/monthlySummary.ts | grep -E "monthCheckins|checkinStatsLines|avgMood|recurringSymptoms"</automated>
  </verify>
  <done>
- Le fichier contient une requete `DailyCheckin.filter({ dog_id: dog.id })` dans la boucle
- `monthCheckins` filtre sur `monthStr`
- `checkinStatsLines` inclut : count, avgMood, avgEnergy, avgAppetite, recurringSymptoms
- L'email body inclut `checkinStatsLines`
- Le corps existant (vetVisits, weightLine, notes) est conserve
  </done>
</task>

</tasks>

<verification>
Verifier dans le fichier :
1. Le fetch DailyCheckin est dans la boucle for (par chien, pas global)
2. `monthCheckins` filtre sur `monthStr` (ex: "2026-02")
3. Le calcul des moyennes est protege par `if (checkinCount > 0)`
4. Les symptoms recurrents ont un seuil >= 2
5. L'email body contient `checkinStatsLines`
6. Les variables existantes `vetVisits`, `weightLine`, `notes` sont toujours dans le body
7. `git diff` ne montre pas de suppressions des parties fonctionnelles existantes
</verification>

<success_criteria>
La fonction monthlySummary.ts :
- Charge les DailyCheckins de chaque chien pour le mois ecoule
- Calcule mood moyen, energy moyen, appetite moyen
- Liste les symptoms signales >= 2 fois
- Affiche le nombre de jours de check-in dans le mois
- Envoie un email enrichi avec ces stats en plus des stats HealthRecord existantes
- Ne casse pas la logique existante (vetVisits, poids, notes, try/catch email)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-ai-enrichment/02-03-SUMMARY.md`
</output>
