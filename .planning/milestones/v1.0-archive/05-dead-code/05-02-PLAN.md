---
phase: 05-dead-code
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AnimatedLogo.jsx
  - src/components/reminders/ReminderAlert.jsx
  - functions/ (lecture seule — audit, pas de suppression attendue)
autonomous: true
requirements:
  - DEAD-03
  - DEAD-04

must_haves:
  truths:
    - "Aucun composant React n'existe dans le repo sans etre importe quelque part"
    - "Chaque fichier .ts dans functions/ est soit appele depuis le frontend, soit identifiable comme CRON job actif"
    - "Les composants orphelins confirmes (AnimatedLogo, ReminderAlert) sont supprimes ou integres"
  artifacts:
    - path: "src/components/AnimatedLogo.jsx"
      provides: "Supprime si orphelin confirme, ou importe dans un fichier si utile"
    - path: "src/components/reminders/ReminderAlert.jsx"
      provides: "Supprime si orphelin confirme, ou importe dans un fichier si utile"
  key_links:
    - from: "src/components/AnimatedLogo.jsx"
      to: "aucun fichier importeur"
      via: "grep -rn 'import.*AnimatedLogo' src/"
      pattern: "import.*AnimatedLogo"
    - from: "src/components/reminders/ReminderAlert.jsx"
      to: "aucun fichier importeur"
      via: "grep -rn 'import.*ReminderAlert' src/"
      pattern: "import.*ReminderAlert"
---

<objective>
Identifier et supprimer les composants React orphelins (non importes nulle part), puis auditer les fichiers backend .ts pour confirmer qu'aucun n'est mort. Deux composants orphelins sont deja confirmes par audit : AnimatedLogo et ReminderAlert.

Purpose: Satisfaire DEAD-03 (zero composant orphelin) et DEAD-04 (zero fichier backend .ts mort). Nettoyer les artefacts inutiles reduit la confusion pour les phases 6-8.

Output: Repo sans composants orphelins. Tous les fichiers functions/*.ts sont justifies (appelable depuis frontend ou CRON job documente).
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages.config.js

<interfaces>
<!-- Composants orphelins confirmes par audit initial (grep -rn "import.*X" src/ → zero resultat) -->

Confirmes orphelins :
- src/components/AnimatedLogo.jsx — zero import dans tout src/
- src/components/reminders/ReminderAlert.jsx — zero import dans tout src/

Fonctions backend appelees depuis le frontend (via base44.functions.invoke) :
  analyzeGrowthPhoto, dailyCheckinProcess, finalDiagnosis, generateDiagnosisPDF,
  generateTrainingProgram, parseHealthFile, pawcoachChat, preDiagnosis,
  processHealthInput, stripeCheckout, stripePortal, vetAccess

Fonctions backend NON appelees depuis frontend (toutes sont des CRON jobs) :
  deleteUser, medicationReminders, monthlySummary, streakReminder, stripeWebhook,
  trialExpiryReminder, vaccineReminders, vetVisitReminders, walkReminder, weeklyInsightGenerate

Note : Sur Base44, les fonctions backend sont auto-enregistrees par nom de fichier.
Il n'existe pas de fichier routes.json ou functions.json a maintenir.
DEAD-04 = verifier qu'aucun .ts dans functions/ n'est completement inutile (ni frontend, ni CRON).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit exhaustif des composants orphelins</name>
  <files>(lecture seule — audit grep)</files>
  <action>
Verifier systematiquement TOUS les composants JSX (hors ui/) pour confirmer lesquels sont orphelins.

Pour chaque composant dans src/components/ (hors ui/) :
```bash
grep -rn "import.*NomComposant" src/ --include="*.jsx" --include="*.js"
```

Deja confirmes orphelins (grep retourne zero) :
- AnimatedLogo → `grep -rn "import.*AnimatedLogo" src/` → zero
- ReminderAlert → `grep -rn "import.*ReminderAlert" src/` → zero

Verifier aussi les composants qui semblent peu utilises (confirmer ou infirmer) :
- Tout composant dans src/components/ dont le nom n'apparait pas dans les pages

METHODE RECOMMANDEE — script de batch :
```bash
for f in $(find src/components -name "*.jsx" ! -path "*/ui/*"); do
  name=$(basename $f .jsx)
  count=$(grep -rn "import.*$name" src/ --include="*.jsx" --include="*.js" | grep -v "^$f:" | wc -l)
  if [ "$count" -eq "0" ]; then
    echo "ORPHAN: $name ($f)"
  fi
done
```

Dresser la liste COMPLETE des orphelins confirmes avant de supprimer quoi que ce soit.

REGLE : Un composant avec `export default` qui n'est importe nulle part dans src/ = orphelin.
EXCEPTION : Ne pas considerer comme orphelin un fichier qui n'exporte pas de composant React (ex: fichier de hooks purs, utilitaires) — verifier le contenu avant de conclure.
  </action>
  <verify>
    <automated>grep -rn "import.*AnimatedLogo\|import.*ReminderAlert" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/ --include="*.jsx" --include="*.js" | wc -l</automated>
  </verify>
  <done>Liste exhaustive et confirmee de tous les composants orphelins etablie. Resultat attendu : au minimum AnimatedLogo et ReminderAlert. Aucune suppression effectuee a ce stade.</done>
</task>

<task type="auto">
  <name>Task 2: Supprimer les composants orphelins confirmes + auditer le backend</name>
  <files>
    src/components/AnimatedLogo.jsx
    src/components/reminders/ReminderAlert.jsx
    (+ tout orphelin supplementaire trouve en Task 1)
  </files>
  <action>
**PARTIE A — Suppression des composants orphelins**

Pour chaque composant confirme orphelin en Task 1 :

1. Lire le fichier avec Read() pour comprendre ce qu'il fait
2. Verifier une derniere fois : `grep -rn "NomComposant" src/` — confirmer zero usage
3. Si le composant n'est plus utilise nulle part ET n'a pas de valeur future evidente → supprimer le fichier
4. Si le composant semble avoir une valeur future (ex: design prepare pour une feature a venir) → commenter dans le SUMMARY mais supprimer quand meme (code mort = code mort)

COMMANDE DE SUPPRESSION : Utiliser l'outil Bash pour `rm` le fichier apres confirmation.

**PARTIE B — Audit des fichiers backend functions/*.ts**

Verifier chaque fichier .ts dans functions/ pour confirmer sa nature :

Deja classes par audit (ne pas re-verifier) :
- FRONTEND (appeles via base44.functions.invoke) : analyzeGrowthPhoto, dailyCheckinProcess, finalDiagnosis, generateDiagnosisPDF, generateTrainingProgram, parseHealthFile, pawcoachChat, preDiagnosis, processHealthInput, stripeCheckout, stripePortal, vetAccess
- CRON JOBS (non appeles depuis frontend, mais actifs) : deleteUser, medicationReminders, monthlySummary, streakReminder, stripeWebhook, trialExpiryReminder, vaccineReminders, vetVisitReminders, walkReminder, weeklyInsightGenerate

Pour confirmer qu'un fichier est bien un CRON job actif et non un fichier mort :
- Il contient `Deno.serve(` → c'est une fonction exposee (actif)
- Il commente "scheduled" ou "CRON" ou "every hour/day" → confirme CRON
- Il contient `asServiceRole` → utilise le contexte service, indique un CRON

Si un fichier .ts dans functions/ n'a aucun de ces indicateurs ET n'est pas appele depuis le frontend → c'est un candidat a la suppression. Signaler dans le SUMMARY.

**CONTRAINTE IMPORTANTE** : Sur Base44, NE PAS supprimer un fichier .ts backend sauf s'il est clairement mort (zero Deno.serve, zero appel frontend). Les CRON jobs sont valides meme sans appel explicite dans le code frontend.
  </action>
  <verify>
    <automated>grep -rn "import.*AnimatedLogo\|import.*ReminderAlert" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/ --include="*.jsx" --include="*.js" | wc -l</automated>
  </verify>
  <done>
    - Tous les composants orphelins confirmes sont supprimes du repo
    - Chaque fichier functions/*.ts est classe : FRONTEND, CRON, ou signale comme suspect dans le SUMMARY
    - Le grep de verification retourne 0 pour chaque composant supprime
    - Aucun fichier backend actif n'a ete supprime
  </done>
</task>

</tasks>

<verification>
Verification finale apres completion :

```bash
# Confirmer zero import vers des fichiers supprimes
grep -rn "AnimatedLogo\|ReminderAlert" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/ --include="*.jsx"

# Confirmer que tous les .ts backend existent et sont accedes
ls /c/Users/smalt/Desktop/app-chien-ia/pawcoach/functions/*.ts | wc -l

# Build frontend ne casse pas
npm run build 2>&1 | tail -5
```

Resultat attendu : zero reference aux composants supprimes, build reussi.
</verification>

<success_criteria>
- DEAD-03 : Zero composant React orphelin dans le repo (verifie par grep sur tous les composants)
- DEAD-04 : Chaque fichier functions/*.ts est soit appele depuis le frontend, soit documente comme CRON job actif — aucun fichier .ts mort
- Build Vite passe sans erreur apres suppression des orphelins
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05-dead-code/05-02-SUMMARY.md` avec :
- Liste des composants orphelins trouves et supprimes
- Liste des composants verifies et confirmes non-orphelins
- Tableau des fichiers functions/*.ts avec leur classification (FRONTEND/CRON/SUSPECT)
- Resultat du build (succes/echec)
- Tout cas limite ou doute rencontre
</output>
