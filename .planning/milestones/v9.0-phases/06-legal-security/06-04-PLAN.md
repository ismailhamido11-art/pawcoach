---
phase: 06-legal-security
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - base44/functions/pawcoachChat/entry.ts
  - base44/functions/preDiagnosis/entry.ts
  - base44/functions/processHealthInput/entry.ts
autonomous: true
requirements:
  - FIX-09
  - FIX-10

must_haves:
  truths:
    - "index.html contient une meta CSP qui bloque les scripts non autorisés"
    - "pawcoachChat rejette les messages utilisateur dépassant 2000 caractères avec HTTP 400"
    - "preDiagnosis rejette les champs symptoms/additional_info dépassant 2000 chars avec HTTP 400"
    - "processHealthInput rejette le champ text dépassant 2000 chars avec HTTP 400"
    - "Les 3 fonctions backend strippent les balises HTML des inputs utilisateur"
  artifacts:
    - path: "index.html"
      provides: "CSP meta tag"
      contains: "Content-Security-Policy"
    - path: "base44/functions/pawcoachChat/entry.ts"
      provides: "Validation longueur messages utilisateur"
      contains: "MAX_MSG_LENGTH"
    - path: "base44/functions/preDiagnosis/entry.ts"
      provides: "Validation longueur symptoms/additional_info"
      contains: "MAX_INPUT_LENGTH"
    - path: "base44/functions/processHealthInput/entry.ts"
      provides: "Validation longueur text input"
      contains: "MAX_INPUT_LENGTH"
  key_links:
    - from: "index.html"
      to: "browser"
      via: "meta http-equiv Content-Security-Policy"
      pattern: "Content-Security-Policy"
    - from: "base44/functions/pawcoachChat/entry.ts"
      to: "rawMessages"
      via: "each message content length check"
      pattern: "MAX_MSG_LENGTH"
---

<objective>
Ajouter la Content Security Policy dans index.html et renforcer la validation des inputs utilisateur dans les 3 fonctions backend AI.

Purpose: FIX-09 (CSP) et FIX-10 (input validation) sont les deux exigences sécurité de cette phase. CSP bloque XSS. Validation backend empêche l'injection de prompt et les abus de quota. P1 sécurité.
Output: meta CSP dans index.html. Validation de longueur + strip HTML dans 3 fonctions Deno.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

**CSP — Contraintes Base44 :**
- Le SDK Base44 appelle `https://api.base44.com`
- Stripe checkout appelle `https://js.stripe.com` et `https://checkout.stripe.com`
- Les images viennent de `https://*.base44.app` et `https://*.amazonaws.com`
- Les styles sont inline (Tailwind JIT génère des styles inline dans certains cas) → `style-src 'unsafe-inline'` requis
- Le SW (service worker) exécute des scripts → `worker-src 'self'`
- canvas-confetti utilise canvas → pas de restriction spéciale

**Validation backend — État actuel :**
- `pawcoachChat/entry.ts` : sanitize(s, max=500) line 14, mais les messages individuels sont tronqués à 2000 via `String(m.content || '').substring(0, 2000)` (line 58). Pas de reject explicite, juste une troncature.
- `preDiagnosis/entry.ts` : sanitize(s, max=2000) existe déjà (line 38). Pas de reject si dépassement.
- `processHealthInput/entry.ts` : sanitize(s, max=2000) existe déjà (line 38). Pas de reject si dépassement.

Objectif FIX-10 : ajouter un **reject explicite** (return HTTP 400) si un input dépasse 2000 chars AVANT de procéder au LLM, en plus du sanitize existant. Cela protège contre les abus même si sanitize est en place.
</context>

<interfaces>
<!-- Contexte code existant -->
From index.html (structure actuelle, ligne 3-12):
```html
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
<!-- Insérer CSP ici, après viewport -->
```

From base44/functions/pawcoachChat/entry.ts (lignes 9-14):
```ts
const { messages: rawMessages, dogId, imageUrl, mode = "chat" } = await req.json();
// Ligne 14 : const sanitize = (s: any, max = 500) => ...
// Ligne 56-59 : messages filtred + each content substring(0, 2000)
// Ajouter validation AVANT ligne 56 : vérifier chaque message individuel > 2000
```

From base44/functions/preDiagnosis/entry.ts (lignes 9-11):
```ts
const { symptoms, duration, additional_info, image_url, ... } = await req.json();
if (!symptoms) return Response.json({ error: 'Symptoms required' }, { status: 400 });
// Ajouter ici : if (symptoms.length > 2000 || ...) return 400
```

From base44/functions/processHealthInput/entry.ts (lignes 9-11):
```ts
const { messages, text, imageUrl, dogId } = await req.json();
if (!dogId) return Response.json({ error: 'dogId required' }, { status: 400 });
// Ajouter ici : if (text && text.length > 2000) return 400
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CSP meta tag dans index.html</name>
  <files>index.html</files>

  <read_first>
    - index.html (lignes 1-43) — fichier complet pour voir la structure head exacte
  </read_first>

  <action>
Insérer le meta tag CSP dans le `<head>` de `index.html`, après la ligne `<meta name="viewport" ...>` et avant `<!-- PWA -->` :

```html
<!-- Content Security Policy — FIX-09 -->
<meta http-equiv="Content-Security-Policy" content="
  default-src 'self';
  script-src 'self' https://js.stripe.com https://api.base44.com;
  style-src 'self' 'unsafe-inline';
  img-src 'self' data: blob: https: ;
  font-src 'self' data:;
  connect-src 'self' https://api.base44.com https://api.stripe.com https://checkout.stripe.com wss://api.base44.com;
  frame-src https://js.stripe.com https://hooks.stripe.com https://checkout.stripe.com;
  worker-src 'self' blob:;
  object-src 'none';
  base-uri 'self';
" />
```

Note : mettre le contenu sur une seule ligne dans le meta tag (le formatage multiligne ci-dessus est pour la lisibilité — dans le fichier HTML, ça doit être une seule ligne ou avec des espaces/newlines préservés correctement par le browser). Utiliser le format sur une seule ligne dans le fichier réel :

```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' https://js.stripe.com https://api.base44.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data:; connect-src 'self' https://api.base44.com https://api.stripe.com https://checkout.stripe.com wss://api.base44.com; frame-src https://js.stripe.com https://hooks.stripe.com https://checkout.stripe.com; worker-src 'self' blob:; object-src 'none'; base-uri 'self';" />
```
  </action>

  <verify>
    <automated>grep "Content-Security-Policy" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Content-Security-Policy" index.html` retourne 1 ligne
    - `grep "default-src.*self" index.html` retourne 1 ligne
    - `grep "api.base44.com" index.html` retourne 1 ligne dans la CSP (en plus de l'og:url)
    - `grep "js.stripe.com" index.html` retourne 1 ligne dans la CSP
  </acceptance_criteria>

  <done>CSP meta tag présent dans index.html avec directives couvrant Base44 SDK, Stripe, et SW.</done>
</task>

<task type="auto">
  <name>Task 2: Validation input longueur dans les 3 fonctions backend AI</name>
  <files>base44/functions/pawcoachChat/entry.ts, base44/functions/preDiagnosis/entry.ts, base44/functions/processHealthInput/entry.ts</files>

  <read_first>
    - base44/functions/pawcoachChat/entry.ts (lignes 9-60) — parsing des inputs et messages, zone de validation
    - base44/functions/preDiagnosis/entry.ts (lignes 9-40) — parsing des inputs, zone de validation
    - base44/functions/processHealthInput/entry.ts (lignes 9-40) — parsing des inputs, zone de validation
  </read_first>

  <action>
**pawcoachChat/entry.ts :**

Après la ligne `const { messages: rawMessages, dogId, imageUrl, mode = "chat" } = await req.json();` (ligne 9), ajouter immédiatement :

```ts
// Input length validation — FIX-10
const MAX_MSG_LENGTH = 2000;
if (Array.isArray(rawMessages)) {
  for (const m of rawMessages) {
    if (m?.role === 'user' && typeof m?.content === 'string' && m.content.length > MAX_MSG_LENGTH) {
      return Response.json({ error: 'Message trop long. Maximum 2000 caractères.' }, { status: 400 });
    }
  }
}
```

**preDiagnosis/entry.ts :**

Après la ligne `if (!symptoms) return Response.json(...)` (ligne 11), ajouter :

```ts
// Input length validation — FIX-10
const MAX_INPUT_LENGTH = 2000;
if (typeof symptoms === 'string' && symptoms.length > MAX_INPUT_LENGTH) {
  return Response.json({ error: 'Symptoms trop longs. Maximum 2000 caractères.' }, { status: 400 });
}
if (typeof additional_info === 'string' && additional_info.length > MAX_INPUT_LENGTH) {
  return Response.json({ error: 'additional_info trop long. Maximum 2000 caractères.' }, { status: 400 });
}
```

**processHealthInput/entry.ts :**

Après la ligne `if (!dogId) return Response.json(...)` (ligne 11-12), ajouter :

```ts
// Input length validation — FIX-10
const MAX_INPUT_LENGTH = 2000;
if (typeof text === 'string' && text.length > MAX_INPUT_LENGTH) {
  return Response.json({ error: 'Texte trop long. Maximum 2000 caractères.' }, { status: 400 });
}
if (Array.isArray(messages)) {
  for (const m of messages) {
    if (m?.role === 'user' && typeof m?.content === 'string' && m.content.length > MAX_INPUT_LENGTH) {
      return Response.json({ error: 'Message trop long. Maximum 2000 caractères.' }, { status: 400 });
    }
  }
}
```

Note : ne pas toucher aux `sanitize()` existants (ils restent en place comme seconde ligne de défense). Ces validations s'ajoutent en amont comme reject explicite.
  </action>

  <verify>
    <automated>grep -n "MAX_MSG_LENGTH\|MAX_INPUT_LENGTH" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/pawcoachChat/entry.ts /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/preDiagnosis/entry.ts /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/processHealthInput/entry.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "MAX_MSG_LENGTH" base44/functions/pawcoachChat/entry.ts` retourne 2 lignes (déclaration + check)
    - `grep "MAX_INPUT_LENGTH" base44/functions/preDiagnosis/entry.ts` retourne 2+ lignes
    - `grep "MAX_INPUT_LENGTH" base44/functions/processHealthInput/entry.ts` retourne 2+ lignes
    - `grep "status: 400" base44/functions/pawcoachChat/entry.ts` retourne 2+ lignes (dont une pour length)
    - `grep "status: 400" base44/functions/preDiagnosis/entry.ts` retourne 2+ lignes (dont une pour length)
    - `grep "status: 400" base44/functions/processHealthInput/entry.ts` retourne 2+ lignes (dont une pour length)
  </acceptance_criteria>

  <done>Les 3 fonctions AI retournent HTTP 400 si un input dépasse 2000 caractères. Validation ajoutée en amont du sanitize existant.</done>
</task>

</tasks>

<verification>
```bash
# CSP en place
grep "Content-Security-Policy" index.html
grep "api.base44.com\|js.stripe.com\|worker-src" index.html

# Validation input longueur
grep "MAX_MSG_LENGTH\|MAX_INPUT_LENGTH" base44/functions/pawcoachChat/entry.ts
grep "MAX_INPUT_LENGTH" base44/functions/preDiagnosis/entry.ts
grep "MAX_INPUT_LENGTH" base44/functions/processHealthInput/entry.ts

# Sanitize existant toujours en place (régression)
grep "sanitize" base44/functions/pawcoachChat/entry.ts
grep "sanitize" base44/functions/preDiagnosis/entry.ts
grep "sanitize" base44/functions/processHealthInput/entry.ts
```
</verification>

<success_criteria>
- index.html : meta CSP présent avec directives default-src, script-src (Base44 + Stripe), style-src, connect-src, frame-src, worker-src, object-src
- pawcoachChat : rejet HTTP 400 si message user > 2000 chars
- preDiagnosis : rejet HTTP 400 si symptoms ou additional_info > 2000 chars
- processHealthInput : rejet HTTP 400 si text ou message user > 2000 chars
- Aucune régression : les sanitize() existants restent en place
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-legal-security/06-04-SUMMARY.md`
</output>
