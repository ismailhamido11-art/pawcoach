---
phase: 06-legal-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Premium.jsx
  - src/components/sante/DiagnosisContent.jsx
  - src/components/profile/SettingsSection.jsx
autonomous: true
requirements:
  - FIX-05
  - FIX-06
  - FIX-07

must_haves:
  truths:
    - "La page Premium affiche une mention de renouvellement automatique visible sous le CTA (les deux CTA : trial et non-trial)"
    - "DiagnosisContent affiche un banner disclaimer vétérinaire en haut du contenu"
    - "SettingsSection contient un bouton 'Exporter mes données' qui déclenche le téléchargement JSON"
  artifacts:
    - path: "src/pages/Premium.jsx"
      provides: "Disclosure renouvellement auto conforme EU Consumer Rights"
      contains: "Renouvellement automatique"
    - path: "src/components/sante/DiagnosisContent.jsx"
      provides: "Banner disclaimer médical visible"
      contains: "vétérinaire"
    - path: "src/components/profile/SettingsSection.jsx"
      provides: "Bouton export données RGPD Art. 20"
      contains: "Exporter mes données"
  key_links:
    - from: "src/pages/Premium.jsx"
      to: "CTA buttons"
      via: "texte disclosure sous chaque bouton S'abonner"
      pattern: "Renouvellement automatique"
    - from: "src/components/sante/DiagnosisContent.jsx"
      to: "contenu diagnostic"
      via: "banner en haut du composant"
      pattern: "vétérinaire"
---

<objective>
Ajouter (1) la disclosure de renouvellement automatique dans Premium, (2) le disclaimer vétérinaire dans DiagnosisContent, (3) le bouton d'export de données RGPD dans SettingsSection.

Purpose: FIX-06 (EU Consumer Rights Directive) et FIX-07 (cohérence disclaimer médical) sont P0/P1 légaux. FIX-05 (portabilité RGPD Art. 20) est P0.
Output: Textes légaux en place + export JSON fonctionnel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

Contexte Premium.jsx : il y a 2 CTA "S'abonner" — un pour `isOnTrial` (ligne 293-300) et un pour non-trial (lignes 302-320). Chaque CTA a déjà une ligne de reassurance dessous (lignes 325-334). Il faut enrichir ou remplacer ces lignes pour inclure la mention de renouvellement auto.

Export données : Base44 expose les entités via le SDK. Dog, DailyLog, HealthRecord, DailyCheckin, FoodScan, UserProgress sont accessibles via les entities. L'export doit faire Dog.filter() + entités associées, puis JSON.stringify + download.

DiagnosisContent : composant qui rend l'onglet Symptômes de Sante.jsx. Pattern disclaimer existant dans Chat.jsx (ligne 351-353) et Home.jsx (ligne 666-669).
</context>

<interfaces>
<!-- Contexte code Premium.jsx — zones à modifier -->
From src/pages/Premium.jsx (lignes 286-334) — zone CTA trial:
```jsx
{/* CTA */}
<motion.div ...>
  {isOnTrial ? (
    <Button onClick={handleSubscribe} ...>
      {loading ? "Chargement..." : `S'abonner — ${plan === "annual" ? "59,99 €/an" : "7,99 €/mois"}`}
    </Button>
  ) : (
    <Button onClick={() => {...}} ...>Commencer</Button>
  )}
</motion.div>
{/* Reassurance — ligne 325-334 */}
<motion.p className="text-center text-xs text-muted-foreground mt-4">
  {isOnTrial
    ? "Sans engagement · Résiliation à tout moment · Paiement sécurisé Stripe"
    : "Tu peux gérer ton abonnement à tout moment depuis ton Profil."}
</motion.p>
```

From src/components/sante/DiagnosisContent.jsx (pattern disclaimer à copier depuis Chat.jsx):
```jsx
{/* Disclaimer médical — pattern Chat.jsx ligne 351-353 */}
<div className="mx-5 mb-4 px-3 py-2 bg-amber-50 rounded-xl border border-amber-200 flex items-start gap-2">
  <span className="text-amber-600 text-xs mt-0.5">⚠</span>
  <p className="text-xs text-amber-800 leading-relaxed">
    PawCoach est un coach bien-être, pas un vétérinaire. Ces informations ne remplacent pas un avis professionnel.
  </p>
</div>
```

From src/components/profile/SettingsSection.jsx — entities disponibles:
```jsx
import { Dog } from "@/api/entities";
// Pattern export: Dog.filter() → JSON.stringify() → Blob → URL.createObjectURL → <a>.click()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Disclosure renouvellement auto dans Premium + disclaimer DiagnosisContent</name>
  <files>src/pages/Premium.jsx, src/components/sante/DiagnosisContent.jsx</files>

  <read_first>
    - src/pages/Premium.jsx (lignes 285-340) — zone CTA principale avec reassurance
    - src/pages/Premium.jsx (lignes 480-520) — vérifier s'il y a un second CTA en bas de page
    - src/components/sante/DiagnosisContent.jsx (lignes 1-50) — structure du composant pour savoir où insérer le banner
  </read_first>

  <action>
**Premium.jsx — Disclosure renouvellement :**

Remplacer la ligne de reassurance existante (motion.p, lignes 325-334) par un texte enrichi :

Pour `isOnTrial` (quand l'essai est actif et que le user clique S'abonner) :
```jsx
<motion.p className="text-center text-xs text-muted-foreground mt-4 leading-relaxed">
  Renouvellement automatique à chaque période. Résiliation à tout moment depuis ton profil.
  {plan === "annual" ? " Facturation annuelle de 59,99 EUR." : " Facturation mensuelle de 7,99 EUR."}
  {" "}Paiement sécurisé via Stripe.
</motion.p>
```

Pour non-trial (message existant "Tu peux gérer..."), enrichir :
```jsx
<motion.p className="text-center text-xs text-muted-foreground mt-4 leading-relaxed">
  Tu peux gérer ton abonnement à tout moment depuis ton Profil. Renouvellement automatique.
</motion.p>
```

Si un second bloc CTA existe en bas de Premium.jsx (scroll down pour vérifier — lignes 480-520), appliquer le même enrichissement sous ce second CTA.

**DiagnosisContent.jsx — Banner disclaimer :**

Lire les premières lignes du composant pour identifier la structure. Insérer au tout début du JSX retourné (après le fragment ou div racine, avant le premier vrai contenu) :

```jsx
{/* Disclaimer médical — FIX-07 */}
<div className="mx-5 mb-4 px-3 py-2.5 bg-amber-50 rounded-xl border border-amber-200 flex items-start gap-2">
  <span className="text-amber-600 text-sm font-bold flex-shrink-0">⚠</span>
  <p className="text-xs text-amber-800 leading-relaxed">
    Ces informations sont fournies à titre indicatif uniquement. Elles ne remplacent pas l'avis d'un vétérinaire qualifié. En cas de doute, consultez votre vétérinaire.
  </p>
</div>
```
  </action>

  <verify>
    <automated>grep -n "Renouvellement automatique" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Premium.jsx && grep -n "vétérinaire\|vétérinaire qualifié" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/sante/DiagnosisContent.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Renouvellement automatique" src/pages/Premium.jsx` retourne 1 ou 2 (selon nombre de CTA)
    - `grep "59,99 EUR\|7,99 EUR" src/pages/Premium.jsx` retourne 1+ ligne dans la disclosure
    - `grep "vétérinaire qualifié\|vétérinaire" src/components/sante/DiagnosisContent.jsx` retourne 1+ ligne
    - `grep "amber-50\|amber-200" src/components/sante/DiagnosisContent.jsx` retourne 2 lignes (banner)
  </acceptance_criteria>

  <done>Disclosure renouvellement visible sous chaque CTA Premium. Disclaimer médical visible en haut de DiagnosisContent.</done>
</task>

<task type="auto">
  <name>Task 2: Bouton export données RGPD dans SettingsSection</name>
  <files>src/components/profile/SettingsSection.jsx</files>

  <read_first>
    - src/components/profile/SettingsSection.jsx (lignes 1-99) — code complet pour trouver le bon emplacement et les imports existants
  </read_first>

  <action>
**Imports à ajouter** en haut de SettingsSection.jsx :

1. Ajouter `Download` aux imports lucide-react (ligne 7) — vérifier si déjà présent, sinon l'ajouter.
2. Ajouter l'import des entités :
```jsx
import { Dog, DailyLog, HealthRecord, DailyCheckin, FoodScan } from "@/api/entities";
```

**État à ajouter** (après les états existants) :
```jsx
const [exporting, setExporting] = useState(false);
```

**Fonction handleExport** à ajouter avant le return :
```jsx
const handleExport = async () => {
  setExporting(true);
  try {
    const [dogs, logs, records, checkins, scans] = await Promise.all([
      Dog.filter({}),
      DailyLog.filter({}),
      HealthRecord.filter({}),
      DailyCheckin.filter({}),
      FoodScan.filter({}),
    ]);
    const exportData = {
      exported_at: new Date().toISOString(),
      dogs,
      daily_logs: logs,
      health_records: records,
      daily_checkins: checkins,
      food_scans: scans,
    };
    const blob = new Blob([JSON.stringify(exportData, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = `pawcoach-export-${new Date().toISOString().split("T")[0]}.json`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    setTimeout(() => URL.revokeObjectURL(url), 5000);
    toast.success("Export téléchargé");
  } catch {
    toast.error("Erreur lors de l'export. Réessaie.");
  } finally {
    setExporting(false);
  }
};
```

**Bouton dans le JSX** — dans la section "Privacy" (bloc `{/* Privacy */}`), après le bouton "Supprimer mon compte" et avant les boutons Privacy/Terms (ajoutés par plan 06-01) :
```jsx
<button
  onClick={handleExport}
  disabled={exporting}
  className="flex items-center gap-2 text-sm text-primary py-1"
>
  {exporting
    ? <Loader2 className="w-3.5 h-3.5 animate-spin" />
    : <Download className="w-3.5 h-3.5" />
  }
  {exporting ? "Export en cours..." : "Exporter mes données"}
</button>
```

Note : `Loader2` est déjà importé (ligne 7). S'assurer que `Download` est ajouté aux imports lucide.
  </action>

  <verify>
    <automated>grep -n "handleExport\|Exporter mes données\|Download" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/profile/SettingsSection.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "handleExport" src/components/profile/SettingsSection.jsx` retourne 3+ lignes (déclaration + onClick + async function)
    - `grep "Exporter mes données" src/components/profile/SettingsSection.jsx` retourne 1 ligne
    - `grep "Download" src/components/profile/SettingsSection.jsx` retourne 2+ lignes (import + JSX)
    - `grep "DailyLog\|HealthRecord\|DailyCheckin\|FoodScan" src/components/profile/SettingsSection.jsx` retourne 4+ lignes
    - `grep "application/json" src/components/profile/SettingsSection.jsx` retourne 1 ligne (Blob MIME type)
  </acceptance_criteria>

  <done>Bouton "Exporter mes données" déclenche téléchargement JSON de toutes les données utilisateur. Toast succès/erreur.</done>
</task>

</tasks>

<verification>
```bash
# FIX-06 : disclosure renouvellement
grep "Renouvellement automatique" src/pages/Premium.jsx

# FIX-07 : disclaimer médical
grep "vétérinaire" src/components/sante/DiagnosisContent.jsx

# FIX-05 : export données
grep "Exporter mes données\|handleExport\|application/json" src/components/profile/SettingsSection.jsx

# Pas de régression sur les imports
grep "from.*entities" src/components/profile/SettingsSection.jsx
```
</verification>

<success_criteria>
- Premium : mention "Renouvellement automatique" + montant visible sous le(s) CTA S'abonner
- DiagnosisContent : banner amber avec disclaimer vétérinaire en haut du composant
- SettingsSection : bouton "Exporter mes données" télécharge un fichier JSON daté avec dogs, logs, records, checkins, scans
- Pas de régression sur la suppression de compte ni le logout
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-legal-security/06-03-SUMMARY.md`
</output>
