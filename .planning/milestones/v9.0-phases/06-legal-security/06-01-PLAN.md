---
phase: 06-legal-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Privacy.jsx
  - src/pages/Terms.jsx
  - src/pages.config.js
  - src/components/profile/SettingsSection.jsx
autonomous: true
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "La page /Privacy est accessible depuis Réglages dans Profile"
    - "La page /Terms est accessible depuis Réglages dans Profile"
    - "Privacy.jsx contient les 7 points RGPD obligatoires (données collectées, finalité, durée, droits, DPO, etc.)"
    - "Terms.jsx contient CGU et mention du paiement Stripe"
  artifacts:
    - path: "src/pages/Privacy.jsx"
      provides: "Page politique de confidentialité RGPD"
      exports: ["default Privacy"]
    - path: "src/pages/Terms.jsx"
      provides: "Page CGU/CGV"
      exports: ["default Terms"]
    - path: "src/pages.config.js"
      provides: "Routes /Privacy et /Terms enregistrées"
      contains: "Privacy"
    - path: "src/components/profile/SettingsSection.jsx"
      provides: "Liens vers Privacy et Terms dans Réglages"
      contains: "createPageUrl(\"Privacy\")"
  key_links:
    - from: "src/components/profile/SettingsSection.jsx"
      to: "src/pages/Privacy.jsx"
      via: "navigate(createPageUrl('Privacy'))"
      pattern: "createPageUrl.*Privacy"
    - from: "src/components/profile/SettingsSection.jsx"
      to: "src/pages/Terms.jsx"
      via: "navigate(createPageUrl('Terms'))"
      pattern: "createPageUrl.*Terms"
---

<objective>
Créer les pages /Privacy (politique de confidentialité RGPD) et /Terms (CGU/CGV), les enregistrer dans le router, et ajouter les liens dans SettingsSection.

Purpose: Sans politique de confidentialité ni CGU, l'app ne peut pas être lancée légalement (RGPD Art. 13 + Stripe requirement). Bloquant P0.
Output: 2 nouvelles pages JSX, 2 routes, 2 liens dans Réglages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Design system : cream bg HSL(37,33%,95%) | forest #1A4D3E | emerald #2D9F82. ZERO orange. Tailwind + shadcn/ui.
Routing : Base44 utilise des routes nommées via `createPageUrl("NomPage")`. Pour enregistrer une nouvelle page, 2 étapes : (1) créer le fichier dans src/pages/, (2) l'ajouter dans src/pages.config.js (import lazy + ajout dans PAGES).
NE PAS modifier src/components/ui/.
</context>

<interfaces>
<!-- Imports nécessaires pour les nouvelles pages -->
From src/pages.config.js (pattern lazy page):
```js
const Privacy = lazy(() => import('./pages/Privacy'));
// puis dans PAGES: "Privacy": Privacy,
```

From src/components/profile/SettingsSection.jsx (pattern navigate):
```jsx
import { useNavigate } from "react-router-dom";
import { createPageUrl } from "@/utils";
// usage: navigate(createPageUrl("Privacy"))
```

From existing pages — pattern back button:
```jsx
import { ArrowLeft } from "lucide-react";
<button onClick={() => navigate(-1)} className="p-2 -ml-2">
  <ArrowLeft className="w-5 h-5 text-foreground" />
</button>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Créer Privacy.jsx et Terms.jsx</name>
  <files>src/pages/Privacy.jsx, src/pages/Terms.jsx</files>

  <read_first>
    - src/pages/Chat.jsx (lignes 1-20) — pattern import/structure d'une page secondaire
    - src/components/profile/SettingsSection.jsx (lignes 1-12) — imports disponibles
  </read_first>

  <action>
Créer `src/pages/Privacy.jsx` : page scrollable avec header "Politique de confidentialité" + bouton retour (ArrowLeft, navigate(-1)). Fond bg-background (cream). Contenu en français structuré en sections h2 :

1. **Responsable du traitement** — "PawCoach, contact : support@pawcoach.app"
2. **Données collectées** — email, prénom, données chien (race, poids, santé, photos), logs quotidiens, scans alimentaires, checkins bien-être
3. **Finalité du traitement** — personnalisation des conseils IA, suivi santé du chien, facturation (Stripe), emails de rappel
4. **Durée de conservation** — données actives jusqu'à suppression du compte. Suppression complète sur demande sous 30 jours.
5. **Vos droits** — accès, rectification, effacement, portabilité (export JSON dispo dans Réglages), opposition. Contact : support@pawcoach.app
6. **Cookies et analytics** — pas de cookies tiers. Événements stockés localement (localStorage) uniquement, non transmis à des tiers.
7. **Hébergement** — serveurs Base44 (Supabase/AWS), données chiffrées en transit (HTTPS) et au repos.
8. **Mise à jour** — date de dernière mise à jour : 27 mars 2026.

Style : `px-5 py-6 max-w-prose mx-auto space-y-6`. Titres `text-base font-bold text-foreground`. Corps `text-sm text-muted-foreground leading-relaxed`.

Créer `src/pages/Terms.jsx` : même structure (header + retour). Sections :

1. **Acceptation** — en utilisant PawCoach, vous acceptez les présentes CGU
2. **Description du service** — coach bien-être canin, pas de substitut vétérinaire
3. **Compte utilisateur** — 1 compte par email, responsabilité des credentials
4. **Abonnement Premium** — 7,99 EUR/mois ou 59,99 EUR/an, facturé via Stripe. Renouvellement automatique. Résiliation à tout moment depuis l'app (Profil > Réglages > Gérer mon abonnement).
5. **Essai gratuit** — 7 jours offerts, convertit automatiquement si non résilié avant expiration
6. **Contenu généré par l'IA** — à titre informatif uniquement, ne remplace pas un avis vétérinaire professionnel
7. **Propriété intellectuelle** — contenu PawCoach protégé, données utilisateur restent propriété de l'utilisateur
8. **Limitation de responsabilité** — PawCoach ne peut être tenu responsable de décisions médicales basées sur les conseils de l'app
9. **Loi applicable** — droit français, tribunal compétent : Paris
10. **Contact** — support@pawcoach.app

Style identique à Privacy.jsx.
  </action>

  <verify>
    <automated>grep -l "Responsable du traitement\|Données collectées" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Privacy.jsx && grep -l "Abonnement Premium\|Renouvellement automatique" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Terms.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Responsable du traitement" src/pages/Privacy.jsx` retourne 1 ligne
    - `grep "Renouvellement automatique" src/pages/Terms.jsx` retourne 1 ligne
    - `grep "navigate(-1)" src/pages/Privacy.jsx` retourne 1 ligne (bouton retour)
    - `grep "navigate(-1)" src/pages/Terms.jsx` retourne 1 ligne
  </acceptance_criteria>

  <done>Privacy.jsx et Terms.jsx existent avec contenu RGPD/CGU complet et bouton retour fonctionnel.</done>
</task>

<task type="auto">
  <name>Task 2: Enregistrer les routes + ajouter les liens dans SettingsSection</name>
  <files>src/pages.config.js, src/components/profile/SettingsSection.jsx</files>

  <read_first>
    - src/pages.config.js (lignes 1-98) — structure complète pour ajouter les imports lazy et les entrées PAGES
    - src/components/profile/SettingsSection.jsx (lignes 1-99) — section "Privacy" existante (ligne 53-66) à enrichir
  </read_first>

  <action>
**pages.config.js** — dans le bloc des pages secondaires lazy (après line 69 `const VetPortal`), ajouter :
```js
const Privacy = lazy(() => import('./pages/Privacy'));
const Terms = lazy(() => import('./pages/Terms'));
```
Dans l'objet `PAGES`, après `"VetPortal": VetPortal,` ajouter :
```js
"Privacy": Privacy,
"Terms": Terms,
```

**SettingsSection.jsx** — dans la section "Privacy" (bloc `{/* Privacy */}`, lignes 53-66), après le bouton "Supprimer mon compte", ajouter deux boutons navigate :
```jsx
<button
  onClick={() => navigate(createPageUrl("Privacy"))}
  className="flex items-center gap-2 text-sm text-primary py-1"
>
  <ShieldCheck className="w-3.5 h-3.5" />
  Politique de confidentialité
</button>
<button
  onClick={() => navigate(createPageUrl("Terms"))}
  className="flex items-center gap-2 text-sm text-primary py-1"
>
  <BookMarked className="w-3.5 h-3.5" />
  Conditions d'utilisation
</button>
```
Note : ShieldCheck et BookMarked sont déjà importés (ligne 7 de SettingsSection.jsx).
  </action>

  <verify>
    <automated>grep -c "Privacy" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages.config.js && grep "createPageUrl.*Privacy\|createPageUrl.*Terms" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/profile/SettingsSection.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Privacy" src/pages.config.js` retourne au moins 2 lignes (import + PAGES entry)
    - `grep "Terms" src/pages.config.js` retourne au moins 2 lignes
    - `grep "createPageUrl.*Privacy" src/components/profile/SettingsSection.jsx` retourne 1 ligne
    - `grep "createPageUrl.*Terms" src/components/profile/SettingsSection.jsx` retourne 1 ligne
  </acceptance_criteria>

  <done>Routes /Privacy et /Terms accessibles via navigate depuis SettingsSection. Pages chargées en lazy.</done>
</task>

</tasks>

<verification>
```bash
# Vérifier que les 2 pages existent
ls src/pages/Privacy.jsx src/pages/Terms.jsx

# Vérifier le contenu RGPD obligatoire
grep "Données collectées\|Vos droits\|portabilité\|support@pawcoach" src/pages/Privacy.jsx

# Vérifier contenu CGU obligatoire
grep "Renouvellement automatique\|Stripe\|résiliation\|vétérinaire" src/pages/Terms.jsx

# Vérifier les routes enregistrées
grep "Privacy\|Terms" src/pages.config.js

# Vérifier les liens dans SettingsSection
grep "createPageUrl.*Privacy\|createPageUrl.*Terms" src/components/profile/SettingsSection.jsx
```
</verification>

<success_criteria>
- Page /Privacy accessible depuis Profile > Réglages > "Politique de confidentialité"
- Page /Terms accessible depuis Profile > Réglages > "Conditions d'utilisation"
- Contenu RGPD Art. 13 complet dans Privacy.jsx (7 sections)
- Mention Stripe + renouvellement auto dans Terms.jsx
- Aucune erreur de routing (pages enregistrées dans pages.config.js)
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-legal-security/06-01-SUMMARY.md`
</output>
