---
phase: 10-performance-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - base44/functions/vetAccess/entry.ts
  - base44/functions/pawcoachChat/entry.ts
  - base44/functions/weeklyInsightGenerate/entry.ts
autonomous: true
requirements: [FIX-59, FIX-60, FIX-61]
must_haves:
  truths:
    - "buildHealthSummaryHTML a une complexite cyclomatique < 15 (etait 28)"
    - "getAge est identique dans pawcoachChat et weeklyInsightGenerate avec un commentaire de synchronisation"
    - "handleDownload est audite et ses blocs redondants sont extraits en sous-fonctions internes"
  artifacts:
    - path: "base44/functions/vetAccess/entry.ts"
      provides: "buildHealthSummaryHTML decompose en 4 sous-fonctions"
      contains: "function buildAlertSection"
    - path: "base44/functions/pawcoachChat/entry.ts"
      provides: "getAge avec commentaire sync"
      contains: "getAge sync"
    - path: "base44/functions/weeklyInsightGenerate/entry.ts"
      provides: "getAge avec commentaire sync"
      contains: "getAge sync"
  key_links:
    - from: "vetAccess buildHealthSummaryHTML"
      to: "sous-fonctions buildAlertSection, buildVaccineSection, buildWeightSection, buildMedSection"
      via: "appels internes dans buildHealthSummaryHTML"
      pattern: "function build.*Section"
---

<objective>
Reduire la dette technique sur les 3 hotspots backend identifies par CGC.

Purpose: buildHealthSummaryHTML (complexite 28) est fragile et difficile a maintenir — tout bug necessite de naviguer 90 lignes de HTML concatene. getAge dupliquee dans 2 backends cree un risque de divergence silencieuse. handleDownload avec 50 appels sortants merite un audit pour extraire les blocs logiques independants.

Output:
- vetAccess/entry.ts : buildHealthSummaryHTML decompose en 4 sous-fonctions thematiques
- pawcoachChat/entry.ts + weeklyInsightGenerate/entry.ts : getAge annote avec commentaire de sync
- DownloadHealthPDF.jsx : audit done, blocs logiques redondants extraits en helpers internes si applicable
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-radiographie/CGC-ANALYSIS-REPORT.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- getAge dans pawcoachChat/entry.ts (ligne 448) -->
<!-- getAge dans weeklyInsightGenerate/entry.ts (ligne 205) -->
<!-- Les deux implementations sont IDENTIQUES — retournent string|null, prennent birthDate: string -->
<!-- Exemple: getAge("2022-01-01") => "2 ans et 3 mois" | "5 mois" | "moins d'un mois" | null -->

<!-- buildHealthSummaryHTML dans vetAccess/entry.ts (ligne 13) -->
<!-- Signature: buildHealthSummaryHTML(dog: any, records: any[]) => string (HTML) -->
<!-- dog: { name, breed, birth_date, sex, neutered, allergies, health_issues } -->
<!-- records: HealthRecord[] avec type in ['vaccine', 'weight', 'vet_visit', 'medication'] -->
<!-- La fonction concatene du HTML via template literals avec if/else par section -->

<!-- CONTRAINTE BASE44 DENO : impossible de partager un fichier entre fonctions backend -->
<!-- Solution pour getAge : commentaire de sync (pattern recommande dans CONCERNS.md) -->
<!-- Solution pour buildHealthSummaryHTML : sous-fonctions dans le meme fichier -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose buildHealthSummaryHTML en sous-fonctions</name>
  <files>base44/functions/vetAccess/entry.ts</files>
  <read_first>base44/functions/vetAccess/entry.ts</read_first>
  <action>
Dans vetAccess/entry.ts, extraire le corps de buildHealthSummaryHTML (lignes 13-103) en 4 sous-fonctions thematiques declarees AVANT buildHealthSummaryHTML. Chaque sous-fonction prend les donnees dont elle a besoin et retourne un fragment HTML string.

Sous-fonctions a creer :

1. `function buildAlertSection(dog)` — retourne le bloc HTML "Alertes sante" (allergies + health_issues) ou '' si aucun. Correspond aux lignes 53-62 actuelles.

2. `function buildVaccineSection(vaccines)` — retourne le bloc HTML "Derniers vaccins" avec `vaccines.slice(0, 5)`. Retourne '' si vaccines.length === 0. Correspond aux lignes 64-74.

3. `function buildMedSection(meds)` — retourne le bloc HTML "Medicaments recents" avec `meds.slice(0, 5)`. Retourne '' si meds.length === 0. Correspond aux lignes 76-86.

4. `function buildWeightSection(weights)` — retourne le bloc HTML "Evolution du poids" avec `weights.slice(0, 6)` comme chips inline. Retourne '' si weights.length <= 1. Correspond aux lignes 88-96.

buildHealthSummaryHTML reste la fonction principale mais son corps appelle ces 4 sous-fonctions au lieu d'avoir tout inline :

```typescript
function buildHealthSummaryHTML(dog, records) {
  const safeRecords = records || [];
  const vaccines = safeRecords.filter(r => r.type === 'vaccine').sort((a,b) => new Date(b.date) - new Date(a.date));
  const weights = safeRecords.filter(r => r.type === 'weight').sort((a,b) => new Date(b.date) - new Date(a.date));
  const visits = safeRecords.filter(r => r.type === 'vet_visit').sort((a,b) => new Date(b.date) - new Date(a.date));
  const meds = safeRecords.filter(r => r.type === 'medication').sort((a,b) => new Date(b.date) - new Date(a.date));

  const age = dog.birth_date ? `${Math.floor((Date.now() - new Date(dog.birth_date)) / (365.25 * 24 * 60 * 60 * 1000))} ans` : 'Non renseigné';
  const lastWeight = weights[0];

  let html = `... header + stats table (inchange) ...`;

  html += buildAlertSection(dog);
  html += buildVaccineSection(vaccines);
  html += buildMedSection(meds);
  html += buildWeightSection(weights);

  html += `... footer (inchange) ...`;
  return html;
}
```

IMPORTANT : Ne modifier ni la logique HTML, ni les styles CSS inline, ni l'output. Seule la structure interne change. Le comportement de buildHealthSummaryHTML doit etre identique avant/apres.

Conserver escapeHtml() et formatDate() tels quels — elles sont reutilisees par les sous-fonctions.
  </action>
  <verify>
    <automated>grep -c "function build.*Section" "base44/functions/vetAccess/entry.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep "function build.*Section" vetAccess/entry.ts retourne 4 correspondances
    - grep "buildAlertSection\|buildVaccineSection\|buildMedSection\|buildWeightSection" vetAccess/entry.ts retourne au moins 8 lignes (4 definitions + 4 appels dans buildHealthSummaryHTML)
    - buildHealthSummaryHTML reste presente et appelle les 4 sous-fonctions
    - Aucune regression fonctionnelle : les actions 'invite' et 'getHealthSummary' appellent toujours buildHealthSummaryHTML avec les memes arguments
  </acceptance_criteria>
  <done>buildHealthSummaryHTML reduit a ~25 lignes, 4 sous-fonctions thematiques extraites, comportement identique</done>
</task>

<task type="auto">
  <name>Task 2: Annoter getAge avec commentaire de synchronisation</name>
  <files>base44/functions/pawcoachChat/entry.ts, base44/functions/weeklyInsightGenerate/entry.ts</files>
  <read_first>
    base44/functions/pawcoachChat/entry.ts (offset 445, limit 20)
    base44/functions/weeklyInsightGenerate/entry.ts (offset 202, limit 20)
  </read_first>
  <action>
CONTRAINTE : Base44 Deno ne peut pas importer de fichiers partages entre fonctions backend. La solution est un commentaire de synchronisation explicite sur chaque copie.

Dans pawcoachChat/entry.ts, localiser la fonction getAge (ligne ~448). Ajouter immediatement avant la definition :

```typescript
// getAge v1 — sync avec weeklyInsightGenerate/entry.ts — NE PAS modifier sans mettre a jour les deux
// Retourne: "moins d'un mois" | "N mois" | "N an(s)" | "N an(s) et N mois" | null
```

Dans weeklyInsightGenerate/entry.ts, localiser la fonction getAge (ligne ~205). Ajouter immediatement avant la definition :

```typescript
// getAge v1 — sync avec pawcoachChat/entry.ts — NE PAS modifier sans mettre a jour les deux
// Retourne: "moins d'un mois" | "N mois" | "N an(s)" | "N an(s) et N mois" | null
```

Verifier que les deux implementations sont effectivement identiques. Si une difference mineure existe (ex: commentaires internes, whitespace), harmoniser pour que le corps soit identique apres les commentaires.

NE PAS modifier la logique de calcul. NE PAS creer de fichier _shared/ (Base44 ne supporte pas les imports cross-function).
  </action>
  <verify>
    <automated>grep -n "getAge sync\|sync avec" "base44/functions/pawcoachChat/entry.ts" "base44/functions/weeklyInsightGenerate/entry.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep "sync avec" pawcoachChat/entry.ts retourne 1 ligne mentionnant weeklyInsightGenerate
    - grep "sync avec" weeklyInsightGenerate/entry.ts retourne 1 ligne mentionnant pawcoachChat
    - Les deux fonctions getAge ont le meme corps (meme logique months/years)
  </acceptance_criteria>
  <done>Les deux copies de getAge ont un commentaire de sync reciproque — tout futur changement est documente</done>
</task>

<task type="auto">
  <name>Task 3: Audit handleDownload — extraction blocs redondants</name>
  <files>src/components/vet/DownloadHealthPDF.jsx</files>
  <read_first>src/components/vet/DownloadHealthPDF.jsx</read_first>
  <action>
Lire DownloadHealthPDF.jsx en entier. handleDownload est une fonction PDF generator avec jsPDF. CGC comptait 50 appels sortants car jsPDF expose de nombreuses methodes (doc.text, doc.setFontSize, doc.rect...).

OBJECTIF : ce n'est PAS un God Function a eliminer — c'est un orchestrateur legitime. L'audit doit identifier les blocs qui se REPETENT et les extraire en helpers internes.

Analyser le code et identifier les patterns repetitifs. Typiquement dans un generateur PDF :
- Desensin d'en-tete de section (titre + separateur) — repete pour chaque categorie
- Rendu d'une ligne de tableau — repete pour chaque record
- Calcul de position Y avec pagination — repete entre sections

Pour chaque pattern repete 3+ fois : extraire en fonction locale `const drawXxx = (...) => { ... }` DANS le scope de handleDownload ou juste avant le return du composant.

Exemple de helper interne legitime :
```javascript
const drawSectionTitle = (title, yPos) => {
  doc.setFontSize(11);
  doc.setFont("helvetica", "bold");
  doc.text(title, 14, yPos);
  return yPos + 8;
};
```

Si des helpers similaires existent deja dans pdfHelpers.js (drawSectionHeader, drawTable), verifier qu'ils sont utilises. S'ils ne sont pas utilises alors qu'ils correspondent a un pattern repete dans handleDownload, les brancher.

NE PAS refactoriser la logique PDF elle-meme. NE PAS modifier le comportement de la fonction. NE PAS toucher aux imports.

Si apres lecture complete il n'y a pas de repetition significative (< 3 occurrences), documenter ce constat dans un commentaire en tete de handleDownload : `// Audit FIX-61: no extractable pattern — N outgoing calls are jsPDF API calls, not app logic duplication` et ne rien extraire.
  </action>
  <verify>
    <automated>grep -c "function draw\|const draw" src/components/vet/DownloadHealthPDF.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier a soit : (a) au moins 1 helper extrait ET le commentaire d'audit absent, ou (b) le commentaire d'audit "Audit FIX-61" present si aucune extraction necessaire
    - Aucune regression : handleDownload appelle toujours base44.functions.invoke("vetAccess", { action: "getHealthSummary", dogId }) en premier
    - Le comportement PDF est identique (pas de logique modifiee)
  </acceptance_criteria>
  <done>handleDownload audite, blocs redondants extraits ou absence de pattern documente</done>
</task>

</tasks>

<verification>
Depuis C:/Users/smalt/Desktop/app-chien-ia/pawcoach :

1. grep -c "function build.*Section" base44/functions/vetAccess/entry.ts
   Attendu : 4

2. grep "sync avec" base44/functions/pawcoachChat/entry.ts base44/functions/weeklyInsightGenerate/entry.ts
   Attendu : 2 lignes (une par fichier)

3. git diff --stat HEAD
   Attendu : 3 fichiers modifies (vetAccess, pawcoachChat, weeklyInsightGenerate) + eventuellement DownloadHealthPDF
</verification>

<success_criteria>
- buildHealthSummaryHTML complexite < 15 (decomposee en 4 sous-fonctions)
- getAge annotee dans les 2 backends avec commentaire de sync reciproque
- handleDownload audite avec extraction ou commentaire explicite
- Zero regression dans les actions vetAccess (invite, getHealthSummary, getDogData)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/10-performance-cleanup/10-01-SUMMARY.md`
</output>
