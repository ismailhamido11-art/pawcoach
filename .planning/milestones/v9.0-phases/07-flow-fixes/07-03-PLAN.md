---
phase: 07-flow-fixes
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/scan/LabelScanMode.jsx
  - src/pages/Chat.jsx
  - src/pages/Nutri.jsx
  - src/pages/Sante.jsx
autonomous: true
requirements:
  - FIX-20
  - FIX-25
  - FIX-26
must_haves:
  truths:
    - "Taper 'Nouvelle analyse' en mode label réaffiche le ModeSwitcher immédiatement"
    - "Les chips de suggestions Chat/NutriCoach ne peuvent pas être tapées pendant un chargement"
    - "Le poids du chien est mis à jour en mémoire dans Sante.jsx après ajout via SectionPoids"
  artifacts:
    - path: "src/components/scan/LabelScanMode.jsx"
      provides: "resetLabel appelle onLabelResult?.(null)"
      contains: "onLabelResult?.(null)"
    - path: "src/pages/Chat.jsx"
      provides: "showSuggestions inclut !loading && !isStreaming"
      contains: "!loading && !isStreaming"
    - path: "src/pages/Nutri.jsx"
      provides: "showQuickActions inclut !loading"
      contains: "showQuickActions.*loading"
    - path: "src/pages/Sante.jsx"
      provides: "callback SectionPoids met à jour dog.weight en mémoire"
      contains: "setDog.*weight"
  key_links:
    - from: "src/components/scan/LabelScanMode.jsx"
      to: "src/pages/Scan.jsx (parent)"
      via: "onLabelResult?.(null) dans resetLabel"
      pattern: "onLabelResult\\?\\."
    - from: "src/pages/Chat.jsx"
      to: "sendMessage"
      via: "showSuggestions bloqué pendant loading"
      pattern: "showSuggestions.*loading"
---

<objective>
Corriger une rupture P0 (LabelScanMode) et deux suspects critiques P1 (Chat suggestions, Sante weight sync).

FIX-20 : LabelScanMode.resetLabel() remet à zéro l'état interne mais n'appelle pas `onLabelResult?.(null)`. Résultat : Scan.jsx parent garde `labelResult` non-null → le ModeSwitcher reste caché après "Nouvelle analyse".

FIX-25 : Chat.jsx et Nutri.jsx — les chips de suggestions/quickActions sont affichées pendant que `loading=true`. Un double-tap rapide envoie 2 messages. Fix : ajouter `!loading && !isStreaming` dans la condition d'affichage.

FIX-26 : Sante.jsx — après ajout d'un poids via SectionPoids, `dog.weight` en mémoire n'est pas mis à jour (seul Dog en DB est mis à jour via `Dog.update` dans SectionPoids). Fix : passer un callback enrichi à SectionPoids qui appelle `setDog(prev => ({...prev, weight: rec.value}))`.

Purpose: Le flux de scan label est complet, les suggestions ne créent pas de doublon, le poids affiché est immédiatement correct.
Output: 4 fichiers corrigés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LabelScanMode — resetLabel doit notifier le parent</name>
  <files>src/components/scan/LabelScanMode.jsx</files>
  <read_first>
    Lire src/components/scan/LabelScanMode.jsx lignes 151-160.
    État actuel confirmé :
    ```javascript
    const resetLabel = () => {
      setLabelFile(null);
      setLabelPreview(null);
      setLabelResult(null);
      setShowIngredients(false);
      setLabelSaved(false);
    };
    ```
    `onLabelResult` est une prop (ligne 45 : `{ ..., onLabelResult }`).
    Elle est appelée dans analyzeLabel (ligne 117 : `onLabelResult?.(ai)`) pour cacher le ModeSwitcher.
    Elle n'est PAS appelée dans resetLabel → le parent (Scan.jsx) garde labelResult non-null.

    Scan.jsx (parent) : `const showModeSwitcher = !result && !scanLimitReached && !labelResult`
    Donc si `labelResult` dans Scan.jsx reste non-null → ModeSwitcher reste caché.
  </read_first>
  <action>
    Ajouter `onLabelResult?.(null);` à la fin de resetLabel :

    ```javascript
    const resetLabel = () => {
      setLabelFile(null);
      setLabelPreview(null);
      setLabelResult(null);
      setShowIngredients(false);
      setLabelSaved(false);
      onLabelResult?.(null); // Informe Scan.jsx parent → réaffiche ModeSwitcher
    };
    ```

    C'est le seul changement dans ce fichier — 1 ligne ajoutée à la fin du corps de resetLabel.
    NE PAS modifier analyzeLabel, saveLabelResult, ni le JSX.
  </action>
  <acceptance_criteria>
    - grep "onLabelResult?.(null)" src/components/scan/LabelScanMode.jsx retourne 1 résultat dans resetLabel
  </acceptance_criteria>
  <verify>
    <automated>grep -n "onLabelResult" src/components/scan/LabelScanMode.jsx</automated>
  </verify>
  <done>`onLabelResult?.(null)` est présent dans resetLabel. Le pattern existe 2 fois : dans analyzeLabel (avec le résultat) et dans resetLabel (avec null).</done>
</task>

<task type="auto">
  <name>Task 2: Chat — désactiver suggestions pendant loading/streaming</name>
  <files>src/pages/Chat.jsx</files>
  <read_first>
    Lire src/pages/Chat.jsx lignes 335-345 et lignes 549-565.
    État actuel confirmé :
    - Ligne 338 : `const showSuggestions = messages.length <= 1 && !isLimitReached;`
    - Ligne 549 : `{showSuggestions && (` — les chips sont visibles même quand loading=true
    - Ligne 403 : `{messages.length <= 1 && !loading && !isStreaming && (` — ce bloc-là est protégé, mais pas les suggestions

    Le problème : showSuggestions ne filtre pas `loading` ni `isStreaming`. Un double-tap rapide sur un chip envoie 2 messages car `sendMessage` ne guard pas sur `loading` (le guard est sur `!content || !dog || loading` — mais si loading démarre APRÈS le premier tap, le second tap arrive avant que loading soit true).
  </read_first>
  <action>
    Modifier la ligne de définition de showSuggestions pour inclure le check loading :

    Changer :
    ```javascript
    const showSuggestions = messages.length <= 1 && !isLimitReached;
    ```
    en :
    ```javascript
    const showSuggestions = messages.length <= 1 && !isLimitReached && !loading && !isStreaming;
    ```

    C'est le seul changement nécessaire — 1 ligne modifiée.
    NE PAS modifier sendMessage, le formulaire d'envoi, ni le reste du composant.
  </action>
  <acceptance_criteria>
    - grep "showSuggestions" src/pages/Chat.jsx | grep "loading" retourne 1 résultat
  </acceptance_criteria>
  <verify>
    <automated>grep -n "showSuggestions" src/pages/Chat.jsx</automated>
  </verify>
  <done>showSuggestions contient `!loading && !isStreaming`. Les chips disparaissent dès que loading passe à true.</done>
</task>

<task type="auto">
  <name>Task 3: Nutri — désactiver quickActions pendant loading + Sante weight sync</name>
  <files>src/pages/Nutri.jsx, src/pages/Sante.jsx</files>
  <read_first>
    Nutri.jsx ligne 387 : `const showQuickActions = messages.length <= 1 && !isLimitReached;`
    Même problème que Chat.jsx — pas de check loading.

    Sante.jsx : chercher la prop `onRecordAdded` passée à SectionPoids.
    État actuel confirmé (ligne ~425 de NotebookContent.jsx) :
    `onRecordAdded={(rec) => setRecords(prev => [...prev, rec])}`
    Ce callback n'appelle pas `setDog`. Or SectionPoids fait `Dog.update(dogId, { weight: w })` en DB
    mais `dog.weight` dans Sante.jsx reste l'ancienne valeur jusqu'au prochain rechargement.

    Chercher dans Sante.jsx comment NotebookContent reçoit ses props et comment setDog est exposé.
    Chercher aussi `handleAddFromSheet` et `handleGrowthAdded` pour voir comment ils mettent à jour dog.
  </read_first>
  <action>
    **Nutri.jsx** — Modifier showQuickActions (même fix que Chat.jsx) :
    ```javascript
    const showQuickActions = messages.length <= 1 && !isLimitReached && !loading;
    ```
    Note : Nutri.jsx n'a pas de variable `isStreaming` distincte — utiliser uniquement `!loading`.
    Vérifier le nom exact de la variable de loading dans Nutri.jsx avant de modifier.

    **Sante.jsx** — Passer un callback enrichi à NotebookContent pour SectionPoids :

    Chercher dans Sante.jsx comment NotebookContent est rendu et quelles props il reçoit.
    Chercher la prop `onRecordAdded` ou une prop équivalente passée à SectionPoids via NotebookContent.

    Dans NotebookContent.jsx, SectionPoids est rendu avec :
    `onRecordAdded={(rec) => setRecords(prev => [...prev, rec])}`

    SectionPoids retourne un HealthRecord avec `type: "weight"` et `value: w` (le poids en kg).

    Dans Sante.jsx, passer une prop `onWeightAdded` à NotebookContent, ou créer un callback spécifique pour SectionPoids. L'approche la plus simple :

    Dans NotebookContent.jsx (ce fichier sera lu pour comprendre sa structure), chercher si NotebookContent accepte une prop `onWeightAdded` ou s'il faut ajouter un callback inline sur SectionPoids.

    La solution propre : dans NotebookContent.jsx, modifier la prop `onRecordAdded` de SectionPoids pour également mettre à jour le chien si c'est un record de poids. Passer `onWeightAdded` comme prop de NotebookContent vers Sante.jsx.

    Alternative plus simple sans modifier NotebookContent : dans Sante.jsx, créer un handler `handleRecordAddedFromNotebook` qui check si le record est de type weight et met à jour dog.

    Implémentation recommandée dans Sante.jsx :
    1. Créer une fonction `handleWeightAdded(weightKg)` :
       ```javascript
       const handleWeightAdded = (weightKg) => {
         if (weightKg) setDog(prev => prev ? { ...prev, weight: weightKg } : prev);
       };
       ```
    2. Passer `onWeightAdded={handleWeightAdded}` à NotebookContent.
    3. Dans NotebookContent.jsx, lire la prop `onWeightAdded` et l'appeler dans SectionPoids :
       - Dans le callback `onRecordAdded` de SectionPoids, si `rec.type === "weight"` → appeler `onWeightAdded?.(rec.value)`

    Lire NotebookContent.jsx et Sante.jsx avant d'implémenter pour confirmer la structure exacte.
  </action>
  <acceptance_criteria>
    - grep "showQuickActions" src/pages/Nutri.jsx | grep "loading" retourne 1 résultat
    - grep "setDog.*weight\|weight.*setDog" src/pages/Sante.jsx retourne >= 1 résultat (hors GrowthTracker)
    - grep "onWeightAdded\|handleWeightAdded" src/pages/Sante.jsx retourne >= 1 résultat
  </acceptance_criteria>
  <verify>
    <automated>grep -n "showQuickActions\|loading" src/pages/Nutri.jsx | grep -E "showQuick|loading" | head -5
grep -n "weight\|setDog\|handleWeight" src/pages/Sante.jsx | grep -i "weight\|setDog" | head -10</automated>
  </verify>
  <done>showQuickActions inclut !loading dans Nutri.jsx. Dans Sante.jsx, le dog.weight en mémoire est mis à jour immédiatement après ajout d'une pesée via SectionPoids sans attendre un rechargement de page.</done>
</task>

</tasks>

<verification>
```bash
# FIX-20 : resetLabel notifie le parent
grep "onLabelResult?.(null)" src/components/scan/LabelScanMode.jsx && echo "OK FIX-20" || echo "FAIL FIX-20"

# FIX-25 : suggestions désactivées pendant loading
grep "showSuggestions" src/pages/Chat.jsx | grep "loading" && echo "OK Chat FIX-25" || echo "FAIL Chat FIX-25"
grep "showQuickActions" src/pages/Nutri.jsx | grep "loading" && echo "OK Nutri FIX-25" || echo "FAIL Nutri FIX-25"

# FIX-26 : weight sync
grep "handleWeightAdded\|onWeightAdded" src/pages/Sante.jsx && echo "OK FIX-26" || echo "FAIL FIX-26"
```
</verification>

<success_criteria>
- `onLabelResult?.(null)` dans resetLabel de LabelScanMode.jsx
- `showSuggestions` dans Chat.jsx contient `!loading && !isStreaming`
- `showQuickActions` dans Nutri.jsx contient `!loading`
- `setDog` est appelé avec le nouveau poids dans Sante.jsx après ajout SectionPoids
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-flow-fixes/07-03-SUMMARY.md` avec :
- Fichiers modifiés et lignes exactes avant/après pour chaque fix
- Confirmation de la structure du callback Sante → NotebookContent → SectionPoids
- Si NotebookContent.jsx a aussi été modifié, le noter avec les lignes touchées
</output>
