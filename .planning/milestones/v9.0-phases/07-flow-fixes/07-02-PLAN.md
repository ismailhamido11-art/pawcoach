---
phase: 07-flow-fixes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/WalkReminderSettings.jsx
  - src/pages/Activite.jsx
  - src/components/sante/NotebookContent.jsx
autonomous: true
requirements:
  - FIX-16
  - FIX-17
  - FIX-18
must_haves:
  truths:
    - "Toggler le rappel balade affiche un toast d'erreur si l'API échoue et rollback l'état UI"
    - "Supprimer un pseudo-record GrowthEntry (ge-*) dans le carnet santé ne déclenche pas d'appel API"
    - "Pull-to-refresh sur Activite ne plante pas silencieusement sur erreur réseau"
  artifacts:
    - path: "src/components/profile/WalkReminderSettings.jsx"
      provides: "handleToggle et handleTimeChange avec rollback et toast.error"
      contains: "rollback\|setEnabled(prev"
    - path: "src/components/sante/NotebookContent.jsx"
      provides: "Guard ge-* dans handleDelete"
      contains: "ge-"
    - path: "src/pages/Activite.jsx"
      provides: "refreshLogs avec try/catch"
      contains: "toast.error"
  key_links:
    - from: "src/components/profile/WalkReminderSettings.jsx"
      to: "onSave (base44.auth.updateMe)"
      via: "try/catch avec rollback sur erreur"
      pattern: "prevEnabled\|prevTime"
    - from: "src/components/sante/NotebookContent.jsx"
      to: "HealthRecord.delete"
      via: "guard ge- avant l'appel API"
      pattern: "startsWith\\(\"ge-\""
    - from: "src/pages/Activite.jsx"
      to: "DailyLog.filter"
      via: "try/catch dans refreshLogs"
      pattern: "toast.error.*refreshLogs\|catch.*refreshLogs"
---

<objective>
Corriger trois ruptures P0 dans la zone Santé/Activité/Profile (fichiers sans intersection).

FIX-16 : WalkReminderSettings.handleToggle et handleTimeChange ont un try/catch mais le catch est vide (juste `finally`). Si `onSave` échoue : l'état UI (enabled/time) est déjà mis à jour mais le backend ne l'est pas. Aucun signal à l'utilisateur, aucun rollback.

FIX-17 : NotebookContent.handleDelete filtre les pseudo-records `dl-` mais pas `ge-` (GrowthEntry). Résultat : tenter `HealthRecord.delete("ge-xxxx")` échoue avec erreur API.

FIX-18 : Activite.refreshLogs appelle `DailyLog.filter` sans try/catch. Une erreur réseau fait planter le PullToRefresh silencieusement.

Purpose: Zéro état incohérent UI/backend, zéro appel API invalide, zéro crash silencieux.
Output: 3 fichiers corrigés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WalkReminderSettings — rollback + toast.error sur échec API</name>
  <files>src/components/profile/WalkReminderSettings.jsx</files>
  <read_first>
    Lire src/components/profile/WalkReminderSettings.jsx en entier.
    État actuel confirmé (lignes 17-37) :
    - handleToggle : setEnabled(val) PUIS try { await onSave(...) } finally { setSaving(false) }
    - handleTimeChange : setTime(val) PUIS try { await onSave(...) } finally { setSaving(false) }
    - Le catch est ABSENT — seul finally existe. Si onSave échoue, l'erreur est avalée silencieusement.
    - L'état UI (enabled, time) a déjà été mis à jour optimistement AVANT l'appel API.
  </read_first>
  <action>
    Modifier handleToggle et handleTimeChange pour ajouter rollback + toast.error :

    ```javascript
    const handleToggle = async (val) => {
      const prevEnabled = enabled;
      setEnabled(val);
      setSaving(true);
      try {
        await onSave({ walk_reminder_enabled: val, walk_reminder_time: time });
        toast.success(val ? `Rappel activé à ${time}` : "Rappel désactivé");
      } catch {
        setEnabled(prevEnabled); // rollback
        toast.error("Impossible de modifier le rappel. Réessaie.");
      } finally {
        setSaving(false);
      }
    };

    const handleTimeChange = async (val) => {
      const prevTime = time;
      setTime(val);
      if (!enabled) return;
      setSaving(true);
      try {
        await onSave({ walk_reminder_time: val, walk_reminder_enabled: true });
        toast.success(`Rappel mis à jour à ${val}`);
      } catch {
        setTime(prevTime); // rollback
        toast.error("Impossible de modifier l'heure. Réessaie.");
      } finally {
        setSaving(false);
      }
    };
    ```

    Points clés :
    - Sauvegarder la valeur précédente AVANT setEnabled/setTime
    - catch = rollback + toast.error (jamais silencieux)
    - finally = setSaving(false) (inchangé)
    - NE PAS modifier le JSX, les imports, ni les props
  </action>
  <acceptance_criteria>
    - grep "prevEnabled" src/components/profile/WalkReminderSettings.jsx retourne >= 1 résultat
    - grep "prevTime" src/components/profile/WalkReminderSettings.jsx retourne >= 1 résultat
    - grep "catch" src/components/profile/WalkReminderSettings.jsx retourne >= 2 blocs catch (un par handler)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "prevEnabled\|prevTime\|catch\|rollback" src/components/profile/WalkReminderSettings.jsx</automated>
  </verify>
  <done>handleToggle et handleTimeChange ont chacun un catch avec rollback (setEnabled/setTime) et toast.error. L'état UI revient à la valeur précédente si l'API échoue.</done>
</task>

<task type="auto">
  <name>Task 2: NotebookContent — ajouter guard ge-* dans handleDelete</name>
  <files>src/components/sante/NotebookContent.jsx</files>
  <read_first>
    Lire src/components/sante/NotebookContent.jsx lignes 116-125.
    État actuel confirmé :
    ```javascript
    const handleDelete = async (id) => {
      if (typeof id === "string" && id.startsWith("dl-")) return;
      // ... HealthRecord.delete(id) ...
    };
    ```
    Les pseudo-records GrowthEntry ont le format "ge-{uuid}" (ligne 172 : `id: \`ge-${g.id}\``).
    Le guard dl- empêche la suppression des pseudo-records DailyLog mais pas des GrowthEntry.
  </read_first>
  <action>
    Ajouter le guard ge-* juste après le guard dl-* existant :

    Changer :
    ```javascript
    if (typeof id === "string" && id.startsWith("dl-")) return;
    ```
    en :
    ```javascript
    if (typeof id === "string" && (id.startsWith("dl-") || id.startsWith("ge-"))) return;
    ```

    C'est le seul changement nécessaire dans ce fichier — 1 ligne modifiée.
    NE PAS modifier le reste de handleDelete ni aucune autre fonction.
    NE PAS modifier src/components/ui/*.
  </action>
  <acceptance_criteria>
    - grep "ge-" src/components/sante/NotebookContent.jsx retourne >= 2 résultats (ligne guard + ligne création pseudo-record)
    - grep "startsWith(\"ge-\")" src/components/sante/NotebookContent.jsx retourne 1 résultat
  </acceptance_criteria>
  <verify>
    <automated>grep -n "startsWith\|ge-\|dl-" src/components/sante/NotebookContent.jsx | head -10</automated>
  </verify>
  <done>La ligne de guard contient `id.startsWith("ge-")`. Supprimer un enregistrement GrowthEntry pseudo-record retourne immédiatement sans appel API.</done>
</task>

<task type="auto">
  <name>Task 3: Activite refreshLogs — wraper dans try/catch avec toast.error</name>
  <files>src/pages/Activite.jsx</files>
  <read_first>
    Lire src/pages/Activite.jsx lignes 89-98.
    État actuel confirmé :
    ```javascript
    const refreshLogs = async () => {
      if (!dog || !user) return;
      const l = await DailyLog.filter({ dog_id: dog.id }, "-date", 30);
      setLogs(l || []);
      invalidateHome();
    };
    ```
    Pas de try/catch. Si DailyLog.filter échoue (erreur réseau), l'exception remonte non catchée dans le PullToRefresh.
  </read_first>
  <action>
    Entourer le corps de refreshLogs dans un try/catch :

    ```javascript
    const refreshLogs = async () => {
      if (!dog || !user) return;
      try {
        const l = await DailyLog.filter({ dog_id: dog.id }, "-date", 30);
        setLogs(l || []);
        invalidateHome();
      } catch {
        toast.error("Impossible de rafraîchir les activités. Vérifie ta connexion.");
      }
    };
    ```

    Vérifier que `toast` est bien importé dans Activite.jsx (chercher `import { toast }` en tête du fichier). Si absent, ajouter `import { toast } from "sonner";` avec les autres imports.
    NE PAS modifier PullToRefresh ni d'autres handlers dans Activite.jsx.
  </action>
  <acceptance_criteria>
    - grep -A5 "refreshLogs" src/pages/Activite.jsx montre un try/catch
    - grep "toast.error" src/pages/Activite.jsx retourne >= 1 résultat dans refreshLogs
  </acceptance_criteria>
  <verify>
    <automated>grep -n "try\|catch\|toast\|refreshLogs" src/pages/Activite.jsx | head -20</automated>
  </verify>
  <done>refreshLogs est protégé par try/catch. Une erreur réseau affiche un toast.error informatif sans crasher le PullToRefresh.</done>
</task>

</tasks>

<verification>
```bash
# FIX-16 : rollbacks présents
grep "prevEnabled\|prevTime" src/components/profile/WalkReminderSettings.jsx && echo "OK rollbacks" || echo "FAIL"

# FIX-17 : guard ge- présent
grep "startsWith(\"ge-\")" src/components/sante/NotebookContent.jsx && echo "OK guard" || echo "FAIL"

# FIX-18 : try/catch dans refreshLogs
grep -A8 "refreshLogs" src/pages/Activite.jsx | grep "try" && echo "OK try/catch" || echo "FAIL"

# Vérification qu'aucune modification des UI components
grep -r "startsWith" src/components/ui/ 2>/dev/null | head -3
```
</verification>

<success_criteria>
- WalkReminderSettings : catch non vide avec rollback + toast.error dans handleToggle et handleTimeChange
- NotebookContent : guard `id.startsWith("ge-")` présent sur la même ligne que le guard `dl-`
- Activite : refreshLogs protégé try/catch avec toast.error
- 0 modification de src/components/ui/
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-flow-fixes/07-02-SUMMARY.md` avec :
- Les 3 fichiers modifiés et les lignes exactes avant/après
- Confirmation guard ge- sur NotebookContent
- Confirmation toast import dans Activite.jsx (existait ou ajouté)
</output>
