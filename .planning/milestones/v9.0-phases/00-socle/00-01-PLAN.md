---
phase: 00-socle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [SOCLE-01, SOCLE-02]
must_haves:
  truths:
    - "CGC est re-indexe sur le codebase post-v8.0 sans erreur"
    - "Les 7 docs architecture dans .planning/codebase/ sont a jour (contenu reflète v8.0 changes)"
  artifacts:
    - path: ".planning/codebase/STACK.md"
      provides: "Stack description updated post-v8.0"
      contains: "React"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Architecture description updated"
      contains: "components"
---

<objective>
Re-indexer CGC et rafraichir les 7 docs architecture pour que tous les audits v9.0 partent de donnees fraiches (post-v8.0, 30 fichiers changes).

Purpose: Sans donnees fraiches, les audits Radiographie/Architecture/Flux/Qualite travaillent sur des infos perimees.
Output: CGC index a jour + 7 docs .planning/codebase/ rafraichis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-indexer CGC sur le codebase post-v8.0</name>
  <files>CGC index (in-memory, not a file)</files>
  <read_first>
    - ./CLAUDE.md (CGC instructions)
  </read_first>
  <action>
Depuis le repertoire `C:/Users/smalt/Desktop/app-chien-ia/pawcoach`, executer :

```bash
cgc index .
```

Verifier que l'indexation se termine sans erreur. Puis valider avec :

```bash
cgc analyze dead-code | head -20
cgc analyze complexity | head -10
```

Si les deux commandes retournent des resultats, l'index est a jour.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && cgc find type function | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cgc index .` termine sans erreur
    - `cgc find type function` retourne > 100 fonctions (codebase PawCoach)
    - `cgc analyze dead-code` retourne des resultats (pas d'erreur)
  </acceptance_criteria>
  <done>CGC re-indexe sur le codebase post-v8.0</done>
</task>

<task type="auto">
  <name>Task 2: Rafraichir les 7 docs architecture via /gsd:map-codebase</name>
  <files>.planning/codebase/*.md</files>
  <read_first>
    - .planning/codebase/STACK.md (current state to compare)
  </read_first>
  <action>
Lancer `/gsd:map-codebase` qui produit 7 docs dans `.planning/codebase/` :
- STACK.md
- ARCHITECTURE.md
- STRUCTURE.md
- CONVENTIONS.md
- TESTING.md
- INTEGRATIONS.md
- CONCERNS.md

Chaque doc est genere par un agent specialise qui scanne le code. Le resultat doit refleter les changements v8.0 (30 fichiers, SmartAlerts, healthStatus, NutritionMealPlan, etc.).

Alternative si map-codebase n'est pas disponible en contexte subagent : lire manuellement les fichiers cles et mettre a jour chaque doc.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && ls -la .planning/codebase/*.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 7 fichiers .md existent dans .planning/codebase/
    - STACK.md contient "React 18" et "Vite"
    - ARCHITECTURE.md mentionne au moins 5 pages
    - Chaque fichier a ete modifie (date > debut v9.0)
  </acceptance_criteria>
  <done>7 docs architecture rafraichis avec donnees post-v8.0</done>
</task>

</tasks>

<verification>
1. `cgc find type function | wc -l` retourne > 100
2. `ls .planning/codebase/*.md | wc -l` retourne 7
3. Les docs reflètent les changements v8.0 (SmartAlerts, healthStatus, NutritionMealPlan, etc.)
</verification>

<success_criteria>
- CGC indexe sans erreur, commandes d'analyse fonctionnelles
- 7 docs architecture presents et a jour
</success_criteria>

<output>
After completion, create `.planning/phases/00-socle/00-01-SUMMARY.md`
</output>
