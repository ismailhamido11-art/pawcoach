---
phase: 09-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Sante.jsx
  - src/pages/Profile.jsx
  - src/pages/Home.jsx
  - src/pages/Dashboard.jsx
  - src/components/achievements/AchievementFeed.jsx
  - src/components/activite/AITrainingProgram.jsx
  - src/components/activite/CompletionCard.jsx
autonomous: true
requirements:
  - FIX-47
  - FIX-48
  - FIX-49

must_haves:
  truths:
    - "grep -n 'px-4' src/pages/Sante.jsx retourne 0 lignes dans les wrappers de contenu de tabs"
    - "Profile.jsx utilise space-y-5, Home.jsx utilise space-y-5, Dashboard.jsx garde space-y-5"
    - "Les tailles custom text-[10px] et text-[13px] remplacees par text-[11px] (caption) ou text-xs (detail)"
  artifacts:
    - path: src/pages/Sante.jsx
      provides: px-4 -> px-5 dans les 4 wrappers de tab content
    - path: src/pages/Profile.jsx
      provides: space-y-4 -> space-y-5 pour le rythme inter-sections
  key_links:
    - from: Sante.jsx tab content wrapper
      to: alignement visuel avec header px-5
      via: className px-5
      pattern: "px-5 pt-4"
---

<objective>
Standardiser le padding horizontal (px-5) et le rythme inter-sections (space-y-5) sur toutes les pages principales. Rationaliser les tailles de police custom vers 3 niveaux.

Purpose: Le saut de 4px (px-4 vs px-5) est visible sur mobile lors de la navigation entre tabs dans Sante. Le rhythm inconstant (space-y-4/5/6) fragmente la perception de coherence.
Output: Pages Sante, Profile, Home, Dashboard alignees sur px-5 / space-y-5. Tailles custom reduites a 3 niveaux : text-[11px] (caption), text-xs (detail 12px), text-sm (body 14px).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md

Regles de standardisation (per FIX-47, FIX-48, FIX-49) :

**FIX-47 — Page padding :**
- Standard cible : px-5 (20px) pour TOUT le padding horizontal de page et de tab content
- Sante.jsx : lignes 226, 269, 300, 316 utilisent px-4 -> remplacer par px-5
- Home.jsx, Dashboard.jsx, Profile.jsx : deja sur px-5, verifier et ne pas casser

**FIX-48 — Section spacing :**
- Standard cible : space-y-5 pour l'espacement inter-sections
- Profile.jsx:131 : space-y-4 -> space-y-5
- Home.jsx:535 : space-y-6 -> space-y-5
- Dashboard.jsx:275 : deja space-y-5, ne pas changer

**FIX-49 — Font sizes :**
- 3 niveaux autorises : text-[11px] (caption, 10-11px), text-xs (detail, 12px), text-sm (body, 14px)
- Remplacements :
  - text-[10px] -> text-[11px]
  - text-[12px] -> text-xs (equivalent exact)
  - text-[13px] -> text-xs (arrondi au plus proche)
  - text-[14px] -> text-sm (equivalent exact)
  - text-[15px] -> text-sm (arrondi au plus proche)
- NE PAS toucher text-[11px] (deja dans la cible)
- Fichiers concernes : AchievementFeed.jsx, AITrainingProgram.jsx, CompletionCard.jsx
- NE PAS toucher les autres fichiers pour FIX-49 (scope controle)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page padding px-5 + section spacing space-y-5 (FIX-47, FIX-48)</name>
  <files>
    src/pages/Sante.jsx,
    src/pages/Profile.jsx,
    src/pages/Home.jsx
  </files>
  <read_first>
    Avant de modifier :
    - Sante.jsx : chercher toutes les occurrences de "px-4" — cibles confirmees : lignes 226, 269, 300, 316 (wrappers de tab content). Verifier qu'il n'y a pas d'autres px-4 qui seraient intentionnels (ex: padding interne de composant).
    - Profile.jsx:131 : chercher "space-y-4" dans le wrapper principal.
    - Home.jsx:535 : chercher "space-y-6" dans le wrapper principal du feed.
    - Dashboard.jsx: LIRE mais NE PAS MODIFIER (deja px-5 space-y-5).

    IMPORTANT : ne modifier QUE les wrappers de niveau page/section, pas les paddings internes de composants (cards, boutons, inputs).
  </read_first>
  <action>
    **Sante.jsx — FIX-47 :**
    Lire le fichier. Identifier les 4 wrappers de contenu de tabs avec px-4.
    Remplacer "px-4 pt-4 pb-2" par "px-5 pt-4 pb-2" (3 occurrences au moins).
    Remplacer "px-4 pt-4 space-y-3" par "px-5 pt-4 space-y-3".
    Remplacer "px-4 pb-4" par "px-5 pb-4".
    Ne toucher QUE ces patterns. Ne pas modifier les paddings internes des cards ou composants imbriques.

    **Profile.jsx — FIX-48 :**
    Lire le fichier. Trouver le wrapper principal avec "space-y-4".
    Remplacer "space-y-4" par "space-y-5" sur le wrapper de contenu principal (ligne ~131).
    Ne pas toucher les space-y internes aux sections.

    **Home.jsx — FIX-48 :**
    Lire le fichier. Trouver le wrapper avec "space-y-6" (ligne ~535).
    Remplacer "space-y-6" par "space-y-5" sur ce wrapper.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "px-4 pt-4\|px-4 pb-4\|px-4 pt-4 space" src/pages/Sante.jsx 2>/dev/null | wc -l</automated>
    Attendu : 0

    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "space-y-4" src/pages/Profile.jsx 2>/dev/null | head -5</automated>
    Attendu : 0 ligne sur le wrapper principal (peut rester dans des sous-composants)
  </verify>
  <done>
    Sante.jsx : tous les wrappers de tab content passent de px-4 a px-5.
    Profile.jsx : le wrapper principal passe de space-y-4 a space-y-5.
    Home.jsx : le wrapper feed passe de space-y-6 a space-y-5.
    Navigation entre tabs : plus de saut de 4px horizontal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rationaliser font sizes custom (FIX-49)</name>
  <files>
    src/components/achievements/AchievementFeed.jsx,
    src/components/activite/AITrainingProgram.jsx,
    src/components/activite/CompletionCard.jsx
  </files>
  <read_first>
    Occurrences confirmees par grep dans ces 3 fichiers :
    - AchievementFeed.jsx : text-[11px] (deja OK, ne pas toucher)
    - AITrainingProgram.jsx : nombreux text-[11px] (OK), verifier si text-[10px]/[12px]/[13px]/[14px]/[15px] presents
    - CompletionCard.jsx : text-[11px] (OK, ne pas toucher)

    Lire chaque fichier et inventorier EXACTEMENT les tailles presentes autres que text-[11px].
    Appliquer uniquement la table de mapping definie dans le contexte.
    Ne pas modifier text-[11px] (deja dans la cible).
  </read_first>
  <action>
    Pour chaque fichier, lire puis appliquer la table de mapping :
    - text-[10px] -> text-[11px]
    - text-[12px] -> text-xs
    - text-[13px] -> text-xs
    - text-[14px] -> text-sm
    - text-[15px] -> text-sm

    Si aucune de ces tailles n'est presente dans un fichier, ne pas le modifier.
    Ne pas toucher text-[11px], text-xs, text-sm, text-base (deja dans la cible ou scale standard).

    Scope strict : UNIQUEMENT ces 3 fichiers. Ne pas etendre a d'autres composants dans ce plan.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "text-\[1[0-9]px\]" src/components/achievements/AchievementFeed.jsx src/components/activite/AITrainingProgram.jsx src/components/activite/CompletionCard.jsx 2>/dev/null | grep -v "text-\[11px\]"</automated>
    Attendu : 0 ligne (seul text-[11px] est autorise dans ces fichiers)
  </verify>
  <done>
    Les 3 fichiers n'utilisent plus que 3 tailles : text-[11px] (caption), text-xs (detail), text-sm (body).
    Elimination de text-[10px], text-[12px], text-[13px], text-[14px], text-[15px] dans ces composants.
  </done>
</task>

</tasks>

<verification>
Apres execution :
1. `grep -n "px-4 pt-4\|px-4 pb-4" src/pages/Sante.jsx` -> 0 resultats
2. `grep -n "space-y-4" src/pages/Profile.jsx` -> 0 sur le wrapper principal
3. `grep -n "text-\[1[0-9]px\]" src/components/achievements/AchievementFeed.jsx src/components/activite/AITrainingProgram.jsx src/components/activite/CompletionCard.jsx | grep -v "text-\[11px\]"` -> 0 resultats
</verification>

<success_criteria>
- Navigation Sante : les tabs content sont alignes avec le header (px-5 partout)
- Profile : le rythme inter-sections est uniforme (space-y-5)
- Les 3 composants cibles n'utilisent que 3 niveaux de taille de police
</success_criteria>

<output>
Apres completion, creer `.planning/phases/09-visual-polish/09-02-SUMMARY.md`
</output>
