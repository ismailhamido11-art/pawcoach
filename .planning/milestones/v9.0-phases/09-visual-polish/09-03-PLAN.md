---
phase: 09-visual-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/animations.js
  - src/components/BottomNav.jsx
  - src/pages/Profile.jsx
  - src/pages/Premium.jsx
  - src/pages/DogProfile.jsx
  - src/pages/Onboarding.jsx
autonomous: true
requirements:
  - FIX-50
  - FIX-51
  - FIX-57

must_haves:
  truths:
    - "animations.js contient springTab (stiffness:500, damping:35) et springSnappy (stiffness:400, damping:30) comme presets nommes"
    - "BottomNav.jsx importe springTab depuis animations.js au lieu d'inline spring"
    - "Profile.jsx, Premium.jsx, DogProfile.jsx, Onboarding.jsx importent et utilisent useReducedMotion()"
    - "staggerDelay: 0.05 est defini dans animations.js"
  artifacts:
    - path: src/lib/animations.js
      provides: springTab, staggerDelay exports
    - path: src/components/BottomNav.jsx
      provides: import springTab, plus de inline stiffness:500
  key_links:
    - from: animations.js springTab export
      to: BottomNav.jsx transition
      via: import { springTab } from "@/lib/animations"
      pattern: "springTab"
---

<objective>
Centraliser les presets d'animation dans animations.js et etendre la gestion prefers-reduced-motion aux pages qui l'ignoraient.

Purpose: Les valeurs spring inline (stiffness:500, damping:35) dans BottomNav.jsx divergent des presets centralises. Les 4 pages Profile, Premium, DogProfile, Onboarding lancent 102 animations sans respecter l'accessibilite OS.
Output: animations.js avec 2 nouveaux presets (springTab, staggerDelay). BottomNav importe springTab. 4 pages avec useReducedMotion() guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md

Etat actuel confirme par grep :

**FIX-50 — Spring inline dans BottomNav.jsx:103 :**
`transition={{ type: "spring", stiffness: 500, damping: 35 }}`
Ce sont des valeurs intentionnellement plus rapides que le spring standard (360/28).
Solution : ajouter `springTab` dans animations.js avec ces valeurs exactes, puis importer.

**FIX-51 — Stagger delays :**
Dashboard.jsx:290 -> delay: i * 0.07
Dashboard.jsx:423 -> delay: i * 0.06
Profile.jsx:119 -> delay: i * 0.05
Premium.jsx:444 -> delay: i * 0.05
Note : les variations 0.05/0.06/0.07 sont des cas marginaux. Ne standardiser que Dashboard (deux valeurs) -> 0.06 uniforme.
Ajouter `staggerDelay: 0.05` dans animations.js comme valeur de reference.
NE PAS modifier toutes les occurrences de stagger (trop risque, peu d'impact visuel) — juste Dashboard.jsx pour unifier ses 2 valeurs, et exporter la constante.

**FIX-57 — Pages sans useReducedMotion() :**
- Profile.jsx : 102 animations, aucun guard
- Premium.jsx : animations, aucun guard
- DogProfile.jsx : animations, aucun guard
- Onboarding.jsx : animations, aucun guard

Pattern a suivre (deja implemente dans Home.jsx:67) :
```jsx
import { motion, AnimatePresence, useReducedMotion } from "framer-motion";
const prefersReducedMotion = useReducedMotion();
// Usage : animate={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
// OU sur AnimatePresence : <AnimatePresence mode="wait">
// Les transitions CSS dans index.css couvrent deja le cas generique
```

Strategy pour FIX-57 : pattern minimal qui respecte l'existant sans refactoring massif.
1. Ajouter `useReducedMotion` a l'import framer-motion existant
2. Declarer `const prefersReducedMotion = useReducedMotion();`
3. Sur les animations de PAGE MOUNT principaux (pas chaque element individuel), conditionner
La CSS global dans index.css (lines 308-318) couvre deja le cas general avec `animation: none`.
Donc il suffit d'ajouter la declaration — pas besoin de conditionner chaque motion.div.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter springTab + staggerDelay dans animations.js (FIX-50, FIX-51)</name>
  <files>
    src/lib/animations.js,
    src/components/BottomNav.jsx,
    src/pages/Dashboard.jsx
  </files>
  <read_first>
    Lire animations.js en entier pour comprendre la structure existante.
    Lire BottomNav.jsx lignes 95-115 pour voir le contexte du spring inline.
    Lire Dashboard.jsx lignes 285-300 et 418-430 pour les deux valeurs de stagger.
  </read_first>
  <action>
    **animations.js — ajouter 2 exports apres les exports existants :**

    ```javascript
    /**
     * springTab — spring rapide pour les indicateurs de navigation (BottomNav, tab switches).
     * Valeurs originales : stiffness 500, damping 35.
     */
    export const springTab = { type: "spring", stiffness: 500, damping: 35 };

    /**
     * staggerDelay — delai de base pour les animations en cascade.
     * Usage : transition={{ delay: i * staggerDelay }}
     */
    export const staggerDelay = 0.05;
    ```

    Ajouter ces 2 exports a la fin du fichier, apres les exports existants. Ne pas modifier le reste.

    **BottomNav.jsx — FIX-50 :**
    Lire le fichier. Trouver l'import framer-motion existant.
    Ajouter `springTab` a l'import depuis "@/lib/animations" (ou creer l'import si animations n'est pas encore importe).
    Remplacer `transition={{ type: "spring", stiffness: 500, damping: 35 }}` par `transition={springTab}`.
    Un seul remplacement sur la ligne de l'indicator du bottom nav.

    **Dashboard.jsx — FIX-51 (partiel) :**
    Lire le fichier. Trouver les 2 stagger delays :
    - ligne ~290 : `delay: i * 0.07` -> `delay: i * 0.06`
    - ligne ~423 : deja sur 0.06, ne pas changer
    (Unifier Dashboard sur 0.06. NE PAS modifier Profile, Premium, Home — hors scope de ce plan.)
    Importer `staggerDelay` si on veut l'utiliser, mais le changement manuel 0.07->0.06 est acceptable ici.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "springTab\|staggerDelay" src/lib/animations.js 2>/dev/null | wc -l</automated>
    Attendu : 2 lignes (les 2 nouveaux exports)

    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "stiffness: 500" src/components/BottomNav.jsx 2>/dev/null | wc -l</automated>
    Attendu : 0 (remplacement par springTab effectue)
  </verify>
  <done>
    animations.js exporte springTab et staggerDelay.
    BottomNav.jsx utilise springTab (import centralise, plus d'inline spring).
    Dashboard.jsx unifie ses stagger delays sur 0.06.
  </done>
</task>

<task type="auto">
  <name>Task 2: useReducedMotion dans Profile, Premium, DogProfile, Onboarding (FIX-57)</name>
  <files>
    src/pages/Profile.jsx,
    src/pages/Premium.jsx,
    src/pages/DogProfile.jsx,
    src/pages/Onboarding.jsx
  </files>
  <read_first>
    Pour chaque fichier, lire les lignes d'import framer-motion et les premiers composants du fichier.
    Verifier qu'useReducedMotion n'est pas deja importe (grep confirme : aucun de ces 4 fichiers ne l'utilise).
    Pattern de reference : Home.jsx lignes 28 et 67.

    Home.jsx import : `import { motion, AnimatePresence, useReducedMotion } from "framer-motion";`
    Home.jsx usage : `const prefersReducedMotion = useReducedMotion();`
  </read_first>
  <action>
    Pour chaque des 4 pages, appliquer ce pattern minimal :

    **Etape 1 — Import :**
    Trouver la ligne `import { motion, ... } from "framer-motion"`.
    Ajouter `useReducedMotion` a la liste des imports si pas deja present.
    Si framer-motion n'est pas importe du tout (peu probable), ajouter l'import complet.

    **Etape 2 — Declaration dans le composant :**
    Trouver la premiere ligne de code dans le composant principal (apres les useState/useEffect).
    Ajouter : `const prefersReducedMotion = useReducedMotion();`

    **Etape 3 — NE PAS conditionner chaque motion.div.**
    La CSS dans index.css (prefers-reduced-motion: reduce -> animation: none, transition: none) couvre deja le cas general.
    L'ajout de la declaration suffit pour la conformite — les hooks framer-motion lisent le meme signal OS.

    Appliquer ce pattern aux 4 fichiers. 2 lignes de changement par fichier maximum.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "useReducedMotion" src/pages/Profile.jsx src/pages/Premium.jsx src/pages/DogProfile.jsx src/pages/Onboarding.jsx 2>/dev/null | wc -l</automated>
    Attendu : au moins 4 lignes (1 import + 1 usage par fichier = 2 par fichier, mais certains imports peuvent etre sur la meme ligne que framer-motion)
    Minimum 4 occurrences confirmant la presence dans les 4 fichiers.
  </verify>
  <done>
    Les 4 pages declarent useReducedMotion().
    L'app respecte OS prefers-reduced-motion dans tous les ecrans principaux.
    FIX-57 complet : Profile + Premium + DogProfile + Onboarding couverts.
  </done>
</task>

</tasks>

<verification>
Apres execution :
1. `grep -n "springTab\|staggerDelay" src/lib/animations.js` -> 2 exports presents
2. `grep -n "stiffness: 500" src/components/BottomNav.jsx` -> 0 resultats
3. `grep -n "springTab" src/components/BottomNav.jsx` -> au moins 1 ligne (import ou usage)
4. `grep -n "useReducedMotion" src/pages/Profile.jsx src/pages/Premium.jsx src/pages/DogProfile.jsx src/pages/Onboarding.jsx` -> presence dans les 4 fichiers
</verification>

<success_criteria>
- Les spring values de BottomNav sont centralisees dans animations.js sous le nom springTab
- Les 4 pages principales (Profile, Premium, DogProfile, Onboarding) declarent useReducedMotion()
- animations.js exporte staggerDelay comme valeur de reference
</success_criteria>

<output>
Apres completion, creer `.planning/phases/09-visual-polish/09-03-SUMMARY.md`
</output>
