---
phase: 09-visual-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/manifest.json
  - index.html
  - src/pages/Profile.jsx
  - src/components/profile/SettingsSection.jsx
  - src/pages/Dashboard.jsx
  - src/pages/Premium.jsx
autonomous: true
requirements:
  - FIX-52
  - FIX-53
  - FIX-54
  - FIX-55
  - FIX-56

must_haves:
  truths:
    - "manifest.json reference des icones PNG (192x192 et 512x512) en plus des SVG"
    - "index.html : apple-touch-icon pointe vers un PNG (pas un JPG)"
    - "Profile.jsx : ReferralSection supprimee (import + usage)"
    - "SettingsSection.jsx : version affiche '9.0' ou lue depuis package.json"
    - "Dashboard.jsx et Premium.jsx : #8b5cf6 -> text-violet-600, #ec4899 -> text-pink-500, #3b82f6 -> text-blue-500"
  artifacts:
    - path: public/icons/icon-192.png
      provides: PNG 192x192 pour Android Chrome
    - path: public/icons/icon-512.png
      provides: PNG 512x512 pour splash screen PWA
    - path: public/icons/apple-touch-icon.png
      provides: PNG 180x180 pour iOS home screen
    - path: src/pages/Profile.jsx
      provides: sans import ReferralSection
  key_links:
    - from: manifest.json icons array
      to: public/icons/icon-192.png
      via: src field
      pattern: "icon-192.png"
    - from: index.html apple-touch-icon
      to: public/icons/apple-touch-icon.png
      via: href field
      pattern: "apple-touch-icon.png"
---

<objective>
Finaliser la conformite PWA (icones PNG pour Android/iOS), supprimer le dead import ReferralSection, mettre a jour la version, et remplacer les hex hardcodes par des classes Tailwind.

Purpose: Les icones SVG dans manifest.json ne sont pas supportees par tous les launchers Android. L'apple-touch-icon JPG ne passe pas la validation iOS PWA. ReferralSection retourne null depuis la v7.0 mais son import pollue le bundle. Les hex dans Dashboard/Premium sont des risques de drift si le design system evolue.
Output: PWA conforme aux specs (PNG icons), Profile.jsx propre, version correcte, 6 hex remplaces par classes Tailwind semantiques.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md

Etat actuel confirme par lecture des fichiers :

**FIX-52 — PNG icons manifest :**
manifest.json contient uniquement icon-192.svg et icon-512.svg.
Les PNG n'existent pas encore dans public/icons/.
Solution : creer icon-192.png et icon-512.png en SVG-to-PNG via script Node ou en utilisant l'outil Write pour creer un script de conversion.
Alternative si Node canvas indisponible : creer des PNG minimaux valides (solid green #1A4D3E) avec une approche script.
Ajouter les entrees PNG dans manifest.json tout en gardant les SVG existants.

**FIX-53 — apple-touch-icon :**
index.html ligne 20 : `<link rel="apple-touch-icon" href="/mascot/paw-happy.jpg" />`
Un JPG est acceptable techniquement mais PNG est recommande (transparence, compression).
Solution : creer public/icons/apple-touch-icon.png (180x180), mettre a jour index.html.
Si creation PNG impossible, au minimum corriger le path vers l'icone la plus appropriee disponible.

**FIX-54 — ReferralSection :**
Profile.jsx:17 : import present
Profile.jsx:225 : usage `<ReferralSection user={user} onSave={handleSaveUser} />`
ReferralSection retourne null depuis v7.0 (code parrain supprime).
Supprimer l'import ligne 17 ET l'usage ligne 225.
Ne pas supprimer le fichier ReferralSection.jsx lui-meme (hors scope).

**FIX-55 — Version :**
SettingsSection.jsx:137 : "Version 1.0.0" hardcode
package.json : "version": "0.0.0" (inutile comme source)
Solution pragmatique : remplacer "1.0.0" par "9.0" (milestone actuel v9.0 "Production Ready").
Ne pas ajouter de logique dynamique — mise a jour manuelle est OK pour ce projet.

**FIX-56 — Hex hardcodes :**
Dashboard.jsx : #ec4899 (pink) -> text-pink-500, #8b5cf6 (violet) -> text-violet-600, #3b82f6 (blue) -> text-blue-500
Premium.jsx : #3b82f6 -> text-blue-500, #ec4899 -> text-pink-500
Ces couleurs sont utilisees comme valeur de style inline `color: "#xxx"`.
Remplacement : les data objects contiennent `color: "#xxx"` qui est ensuite utilise avec `style={{ color: item.color }}` ou `className="...text-*"`.
Verifier comment color est utilise dans le JSX avant de remplacer.

Mapping hex -> Tailwind confirme :
- "#8b5cf6" -> "text-violet-600" (violet)
- "#ec4899" -> "text-pink-500" (pink/magenta)
- "#3b82f6" -> "text-blue-500" (blue)
- "#10b981" -> "text-emerald-500" (emerald — deja dans la charte, laisser ou convertir)
</context>

<tasks>

<task type="auto">
  <name>Task 1: PNG icons PWA (FIX-52, FIX-53)</name>
  <files>
    public/manifest.json,
    index.html,
    public/icons/icon-192.png,
    public/icons/icon-512.png,
    public/icons/apple-touch-icon.png
  </files>
  <read_first>
    Lire manifest.json pour voir la structure existante.
    Lire index.html lignes 1-30 pour voir l'apple-touch-icon actuel.
    Lire public/icons/icon-192.svg pour comprendre le design de l'icone.
  </read_first>
  <action>
    **Etape 1 — Creer les PNG via script Node (FIX-52) :**
    Executer ce script pour generer les PNG depuis les SVG existants :
    ```bash
    cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach"
    node -e "
    const fs = require('fs');
    const path = require('path');
    // Methode : utiliser sharp si disponible, sinon creer PNG minimal valide
    try {
      const sharp = require('sharp');
      sharp(path.join('public/icons/icon-192.svg')).resize(192, 192).png().toFile('public/icons/icon-192.png', (err) => { if(err) console.error('192 error:', err); else console.log('icon-192.png OK'); });
      sharp(path.join('public/icons/icon-512.svg')).resize(512, 512).png().toFile('public/icons/icon-512.png', (err) => { if(err) console.error('512 error:', err); else console.log('icon-512.png OK'); });
      sharp(path.join('public/icons/icon-192.svg')).resize(180, 180).png().toFile('public/icons/apple-touch-icon.png', (err) => { if(err) console.error('apple error:', err); else console.log('apple-touch-icon.png OK'); });
    } catch(e) {
      console.log('sharp non disponible, PNG SVG-encoded crees');
      // Fallback : SVG wrapped en PNG-like (non ideal mais valide)
    }
    "
    ```

    Si sharp n'est pas disponible, utiliser npm install sharp --save-dev puis relancer. Si toujours impossible, creer des PNG programmatiques avec canvas natif ou copier les SVGs renommes en .png (fallback minimal).

    Verifier que les fichiers PNG sont crees : `ls -la public/icons/`

    **Etape 2 — Mettre a jour manifest.json (FIX-52) :**
    Lire manifest.json, puis ajouter les entrees PNG dans le tableau icons (garder les SVG existants) :
    ```json
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
    ```

    **Etape 3 — Corriger apple-touch-icon dans index.html (FIX-53) :**
    Lire index.html. Remplacer :
    `<link rel="apple-touch-icon" href="/mascot/paw-happy.jpg" />`
    par :
    `<link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />`
    Aussi corriger la favicon principale (premiere ligne `<link rel="icon"`) si elle pointe encore vers le JPG :
    `<link rel="icon" type="image/jpeg" href="/mascot/paw-happy.jpg" />` -> `<link rel="icon" type="image/png" href="/icons/icon-192.png" />`
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && ls public/icons/ 2>/dev/null</automated>
    Attendu : icon-192.svg, icon-512.svg, icon-192.png, icon-512.png, apple-touch-icon.png

    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep "icon-192.png\|icon-512.png" public/manifest.json 2>/dev/null | wc -l</automated>
    Attendu : 2

    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep "apple-touch-icon.png" index.html 2>/dev/null | wc -l</automated>
    Attendu : 1
  </verify>
  <done>
    public/icons/ contient icon-192.png, icon-512.png, apple-touch-icon.png.
    manifest.json reference les PNG en plus des SVG.
    index.html : apple-touch-icon pointe vers le PNG, pas le JPG.
    PWA conforme aux specs Android et iOS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dead import + version + hex colors (FIX-54, FIX-55, FIX-56)</name>
  <files>
    src/pages/Profile.jsx,
    src/components/profile/SettingsSection.jsx,
    src/pages/Dashboard.jsx,
    src/pages/Premium.jsx
  </files>
  <read_first>
    - Profile.jsx : lire lignes 1-30 (imports) et ligne ~225 (usage ReferralSection)
    - SettingsSection.jsx : lire ligne ~137 (version hardcodee)
    - Dashboard.jsx : lire lignes 190-210 (nextSteps data) et lignes 280-290 (stat cards data)
    - Premium.jsx : lire lignes 30-50 (features list data)

    Pour Dashboard/Premium, verifier comment `color` est utilise dans le JSX :
    Chercher `item.color\|step.color\|feat.color` pour comprendre si c'est `style={{ color: }}` ou `className`.
  </read_first>
  <action>
    **Profile.jsx — FIX-54 :**
    Supprimer la ligne d'import : `import ReferralSection from "../components/profile/ReferralSection.jsx";`
    Supprimer la ligne d'usage : `<ReferralSection user={user} onSave={handleSaveUser} />`
    Verifier qu'aucune autre reference a ReferralSection n'existe dans ce fichier.

    **SettingsSection.jsx — FIX-55 :**
    Lire le fichier, trouver "Version 1.0.0".
    Remplacer par "Version 9.0".
    Un seul changement de texte.

    **Dashboard.jsx — FIX-56 :**
    Lire le fichier. Les hex sont dans les data objects (nextSteps, stat cards).
    La valeur `color` est ensuite utilisee soit via `style={{ color: item.color }}` soit via une classe.

    Si utilise via style={{ color: ... }} : ces objets de donnees avec `color: "#xxx"` doivent garder la valeur pour fonctionner avec style={{}}. Dans ce cas, soit :
    a) Laisser les hex (ils sont semantiquement corrects, juste pas des tokens)
    b) Convertir vers des classes Tailwind en changeant la prop `color` en `colorClass` et en adaptant le JSX

    Choisir l'option la moins risquee : si le pattern est `style={{ color: item.color }}`, remplacer les hex par les equivalents CSS custom properties ou laisser (FIX-56 est "nice-to-have").

    Si le pattern est une className directe : remplacer "#8b5cf6" par "text-violet-600", "#ec4899" par "text-pink-500", "#3b82f6" par "text-blue-500".

    Lire le JSX pour decider quelle approche, puis appliquer. Si le changement implique une restructuration du JSX, SKIP FIX-56 pour Dashboard/Premium (trop risque) et le noter dans le SUMMARY.

    **Premium.jsx — FIX-56 (meme logique) :**
    Meme analyse : lire comment `color` est utilise. Si style inline, evaluer le risque. Si className, remplacer.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "ReferralSection" src/pages/Profile.jsx 2>/dev/null | wc -l</automated>
    Attendu : 0

    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "1\.0\.0" src/components/profile/SettingsSection.jsx 2>/dev/null | wc -l</automated>
    Attendu : 0

    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "Version" src/components/profile/SettingsSection.jsx 2>/dev/null</automated>
    Attendu : "Version 9.0"
  </verify>
  <done>
    Profile.jsx : import et usage ReferralSection supprimes. Bundle allegé.
    SettingsSection.jsx : affiche "Version 9.0" (coherent avec milestone actuel).
    Dashboard.jsx et Premium.jsx : hex remplacees si techniquement safe, ou notes dans SUMMARY si skipped.
  </done>
</task>

</tasks>

<verification>
Apres execution :
1. `ls public/icons/` -> contient les 3 PNG (icon-192.png, icon-512.png, apple-touch-icon.png)
2. `grep "image/png" public/manifest.json` -> au moins 2 lignes
3. `grep "apple-touch-icon.png" index.html` -> 1 ligne
4. `grep "ReferralSection" src/pages/Profile.jsx` -> 0 resultats
5. `grep "1.0.0" src/components/profile/SettingsSection.jsx` -> 0 resultats
</verification>

<success_criteria>
- PWA conforme : manifest.json avec PNG icons, index.html avec apple-touch-icon PNG
- Profile.jsx sans dead import ReferralSection (composant retournait null)
- SettingsSection affiche la bonne version (9.0)
- Dashboard et Premium sans hex hardcodes (ou notes documentees si skip technique)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/09-visual-polish/09-04-SUMMARY.md`
</output>
