---
phase: "06"
plan: "01"
name: "PERF — Leaflet lazy, index-as-key, empty catches"
type: auto
autonomous: true
wave: 1
depends_on: []
requirements: [PERF-01, PERF-02, PERF-03]
---

# Phase 6 Plan 1: PERF — Bundle & Error Visibility

## Objective

Alleger le bundle initial (Leaflet lazy-loaded), corriger les index-as-key sur listes dynamiques, et rendre visibles les erreurs silencieuses sur mutations de donnees.

## Context

- `src/components/sante/FindVetContent.jsx` — importe react-leaflet en top-level (deja lazy dans Sante.jsx)
- `src/components/tracker/NearbyParks.jsx` — importe react-leaflet en top-level (deja lazy dans WalkMode.jsx)
- `src/components/tracker/WalkMap.jsx` — importe react-leaflet en top-level (deja lazy dans WalkMode.jsx)
- 15+ fichiers avec `key={i}` sur listes dynamiques
- ~8 catch vides sur mutations de donnees (Dog.update, walk save, mood save)

## Pre-execution Discovery

PERF-01 — Etat reel avant implementation :
- `Sante.jsx:23` -> `const FindVetContent = lazy(() => import("@/components/sante/FindVetContent"))` + Suspense a ligne 280
- `WalkMode.jsx:9-10` -> `WalkMap` et `NearbyParks` sont deja lazy avec Suspense

Conclusion : les 3 composants Leaflet sont deja lazy-charges au niveau parent. PERF-01 est DONE — aucune modification necessaire.

## Tasks

### Task 1: PERF-02 — Remplacer index-as-key sur listes dynamiques

22 fichiers modifies avec des cles stables sur les listes de donnees API.

### Task 2: PERF-03 — console.warn sur catch vides de mutations de donnees

5 mutations de donnees loggees :
- SectionPoids, WeightCard, SmartHealthAssistant : Dog.weight sync
- HealthImportContent : Dog.weight sync import
- WalkMode : offline sync + mood save

## Verification

- `git log --oneline -5` confirme les 2 commits de fix
- Aucun `key={i}` sur des `.map()` de donnees API dans les fichiers modifies
- Les mutations Dog.update / DailyLog.update ont toutes un console.warn

## Success Criteria

1. Les listes dynamiques (donnees API) utilisent des cles stables
2. Les mutations de donnees silencieusement echouees sont loggees
3. Aucune regression visuelle (memes composants, meme structure)
