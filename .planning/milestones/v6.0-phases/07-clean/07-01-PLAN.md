---
phase: "07"
plan: "01"
name: "CLEAN"
type: "auto"
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05]
---

# Phase 7 Plan 1: CLEAN — Nettoyage final du codebase

## Objective
Supprimer les deps npm inutilisees, deplacer LabelScanMode, ajouter idempotency au webhook Stripe, deduplicquer useReducedMotion, paralleliser les requetes DailyLog dans walkReminder.

## Tasks

### Task 1 — CLEAN-01: Supprimer les deps npm inutilisees
- Supprimer `@hello-pangea/dnd` (aucune utilisation dans src/)
- Supprimer `@stripe/react-stripe-js` (aucune utilisation dans src/)
- Garder `cmdk`, `vaul`, `input-otp` (utilises dans src/components/ui/ — shadcn)

### Task 2 — CLEAN-02: Deplacer LabelScanMode.jsx
- Creer `src/components/scan/` (si inexistant)
- Deplacer `src/pages/LabelScanMode.jsx` vers `src/components/scan/LabelScanMode.jsx`
- Mettre a jour l'import dans `src/pages/Scan.jsx`

### Task 3 — CLEAN-03: Idempotency stripeWebhook
- Lire `base44/functions/stripeWebhook/entry.ts`
- Ajouter un check pour eviter de retraiter le meme event.id
- Pattern : verifier si stripe_subscription_id est deja le meme avant d'updater (checkout.session.completed)

### Task 4 — CLEAN-04: Deduplication useReducedMotion
- Supprimer `src/hooks/useReducedMotion.js` (non importe par aucun fichier)
- Tous les fichiers utilisent deja `useReducedMotion` depuis `framer-motion` directement

### Task 5 — CLEAN-05: Paralleliser DailyLog dans walkReminder
- Lire `base44/functions/walkReminder/entry.ts`
- Les requetes DailyLog.filter sont sequentielles dans une boucle for
- Refactorer avec Promise.all pour paralleliser

## Success Criteria
1. `@hello-pangea/dnd` et `@stripe/react-stripe-js` absents de package.json
2. `src/components/scan/LabelScanMode.jsx` existe, `src/pages/LabelScanMode.jsx` supprime, Scan.jsx importe depuis le bon chemin
3. stripeWebhook skip les events deja traites
4. `src/hooks/useReducedMotion.js` supprime — seul framer-motion est utilise
5. walkReminder utilise Promise.all pour les DailyLog.filter
