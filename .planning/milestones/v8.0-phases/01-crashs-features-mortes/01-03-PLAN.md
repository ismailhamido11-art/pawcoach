---
phase: 01-crashs-features-mortes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DogPublicProfile.jsx
autonomous: true
requirements:
  - CRASH-03

must_haves:
  truths:
    - "DogPublicProfile s'affiche sans crash pour un chien ayant des records de type vet_visit ou medication"
    - "Les icones Stethoscope et Pill sont rendues correctement dans RecordItem"
    - "Aucune erreur 'Stethoscope is not defined' ou 'Pill is not defined' dans la console"
  artifacts:
    - path: "src/pages/DogPublicProfile.jsx"
      provides: "imports Stethoscope et Pill depuis lucide-react"
      contains: "Stethoscope, Pill"
  key_links:
    - from: "DogPublicProfile.jsx:19 (TYPE_CONFIG)"
      to: "Stethoscope et Pill icons"
      via: "import depuis lucide-react ligne 5"
      pattern: "Stethoscope.*Pill|Pill.*Stethoscope"
---

<objective>
Corriger CRASH-03 : DogPublicProfile.jsx utilise `Stethoscope` et `Pill` dans TYPE_CONFIG (ligne 19) mais ces deux icones ne sont pas importees en ligne 5. Si un HealthRecord de type `vet_visit` ou `medication` existe pour ce chien, `Icon` dans RecordItem sera `undefined` et le rendu crashe.

Purpose: La fiche publique QR code est utilisee en urgence (identification du chien perdu, info vet). Un crash partiel (records incomplets) la rend inutile precisement quand elle est le plus critique.
Output: DogPublicProfile.jsx avec `Stethoscope` et `Pill` ajoutes dans l'import lucide-react existant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/audit/SFA-PROFILE.md

<interfaces>
<!-- Extrait de src/pages/DogPublicProfile.jsx — lire ce fichier avant de modifier -->

DogPublicProfile.jsx:5 — import actuel (INCOMPLET) :
```jsx
import { Syringe, Weight, AlertTriangle, Calendar, MapPin, PawPrint, ShieldCheck, FileText, Mail, Fingerprint } from "lucide-react";
```

DogPublicProfile.jsx:16-22 — TYPE_CONFIG utilisant les icones manquantes :
```jsx
const TYPE_CONFIG = {
  vaccine:    { icon: Syringe,     label: "Vaccin",      color: "#2d9f82", bg: "#2d9f8215" },
  weight:     { icon: Weight,      label: "Poids",       color: "#3b82f6", bg: "#3b82f615" },
  vet_visit:  { icon: Stethoscope, label: "Visite veto", color: "#8b5cf6", bg: "#8b5cf615" },  // MANQUANT
  medication: { icon: Pill,        label: "Medicament",  color: "#10b981", bg: "#10b98115" },   // MANQUANT
  note:       { icon: FileText,    label: "Note",        color: "#6b7280", bg: "#6b728015" },
};
```

CGC evidence : "Stethoscope" trouve dans AIDiagnosisModal.jsx, Dashboard.jsx, DiagnosisContent.jsx, etc. — confirme que l'icone existe bien dans lucide-react et est utilisee partout ailleurs dans le projet.

Fix minimal : ajouter `Stethoscope, Pill` dans l'import ligne 5. Aucun autre changement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les imports manquants Stethoscope et Pill</name>
  <files>src/pages/DogPublicProfile.jsx</files>
  <read_first>src/pages/DogPublicProfile.jsx lignes 1-10</read_first>
  <action>
Dans src/pages/DogPublicProfile.jsx, modifier uniquement la ligne d'import lucide-react (ligne 5) pour ajouter `Stethoscope` et `Pill`.

Remplacer :
```jsx
import { Syringe, Weight, AlertTriangle, Calendar, MapPin, PawPrint, ShieldCheck, FileText, Mail, Fingerprint } from "lucide-react";
```

Par :
```jsx
import { Syringe, Weight, AlertTriangle, Calendar, MapPin, PawPrint, ShieldCheck, FileText, Mail, Fingerprint, Stethoscope, Pill } from "lucide-react";
```

Aucun autre changement dans ce fichier. TYPE_CONFIG ligne 19 est deja correct — il n'a besoin que des imports.
  </action>
  <verify>
    <automated>grep -n "Stethoscope, Pill" src/pages/DogPublicProfile.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Stethoscope" src/pages/DogPublicProfile.jsx retourne exactement 2 lignes (import + TYPE_CONFIG)
    - grep "Pill" src/pages/DogPublicProfile.jsx retourne exactement 2 lignes (import + TYPE_CONFIG)
    - La ligne d'import est unique (pas de doublon d'import lucide-react)
    - TYPE_CONFIG est inchange
  </acceptance_criteria>
  <done>Stethoscope et Pill sont importes — DogPublicProfile ne crashe plus sur les records vet_visit et medication.</done>
</task>

</tasks>

<verification>
```bash
# Verifier les deux imports
grep -n "Stethoscope" src/pages/DogPublicProfile.jsx
grep -n "Pill" src/pages/DogPublicProfile.jsx

# Verifier qu'il n'y a pas de doublon import lucide-react
grep -n "from \"lucide-react\"" src/pages/DogPublicProfile.jsx

# Verifier TYPE_CONFIG intact
grep -n "TYPE_CONFIG" src/pages/DogPublicProfile.jsx
```
</verification>

<success_criteria>
- `Stethoscope` et `Pill` apparaissent dans l'import lucide-react et dans TYPE_CONFIG (2 occurrences chacun)
- Un seul import lucide-react dans le fichier
- DogPublicProfile ne lance plus d'erreur "X is not defined" pour ces icones
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-crashs-features-mortes/01-03-SUMMARY.md`
</output>
