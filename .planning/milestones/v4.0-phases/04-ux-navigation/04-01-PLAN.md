---
phase: 04-ux-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Nutri.jsx
  - src/components/nutrition/DietPreferencesPanel.jsx
  - src/components/CombinedFAB.jsx
  - src/components/profile/SettingsSection.jsx
  - src/pages/Library.jsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-05
  - UX-06

must_haves:
  truths:
    - "Apres avoir sauvegarde les preferences diet, Nutri.jsx affiche immediatement les nouvelles prefs sans rechargement de page"
    - "Logger un poids via le FAB cree un HealthRecord type=weight visible dans Sante/Growth"
    - "Cliquer 'Se deconnecter' affiche un dialogue de confirmation avant de deconnecter"
    - "Supprimer un plan nutrition dans Library demande une confirmation (window.confirm)"
  artifacts:
    - path: "src/components/nutrition/DietPreferencesPanel.jsx"
      provides: "Callback onPreferencesSaved appele apres succes"
      contains: "onPreferencesSaved"
    - path: "src/pages/Nutri.jsx"
      provides: "Re-fetch dietPrefs apres callback"
      contains: "setDietPrefs"
    - path: "src/components/CombinedFAB.jsx"
      provides: "Creation HealthRecord weight en plus de DailyLog"
      contains: "HealthRecord"
    - path: "src/components/profile/SettingsSection.jsx"
      provides: "Dialog confirmation logout"
      contains: "showLogoutConfirm"
    - path: "src/pages/Library.jsx"
      provides: "window.confirm avant suppression NutritionPlan"
      contains: "window.confirm"
  key_links:
    - from: "src/components/nutrition/DietPreferencesPanel.jsx"
      to: "src/pages/Nutri.jsx"
      via: "prop onPreferencesSaved callback"
      pattern: "onPreferencesSaved"
    - from: "src/components/CombinedFAB.jsx"
      to: "HealthRecord entity"
      via: "HealthRecord.create avec type=weight"
      pattern: "HealthRecord\\.create"
---

<objective>
Corriger 4 bugs UX independants : preferences diet stales, poids FAB desync avec Sante, deconnexion sans confirmation, suppression plan nutrition sans confirmation.

Purpose: Chaque bug degrade la confiance de l'utilisateur — soit des donnees semblent perdues (poids), soit une action destructive est trop facile (logout/delete).
Output: 5 fichiers modifies, comportements coherents et proteges.
</objective>

<execution_context>
@/c/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Interfaces cles extraites du code — lire avant de modifier -->

DietPreferencesPanel signature actuelle (src/components/nutrition/DietPreferencesPanel.jsx:28):
```jsx
export default function DietPreferencesPanel({ dog, user })
// Apres handleSave reussi : setSaved(true), setTimeout reset — PAS de callback
```

Nutri.jsx appel de DietPreferencesPanel (onglet "prefs"):
```jsx
// Nutri.jsx passe dog et user comme props. dietPrefs est en state local ligne 85.
// setDietPrefs(prefs[0]) est appele dans init() ligne 233.
// La fonction refreshPlans() (ligne 185) existe deja pour rafraichir les plans.
```

CombinedFAB — handleSave actuel (src/components/CombinedFAB.jsx:38-91):
```jsx
import { DailyLog } from "@/api/entities"; // seulement DailyLog
// payload inclut weight_kg si saisi
// Apres save DailyLog : onLogSaved?.() callback
```

HealthRecord.create schema (confirme par SectionPoids.jsx:24-29):
```js
HealthRecord.create({
  dog_id: dogId,
  type: "weight",
  title: "Pesée",
  date: "YYYY-MM-DD",  // getTodayString()
  value: number,       // weight_kg
})
```

SettingsSection — logout actuel (src/components/profile/SettingsSection.jsx:83-92):
```jsx
<Button onClick={() => base44.auth.logout()} ...>Se déconnecter</Button>
// Aucun state de confirmation — action directe
// showDeleteConfirm/setShowDeleteConfirm existent deja pour le delete account
```

Library — handleDeleteNutritionPlan actuel (src/pages/Library.jsx:102-111):
```jsx
const handleDeleteNutritionPlan = async (id) => {
  // PAS de window.confirm — suppression directe
  // handleDelete (Bookmark) et handleDeleteScan ont window.confirm
  await NutritionPlan.delete(id);
  ...
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: UX-01 — DietPreferencesPanel callback + Nutri re-fetch</name>
  <files>src/components/nutrition/DietPreferencesPanel.jsx, src/pages/Nutri.jsx</files>
  <action>
**DietPreferencesPanel.jsx :**
1. Ajouter `onPreferencesSaved` dans les props : `export default function DietPreferencesPanel({ dog, user, onPreferencesSaved })`
2. Dans `handleSave`, apres `setSaved(true)` et avant le `setTimeout`, appeler `onPreferencesSaved?.()`.

**Nutri.jsx :**
1. Ajouter une fonction `refreshDietPrefs` qui re-fetch les preferences :
```js
const refreshDietPrefs = async () => {
  if (!dog || !user) return;
  try {
    const prefs = await DietPreferences.filter({ dog_id: dog.id, owner_email: user.email }).catch(() => []);
    if (prefs?.length > 0) setDietPrefs(prefs[0]);
    else setDietPrefs(null);
  } catch { /* ignore */ }
};
```
2. Dans le JSX, sur le composant `<DietPreferencesPanel>`, passer `onPreferencesSaved={refreshDietPrefs}`.

Ne pas modifier l'import DietPreferences (deja present ligne 4 de Nutri.jsx).
  </action>
  <verify>
    <automated>grep -n "onPreferencesSaved" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/nutrition/DietPreferencesPanel.jsx && grep -n "refreshDietPrefs\|onPreferencesSaved" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Nutri.jsx</automated>
  </verify>
  <done>DietPreferencesPanel accepte onPreferencesSaved prop et l'appelle apres save. Nutri.jsx passe refreshDietPrefs qui re-fetch DietPreferences apres save.</done>
</task>

<task type="auto">
  <name>Task 2: UX-02 — CombinedFAB cree aussi un HealthRecord type=weight</name>
  <files>src/components/CombinedFAB.jsx</files>
  <action>
1. Ajouter `HealthRecord` a l'import entities ligne 5 : `import { DailyLog, HealthRecord } from "@/api/entities";`
2. Dans `handleSave`, apres le bloc `DailyLog.update/create` (et avant `checkWalkBadges`), ajouter la creation du HealthRecord si weight_kg est present :
```js
// Create HealthRecord for weight if provided (so Sante/Growth can read it)
if (payload.weight_kg) {
  try {
    await HealthRecord.create({
      dog_id: dog.id,
      type: "weight",
      title: "Pesée",
      date: payload.date,
      value: payload.weight_kg,
    });
  } catch (e) {
    console.warn("CombinedFAB: HealthRecord weight create failed", e);
  }
}
```

Placer ce bloc APRES le `await DailyLog.update/create` et AVANT `checkWalkBadges`. Ne jamais bloquer le flow principal si ce create echoue (le try/catch isole l'erreur).
  </action>
  <verify>
    <automated>grep -n "HealthRecord" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/CombinedFAB.jsx</automated>
  </verify>
  <done>CombinedFAB importe HealthRecord et cree un enregistrement type=weight quand weight_kg est logue. L'erreur HealthRecord ne bloque pas le save DailyLog.</done>
</task>

<task type="auto">
  <name>Task 3: UX-05 + UX-06 — Confirmation logout et suppression NutritionPlan</name>
  <files>src/components/profile/SettingsSection.jsx, src/pages/Library.jsx</files>
  <action>
**SettingsSection.jsx — Confirmation logout :**
1. Ajouter un state `const [showLogoutConfirm, setShowLogoutConfirm] = useState(false);` (ligne apres `showDeleteConfirm`).
2. Remplacer le `onClick` du bouton logout : `onClick={() => setShowLogoutConfirm(true)}`
3. Ajouter le dialog de confirmation logout apres le dialog delete (meme style : fixed inset-0, motion.div, bg-white rounded-3xl). Contenu :
   - Icone : `<LogOut className="w-7 h-7 text-foreground" />` dans un div `bg-slate-100 rounded-full`
   - Titre : "Se deconnecter ?"
   - Description : "Tu seras redirige vers la page de connexion."
   - Bouton Annuler : `onClick={() => setShowLogoutConfirm(false)}`, variant="outline"
   - Bouton Se deconnecter : `onClick={() => base44.auth.logout()}`, style standard (pas rouge)

**Library.jsx — Confirmation suppression NutritionPlan :**
Ajouter `window.confirm` en debut de `handleDeleteNutritionPlan` (meme pattern que `handleDelete` ligne 79 et `handleDeleteScan` ligne 114) :
```js
const handleDeleteNutritionPlan = async (id) => {
  if (!window.confirm("Supprimer ce plan nutrition ?")) return;
  // ... reste inchange
};
```
  </action>
  <verify>
    <automated>grep -n "showLogoutConfirm\|setShowLogoutConfirm" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/profile/SettingsSection.jsx && grep -n "window.confirm" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Library.jsx</automated>
  </verify>
  <done>SettingsSection a un state showLogoutConfirm et un dialog de confirmation avant base44.auth.logout(). Library.handleDeleteNutritionPlan a window.confirm comme handleDelete et handleDeleteScan.</done>
</task>

</tasks>

<verification>
Verifications globales du plan :
1. `grep -n "onPreferencesSaved" src/components/nutrition/DietPreferencesPanel.jsx` — doit matcher
2. `grep -n "onPreferencesSaved" src/pages/Nutri.jsx` — doit matcher
3. `grep -n "HealthRecord" src/components/CombinedFAB.jsx` — doit matcher (import + create)
4. `grep -n "showLogoutConfirm" src/components/profile/SettingsSection.jsx` — doit matcher
5. `grep -c "window.confirm" src/pages/Library.jsx` — doit retourner 3 (Bookmark + NutritionPlan + FoodScan)
</verification>

<success_criteria>
- UX-01: DietPreferencesPanel appelle onPreferencesSaved apres save succes, Nutri.jsx re-fetch et met a jour dietPrefs state
- UX-02: CombinedFAB cree HealthRecord type=weight quand weight_kg est present, erreur isolee
- UX-05: Bouton deconnexion ouvre dialog de confirmation, pas de logout immediat
- UX-06: handleDeleteNutritionPlan a window.confirm, coherent avec les 2 autres suppressions de Library
</success_criteria>

<output>
Apres completion, creer `.planning/phases/04-ux-navigation/04-01-SUMMARY.md`
</output>
