---
phase: 04-ux-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tracker/WalkMode.jsx
  - src/components/ui/VoiceInput.jsx
  - src/pages/VetDogView.jsx
  - src/components/ErrorBoundary.jsx
autonomous: true
requirements:
  - UX-03
  - UX-04
  - NAV-04

must_haves:
  truths:
    - "Les erreurs GPS code 2 et 3 dans WalkMode affichent un message toast en francais (pas de silence)"
    - "L'erreur micro (VoiceInput) affiche un message toast en francais au lieu de se taire"
    - "Les erreurs backend dans VetDogView s'affichent en francais"
    - "ErrorBoundary redirige vers Home via createPageUrl('Home'), pas vers '/'"
  artifacts:
    - path: "src/components/tracker/WalkMode.jsx"
      provides: "Toast francais pour GPS codes 1, 2 et 3"
      contains: "code === 2\|code === 3"
    - path: "src/components/ui/VoiceInput.jsx"
      provides: "Toast francais dans onerror"
      contains: "toast.error"
    - path: "src/pages/VetDogView.jsx"
      provides: "Traduction erreurs backend en francais"
      contains: "ERROR_MESSAGES\|setError.*francais"
    - path: "src/components/ErrorBoundary.jsx"
      provides: "Redirection Home via createPageUrl"
      contains: "createPageUrl"
  key_links:
    - from: "src/components/tracker/WalkMode.jsx"
      to: "navigator.geolocation.watchPosition error callback"
      via: "err.code switch"
      pattern: "err\\.code"
    - from: "src/components/ui/VoiceInput.jsx"
      to: "recognition.onerror"
      via: "toast.error avec message"
      pattern: "recognition\\.onerror"
    - from: "src/components/ErrorBoundary.jsx"
      to: "createPageUrl('Home')"
      via: "import createPageUrl depuis @/utils"
      pattern: "createPageUrl"
---

<objective>
Corriger les erreurs silencieuses : GPS codes 2/3 dans WalkMode, micro dans VoiceInput, messages anglais dans VetDogView, et redirection "/" dans ErrorBoundary.

Purpose: Des erreurs silencieuses ou anglaises laissent l'utilisateur bloque sans comprendre pourquoi. Ces 4 fixes donnent des retours clairs en francais.
Output: 4 fichiers modifies avec messages d'erreur francais explicites.
</objective>

<execution_context>
@/c/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Interfaces cles extraites du code -->

WalkMode.jsx — GPS error callback actuel (ligne 201-205):
```js
(err) => {
  if (err.code === 1) {
    toast.info("GPS désactivé — la distance ne sera pas mesurée", { id: "gps-warn" });
  }
  // code 2 (POSITION_UNAVAILABLE) et code 3 (TIMEOUT) : SILENCIEUX
},
```
GeolocationPositionError codes : 1=PERMISSION_DENIED, 2=POSITION_UNAVAILABLE, 3=TIMEOUT

VoiceInput.jsx — onerror actuel (ligne 32):
```js
recognition.onerror = () => setListening(false);
// Aucun toast — l'utilisateur voit juste le micro revenir en mode normal
```
recognition.error event a une propriete `error` : "not-allowed" (refus micro), "no-speech", "network", etc.

VetDogView.jsx — affichage erreur (ligne 57-62):
```jsx
if (error) return (
  <div className="min-h-screen flex flex-col items-center justify-center p-6 text-center">
    <p className="text-sm text-red-600 mb-4">{error}</p>  // error vient du backend (anglais)
    ...
  </div>
);
// error est setError(res.data.error) ou setError(e?.message || String(e))
```

ErrorBoundary.jsx — bouton retour accueil (ligne 139-153):
```jsx
<button onClick={() => { window.location.href = '/'; }}>
  Retour à l'accueil
</button>
// createPageUrl est disponible dans @/utils (utilise partout dans l'app)
// ErrorBoundary est un class component — importer createPageUrl en top-level
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: UX-03 — Messages francais pour GPS codes 2/3 et erreur micro VoiceInput</name>
  <files>src/components/tracker/WalkMode.jsx, src/components/ui/VoiceInput.jsx</files>
  <action>
**WalkMode.jsx — GPS error callback :**
Remplacer le callback d'erreur de `watchPosition` (lignes 201-205) par :
```js
(err) => {
  if (err.code === 1) {
    toast.info("GPS désactivé — la distance ne sera pas mesurée", { id: "gps-warn" });
  } else if (err.code === 2) {
    toast.error("Signal GPS indisponible — la distance ne sera pas mesurée.", { id: "gps-warn" });
  } else if (err.code === 3) {
    toast.error("GPS trop lent à répondre — la distance ne sera pas mesurée.", { id: "gps-warn" });
  }
},
```
Ne pas modifier les autres parties de `startGPS`. Le `{ id: "gps-warn" }` evite les doublons de toast.

**VoiceInput.jsx — onerror :**
Remplacer `recognition.onerror = () => setListening(false);` par :
```js
recognition.onerror = (e) => {
  setListening(false);
  if (e.error === "not-allowed") {
    toast.error("Accès au microphone refusé. Autorise-le dans les réglages de ton navigateur.");
  } else if (e.error === "no-speech") {
    toast.info("Aucune voix détectée. Réessaie.");
  } else {
    toast.error("Erreur de dictée vocale. Réessaie.");
  }
};
```
`toast` est deja importé dans VoiceInput.jsx (ligne 2).
  </action>
  <verify>
    <automated>grep -n "code === 2\|code === 3" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/tracker/WalkMode.jsx && grep -n "not-allowed\|onerror" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/ui/VoiceInput.jsx</automated>
  </verify>
  <done>WalkMode gere codes 1, 2 et 3 avec toast francais. VoiceInput.onerror affiche un toast contextuel au lieu de se taire.</done>
</task>

<task type="auto">
  <name>Task 2: UX-04 — Traduction erreurs backend VetDogView + NAV-04 ErrorBoundary createPageUrl</name>
  <files>src/pages/VetDogView.jsx, src/components/ErrorBoundary.jsx</files>
  <action>
**VetDogView.jsx — traduction erreurs backend :**
1. Ajouter une map de traduction apres les imports, avant le composant :
```js
const ERROR_MESSAGES = {
  "Access denied": "Accès refusé. Tu n'es pas autorisé à consulter ce chien.",
  "No active access": "Aucun accès actif pour ce chien. Demande au propriétaire de te partager un lien valide.",
  "Access expired": "Ton accès à ce chien a expiré. Demande un nouveau lien.",
  "Dog not found": "Chien introuvable.",
  "No dog specified": "Aucun chien spécifié.",
  "Aucun chien spécifié": "Aucun chien spécifié.",
};
const translateError = (msg) => ERROR_MESSAGES[msg] || msg;
```
2. Dans `loadData`, remplacer `setError(res.data.error)` par `setError(translateError(res.data.error))` et `setError(e?.message || String(e))` par `setError(translateError(e?.message || String(e)))`.
3. Dans le JSX du bloc `if (error)`, remplacer aussi `setError("Aucun chien spécifié")` (ligne 31) par `setError("Aucun chien spécifié")` — deja en francais, laisser tel quel.

**ErrorBoundary.jsx — createPageUrl :**
1. Ajouter l'import en haut du fichier (apres `import { Component } from 'react';`) :
```js
import { createPageUrl } from '@/utils';
```
2. Remplacer `window.location.href = '/'` par `window.location.href = createPageUrl('Home')`.

Note : ErrorBoundary est un class component — `createPageUrl` est importe au module level, pas dans un hook, ce qui est correct.
  </action>
  <verify>
    <automated>grep -n "ERROR_MESSAGES\|translateError" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/VetDogView.jsx && grep -n "createPageUrl" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/ErrorBoundary.jsx</automated>
  </verify>
  <done>VetDogView traduit les erreurs backend via ERROR_MESSAGES map. ErrorBoundary importe createPageUrl et l'utilise pour le bouton retour accueil.</done>
</task>

</tasks>

<verification>
Verifications globales :
1. `grep -c "code === 2" src/components/tracker/WalkMode.jsx` — doit retourner 1
2. `grep -c "code === 3" src/components/tracker/WalkMode.jsx` — doit retourner 1
3. `grep -n "onerror" src/components/ui/VoiceInput.jsx` — doit avoir toast.error
4. `grep -n "ERROR_MESSAGES" src/pages/VetDogView.jsx` — doit matcher
5. `grep -n "createPageUrl" src/components/ErrorBoundary.jsx` — doit matcher
</verification>

<success_criteria>
- UX-03: GPS codes 2 et 3 affichent un toast francais explicite. Micro refus affiche "Acces au microphone refuse..."
- UX-04: Les erreurs backend VetDogView passent par translateError et sont affichees en francais
- NAV-04: ErrorBoundary utilise createPageUrl('Home') pour le bouton retour accueil
</success_criteria>

<output>
Apres completion, creer `.planning/phases/04-ux-navigation/04-02-SUMMARY.md`
</output>
