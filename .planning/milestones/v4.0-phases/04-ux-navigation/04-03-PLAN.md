---
phase: 04-ux-navigation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ChatFAB.jsx
  - src/components/BottomNav.jsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Le ChatFAB n'est plus cliquable quand le backdrop CombinedFAB est ouvert"
    - "Les pages VetPortal et VetDogView highlightent un onglet dans la BottomNav"
    - "Aucun acces sessionStorage dans BottomNav ne peut lever une exception en navigation privee"
  artifacts:
    - path: "src/components/ChatFAB.jsx"
      provides: "z-index reduit a z-[41] ou masquage quand CombinedFAB ouvert"
      contains: "z-\\[41\\]\\|fabOpen\\|isOpen"
    - path: "src/components/BottomNav.jsx"
      provides: "VetPortal et VetDogView dans SECONDARY_PAGE_PARENT"
      contains: "VetPortal\|VetDogView"
    - path: "src/components/BottomNav.jsx"
      provides: "sessionStorage dans try/catch"
      contains: "try.*sessionStorage\|sessionStorage.*try"
  key_links:
    - from: "src/components/ChatFAB.jsx"
      to: "CombinedFAB backdrop z-[42]"
      via: "ChatFAB z-index < 42 quand FAB ouvert"
      pattern: "z-\\[4"
    - from: "src/components/BottomNav.jsx"
      to: "SECONDARY_PAGE_PARENT map"
      via: "VetPortal et VetDogView mappes vers 'Profil' ou page parent"
      pattern: "VetPortal.*Profile\\|VetDogView.*Profile"
---

<objective>
Corriger 3 bugs de navigation : ChatFAB cliquable au-dessus du backdrop, tabs VetPortal/VetDogView sans highlight BottomNav, sessionStorage sans protection en navigation privee.

Purpose: Ces bugs degradent la navigation — action involontaire (ChatFAB), desorienation visuelle (BottomNav), crash potentiel (sessionStorage).
Output: 2 fichiers modifies, navigation robuste et coherente.
</objective>

<execution_context>
@/c/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Interfaces cles extraites du code -->

ChatFAB actuel (src/components/ChatFAB.jsx):
```jsx
export default function ChatFAB({ offsetBottom = 0 }) {
  // z-[45] — au-dessus du backdrop CombinedFAB z-[42]
  // Pas de prop pour savoir si CombinedFAB est ouvert
  className="fixed right-4 z-[45] ..."
}
```

CombinedFAB backdrop z-index (src/components/CombinedFAB.jsx:102):
```jsx
className="fixed inset-0 z-[42] bg-black/30 backdrop-blur-sm"
// Sheet : z-50
// FAB button : z-[45]
```

ChatFAB est rendu depuis Layout.jsx ou une page parente — verifier :

BottomNav SECONDARY_PAGE_PARENT actuel (src/components/BottomNav.jsx:16-21):
```js
const SECONDARY_PAGE_PARENT = {
  Library:    "Profile",
  Dashboard:  "Profile",
  DogProfile: "Profile",
  Chat:       "Home",
  // VetPortal et VetDogView ABSENTS — pas de highlight
};
```

BottomNav sessionStorage actuel (src/components/BottomNav.jsx) — acces non proteges :
- `sessionStorage.getItem('tab_${page}')` dans getNavUrl() (ligne 30)
- `sessionStorage.setItem('scroll_${currentPage}', ...)` dans handleTabClick (ligne 49)
- `sessionStorage.getItem('scroll_${currentPage}')` dans useEffect (ligne 41)
- `sessionStorage.removeItem(...)` x3 dans handleTabClick (lignes 54-56)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: NAV-01 — ChatFAB z-index sous le backdrop CombinedFAB</name>
  <files>src/components/ChatFAB.jsx</files>
  <action>
L'approche la plus simple (sans prop) : passer ChatFAB de `z-[45]` a `z-[41]`. Le backdrop CombinedFAB est a `z-[42]`, donc il couvrira le ChatFAB quand il est ouvert. Le Sheet CombinedFAB est a `z-50`, la Sheet est toujours au-dessus.

Dans ChatFAB.jsx, remplacer `z-[45]` par `z-[41]` dans la className du motion.div.

Avant : `className="fixed right-4 z-[45] w-14 h-14 ..."`
Apres : `className="fixed right-4 z-[41] w-14 h-14 ..."`

Cette solution est atomique et non-cassante. Le ChatFAB reste visible et cliquable quand CombinedFAB est ferme (z-[41] > z-40 de la BottomNav). Quand CombinedFAB est ouvert, le backdrop z-[42] le couvre.
  </action>
  <verify>
    <automated>grep -n "z-\[41\]" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/ChatFAB.jsx</automated>
  </verify>
  <done>ChatFAB est a z-[41]. Le backdrop CombinedFAB z-[42] le couvre quand ouvert. ChatFAB reste cliquable au-dessus de la BottomNav z-40 quand CombinedFAB est ferme.</done>
</task>

<task type="auto">
  <name>Task 2: NAV-02 + NAV-03 — BottomNav VetPortal/VetDogView highlight + sessionStorage try/catch</name>
  <files>src/components/BottomNav.jsx</files>
  <action>
**NAV-02 — Ajouter VetPortal et VetDogView dans SECONDARY_PAGE_PARENT :**
Les pages vet sont des sous-pages du Profil (acces depuis Profile > portail vet). Ajouter :
```js
const SECONDARY_PAGE_PARENT = {
  Library:    "Profile",
  Dashboard:  "Profile",
  DogProfile: "Profile",
  Chat:       "Home",
  VetPortal:  "Profile",   // <-- ajouter
  VetDogView: "Profile",   // <-- ajouter
};
```

**NAV-03 — Wrapper tous les acces sessionStorage dans try/catch :**

1. Remplacer la fonction `getNavUrl` :
```js
function getNavUrl(page) {
  const base = createPageUrl(page);
  if (STACK_PAGES.includes(page)) {
    try {
      const saved = sessionStorage.getItem(`tab_${page}`);
      if (saved) return `${base}?tab=${saved}`;
    } catch { /* navigation privee — ignorer */ }
  }
  return base;
}
```

2. Dans le `useEffect` de restauration du scroll :
```js
useEffect(() => {
  try {
    const saved = sessionStorage.getItem(`scroll_${currentPage}`);
    if (saved) {
      requestAnimationFrame(() => window.scrollTo({ top: parseInt(saved, 10), behavior: "instant" }));
    }
  } catch { /* navigation privee — ignorer */ }
}, [currentPage]);
```

3. Dans `handleTabClick`, wrapper tous les acces sessionStorage :
```js
const handleTabClick = (e, page) => {
  try { sessionStorage.setItem(`scroll_${currentPage}`, window.scrollY); } catch { /* ignorer */ }

  if (currentPage === page) {
    e.preventDefault();
    try {
      sessionStorage.removeItem(`scroll_${page}`);
      sessionStorage.removeItem(`tab_${page}`);
      sessionStorage.removeItem(`journey_${page}`);
      sessionStorage.removeItem(`exercise_${page}`);
    } catch { /* ignorer */ }
    navigate(createPageUrl(page), { replace: true });
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
};
```

Garder le `sessionStorage.setItem("tab_Nutri", activeTab)` dans Nutri.jsx intact — ce plan ne touche pas Nutri.jsx.
  </action>
  <verify>
    <automated>grep -n "VetPortal\|VetDogView" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/BottomNav.jsx && grep -c "try {" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/BottomNav.jsx</automated>
  </verify>
  <done>BottomNav.SECONDARY_PAGE_PARENT contient VetPortal et VetDogView mappes sur Profile. Tous les acces sessionStorage sont dans try/catch. `grep -c "try {"` retourne >= 3.</done>
</task>

</tasks>

<verification>
Verifications globales :
1. `grep -n "z-\[41\]" src/components/ChatFAB.jsx` — doit matcher (plus de z-[45])
2. `grep -c "z-\[45\]" src/components/ChatFAB.jsx` — doit retourner 0
3. `grep -n "VetPortal" src/components/BottomNav.jsx` — doit matcher
4. `grep -n "VetDogView" src/components/BottomNav.jsx` — doit matcher
5. `grep -c "try {" src/components/BottomNav.jsx` — doit retourner >= 3
</verification>

<success_criteria>
- NAV-01: ChatFAB z-[41], backdrop CombinedFAB z-[42] le couvre quand ouvert
- NAV-02: VetPortal et VetDogView dans SECONDARY_PAGE_PARENT → onglet Profile highlight actif
- NAV-03: Tous les acces sessionStorage dans BottomNav sont proteges par try/catch
</success_criteria>

<output>
Apres completion, creer `.planning/phases/04-ux-navigation/04-03-SUMMARY.md`
</output>
