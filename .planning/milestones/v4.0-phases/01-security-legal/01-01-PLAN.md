---
phase: 01-security-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/base44/functions/deleteUser/entry.ts
  - pawcoach/base44/functions/vetAccess/entry.ts
  - pawcoach/src/components/vet/VetNoteForm.jsx
  - pawcoach/src/pages/DogPublicProfile.jsx
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "Un utilisateur qui supprime son compte n'existe plus en base (email, nom, is_premium supprimes)"
    - "Un vet ne peut pas creer une note sur un chien sans acces SharedVetAccess actif verifie cote serveur"
    - "L'email du proprietaire n'est plus visible sur le profil public du chien"
  artifacts:
    - path: "pawcoach/base44/functions/deleteUser/entry.ts"
      provides: "Suppression complete du compte User incluant l'entite User elle-meme"
      contains: "User.delete"
    - path: "pawcoach/base44/functions/vetAccess/entry.ts"
      provides: "Action addVetNote avec verification acces actif avant creation"
      contains: "addVetNote"
    - path: "pawcoach/src/components/vet/VetNoteForm.jsx"
      provides: "Appel via base44.functions.vetAccess au lieu de VetNote.create direct"
      contains: "vetAccess"
    - path: "pawcoach/src/pages/DogPublicProfile.jsx"
      provides: "Section contact proprietaire sans exposer l'email"
  key_links:
    - from: "VetNoteForm.jsx"
      to: "vetAccess/entry.ts (action: addVetNote)"
      via: "base44.functions.vetAccess({ action: 'addVetNote', ... })"
      pattern: "addVetNote"
    - from: "vetAccess/entry.ts addVetNote"
      to: "SharedVetAccess.filter({ dog_id, vet_email, status: 'active' })"
      via: "verification acces avant VetNote.create"
      pattern: "status.*active"
---

<objective>
Corriger trois failles RGPD/securite identifiees par l'audit E2E :
1. deleteUser ne supprime pas l'entite User elle-meme (le compte reste en base apres suppression)
2. VetNote peut etre cree sans verification d'acces (n'importe quel user authentifie connait l'API)
3. DogPublicProfile expose l'email du proprietaire publiquement

Purpose: Conformite RGPD (droit a l'effacement) et prevention d'acces non autorises aux donnees medicales.
Output: 4 fichiers modifies, 0 changement de schema, 0 credit Build prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Workflow : Git direct (0 credit). Modifier les fichiers dans pawcoach/ puis git push.
Ne jamais modifier pawcoach/src/components/ui/ (shadcn).
</context>

<interfaces>
<!-- Contrats existants que l'executeur doit connaitre -->

Pattern Base44 SDK dans les fonctions backend Deno :
```typescript
const base44 = createClientFromRequest(req);
const user = await base44.auth.me();
// Acces service role pour bypasser les regles d'acces :
await base44.asServiceRole.entities.EntityName.delete(id);
await base44.asServiceRole.entities.EntityName.filter({ field: value });
await base44.asServiceRole.entities.EntityName.create({ ... });
```

Pattern appel fonction backend depuis le frontend (Base44 SDK) :
```javascript
import { base44 } from "@/api/base44Client";
// Appel d'une fonction nommee "vetAccess" :
const result = await base44.functions.vetAccess({ action: 'addVetNote', dogId, ... });
```

Structure actuelle vetAccess/entry.ts — actions disponibles :
- 'invite' : owner invite un vet
- 'accept' : vet accepte l'invitation
- 'revoke' : owner revoque l'acces
- 'listByDog' : owner liste les acces d'un chien
- 'listMyAccess' : vet liste ses acces
- 'getDogData' : vet lit les donnees d'un chien (avec check acces actif)
- 'getHealthSummary' : owner lit le resume de sante

Structure actuelle deleteUser/entry.ts :
- Step 0 : Annule l'abonnement Stripe (best-effort)
- Step 1 : Recupere les dog IDs de l'utilisateur
- Step 2 : Supprime toutes les entites liees (HealthRecord, DailyLog, etc.)
- Step 3 : Supprime les entites owner-level (Dog, Bookmark, etc.)
- MANQUE : suppression de l'entite User elle-meme

Champ user.id disponible : l'objet user retourne par base44.auth.me() contient un champ id.
Methode de suppression d'un user : base44.asServiceRole.entities.User.delete(users[0].id)
Pour obtenir l'id : dans deleteUser, users est deja fetch a la ligne 17 pour Stripe.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SEC-01 — deleteUser supprime l'entite User elle-meme</name>
  <files>pawcoach/base44/functions/deleteUser/entry.ts</files>
  <read_first>
    - pawcoach/base44/functions/deleteUser/entry.ts (lire AVANT toute modification — voir le Step 0 qui fetch deja users[0])
  </read_first>
  <action>
Dans pawcoach/base44/functions/deleteUser/entry.ts, ajouter la suppression de l'entite User juste avant le return final (ligne 61).

La variable `users` est deja disponible dans le scope du Step 0 (ligne 17) : `const users = await base44.asServiceRole.entities.User.filter({ email: user.email });`

Probleme : `users` est declare dans un bloc `try/catch` interne (Step 0, lignes 16-25), donc hors scope a la ligne 61.

Solution : Declarer une variable `userEntityId` avant le try/catch du Step 0, la remplir a l'interieur, puis l'utiliser pour la suppression.

Code exact a ajouter/modifier :

1. Juste apres `const user = await base44.auth.me();` (ligne 9), ajouter :
```typescript
let userEntityId: string | null = null;
```

2. Dans le bloc Step 0 (try interne), apres la ligne `const subscriptionId = users[0]?.stripe_subscription_id;`, ajouter :
```typescript
if (users[0]?.id) userEntityId = users[0].id;
```

3. Juste avant le `return Response.json({ success: true, ... })` final (ligne 61), ajouter :
```typescript
// Step 4: Delete the User entity itself (RGPD — right to erasure)
if (userEntityId) {
  await base44.asServiceRole.entities.User.delete(userEntityId).catch((err: unknown) => {
    console.error('User entity deletion failed:', err);
  });
}
```

Ne pas envelopper dans un nouveau try/catch global — utiliser .catch() inline pour ne pas bloquer en cas d'erreur partielle.
  </action>
  <verify>
    <automated>grep -n "User.delete\|userEntityId" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/deleteUser/entry.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier contient la string "User.delete" (verifiable par grep)
    - Le fichier contient "userEntityId" declare avant le premier try/catch interne
    - Le fichier contient "Step 4" commentaire avant l'appel User.delete
    - Le return final success:true est toujours present apres le Step 4
    - Le fichier ne contient pas de syntax errors evidentes (TypeScript valide)
  </acceptance_criteria>
  <done>deleteUser supprime l'entite User elle-meme via asServiceRole apres avoir supprime toutes les donnees liees. Un compte supprime n'existe plus en base.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-02 — VetNote passe par vetAccess avec verification d'acces</name>
  <files>
    pawcoach/base44/functions/vetAccess/entry.ts
    pawcoach/src/components/vet/VetNoteForm.jsx
  </files>
  <read_first>
    - pawcoach/base44/functions/vetAccess/entry.ts (lire la structure complete — surtout le pattern getDogData pour comprendre comment la verification d'acces est faite)
    - pawcoach/src/components/vet/VetNoteForm.jsx (lire les lignes 1-50 — voir les props disponibles : dogId, vetEmail, vetName, onNoteAdded)
  </read_first>
  <action>
**Partie A — Backend (vetAccess/entry.ts) :**

Ajouter une nouvelle action 'addVetNote' dans la chaine if/else de vetAccess/entry.ts, AVANT le return final `Response.json({ error: 'Unknown action' })`.

Inserer ce bloc :
```typescript
// --- VET: Add a note (with access check) ---
if (action === 'addVetNote') {
  if (!dogId) return Response.json({ error: 'dogId required' }, { status: 400 });
  const { title, content, category, is_urgent } = await req.json().then(b => b).catch(() => ({}));
  // Re-parse body — already parsed above, use destructured values from top-level destructuring
  // Note: body is already destructured at line 111 as { action, dogId, vetEmail, vetName, sections, accessId, inviteCode }
  // We need title/content/category/is_urgent from same body — add them to the top-level destructure

  // Verify active access
  const accesses = await base44.asServiceRole.entities.SharedVetAccess.filter({ dog_id: dogId, vet_email: user.email, status: 'active' });
  if (!accesses || accesses.length === 0) {
    return Response.json({ error: 'No active access to this dog' }, { status: 403 });
  }

  const note = await base44.asServiceRole.entities.VetNote.create({
    dog_id: dogId,
    vet_email: user.email,
    vet_name: user.full_name || vetName || '',
    title: String(title || '').trim(),
    content: String(content || '').trim(),
    category: category || 'observation',
    is_urgent: Boolean(is_urgent),
  });

  return Response.json({ success: true, note });
}
```

IMPORTANT : La ligne 111 destructure le body : `const { action, dogId, vetEmail, vetName, sections, accessId, inviteCode } = await req.json();`
Il faut ajouter `title, content, category, is_urgent` a cette destructuration :
```typescript
const { action, dogId, vetEmail, vetName, sections, accessId, inviteCode, title, content, category, is_urgent } = await req.json();
```

**Partie B — Frontend (VetNoteForm.jsx) :**

Remplacer l'import de `VetNote` par l'import de `base44` :
- Supprimer : `import { VetNote } from "@/api/entities";`
- Ajouter : `import { base44 } from "@/api/base44Client";`

Remplacer le bloc handleSubmit (lignes 30-38) :
```javascript
// AVANT (a supprimer) :
const note = await VetNote.create({
  dog_id: dogId,
  vet_email: vetEmail,
  vet_name: vetName,
  title: title.trim(),
  content: content.trim(),
  category,
  is_urgent: isUrgent,
});

// APRES (remplacer par) :
const { note } = await base44.functions.vetAccess({
  action: 'addVetNote',
  dogId,
  vetEmail,
  vetName,
  title: title.trim(),
  content: content.trim(),
  category,
  is_urgent: isUrgent,
});
```

La variable `note` reste disponible pour `onNoteAdded(note)` ligne 45.
  </action>
  <verify>
    <automated>grep -n "addVetNote\|base44Client\|vetAccess" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/vet/VetNoteForm.jsx && grep -n "addVetNote\|status.*active" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/vetAccess/entry.ts | grep -A2 -B2 "addVetNote"</automated>
  </verify>
  <acceptance_criteria>
    - VetNoteForm.jsx ne contient plus "VetNote" (import supprime)
    - VetNoteForm.jsx contient "base44Client" dans l'import
    - VetNoteForm.jsx contient "base44.functions.vetAccess"
    - VetNoteForm.jsx contient "action: 'addVetNote'"
    - vetAccess/entry.ts contient le bloc "if (action === 'addVetNote')"
    - vetAccess/entry.ts contient "SharedVetAccess.filter" avec "status: 'active'" dans le bloc addVetNote
    - vetAccess/entry.ts contient "VetNote.create" dans le bloc addVetNote
    - La destructuration du body (req.json()) inclut title, content, category, is_urgent
  </acceptance_criteria>
  <done>La creation de VetNote passe obligatoirement par vetAccess backend qui verifie qu'un SharedVetAccess actif existe pour ce vet sur ce chien. Un appel direct a VetNote.create depuis le frontend n'est plus possible.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-03 — Masquer l'email du proprietaire sur DogPublicProfile</name>
  <files>pawcoach/src/pages/DogPublicProfile.jsx</files>
  <read_first>
    - pawcoach/src/pages/DogPublicProfile.jsx lignes 246-270 (voir le bloc "Contacter le proprietaire" exact avec le mailto et l'affichage de dog.owner)
  </read_first>
  <action>
Dans pawcoach/src/pages/DogPublicProfile.jsx, remplacer le bloc "Contacter le proprietaire" (lignes 247-258) qui affiche l'email brut.

**AVANT (a remplacer) :**
```jsx
{dog.owner && (
  <div className="bg-emerald-50 border border-emerald-200 rounded-2xl p-4">
    <h2 className="text-xs font-black text-emerald-700 uppercase tracking-widest mb-2">Contacter le propriétaire</h2>
    <a
      href={`mailto:${dog.owner}`}
      className="flex items-center gap-2 text-emerald-700 hover:text-emerald-800 transition-colors"
    >
      <Mail className="w-4 h-4 flex-shrink-0" />
      <span className="text-sm font-semibold break-all">{dog.owner}</span>
    </a>
  </div>
)}
```

**APRES (remplacer par) :**
```jsx
{dog.owner && (
  <div className="bg-emerald-50 border border-emerald-200 rounded-2xl p-4">
    <h2 className="text-xs font-black text-emerald-700 uppercase tracking-widest mb-2">Contacter le propriétaire</h2>
    <a
      href={`mailto:${dog.owner}`}
      className="flex items-center gap-2 text-emerald-700 hover:text-emerald-800 transition-colors"
    >
      <Mail className="w-4 h-4 flex-shrink-0" />
      <span className="text-sm font-semibold">Envoyer un message au propriétaire</span>
    </a>
  </div>
)}
```

Le lien mailto fonctionne toujours (le vet peut cliquer pour envoyer un email), mais l'adresse email n'est plus affichee en clair dans l'interface.

Ne pas supprimer l'import de `Mail` — il reste utilise.
  </action>
  <verify>
    <automated>grep -n "dog\.owner\|break-all\|Envoyer un message" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/DogPublicProfile.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier ne contient plus "{dog.owner}" dans un span visible (le span avec break-all et le contenu email est supprime)
    - Le fichier contient "Envoyer un message au propriétaire" comme texte du lien
    - Le fichier contient toujours "href={`mailto:${dog.owner}`}" (le lien fonctionnel est preserve)
    - Le fichier contient toujours "import { ... Mail ..." en haut
    - La string "break-all" n'apparait plus dans ce composant (elle servait uniquement a l'email)
  </acceptance_criteria>
  <done>L'email du proprietaire n'est plus visible en clair sur le profil public. Le lien de contact reste fonctionnel via mailto sans afficher l'adresse.</done>
</task>

</tasks>

<verification>
Apres les 3 taches, verifier :

```bash
# SEC-01 : User.delete present dans deleteUser
grep -c "User.delete" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/deleteUser/entry.ts
# Attendu : 1

# SEC-02 : addVetNote present dans vetAccess
grep -c "addVetNote" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/vetAccess/entry.ts
# Attendu : >= 2 (if condition + eventuellement commentaire)

# SEC-02 : VetNote.create absent du frontend
grep -c "VetNote" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/vet/VetNoteForm.jsx
# Attendu : 0

# SEC-03 : Email masque
grep -c "break-all" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/DogPublicProfile.jsx
# Attendu : 0
```
</verification>

<success_criteria>
1. deleteUser/entry.ts appelle User.delete sur l'entite User apres avoir nettoye toutes les donnees liees
2. vetAccess/entry.ts gere l'action 'addVetNote' avec verification SharedVetAccess status='active' avant creation
3. VetNoteForm.jsx appelle base44.functions.vetAccess au lieu de VetNote.create directement
4. DogPublicProfile.jsx affiche "Envoyer un message au proprietaire" sans exposer l'email brut
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-security-legal/01-01-SUMMARY.md` avec :
- Les 3 fichiers modifies et description exacte du changement
- Confirmation que les grep de verification passent
- Toute deviation par rapport au plan et raison
</output>
