---
phase: 03-scalabilite-premium
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useActionCredits.js
  - src/components/ui/AICreditsGate.jsx
  - src/components/activite/AITrainingProgram.jsx
  - src/components/profile/ReferralSection.jsx
autonomous: true
requirements:
  - PREM-02
  - PREM-03
  - PREM-04
must_haves:
  truths:
    - "Double-cliquer sur Generer dans AITrainingProgram ne lance pas deux appels API en parallele"
    - "consume() dans useActionCredits ne peut pas etre appele deux fois en parallele"
    - "UpgradePrompt ne s'affiche plus pendant le chargement initial des credits (loading=true)"
    - "La section Parrainer un ami est retiree du profil — aucun backend de validation n'existe"
  artifacts:
    - path: "src/hooks/useActionCredits.js"
      provides: "guard anti-double-appel sur consume()"
      contains: "consuming"
    - path: "src/components/activite/AITrainingProgram.jsx"
      provides: "disabled={generating} sur le bouton Generer"
      contains: "disabled.*generating"
    - path: "src/components/ui/AICreditsGate.jsx"
      provides: "UpgradePrompt conditionne par loading state"
      contains: "loading"
    - path: "src/components/profile/ReferralSection.jsx"
      provides: "Composant vide ou suppression de la section"
      contains: "null"
  key_links:
    - from: "src/hooks/useActionCredits.js consume()"
      to: "guard flag consuming"
      via: "if (consuming) return false — atomique dans le closure React"
      pattern: "consuming"
    - from: "src/components/activite/AITrainingProgram.jsx generate()"
      to: "bouton Generer"
      via: "disabled={generating} — React re-render desactive le bouton pendant la generation"
      pattern: "disabled.*generating"
---

<objective>
Quatre fixes ciblés : (1) guard anti-double-clic sur consume() dans useActionCredits, (2) disabled sur le bouton Générer d'AITrainingProgram, (3) ne pas afficher UpgradePrompt pendant le chargement initial des crédits, (4) retirer la section Parrainer un ami sans backend.

Purpose: Éviter la consommation double de crédits et supprimer une feature morte.
Output: 4 fichiers modifiés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Extrait de src/hooks/useActionCredits.js — état actuel -->

```javascript
export function useActionCredits() {
  const [credits, setCredits] = useState(null);
  const [isPremium, setIsPremium] = useState(false);
  const [loading, setLoading] = useState(true);

  // ... useEffect init ...

  const consume = async () => {
    if (isPremium) return true;
    if ((credits ?? 0) <= 0) return false;
    const newRemaining = await consumeActionCredit(credits);
    setCredits(newRemaining);
    return true;
  };

  const hasCredits = isPremium || (credits ?? 0) > 0;
  return { credits, hasCredits, isPremium, loading, consume };
}
```

Problème : consume() est async mais pas protégée contre les appels concurrents. Un double-clic peut lancer deux consume() en parallèle : les deux lisent credits=3, les deux écrivent 2.

<!-- Extrait de src/components/activite/AITrainingProgram.jsx -->

```javascript
async function generate() {
  if (!isPremium && !hasCredits) return;
  setGenerating(true);      // ← state mis à true
  // ...long async...
  if (!isPremium) await consume();
  // ...
  setGenerating(false);
}
// Bouton :
<Button onClick={generate} className="w-full bg-gradient-to-r ...">
  Générer mon programme 7 jours
</Button>
```

Problème : le bouton n'a pas `disabled={generating}` — React re-render ne désactive pas le bouton pendant la génération.

<!-- src/components/ui/AICreditsGate.jsx — comportement actuel -->

UpgradePrompt est rendu dans les parents avec `{!isPremium && !hasCredits ? <UpgradePrompt /> : <Button>}`.
Pendant loading=true, credits=null → hasCredits = false → UpgradePrompt s'affiche brièvement.
`loading` est retourné par useActionCredits mais jamais consommé dans le rendu conditionnel.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: useActionCredits — guard anti-double-appel sur consume()</name>
  <files>src/hooks/useActionCredits.js</files>
  <read_first>src/hooks/useActionCredits.js — lire entier avant modification</read_first>
  <action>
Ajouter un flag `consuming` (useRef ou variable de closure) pour empêcher deux appels concurrents à `consume()`.

Remplacer la fonction `consume` par :

```javascript
const consumingRef = useRef(false);

const consume = async () => {
  if (isPremium) return true;
  if ((credits ?? 0) <= 0) return false;
  if (consumingRef.current) return false; // guard anti-double-appel
  consumingRef.current = true;
  try {
    const newRemaining = await consumeActionCredit(credits);
    setCredits(newRemaining);
    return true;
  } finally {
    consumingRef.current = false;
  }
};
```

Ajouter `useRef` à l'import React en haut du fichier si pas déjà présent :
`import { useState, useEffect, useRef } from "react";`

Le `consumingRef` doit être déclaré à l'intérieur de la fonction `useActionCredits`, avant `consume`.
  </action>
  <verify>
    <automated>grep -n "consumingRef\|useRef\|consuming\.current" src/hooks/useActionCredits.js</automated>
  </verify>
  <acceptance_criteria>
- `useRef` importé de react
- `consumingRef` déclaré avec `useRef(false)` dans useActionCredits
- `if (consumingRef.current) return false;` présent dans consume()
- `consumingRef.current = true/false` dans try/finally
  </acceptance_criteria>
  <done>consume() retourne false immédiatement si un appel est déjà en cours — double-clic ne consomme plus 2 crédits</done>
</task>

<task type="auto">
  <name>Task 2: AITrainingProgram — disabled sur bouton Générer + guard UpgradePrompt pendant chargement</name>
  <files>src/components/activite/AITrainingProgram.jsx</files>
  <read_first>src/components/activite/AITrainingProgram.jsx lignes 467-469 (credits/generating state) et 885-895 (bouton) et 679 (generate function)</read_first>
  <action>
Deux modifications dans ce fichier :

**1. Ajouter `if (generating) return;` en début de generate() :**

```javascript
async function generate() {
  if (generating) return; // guard double-clic
  if (!isPremium && !hasCredits) return;
  setGenerating(true);
  // ... reste inchangé
```

**2. Ajouter `disabled={generating}` sur le bouton Générer (ligne ~890) :**

```jsx
<Button
  onClick={generate}
  disabled={generating}
  className="w-full bg-gradient-to-r from-violet-600 to-purple-600 hover:from-violet-700 hover:to-purple-700 text-white"
>
  <Sparkles className="w-4 h-4 mr-2" />
  Générer mon programme 7 jours
</Button>
```

**3. Modifier le rendu conditionnel pour ignorer `loading` :**

Le bloc conditionnel actuel (ligne ~885) est :
```jsx
{!isPremium && !hasCredits ? (
  <UpgradePrompt type="action" from="training" />
) : ( ... )}
```

Remplacer par :
```jsx
{!isPremium && !loading && !hasCredits ? (
  <UpgradePrompt type="action" from="training" />
) : ( ... )}
```

`loading` est déjà retourné par `useActionCredits()` sur la ligne 467. Vérifier qu'il est bien destructuré :
`const { credits, hasCredits, isPremium, consume, loading } = useActionCredits();`

Si `loading` n'est pas dans le destructuring, l'ajouter.
  </action>
  <verify>
    <automated>grep -n "disabled.*generating\|if.*generating.*return\|!loading.*hasCredits\|loading.*useActionCredits" src/components/activite/AITrainingProgram.jsx</automated>
  </verify>
  <acceptance_criteria>
- `if (generating) return;` présent en début de generate()
- `disabled={generating}` sur le bouton Générer
- `!loading` présent dans le conditionnel UpgradePrompt
- `loading` dans le destructuring de useActionCredits()
  </acceptance_criteria>
  <done>Bouton Générer désactivé pendant la génération. UpgradePrompt ne s'affiche pas pendant le chargement initial des crédits.</done>
</task>

<task type="auto">
  <name>Task 3: ReferralSection — retirer le composant (feature morte sans backend)</name>
  <files>src/components/profile/ReferralSection.jsx</files>
  <read_first>src/components/profile/ReferralSection.jsx — lire entier. Chercher aussi où ce composant est importé/utilisé.</read_first>
  <action>
**Étape 1 : Trouver tous les usages de ReferralSection**

Avant de modifier, vérifier où ReferralSection est importé :
```bash
grep -rn "ReferralSection" src/
```

**Étape 2 : Remplacer le composant par null**

Remplacer tout le contenu de `src/components/profile/ReferralSection.jsx` par un composant vide qui retourne null — évite de casser les imports existants :

```jsx
// Section referral retiree : aucun backend de validation du code parrain (PREM-04)
export default function ReferralSection() {
  return null;
}
```

NE PAS supprimer le fichier (les imports dans d'autres fichiers casseraient le build). Retourner null est la solution propre — React n'affiche rien, aucune régression.

**Étape 3 : Vérifier les imports dans la page Profile**

Si `grep -rn "ReferralSection" src/` montre un import dans `src/pages/Profile.jsx` (ou similaire), NE PAS toucher cet import — le composant retournant null est suffisant, rien ne s'affiche.
  </action>
  <verify>
    <automated>grep -n "return null\|PREM-04" src/components/profile/ReferralSection.jsx</automated>
  </verify>
  <acceptance_criteria>
- Le fichier contient uniquement `export default function ReferralSection() { return null; }`
- Le commentaire PREM-04 est présent
- Aucun code JSX de la section parrainage n'est rendu
  </acceptance_criteria>
  <done>ReferralSection retourne null — section Parrainer un ami disparue de l'UI, aucun import cassé</done>
</task>

</tasks>

<verification>
```bash
# PREM-02 guard consume
grep -c "consumingRef\|consuming\.current" src/hooks/useActionCredits.js

# PREM-02 guard generate
grep -n "if.*generating.*return" src/components/activite/AITrainingProgram.jsx

# PREM-03 loading guard
grep -n "!loading.*hasCredits\|hasCredits.*!loading" src/components/activite/AITrainingProgram.jsx

# PREM-04 section retiree
grep -n "return null" src/components/profile/ReferralSection.jsx
```
</verification>

<success_criteria>
1. consume() : consumingRef guard présent — double-clic retourne false immédiatement
2. generate() : guard `if (generating) return` + `disabled={generating}` sur le bouton
3. AITrainingProgram : `!loading` dans le conditionnel UpgradePrompt — pas de flash au chargement
4. ReferralSection : retourne null — section disparue de l'UI, build intact
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-scalabilite-premium/03-03-SUMMARY.md`
</output>
