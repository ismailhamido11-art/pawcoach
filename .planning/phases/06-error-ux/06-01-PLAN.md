---
phase: 06-error-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
  - src/pages/Sante.jsx
  - src/pages/Dashboard.jsx
  - src/pages/Training.jsx
  - src/pages/Profile.jsx
  - src/pages/Activite.jsx
  - src/components/dogprofile/DogEditModal.jsx
autonomous: true
requirements:
  - ERR-01
  - ERR-04

must_haves:
  truths:
    - "Un utilisateur sur Home qui perd sa connexion au chargement voit un message toast en français, pas un écran blanc silencieux"
    - "Un utilisateur sur Santé qui perd sa connexion voit un message toast en français"
    - "Un utilisateur sur Dashboard qui perd sa connexion voit un message toast en français"
    - "Un utilisateur sur Training qui perd sa connexion voit un message toast en français"
    - "Un utilisateur sur Profile qui perd sa connexion voit un message toast en français"
    - "La page Santé affiche un skeleton pendant le chargement, pas un flash de contenu vide"
    - "La page Activité affiche un skeleton pendant le chargement au lieu de la structure vide"
    - "L'upload photo dans DogEditModal affiche un toast d'erreur si l'upload échoue"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "Bloc catch avec toast.error en français"
      contains: "toast.error"
    - path: "src/pages/Sante.jsx"
      provides: "Bloc if (loading) avec skeleton + catch avec toast.error"
      contains: "animate-pulse"
    - path: "src/pages/Dashboard.jsx"
      provides: "Bloc catch avec toast.error en français"
      contains: "toast.error"
    - path: "src/pages/Training.jsx"
      provides: "Bloc catch avec toast.error en français"
      contains: "toast.error"
    - path: "src/pages/Profile.jsx"
      provides: "Bloc catch avec toast.error en français"
      contains: "toast.error"
    - path: "src/pages/Activite.jsx"
      provides: "Bloc if (loading) avec skeleton"
      contains: "animate-pulse"
    - path: "src/components/dogprofile/DogEditModal.jsx"
      provides: "Catch photo upload avec toast.error"
      contains: "toast.error"
  key_links:
    - from: "catch block in loadData/useEffect"
      to: "toast.error()"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.error"
    - from: "loading state true"
      to: "skeleton render block"
      via: "if (loading) { return <skeleton> }"
      pattern: "animate-pulse"
---

<objective>
Corriger les catch silencieux sur 5 pages principales et DogEditModal, et ajouter les squelettes de chargement manquants pour Santé et Activité.

Purpose: Quand une API échoue, l'utilisateur voit actuellement un écran blanc ou rien — il ne sait pas si l'app est cassée ou en train de charger. Ces corrections donnent un retour immédiat sur ce qui se passe.
Output: 7 fichiers modifiés — chaque catch de chargement affiche un toast français, Santé et Activité ont un skeleton pendant le loading.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/pages/Home.jsx
@src/pages/Sante.jsx
@src/pages/Dashboard.jsx
@src/pages/Training.jsx
@src/pages/Profile.jsx
@src/pages/Activite.jsx
@src/components/dogprofile/DogEditModal.jsx

<interfaces>
<!-- Pattern toast existant dans le projet (extrait de Home.jsx, Activite.jsx, Scan.jsx) -->
```jsx
import { toast } from "sonner";

// Usage dans un catch :
} catch (err) {
  console.error(err);
  toast.error("Impossible de charger les données. Vérifie ta connexion.");
}
```

<!-- Pattern skeleton existant dans le projet (extrait de Dashboard.jsx, Training.jsx) -->
```jsx
if (loading) {
  return (
    <div className="min-h-screen bg-background pb-28">
      <div className="gradient-primary safe-pt-16 pb-8 px-5">
        <div className="h-6 w-40 bg-white/20 rounded animate-pulse" />
        <div className="h-4 w-56 bg-white/10 rounded animate-pulse mt-2" />
      </div>
      <div className="px-5 pt-5 space-y-4">
        {[0, 1, 2].map(i => (
          <div key={i} className="bg-white rounded-2xl p-4 border border-border/40 h-20 animate-pulse" />
        ))}
      </div>
      <BottomNav currentPage="Sante" />
    </div>
  );
}
```

<!-- Sante.jsx : la page commence à la ligne 119 avec return() sans bloc if(loading) -->
<!-- Activite.jsx : loading state existe (ligne 34) mais aucun bloc if(loading) { return skeleton } -->
<!-- Home.jsx catch bloc ligne 148 : console.error(err) sans toast -->
<!-- Sante.jsx catch bloc ligne 100 : console.error(e) sans toast -->
<!-- Dashboard.jsx catch bloc ligne 122 : console.error("Dashboard load error:", err) sans toast -->
<!-- Training.jsx catch bloc ligne 186 : console.error("Training load error:", err) sans toast -->
<!-- Profile.jsx catch bloc ligne 40 : console.error("Profile load error:", err) sans toast -->
<!-- DogEditModal.jsx catch photo upload ligne 41 : console.error(err) sans toast -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger les catch silencieux sur les 5 pages principales</name>
  <files>src/pages/Home.jsx, src/pages/Sante.jsx, src/pages/Dashboard.jsx, src/pages/Training.jsx, src/pages/Profile.jsx</files>
  <action>
Pour chaque fichier, lire le contenu complet puis ajouter `toast.error(...)` dans le bloc catch du chargement initial. `toast` est déjà importé dans Home.jsx et Training.jsx — l'ajouter aux imports dans Sante, Dashboard, Profile si absent.

**Home.jsx** (ligne ~148) — le catch du useEffect loadData() ne contient que `console.error(err)`. Ajouter après le console.error :
```jsx
toast.error("Impossible de charger les données. Vérifie ta connexion.");
```

**Sante.jsx** (ligne ~100) — le catch du loadData() ne contient que `console.error(e)`. Ajouter `import { toast } from "sonner";` en haut si absent. Ajouter après le console.error :
```jsx
toast.error("Impossible de charger les données de santé. Vérifie ta connexion.");
```

**Dashboard.jsx** (ligne ~122) — le catch ne contient que `console.error("Dashboard load error:", err)`. Ajouter `import { toast } from "sonner";` en haut si absent. Ajouter après le console.error :
```jsx
toast.error("Impossible de charger le tableau de bord. Vérifie ta connexion.");
```

**Training.jsx** (ligne ~186) — le catch `console.error("Training load error:", err)`. `toast` est déjà importé. Ajouter après :
```jsx
toast.error("Impossible de charger les exercices. Vérifie ta connexion.");
```

**Profile.jsx** (ligne ~40) — le catch `console.error("Profile load error:", err)`. Ajouter `import { toast } from "sonner";` en haut si absent. Ajouter après :
```jsx
toast.error("Impossible de charger le profil. Vérifie ta connexion.");
```

NE PAS modifier la logique de navigation (ex: `navigate(createPageUrl("Onboarding"))`) qui se trouve dans d'autres blocs. NE PAS toucher aux catch des actions mutation (checkin, update, delete) — ils ont déjà leurs propres toasts.
  </action>
  <verify>
    <automated>grep -n "toast.error" src/pages/Home.jsx src/pages/Sante.jsx src/pages/Dashboard.jsx src/pages/Training.jsx src/pages/Profile.jsx</automated>
  </verify>
  <done>Chaque fichier a au moins un toast.error dans son bloc catch de chargement initial. La commande grep retourne au moins 1 ligne par fichier.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter skeletons pour Santé et Activité + toast dans DogEditModal</name>
  <files>src/pages/Sante.jsx, src/pages/Activite.jsx, src/components/dogprofile/DogEditModal.jsx</files>
  <action>
**Sante.jsx** — Ajouter un bloc `if (loading) { return <skeleton> }` juste avant le `return (` principal (ligne ~119). Le skeleton doit :
- Afficher le header gradient avec des blocs animate-pulse blancs pour le titre et les stats
- Afficher les tabs sous forme de blocs pill animate-pulse
- Afficher 3 cards de contenu animate-pulse
- Inclure `<BottomNav currentPage="Sante" />`

Structure exacte à insérer avant `return (` à la ligne ~119 :
```jsx
if (loading) {
  return (
    <div className="min-h-screen bg-background flex flex-col">
      <WellnessBanner />
      <div className="gradient-primary px-5 safe-pt-14 pb-3 relative overflow-hidden mt-8">
        <div className="flex items-end gap-3 mb-3">
          <div className="flex-1 pb-1">
            <div className="h-3 w-16 bg-white/20 rounded animate-pulse mb-2" />
            <div className="h-7 w-28 bg-white/20 rounded animate-pulse" />
          </div>
          <div className="w-20 h-20 rounded-full bg-white/10 animate-pulse flex-shrink-0" />
        </div>
        <div className="grid grid-cols-5 gap-1.5 mt-1">
          {[0,1,2,3,4].map(i => (
            <div key={i} className="h-16 rounded-2xl bg-white/10 animate-pulse" />
          ))}
        </div>
      </div>
      <div className="flex-1 px-4 pt-4 space-y-3">
        {[0,1,2].map(i => (
          <div key={i} className="bg-white rounded-2xl border border-border/40 h-32 animate-pulse" />
        ))}
      </div>
      <BottomNav currentPage="Sante" />
    </div>
  );
}
```

**Activite.jsx** — Ajouter un bloc `if (loading) { return <skeleton> }` juste avant le `return (` principal (après le `useEffect` de load). Le skeleton doit :
- Afficher le hero gradient avec titre et 4 tabs pill animate-pulse
- Afficher 2 cards de contenu animate-pulse
- Inclure `<BottomNav currentPage="Activite" />`

Structure exacte :
```jsx
if (loading) {
  return (
    <div className="min-h-screen bg-background flex flex-col">
      <WellnessBanner />
      <div className="gradient-primary px-5 safe-pt-14 pb-3 relative overflow-hidden mt-8">
        <div className="flex items-end gap-3 mb-3">
          <div className="flex-1 pb-1">
            <div className="h-3 w-16 bg-white/20 rounded animate-pulse mb-2" />
            <div className="h-7 w-24 bg-white/20 rounded animate-pulse" />
          </div>
          <div className="w-20 h-20 rounded-full bg-white/10 animate-pulse flex-shrink-0" />
        </div>
        <div className="grid grid-cols-4 gap-1.5 mt-1">
          {[0,1,2,3].map(i => (
            <div key={i} className="h-16 rounded-2xl bg-white/10 animate-pulse" />
          ))}
        </div>
      </div>
      <div className="flex-1 px-4 pt-4 space-y-3">
        {[0,1].map(i => (
          <div key={i} className="bg-white rounded-2xl border border-border/40 h-40 animate-pulse" />
        ))}
      </div>
      <BottomNav currentPage="Activite" />
    </div>
  );
}
```

**DogEditModal.jsx** — Dans le catch du `handlePhotoUpload` (ligne ~41), ajouter `import { toast } from "sonner";` en haut du fichier si absent, puis après `console.error(err)` ajouter :
```jsx
toast.error("Impossible d'uploader la photo. Réessaie.");
```
  </action>
  <verify>
    <automated>grep -n "animate-pulse" src/pages/Sante.jsx src/pages/Activite.jsx</automated>
  </verify>
  <done>
  - Sante.jsx contient un bloc if (loading) avec animate-pulse avant le return principal
  - Activite.jsx contient un bloc if (loading) avec animate-pulse avant le return principal
  - DogEditModal.jsx a un toast.error dans son catch handlePhotoUpload
  </done>
</task>

</tasks>

<verification>
```bash
# ERR-01 : toast.error dans tous les catch de chargement
grep -c "toast.error" src/pages/Home.jsx src/pages/Sante.jsx src/pages/Dashboard.jsx src/pages/Training.jsx src/pages/Profile.jsx

# ERR-04 : skeletons présents
grep -c "animate-pulse" src/pages/Sante.jsx src/pages/Activite.jsx

# Pas de régression build
cd /c/Users/smalt/Desktop/app-chien-ia/pawcoach && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- ERR-01 : 5 pages + DogEditModal ont chacune au moins 1 toast.error dans leur bloc catch de chargement initial
- ERR-04 : Sante.jsx et Activite.jsx ont un bloc if(loading) avec animate-pulse qui s'affiche pendant le chargement
- Build Vite passe sans erreur
- Aucune régression sur les toasts de mutation existants (checkin, delete, update)
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-error-ux/06-01-SUMMARY.md` avec :
- Fichiers modifiés et ce qui a changé dans chacun
- Toast messages ajoutés (copier les strings exactes)
- Confirmation build OK
</output>
