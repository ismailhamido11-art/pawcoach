---
phase: 06-error-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.jsx
  - src/components/notebook/PremiumSection.jsx
  - src/components/dogprofile/DogEditModal.jsx
autonomous: true
requirements:
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "Un utilisateur sur Dashboard sans pesées voit un message 'Enregistre ton premier poids' avec un lien vers la page Santé"
    - "Un utilisateur sur Dashboard sans balades voit un message 'Lance ta première balade' avec un lien vers la page Activité"
    - "Un utilisateur sur l'onglet Médicaments/Visites vétérinaire/Notes vide voit une illustration et un message contextuel"
    - "Un utilisateur qui tente de sauvegarder DogEditModal sans nom de chien voit un message d'erreur sous le champ Nom"
    - "Un utilisateur qui tente de sauvegarder DogEditModal sans sélectionner le sexe voit un message d'erreur sous la section Sexe"
  artifacts:
    - path: "src/pages/Dashboard.jsx"
      provides: "États vides illustrés pour graphiques poids et balades"
      contains: "Enregistre"
    - path: "src/components/notebook/PremiumSection.jsx"
      provides: "État vide illustré avec Illustration component"
      contains: "Illustration"
    - path: "src/components/dogprofile/DogEditModal.jsx"
      provides: "Validation name et sex avec messages inline"
      contains: "nameError"
  key_links:
    - from: "Dashboard.jsx weightData.length < 2 condition"
      to: "Link vers /Sante"
      via: "createPageUrl('Sante')"
      pattern: "createPageUrl.*Sante"
    - from: "DogEditModal handleSave"
      to: "validation name.trim() && sex check"
      via: "if (!form.name.trim()) { setNameError(true); return; }"
      pattern: "nameError"
---

<objective>
Ajouter les états vides illustrés manquants sur Dashboard et PremiumSection, et corriger la validation du formulaire DogEditModal.

Purpose: Un utilisateur nouveau voit des sections vides sans contexte — il ne sait pas quoi faire. Un formulaire sans validation requiert du code backend pour rejeter des données invalides. Ces corrections rendent l'app auto-explicative.
Output: 3 fichiers modifiés — Dashboard a des états vides avec action, PremiumSection a une illustration, DogEditModal valide les champs requis.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/pages/Dashboard.jsx
@src/components/notebook/PremiumSection.jsx
@src/components/dogprofile/DogEditModal.jsx

<interfaces>
<!-- Illustration component disponible (extrait de src/components/illustrations/Illustration.jsx) -->
```jsx
// Noms disponibles : adoptAPet, goodDoggy, dogHighFive, petFood, dogWalking, veterinary,
//                   petCare, qualityTime, cautiousDog, dogPaw, petGrooming, walkingAround
import Illustration from "../illustrations/Illustration";
<Illustration name="goodDoggy" className="w-24 h-24 opacity-80" alt="chien content" />
```

<!-- Pattern état vide illustré existant (TrackerHistory.jsx) -->
```jsx
if (sorted.length === 0) {
  return (
    <div className="flex flex-col items-center justify-center py-16 text-center space-y-3">
      <span className="text-5xl">🦮</span>
      <p className="font-bold text-foreground">Aucune balade enregistrée</p>
      <p className="text-xs text-muted-foreground">Lance une balade pour voir ton historique ici.</p>
    </div>
  );
}
```

<!-- Dashboard.jsx : graphiques conditionnels existants (lignes 363-441) -->
<!-- weightData.length >= 2 guard : si < 2, rien n'est affiché -->
<!-- walkData.length >= 2 guard : si < 2, rien n'est affiché -->
<!-- checkinChart utilisé dans le 3e graphique conditionnel -->
<!-- Link et createPageUrl déjà importés dans Dashboard.jsx -->

<!-- PremiumSection.jsx : état vide actuel (ligne 170-175) -->
```jsx
{filtered.length === 0 && !showForm && (
  <div className="text-center py-8">
    <ConfigIcon className={`w-8 h-8 mx-auto mb-2 ${config.textClass} opacity-40`} />
    <p className="text-muted-foreground text-sm">{config.emptyText}</p>
  </div>
)}
```
<!-- Illustration n'est pas importé dans PremiumSection — à ajouter -->

<!-- DogEditModal.jsx : handleSave actuel (ligne 21-32) -->
```jsx
const handleSave = async () => {
  if (form.birth_date && new Date(form.birth_date) > new Date()) {
    setDateError(true);
    return;
  }
  setDateError(false);
  setSaving(true);
  // ... pas de validation name ni sex
```
<!-- State dateError existe mais nameError/sexError n'existent pas encore -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: États vides Dashboard pour les graphiques sans données</name>
  <files>src/pages/Dashboard.jsx</files>
  <action>
Lire le fichier complet. Les deux blocs de graphiques (poids et balades) utilisent des gardes `{weightData.length >= 2 && (...)}` et `{walkData.length >= 2 && (...)}` — quand il n'y a pas assez de données, la section disparaît silencieusement.

**Remplacer** chaque bloc conditionnel par une structure avec état vide :

**Graphique poids (remplacer le bloc `{weightData.length >= 2 && ...}`) :**
```jsx
{weightData.length < 2 ? (
  <div className="bg-white rounded-2xl p-5 shadow-sm border border-border/40 flex flex-col items-center text-center gap-2">
    <Illustration name="dogPaw" className="w-16 h-16 opacity-60" alt="" />
    <p className="font-semibold text-sm text-foreground">Suis l'évolution du poids</p>
    <p className="text-xs text-muted-foreground">Enregistre au moins 2 pesées pour voir le graphique</p>
    <Link to={createPageUrl("Sante") + "?tab=carnet"} className="mt-1 text-xs font-bold text-primary hover:underline">
      Ajouter une pesée →
    </Link>
  </div>
) : (
  /* ... le bloc AreaChart existant inchangé ... */
)}
```

**Graphique balades (remplacer le bloc `{walkData.length >= 2 && ...}`) :**
```jsx
{walkData.length < 2 ? (
  <div className="bg-white rounded-2xl p-5 shadow-sm border border-border/40 flex flex-col items-center text-center gap-2">
    <Illustration name="dogWalking" className="w-16 h-16 opacity-60" alt="" />
    <p className="font-semibold text-sm text-foreground">Suis les balades quotidiennes</p>
    <p className="text-xs text-muted-foreground">Enregistre au moins 2 balades pour voir le graphique</p>
    <Link to={createPageUrl("Activite") + "?tab=balade"} className="mt-1 text-xs font-bold text-primary hover:underline">
      Lancer une balade →
    </Link>
  </div>
) : (
  /* ... le bloc BarChart existant inchangé ... */
)}
```

`Illustration` n'est pas encore importé dans Dashboard.jsx — ajouter l'import :
```jsx
import Illustration from "../components/illustrations/Illustration";
```
(Dashboard.jsx importe déjà `Link` et `createPageUrl` — ne pas dupliquer ces imports.)

NE PAS modifier la logique de calcul des données (useMemo). NE PAS toucher aux StatCards ni aux SmartAlerts.
  </action>
  <verify>
    <automated>grep -n "Illustration\|Enregistre\|Ajouter une pesée\|Lancer une balade" src/pages/Dashboard.jsx</automated>
  </verify>
  <done>Dashboard.jsx contient les deux états vides illustrés avec des liens d'action vers Santé et Activité. La commande grep retourne des lignes pour chaque message.</done>
</task>

<task type="auto">
  <name>Task 2: État vide illustré dans PremiumSection + validation DogEditModal</name>
  <files>src/components/notebook/PremiumSection.jsx, src/components/dogprofile/DogEditModal.jsx</files>
  <action>
**PremiumSection.jsx** — Lire le fichier complet. Ajouter l'import de l'Illustration en haut du fichier, puis enrichir l'état vide existant (lignes ~170-175) avec une illustration et une phrase d'action :

Ajouter l'import :
```jsx
import Illustration from "../illustrations/Illustration";
```

Remplacer le bloc état vide actuel :
```jsx
{filtered.length === 0 && !showForm && (
  <div className="text-center py-8">
    <ConfigIcon className={`w-8 h-8 mx-auto mb-2 ${config.textClass} opacity-40`} />
    <p className="text-muted-foreground text-sm">{config.emptyText}</p>
  </div>
)}
```

Par :
```jsx
{filtered.length === 0 && !showForm && (
  <div className="flex flex-col items-center text-center py-8 gap-2">
    <Illustration name="veterinary" className="w-20 h-20 opacity-70" alt="" />
    <p className="font-semibold text-sm text-foreground">{config.emptyText}</p>
    <p className="text-xs text-muted-foreground">Utilise le bouton ci-dessus pour en ajouter un</p>
  </div>
)}
```

---

**DogEditModal.jsx** — Lire le fichier complet. Ajouter deux états d'erreur et leur validation dans `handleSave`.

Ajouter les states après `const [dateError, setDateError] = useState(false);` :
```jsx
const [nameError, setNameError] = useState(false);
const [sexError, setSexError] = useState(false);
```

Modifier `handleSave` pour valider name et sex avant la validation date :
```jsx
const handleSave = async () => {
  // Valider name (requis, non vide)
  if (!form.name.trim()) {
    setNameError(true);
    return;
  }
  setNameError(false);
  // Valider sex (requis)
  if (!form.sex) {
    setSexError(true);
    return;
  }
  setSexError(false);
  // Validation date existante
  if (form.birth_date && new Date(form.birth_date) > new Date()) {
    setDateError(true);
    return;
  }
  setDateError(false);
  setSaving(true);
  const cleanForm = { ...form, name: sanitizeName(form.name) };
  await onSave(cleanForm);
  setSaving(false);
  onClose();
};
```

Ajouter le message d'erreur sous le champ Nom (dans le map des champs, après l'input `name`) — modifier la condition de rendu du message d'erreur dans le map :
```jsx
{field === "name" && nameError && (
  <p className="text-xs text-red-500 font-semibold mt-1">Le nom est requis</p>
)}
```

Ajouter le message d'erreur sous la section Sexe (après la div des boutons mâle/femelle) :
```jsx
{sexError && (
  <p className="text-xs text-red-500 font-semibold mt-1">Sélectionne le sexe</p>
)}
```

Réinitialiser `nameError` quand l'utilisateur modifie le champ nom (dans le onChange de l'input name) :
```jsx
onChange={e => { setForm(f => ({ ...f, [field]: e.target.value })); if (field === "birth_date") setDateError(false); if (field === "name") setNameError(false); }}
```

Réinitialiser `sexError` quand l'utilisateur clique sur un bouton de sexe :
```jsx
onClick={() => { setForm(f => ({ ...f, sex: o.value })); setSexError(false); }}
```
  </action>
  <verify>
    <automated>grep -n "nameError\|sexError\|Illustration" src/components/notebook/PremiumSection.jsx src/components/dogprofile/DogEditModal.jsx</automated>
  </verify>
  <done>
  - PremiumSection.jsx importe Illustration et affiche l'état vide avec l'image veterinary
  - DogEditModal.jsx a nameError et sexError states avec messages inline sous les champs
  - La commande grep retourne des lignes pour chaque fichier
  </done>
</task>

</tasks>

<verification>
```bash
# ERR-02 : états vides illustrés présents
grep -n "Illustration" src/pages/Dashboard.jsx src/components/notebook/PremiumSection.jsx

# ERR-03 : validation formulaire présente
grep -n "nameError\|sexError" src/components/dogprofile/DogEditModal.jsx

# Pas de régression build
cd /c/Users/smalt/Desktop/app-chien-ia/pawcoach && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- ERR-02 : Dashboard affiche un état vide illustré pour poids ET balades quand < 2 entrées, avec lien d'action. PremiumSection affiche l'illustration veterinary dans ses états vides.
- ERR-03 : DogEditModal bloque la sauvegarde si name vide (message "Le nom est requis" sous le champ) et si sex non sélectionné (message "Sélectionne le sexe")
- Build Vite passe sans erreur
- Aucune régression sur la logique de sauvegarde existante (validations date, sanitization name)
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-error-ux/06-02-SUMMARY.md` avec :
- Fichiers modifiés et changements apportés dans chacun
- États vides ajoutés (quelles sections)
- Validations ajoutées (quels champs)
- Confirmation build OK
</output>
