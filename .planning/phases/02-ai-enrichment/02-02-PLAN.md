---
phase: 02-ai-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/functions/weeklyInsightGenerate.ts
autonomous: true
requirements:
  - AI-02
must_haves:
  truths:
    - "Le weekly insight mentionne un vaccin en retard s'il existe dans les HealthRecord de la semaine (type=vaccination, overdue=true ou date depassee)"
    - "Le weekly insight mentionne un medicament en cours s'il existe dans les HealthRecord (type=medication avec next_date futur)"
    - "Le weekly insight mentionne une visite vet passee cette semaine si elle figure dans les HealthRecord (type=vet_visit)"
    - "Si aucun evenement sante n'existe, le prompt ne mentionne pas les HealthRecord (pas de fausse information)"
    - "Les behavior_notes et notes des check-ins de la semaine sont incluses dans le contexte prompt quand elles existent"
  artifacts:
    - path: "pawcoach/functions/weeklyInsightGenerate.ts"
      provides: "Weekly insight enrichi avec HealthRecord et notes check-ins"
      contains: "dogHealthRecords|healthContext|notesContext"
  key_links:
    - from: "pawcoach/functions/weeklyInsightGenerate.ts"
      to: "entities.HealthRecord.filter"
      via: "requete des HealthRecord du chien filtre sur la semaine"
      pattern: "dogHealthRecords|HealthRecord"
---

<objective>
Enrichir weeklyInsightGenerate.ts pour que le bilan hebdomadaire integre les evenements sante (HealthRecord : vaccins en retard, visites vet, medicaments en cours) et les notes textuelles des check-ins de la semaine.

Purpose: Aujourd'hui le prompt recoit check-ins, exercices, scans, balades — mais ignore completement les HealthRecord. Un chien qui a eu un vaccin en retard ou une visite vet cette semaine devrait voir ca dans son bilan. Pareil pour les behavior_notes laissees lors des check-ins.

Output: weeklyInsightGenerate.ts modifie — ajout fetch HealthRecord + construction healthContext + notesContext injectes dans le systemPrompt et userMessage
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pawcoach/.planning/PROJECT.md
@pawcoach/.planning/ROADMAP.md
@pawcoach/.planning/STATE.md

<interfaces>
<!-- Entite HealthRecord — champs pertinents -->
HealthRecord: {
  id: string,
  dog_id: string,
  date: string,        // "YYYY-MM-DD"
  type: string,        // "vaccination" | "vet_visit" | "medication" | "weight" | "note"
  label: string,       // description (ex: nom du vaccin, nom du medicament)
  value: number,       // pour weight
  next_date: string,   // "YYYY-MM-DD" — prochaine echeance (vaccin rappel, prochain RDV, fin traitement)
  notes: string        // optionnel
}

<!-- DailyCheckin — champs notes presents dans weekCheckins deja charge -->
DailyCheckin: {
  notes: string,           // note libre du proprietaire
  behavior_notes: string,  // observations comportementales
  symptoms: string[]
}

<!-- Pattern de requete existant (ligne 45-49) -->
const [checkins, dogProgress, dogScans, dogDailyLogs] = await Promise.all([
  base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.UserProgress.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.FoodScan.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.DailyLog.filter({ dog_id: dog.id }).catch(() => []),
]);

<!-- Variables deja disponibles : weekStart, weekEnd, weekCheckins, safeName -->
<!-- Le userMessage est construit ligne 184, le systemPrompt ligne 182 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter fetch HealthRecord et construire les contextes sante et notes</name>
  <files>pawcoach/functions/weeklyInsightGenerate.ts</files>
  <action>
Lire le fichier complet AVANT toute modification.

**Etape 1 : Ajouter HealthRecord au Promise.all existant (ligne 45)**

Remplacer :
```typescript
const [checkins, dogProgress, dogScans, dogDailyLogs] = await Promise.all([
  base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.UserProgress.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.FoodScan.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.DailyLog.filter({ dog_id: dog.id }).catch(() => []),
]);
```
Par :
```typescript
const [checkins, dogProgress, dogScans, dogDailyLogs, dogHealthRecords] = await Promise.all([
  base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.UserProgress.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.FoodScan.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.DailyLog.filter({ dog_id: dog.id }).catch(() => []),
  base44.asServiceRole.entities.HealthRecord.filter({ dog_id: dog.id }).catch(() => []),
]);
```

**Etape 2 : Construire healthContext apres le calcul des symptomes (apres la ligne symptomText)**

Ajouter apres le bloc `const symptomText = ...` :

```typescript
// Health events from HealthRecord for this week
const today = new Date().toISOString().slice(0, 10);
const weekHealthRecords = (dogHealthRecords || []).filter(r => r.date >= weekStart && r.date <= weekEnd);
const healthEvents: string[] = [];

// Vet visits this week
const vetVisits = weekHealthRecords.filter(r => r.type === "vet_visit");
if (vetVisits.length > 0) {
  healthEvents.push(`${vetVisits.length} visite(s) vet cette semaine${vetVisits[0].label ? ` (${String(vetVisits[0].label).substring(0, 50)})` : ""}`);
}

// Overdue vaccinations (next_date < today)
const overdueVaccines = (dogHealthRecords || []).filter(r =>
  r.type === "vaccination" && r.next_date && r.next_date < today
);
if (overdueVaccines.length > 0) {
  const names = overdueVaccines.slice(0, 3).map(r => String(r.label || "vaccin").substring(0, 30)).join(", ");
  healthEvents.push(`vaccin(s) en retard : ${names}`);
}

// Active medications (next_date >= today = still ongoing)
const activeMeds = (dogHealthRecords || []).filter(r =>
  r.type === "medication" && r.next_date && r.next_date >= today
);
if (activeMeds.length > 0) {
  const names = activeMeds.slice(0, 3).map(r => String(r.label || "medicament").substring(0, 30)).join(", ");
  healthEvents.push(`medicament(s) en cours : ${names}`);
}

const healthContext = healthEvents.length > 0
  ? `\n\nEVENEMENTS SANTE DE LA SEMAINE (a mentionner si pertinent) : ${healthEvents.join(". ")}.`
  : "";

// Notes and behavior_notes from check-ins
const weekNotes: string[] = [];
weekCheckins.forEach(c => {
  if (c.notes && String(c.notes).trim()) weekNotes.push(String(c.notes).substring(0, 100));
  if (c.behavior_notes && String(c.behavior_notes).trim()) weekNotes.push(String(c.behavior_notes).substring(0, 100));
});
const notesContext = weekNotes.length > 0
  ? `\n\nNOTES DU PROPRIETAIRE CETTE SEMAINE : ${weekNotes.slice(0, 5).join(" | ")}.`
  : "";
```

**Etape 3 : Injecter healthContext et notesContext dans le systemPrompt**

Le systemPrompt actuel (ligne 182) se termine par `behavior_summary (profil...)`. Ajouter `${healthContext}${notesContext}` a la fin du systemPrompt, avant le dernier backtick :

Remplacer la fin du systemPrompt :
```typescript
...behavior_summary (profil comportemental synthetique de ${safeName} en 2-3 phrases — patterns d'humeur, energie, alertes, evolution par rapport au profil precedent si disponible).`;
```
Par :
```typescript
...behavior_summary (profil comportemental synthetique de ${safeName} en 2-3 phrases — patterns d'humeur, energie, alertes, evolution par rapport au profil precedent si disponible).${healthContext}${notesContext}`;
```

Aucune autre modification. Le userMessage, la logique de creation WeeklyInsight, et le reste du flux restent identiques.

IMPORTANT : `today` est deja defini en dehors de la boucle (ligne 9 du fichier original). Dans le contexte de la boucle for, redefinir `today` localement pour le filtre `overdueVaccines` est acceptable car c'est une constante. Verifier qu'il n'y a pas de conflit de variable avec la declaration externe.
  </action>
  <verify>
    <automated>cd C:/Users/smalt/Desktop/app-chien-ia && git diff HEAD pawcoach/functions/weeklyInsightGenerate.ts | grep -E "dogHealthRecords|healthContext|notesContext|EVENEMENTS"</automated>
  </verify>
  <done>
- Le Promise.all inclut `dogHealthRecords`
- `healthContext` est construit avec : visites vet semaine, vaccins en retard, medicaments en cours
- `notesContext` est construit a partir des notes/behavior_notes des weekCheckins
- Les deux sont injectes dans le systemPrompt
- La logique existante (creation WeeklyInsight, comportement summary, etc.) est inchangee
  </done>
</task>

</tasks>

<verification>
Verifier dans le fichier :
1. Le destructuring du Promise.all a bien 5 elements (ajout de `dogHealthRecords`)
2. `weekHealthRecords` filtre bien sur weekStart/weekEnd
3. `overdueVaccines` compare next_date < today (date string "YYYY-MM-DD" — comparaison lexicographique valide)
4. `healthContext` et `notesContext` sont dans le systemPrompt
5. Pas de doublon de declaration de variable `today`
6. `git diff` ne montre pas de suppressions de logique existante
</verification>

<success_criteria>
La fonction weeklyInsightGenerate.ts :
- Charge les HealthRecord du chien en plus des autres entites (Promise.all etendu)
- Detecte : visites vet de la semaine, vaccins en retard, medicaments en cours
- Collecte les notes textuelles des check-ins de la semaine
- Injecte ces contextes dans le systemPrompt pour guider la generation IA
- Reste silencieuse (contextes vides) quand aucune donnee sante n'existe
- Ne casse aucune logique existante (aggregats, creation, behavior_summary)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-ai-enrichment/02-02-SUMMARY.md`
</output>
