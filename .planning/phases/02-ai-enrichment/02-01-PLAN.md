---
phase: 02-ai-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/functions/dailyCheckinProcess.ts
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - "Quand les 7 derniers check-ins montrent une tendance (ex: fatigue 3 jours de suite), la reponse post check-in la mentionne explicitement"
    - "Quand les 7 derniers check-ins sont uniformes ou insuffisants (<3), la reponse reste generique (pas de fausse tendance inventee)"
    - "Le check-in du jour est toujours cree en base, meme si la detection de tendance echoue"
  artifacts:
    - path: "pawcoach/functions/dailyCheckinProcess.ts"
      provides: "Fonction backend check-in enrichie avec analyse des 7 derniers check-ins"
      contains: "recentCheckins"
  key_links:
    - from: "pawcoach/functions/dailyCheckinProcess.ts"
      to: "entities.DailyCheckin.filter"
      via: "requete des 7 derniers check-ins avant appel LLM"
      pattern: "recentCheckins|last.*check"
---

<objective>
Enrichir dailyCheckinProcess.ts pour que la reponse IA post check-in utilise les 7 derniers check-ins du chien et detecte les tendances (mood, energy, appetite, symptoms).

Purpose: Aujourd'hui le prompt ne connait que le check-in actuel. L'IA ne peut pas dire "tu signales de la fatigue depuis 3 jours" car elle n'a acces qu'a aujourd'hui. Charger l'historique recent rend les analyses actionnables.

Output: dailyCheckinProcess.ts modifie — requete + analyse tendances + injection dans prompt
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pawcoach/.planning/PROJECT.md
@pawcoach/.planning/ROADMAP.md
@pawcoach/.planning/STATE.md

<interfaces>
<!-- Entite DailyCheckin — champs pertinents -->
DailyCheckin: {
  id: string,
  dog_id: string,
  owner_email: string,
  date: string,        // "YYYY-MM-DD"
  mood: number,        // 1-4
  energy: number,      // 1-3
  appetite: number,    // 1-3
  symptoms: string[],  // optionnel
  notes: string,       // optionnel
  behavior_notes: string, // optionnel
  ai_response: string  // optionnel
}

<!-- Pattern de requete existant dans le fichier -->
// Requete existante (check doublon aujourd'hui) :
const existing = await base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dogId, date: today });

<!-- La creation du check-in se fait a la ligne 61, la requete des 7 derniers doit se faire AVANT la construction du systemPrompt (ligne 138) -->
<!-- Le systemPrompt est construit a la ligne 138, le userMessage a la ligne 144 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Charger les 7 derniers check-ins et construire l'analyse de tendances</name>
  <files>pawcoach/functions/dailyCheckinProcess.ts</files>
  <action>
Lire le fichier complet AVANT toute modification.

Apres la creation du checkin (ligne 61, apres `const checkin = await ...`), ajouter :

1. Requete des 7 derniers check-ins (hors aujourd'hui) :
```typescript
// Fetch last 7 check-ins for trend analysis (excluding today)
const allRecentCheckins = await base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dogId }).catch(() => []);
const recentCheckins = (allRecentCheckins || [])
  .filter(c => c.date < today)
  .sort((a, b) => b.date.localeCompare(a.date))
  .slice(0, 7);
```

2. Construire le bloc de tendances a injecter dans le prompt. Logique :
- Ne detecter une tendance que si >= 3 points de donnees existent
- Tendance mood bas : mood <= 2 sur les 3 derniers jours -> "humeur basse depuis X jours"
- Tendance energy basse : energy == 1 sur les 3 derniers jours -> "fatigue depuis X jours"
- Tendance appetite bas : appetite == 1 sur les 3 derniers jours -> "appetit reduit depuis X jours"
- Symptoms recurrents : symptom present sur >= 2 des 7 derniers check-ins -> "symptome X signale X fois cette semaine"

```typescript
let trendContext = "";
if (recentCheckins.length >= 3) {
  const trends: string[] = [];

  // Mood trend (low = 1-2)
  const recentMoodLow = recentCheckins.slice(0, 3).filter(c => c.mood <= 2).length;
  if (recentMoodLow >= 3) trends.push(`humeur basse signalée ${recentMoodLow} jours de suite`);

  // Energy trend (low = 1)
  const recentEnergyLow = recentCheckins.slice(0, 3).filter(c => c.energy === 1).length;
  if (recentEnergyLow >= 3) trends.push(`fatigue signalée ${recentEnergyLow} jours de suite`);

  // Appetite trend (low = 1)
  const recentAppetiteLow = recentCheckins.slice(0, 3).filter(c => c.appetite === 1).length;
  if (recentAppetiteLow >= 3) trends.push(`appetit reduit ${recentAppetiteLow} jours de suite`);

  // Recurring symptoms
  const symptomCounts: Record<string, number> = {};
  recentCheckins.forEach(c => {
    if (c.symptoms?.length) c.symptoms.forEach((s: string) => { symptomCounts[s] = (symptomCounts[s] || 0) + 1; });
  });
  Object.entries(symptomCounts).forEach(([s, n]) => {
    if (n >= 2) trends.push(`"${s}" signale ${n} fois sur les 7 derniers jours`);
  });

  if (trends.length > 0) {
    trendContext = `\n\nTENDANCES DETECTEES SUR LES 7 DERNIERS JOURS (utilise-les dans ta reponse si pertinent) : ${trends.join(". ")}.`;
  }
}
```

3. Injecter `trendContext` dans le systemPrompt existant (ligne 138). Concatener a la fin du systemPrompt, juste avant le point final :

Remplacer :
```typescript
const systemPrompt = `Tu es PawCoach...${segmentContext} Ne diagnostique jamais, ne prescris jamais.`;
```
Par :
```typescript
const systemPrompt = `Tu es PawCoach...${segmentContext} Ne diagnostique jamais, ne prescris jamais.${trendContext}`;
```

Aucune autre modification. Le create du checkin, la gestion du streak, et le reste du flux restent identiques.

IMPORTANT : verifier que `recentCheckins` est declare APRES la creation du checkin (ligne 61) mais AVANT la construction du systemPrompt (ligne 138). Ainsi le check doublon existant (ligne 46) n'est pas affecte.
  </action>
  <verify>
    <automated>cd C:/Users/smalt/Desktop/app-chien-ia && git diff HEAD pawcoach/functions/dailyCheckinProcess.ts | grep -E "recentCheckins|trendContext|TENDANCES"</automated>
  </verify>
  <done>
- Le fichier contient une requete `recentCheckins` pour les 7 derniers check-ins
- Le systemPrompt inclut `trendContext` (vide si pas de tendance, rempli si >= 3 signaux)
- La logique de creation du checkin et de streak est inchangee
- Aucune regression : le check doublon (existing) est toujours en place
  </done>
</task>

</tasks>

<verification>
Verifier manuellement dans le fichier :
1. `recentCheckins` est defini apres `const checkin = await ...`
2. `trendContext` est construit avec la logique de seuil >= 3
3. `trendContext` est injecte dans `systemPrompt`
4. La variable `existing` (check doublon) est toujours presente et inchangee
5. `git diff` montre uniquement des ajouts, pas de suppressions de logique existante
</verification>

<success_criteria>
La fonction dailyCheckinProcess.ts :
- Charge les 7 derniers check-ins avant d'appeler le LLM
- Detecte les tendances (mood/energy/appetite bas 3 jours consecutifs, symptoms 2+ fois)
- Injecte les tendances detectees dans le systemPrompt
- Reste silencieuse (trendContext vide) quand les donnees sont insuffisantes (<3 check-ins)
- Ne casse aucune logique existante (creation, streak, dedup)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-ai-enrichment/02-01-SUMMARY.md`
</output>
