---
phase: 08-ux-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Onboarding.jsx
  - src/components/onboarding/WelcomeScreen.jsx
autonomous: true
requirements: [FIX-31, FIX-32, FIX-37]
must_haves:
  truths:
    - "L'onboarding se complète en 5-6 étapes (Welcome + 5 steps interview + GDPR consent)"
    - "Un lien 'Remplir plus tard' permet de créer un profil minimal (prénom seulement) depuis l'étape 3"
    - "La WelcomeScreen affiche 2-3 phrases spécifiques à la race et l'âge du chien"
    - "L'écran RGPD ajouté en Phase 6 est intact et conservé"
  artifacts:
    - path: "src/pages/Onboarding.jsx"
      provides: "Onboarding consolidé 5 étapes + quick-start"
      contains: "Remplir plus tard"
    - path: "src/components/onboarding/WelcomeScreen.jsx"
      provides: "Welcome personnalisée avec race/âge"
      contains: "dogBreed"
  key_links:
    - from: "Onboarding.jsx"
      to: "WelcomeScreen.jsx"
      via: "props dogBreed + dogAge"
      pattern: "dogBreed.*dogAge|breed.*age"
    - from: "Onboarding.jsx quick-start"
      to: "Dog.create"
      via: "name only, rest null"
      pattern: "Remplir plus tard"
---

<objective>
Consolider l'onboarding de 10 étapes à 5-6, ajouter un quick-start "Remplir plus tard", et personnaliser la WelcomeScreen avec des données race/âge collectées pendant l'onboarding.

Purpose: Réduire l'abandon d'onboarding. Chaque étape supprimée = +10% de completion selon la règle empirique mobile.
Output: Onboarding.jsx restructuré + WelcomeScreen.jsx enrichi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

CGC — blast radius avant modification :
```bash
cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach"
cgc analyze callers Onboarding
cgc analyze callers WelcomeScreen
```
</context>

<interfaces>
<!-- État actuel d'Onboarding.jsx (lu le 2026-03-27) -->
<!-- 10 INTERVIEW_STEPS (index 0-9) : choice, photo, voice x8 -->
<!-- OPTIONAL_STEPS = Set([3, 9]) — race (idx 3) et santé (idx 9) -->
<!-- step state: 0..9, answers: Array(10) -->
<!-- handleNext() : step < 9 → setStep+1, step === 9 → setSaveConsent(true) -->
<!-- handleFinish() : Dog.create avec gdprConsent guard (Phase 6 — NE PAS toucher) -->
<!-- showConsent state + GDPR screen : conservé intégralement -->
<!-- WelcomeScreen reçoit : dogName, dogPhoto, isPremium, onDiscover -->
<!-- answers[2] = prénom, answers[1] = photo, answers[3] = race, answers[4] = âge -->

Mapping réponses → données chien :
- answers[0] → goal (choice)
- answers[1] → photo URL
- answers[2] → prénom (voice)
- answers[3] → race (voice, optional)
- answers[4] → âge (voice)
- answers[5] → sexe (voice)
- answers[6] → poids (voice)
- answers[7] → activité (voice)
- answers[8] → environnement (voice)
- answers[9] → santé/allergies (voice, optional)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Consolider INTERVIEW_STEPS de 10 à 5 + ajouter quick-start</name>
  <files>src/pages/Onboarding.jsx</files>
  <read_first>src/pages/Onboarding.jsx — lire intégralement avant toute modification</read_first>
  <action>
Restructurer INTERVIEW_STEPS de 10 steps à 5 steps en fusionnant les questions par groupe logique.

**Nouvelle structure INTERVIEW_STEPS (5 étapes) :**
```
Step 0: { type: "choice", question: "Qu'est-ce qui compte le plus pour toi ?", ... }  // Goal — inchangé
Step 1: { type: "photo_name", question: "Présente-nous ton chien", ... }              // Photo + Prénom FUSION
Step 2: { type: "breed_age", question: "Race et âge de ton chien ?", ... }            // Race + Âge FUSION
Step 3: { type: "details", question: "Quelques détails supplémentaires", ... }        // Sexe + Poids + Activité FUSION
Step 4: { type: "health", question: "Sa santé en quelques mots", ... }                // Environnement + Santé FUSION
```

**Implémentation fusionnée :**
- Step 1 (photo_name) : garder le UI photo existant + ajouter un Input prénom en dessous. L'answer est un objet JSON stringifié `{"photo": url, "name": "Rex"}`, ou utiliser deux champs d'état séparés `photoAnswer` et `nameAnswer`.
- Step 2 (breed_age) : deux Inputs côte à côte (race optionnelle, âge requis). answer = `{"breed": "Beagle", "age": "2 ans"}`.
- Step 3 (details) : trois Inputs empilés (sexe, poids, activité). answer = JSON.
- Step 4 (health) : deux Inputs (environnement, santé optionnelle). answer = JSON.

**Alternative simplifiée (préférable) :** Garder `answers` comme Array(5) mais avec des champs composites. Pour chaque step fusionné, stocker les valeurs dans des états locaux séparés et les combiner dans l'objet answers au moment de passer à l'étape suivante.

**Approche concrète recommandée :**
Garder `answers` Array(10) existant INTACT pour ne pas casser handleFinish() qui parse answers[0..9]. Seulement changer l'affichage pour montrer plusieurs champs par step visible. Chaque "étape visible" avance answer index de +1 pour chaque champ répondu.

Donc : plutôt que de restructurer INTERVIEW_STEPS, introduire un `STEP_GROUPS` :
```js
const STEP_GROUPS = [
  { label: "1/5", steps: [0] },              // Goal
  { label: "2/5", steps: [1, 2] },           // Photo + Prénom
  { label: "3/5", steps: [3, 4] },           // Race + Âge
  { label: "4/5", steps: [5, 6, 7] },        // Sexe + Poids + Activité
  { label: "5/5", steps: [8, 9] },           // Environnement + Santé
];
```

Avec ce mapping, `step` state reste 0..9 mais le progress bar et le compteur affichent le groupe courant (pas l'index brut). La barre de progression calcule `groupProgress = (groupIndex + 1) / 5 * 100`.

Trouver le groupe actif :
```js
const currentGroup = STEP_GROUPS.find(g => g.steps.includes(step));
const groupIndex = STEP_GROUPS.indexOf(currentGroup);
```

Pour les groupes multi-steps, afficher tous les champs du groupe ensemble (une colonne de questions). Le bouton "Suivant" avance vers le premier step du groupe suivant (pas step+1).

**Quick-start "Remplir plus tard" (FIX-32) :**
- Afficher un lien texte sous le bouton Suivant à partir du groupe 1 (après goal selection) :
```jsx
{groupIndex >= 1 && (
  <button
    onClick={handleQuickStart}
    className="mt-3 text-sm text-muted-foreground underline underline-offset-2"
  >
    Remplir plus tard
  </button>
)}
```
- `handleQuickStart()` :
  1. Récupérer le prénom depuis `answers[2]` (s'il est rempli) ou `"Mon chien"`
  2. Récupérer le goal depuis `answers[0]`
  3. Appeler `handleFinishWithMinimalData({ name, owner_goal: goal })` qui bypasse les étapes restantes et crée directement le dog
  4. Appeler `setShowConsent(true)` (le consentement GDPR reste obligatoire même en quick-start)
  5. Marquer `isQuickStart = true` dans un ref pour que handleFinish() crée le dog avec name seulement (breed/weight/etc. null)

**Consentement GDPR (Phase 6) :** NE PAS modifier le bloc `if (showConsent)`. Il s'affiche après la dernière étape ET après quick-start. Seul `handleFinish()` change : si `isQuickStart`, créer Dog avec seulement `{ name, owner_goal, photo, onboarding_completed: false }` (onboarding_completed: false signale que le profil est incomplet).

**Progress bar :** Remplacer `{step + 1}/{INTERVIEW_STEPS.length}` par `{groupIndex + 1}/5`.

**Bouton Précédent :** Retourne au premier step du groupe précédent.

**OPTIONAL_STEPS :** Garder la logique existante. Dans les groupes multi-steps, le bouton Suivant est actif si au moins les champs requis sont remplis (le champ race et santé restent optionnels).
  </action>
  <verify>
    <automated>grep -c "Remplir plus tard" src/pages/Onboarding.jsx && grep "STEP_GROUPS\|groupIndex\|1/5\|2/5" src/pages/Onboarding.jsx && grep "showConsent" src/pages/Onboarding.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Remplir plus tard" retourne 1+ occurrence
    - Le progress bar affiche X/5 (pas X/10)
    - Le bloc showConsent GDPR est préservé à l'identique
    - Pas de régression sur handleFinish() et Dog.create()
  </acceptance_criteria>
  <done>L'onboarding affiche 5 groupes d'étapes, un lien "Remplir plus tard" apparaît dès l'étape 2, et le consentement GDPR est déclenché dans les deux chemins (complet et quick-start).</done>
</task>

<task type="auto">
  <name>Task 2: Personnaliser WelcomeScreen avec race + âge (FIX-37)</name>
  <files>src/components/onboarding/WelcomeScreen.jsx, src/pages/Onboarding.jsx</files>
  <read_first>
    - src/components/onboarding/WelcomeScreen.jsx — lire intégralement
    - src/pages/Onboarding.jsx — vérifier l'appel à WelcomeScreen (ligne ~308)
  </read_first>
  <action>
**Dans Onboarding.jsx :** Passer `dogBreed` et `dogAge` à WelcomeScreen.
```jsx
// Ligne ~308 (après Dog.create, dogData est set)
<WelcomeScreen
  dogName={dogData.name}
  dogPhoto={dogData.photo}
  isPremium={isUserPremium(currentUser)}
  onDiscover={() => navigate(createPageUrl(destination))}
  dogBreed={answers[3] || null}   // Race from onboarding step
  dogAge={answers[4] || null}     // Âge from onboarding step
/>
```

**Dans WelcomeScreen.jsx :** Ajouter props `dogBreed` et `dogAge`. Remplacer la ligne statique `"Alimentation · Bien-être · Dressage"` par 2-3 phrases personnalisées si les données sont disponibles.

Logique de personnalisation :
```jsx
function getPersonalizedLines(dogName, dogBreed, dogAge) {
  if (!dogBreed && !dogAge) {
    return ["Alimentation · Bien-être · Dressage"];
  }
  const lines = [];
  const breedLabel = dogBreed ? `Pour un ${dogBreed}` : `Pour ${dogName}`;
  const ageLabel = dogAge ? ` de ${dogAge}` : "";
  lines.push(`${breedLabel}${ageLabel}, on va suivre :`);
  lines.push("Santé · Nutrition · Activité");
  // Breed-specific hint (simple mapping, no external data)
  const breedLower = (dogBreed || "").toLowerCase();
  if (breedLower.includes("labrador") || breedLower.includes("golden")) {
    lines.push("Spécial : surveillance du poids et des articulations.");
  } else if (breedLower.includes("berger") || breedLower.includes("border")) {
    lines.push("Spécial : exercice quotidien et stimulation mentale.");
  } else if (breedLower.includes("bouledogue") || breedLower.includes("carlin")) {
    lines.push("Spécial : surveillance respiratoire et chaleur.");
  } else if (dogAge) {
    const isYoung = dogAge.includes("mois") || dogAge.includes("1 an") || dogAge.includes("an");
    if (isYoung) lines.push("Spécial : croissance et socialisation en cours.");
  }
  return lines;
}
```

Remplacer le `<p className="text-white/60 text-sm mb-4">Alimentation · Bien-être · Dressage</p>` par :
```jsx
{getPersonalizedLines(dogName, dogBreed, dogAge).map((line, i) => (
  <p key={i} className={`text-white/70 text-sm mb-1 ${i === 0 ? "font-semibold" : ""}`}>
    {line}
  </p>
))}
```

NE PAS modifier la structure globale du composant (gradient, photo, badge, CTA, PremiumNudgeSheet).
  </action>
  <verify>
    <automated>grep "dogBreed\|dogAge\|getPersonalizedLines" src/components/onboarding/WelcomeScreen.jsx && grep "dogBreed.*answers\[3\]\|answers\[3\].*dogBreed" src/pages/Onboarding.jsx</automated>
  </verify>
  <acceptance_criteria>
    - WelcomeScreen.jsx reçoit et utilise dogBreed + dogAge en props
    - La fonction getPersonalizedLines() est définie et retourne 2-3 lignes
    - Si dogBreed et dogAge sont null, affichage fallback "Alimentation · Bien-être · Dressage"
    - PremiumNudgeSheet et le reste du composant sont inchangés
  </acceptance_criteria>
  <done>Après onboarding complet, la WelcomeScreen affiche des phrases personnalisées mentionnant la race et l'âge du chien. Si l'utilisateur a fait quick-start, le fallback générique s'affiche.</done>
</task>

</tasks>

<verification>
1. grep "Remplir plus tard" src/pages/Onboarding.jsx — doit retourner 1+ ligne
2. grep "STEP_GROUPS\|groupIndex" src/pages/Onboarding.jsx — doit exister
3. grep "showConsent\|gdprConsent\|setShowConsent" src/pages/Onboarding.jsx — bloc GDPR intact
4. grep "dogBreed\|dogAge" src/components/onboarding/WelcomeScreen.jsx — personnalisation présente
5. Vérifier que handleFinish() contient toujours Dog.create() et setAnalyticsConsent(true)
</verification>

<success_criteria>
- Onboarding en 5 groupes d'étapes (progress bar: 1/5 à 5/5)
- Lien "Remplir plus tard" présent et fonctionnel à partir de l'étape 2
- Consentement GDPR déclenché dans les deux chemins (complet et quick-start)
- WelcomeScreen affiche des phrases spécifiques race/âge quand disponibles
- Zéro régression sur les fonctionnalités existantes (photo upload, voice input, Dog.create)
</success_criteria>

<output>
Après completion, créer `.planning/phases/08-ux-activation/08-01-SUMMARY.md`
</output>
