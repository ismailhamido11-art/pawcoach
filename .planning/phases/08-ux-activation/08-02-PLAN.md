---
phase: 08-ux-activation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
autonomous: true
requirements: [FIX-33, FIX-35, FIX-36]
must_haves:
  truths:
    - "Un élément cliquable 'Parle au coach IA' est visible sur Home sans scroller"
    - "Un raccourci 'Trouver un véto' est accessible en 1-2 taps depuis Home"
    - "Le Tableau de bord est mis en avant sur Home (pas juste dans le bas de page)"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "Home enrichie avec IA card, Vet shortcut, Dashboard prominence"
      contains: "Parle au coach IA"
  key_links:
    - from: "Home.jsx AI card"
      to: "Chat page"
      via: "navigate(createPageUrl('Chat'))"
      pattern: "createPageUrl.*Chat"
    - from: "Home.jsx Vet shortcut"
      to: "Sante page ?tab=findvet"
      via: "navigate(createPageUrl('Sante') + '?tab=findvet')"
      pattern: "findvet"
---

<objective>
Rendre l'IA visible et le vétérinaire accessible depuis Home dès la première visite. Repositionner le Dashboard plus haut dans le flux.

Purpose: Le différenciateur principal de PawCoach (IA) est actuellement invisible depuis Home. Un nouvel utilisateur ne le découvre pas. FIX-33 corrige ça directement.
Output: Home.jsx enrichie avec une AI card dans le feed, un raccourci vétérinaire, et le Dashboard card repositionné.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

CGC blast radius :
```bash
cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach"
cgc analyze callers Home
cgc find content "createPageUrl.*Chat"
```
</context>

<interfaces>
<!-- Structure actuelle de Home.jsx (render, après le fold) :
  1. CoachHomeHeader
  2. isDataStale indicator
  3. DailyBriefing (ref dailyBriefingRef)
  4. [motion.div px-5 space-y-6]
     a. TrialExpiryBanner
     b. FirstDayGuide
     c. Hero illustration card (état mood)
     d. CalendarStrip
     e. DailyProgress
     f. Dashboard button (blue-50 card, navigate Dashboard)  ← LIGNE 570
     g. Quick Actions (4 boutons: Scanner, Balade, Santé, Dressage)
     h. Nutrition tip card
     i. ActiveProgramCards
     j. Streak Card
     k. Health card
     l. EmotionalTip
     m. WeeklyInsight ou Premium prompt
     n. Disclaimer

ChatFAB est positionné en bas (offsetBottom=4.5) — existant, garde.
CombinedFAB — existant, garde.

Imports disponibles : Stethoscope, BookOpen, Lock déjà importés ligne 25.
navigate et createPageUrl déjà importés.
StorysetIllustration déjà importé.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter AI Chat card + Vet shortcut + repositionner Dashboard sur Home</name>
  <files>src/pages/Home.jsx</files>
  <read_first>src/pages/Home.jsx lignes 560-620 (zone Dashboard button + Quick Actions) — lire pour identifier l'insertion exacte</read_first>
  <action>
Trois modifications dans la section `{/* === Below the fold === */}` de Home.jsx :

**A. AI Chat Card (FIX-33) — insérer AVANT le DailyBriefing ou juste APRES (position prioritaire)**

Insérer juste après le bloc `DailyBriefing` (après la ligne `</div>` qui ferme la ref dailyBriefingRef), avant le `motion.div px-5 space-y-6` :

```jsx
{/* AI Coach Card — différenciateur visible dès la première visite */}
<div className="px-5 pt-4">
  <motion.button
    onClick={() => navigate(createPageUrl("Chat"))}
    whileTap={{ scale: 0.98 }}
    className="w-full gradient-primary rounded-3xl p-4 flex items-center gap-4 shadow-md text-left"
  >
    <div className="w-12 h-12 rounded-2xl bg-white/20 flex items-center justify-center flex-shrink-0">
      <Sparkles className="w-6 h-6 text-white" />
    </div>
    <div className="flex-1 min-w-0">
      <p className="text-white font-bold text-[14px]">Parle au coach IA</p>
      <p className="text-white/70 text-[11px] mt-0.5">Pose une question sur {dog?.name || "ton chien"}</p>
    </div>
    <ChevronRight className="w-5 h-5 text-white/60 flex-shrink-0" />
  </motion.button>
</div>
```

Ajouter `Sparkles, ChevronRight` aux imports lucide-react si pas déjà présents (vérifier ligne 25).

**B. Dashboard prominence (FIX-36) — remonter la Dashboard card**

Le Dashboard button est actuellement à la position f (après CalendarStrip + DailyProgress). Le repositionner AVANT CalendarStrip pour qu'il soit plus haut dans le scroll.

Déplacer le bloc complet (lignes ~569-583) :
```jsx
{/* Dashboard access — SmartAlerts */}
<button
  onClick={() => navigate(createPageUrl("Dashboard"))}
  className="w-full bg-gradient-to-r from-blue-50 to-indigo-50/50 ..."
>
  ...
</button>
```

Vers une position APRÈS TrialExpiryBanner + FirstDayGuide, AVANT Hero illustration card.

**C. Find Vet shortcut (FIX-35) — ajouter dans les Quick Actions**

Les Quick Actions actuelles sont 4 boutons (Scanner, Balade, Santé, Dressage). Ajouter un 5ème bouton "Véto" qui navigue vers `createPageUrl("Sante") + "?tab=findvet"`.

```js
{
  label: "Véto",
  bgClass: "bg-gradient-to-br from-rose-400 to-rose-600 shadow-lg shadow-rose-200/50",
  page: null, // handled with custom onClick
  onClick: () => navigate(createPageUrl("Sante") + "?tab=findvet"),
  svg: (
    <svg viewBox="0 0 32 32" className="w-7 h-7" fill="none">
      <path d="M16 8a6 6 0 016 6c0 4-3 7-6 10-3-3-6-6-6-10a6 6 0 016-6z" fill="white" fillOpacity="0.9"/>
      <path d="M13 14h6M16 11v6" stroke="#9f1239" strokeWidth="2.5" strokeLinecap="round"/>
    </svg>
  ),
},
```

Adapter le `quickActions.map()` pour accepter `onClick` optionnel :
```jsx
onClick={() => qa.onClick ? qa.onClick() : navigate(createPageUrl(qa.page))}
```

Si 5 boutons dans la même rangée deviennent trop serrés sur mobile, utiliser un scroll horizontal `overflow-x-auto` sur le container plutôt qu'une grille fixe.

**Ordre final dans le feed :**
1. AI Chat card (gradient vert, full-width)
2. TrialExpiryBanner
3. FirstDayGuide
4. Dashboard card (remonté)
5. Hero illustration card
6. CalendarStrip
7. DailyProgress
8. Quick Actions (5 boutons avec Véto)
9. ... (reste inchangé)

**NE PAS modifier :** ChatFAB, CombinedFAB, DailyBriefing, CoachHomeHeader, la logique de données.
  </action>
  <verify>
    <automated>grep "Parle au coach IA\|createPageUrl.*Chat" src/pages/Home.jsx && grep "findvet\|Véto" src/pages/Home.jsx && grep "Dashboard" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Parle au coach IA" retourne 1 occurrence
    - grep "findvet" retourne 1 occurrence dans le onClick du bouton Véto
    - Le Dashboard card existe et est accessible depuis le fichier
    - L'import Sparkles est présent (ou ChevronRight s'il manquait)
    - La build ne produit pas d'erreur TS/import
  </acceptance_criteria>
  <done>Home affiche une AI card gradient au sommet du feed, un bouton Véto dans les quick actions, et le Dashboard est positionné plus haut dans le scroll. Tout ceci sans modifier la logique de données ni les FAB existants.</done>
</task>

</tasks>

<verification>
1. grep "Parle au coach IA" src/pages/Home.jsx
2. grep "findvet" src/pages/Home.jsx
3. grep "Sparkles" src/pages/Home.jsx — import présent
4. Pas de import manquant (les icônes Lucide utilisées sont importées)
</verification>

<success_criteria>
- Un élément "Parle au coach IA" (card cliquable) visible sur Home avant le fold ou juste après
- Bouton "Véto" dans les quick actions naviguant vers Sante?tab=findvet
- Dashboard card remontée avant la Hero illustration card
- Zéro régression sur le rendering de Home (dog null guard, loading state)
</success_criteria>

<output>
Après completion, créer `.planning/phases/08-ux-activation/08-02-SUMMARY.md`
</output>
