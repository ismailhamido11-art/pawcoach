---
phase: 01-data-coherence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/src/pages/Scan.jsx
  - pawcoach/src/components/nutrition/FoodComparator.jsx
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "Le scanner aliments affiche un avertissement si un ingredient correspond a DietPreferences.disliked_foods du chien"
    - "Le comparateur affiche un avertissement si un ingredient correspond a DietPreferences.disliked_foods du chien"
    - "Les allergies medicales (dog.allergies) restent presentes dans les prompts — pas de regression"
  artifacts:
    - path: "pawcoach/src/pages/Scan.jsx"
      provides: "Charge DietPreferences et injecte disliked_foods dans les 2 prompts LLM"
      contains: "disliked_foods"
    - path: "pawcoach/src/components/nutrition/FoodComparator.jsx"
      provides: "Recoit dietPreferences en prop et injecte disliked_foods dans les prompts"
      contains: "disliked_foods"
  key_links:
    - from: "Scan.jsx loadData()"
      to: "base44.entities.DietPreferences.filter"
      via: "fetch parallele avec Dog"
      pattern: "DietPreferences\\.filter"
    - from: "FoodComparator.jsx"
      to: "dietPreferences prop"
      via: "prop recue depuis Nutri.jsx (qui charge deja DietPreferences)"
      pattern: "dietPreferences"
---

<objective>
Brancher DietPreferences.disliked_foods dans les prompts IA du scanner aliments et du comparateur.

Purpose: Actuellement les deux outils ne connaissent que dog.allergies (allergies medicales). DietPreferences.disliked_foods contient les aliments que le chien n'aime pas ou ne tolere pas — information ignoree completement. Un utilisateur qui a renseigne "poulet, ble" comme aliments indesirables ne voit aucun avertissement quand il scanne du poulet.

Output: Les 2 prompts LLM incluent disliked_foods en plus des allergies. L'IA peut generer un avertissement specifique si elle detecte un ingredient indesirable.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Decisions cles (non negociables) :
- NE PAS merger dog.allergies et DietPreferences en une seule entite — ce sont deux concepts differents (allergies medicales vs preferences alimentaires). On les combine uniquement dans les prompts.
- 0 Build prompt pour ce milestone — tout via Git push, aucun schema change.
- Risque regression eleve : lire "qui utilise ce fichier" avant toute modification d'utilitaire partage.

Architecture pertinente :
- Nutri.jsx charge deja DietPreferences en parallele (ligne 246) — FoodComparator est un child de Nutri.jsx, donc passer dietPreferences en prop est la voie la moins risquee.
- Scan.jsx est une page standalone qui charge Dog seul dans loadData() — elle doit charger DietPreferences elle-meme.
- FoodComparator.jsx signature actuelle : `export default function FoodComparator({ dog, user })` — ajouter `dietPreferences` comme prop optionnel.
</context>

<interfaces>
<!-- Interfaces cles extraites du codebase pour l'executeur -->

De Scan.jsx (loadData actuel) :
```javascript
const loadData = async () => {
  const u = await base44.auth.me();
  setUser(u);
  const dogs = await base44.entities.Dog.filter({ owner: u.email });
  if (dogs?.length > 0) {
    const activeDog = getActiveDog(dogs);
    setDog(activeDog);
    const scans = await base44.entities.FoodScan.filter({ dog_id: activeDog.id });
    setHistory(...);
  }
};
```

De Scan.jsx — prompt analyzeFood (ligne 249) :
```javascript
const prompt = `...allergies : ${dog.allergies || "aucune"}...`;
```

De Scan.jsx — prompt analyzeLabel (ligne 332-343) :
```javascript
const prompt = `...Allergies : ${dog.allergies || "aucune connue"}...`;
```

De FoodComparator.jsx (ligne 165 — analyzeProduct) :
```javascript
const prompt = `...allergies : ${dog?.allergies || "aucune"}, régime : ${dog?.diet_type || "inconnu"}.`;
```

De FoodComparator.jsx (ligne 219 — compare) :
```javascript
const prompt = `...allergies : ${dog?.allergies || "aucune"}).`;
```

De Nutri.jsx (ligne 246 — deja charge) :
```javascript
base44.entities.DietPreferences.filter({ dog_id: d.id, owner_email: u.email }).catch(() => []),
```

De Nutri.jsx — FoodComparator rendu (ligne 468) :
```javascript
<DietPreferencesPanel dog={dog} user={user} />
// FoodComparator est dans un autre tab — chercher son rendu dans Nutri.jsx
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scan.jsx — charger DietPreferences et injecter disliked_foods dans les prompts</name>
  <files>pawcoach/src/pages/Scan.jsx</files>
  <action>
Lire Scan.jsx en entier avant de modifier.

1. Dans loadData(), ajouter un fetch parallele de DietPreferences apres avoir obtenu activeDog :
```javascript
const [scans, dietPrefs] = await Promise.all([
  base44.entities.FoodScan.filter({ dog_id: activeDog.id }),
  base44.entities.DietPreferences.filter({ dog_id: activeDog.id }).catch(() => []),
]);
setDietPreferences(dietPrefs?.[0] || null);
```
Ajouter `const [dietPreferences, setDietPreferences] = useState(null);` dans les states.

2. Dans le prompt de analyzeFood() (mode aliment brut), apres la ligne allergies, ajouter :
```
- Aliments indesirables (preferences) : ${dietPreferences?.disliked_foods || "aucun"}
```
Instruction complementaire : "Si un des aliments identifies correspond aux aliments indesirables, indique-le clairement dans la recommendation, meme si ce n'est pas toxique."

3. Dans le prompt de analyzeLabel() (mode etiquette), dans le bloc "Profil du chien", apres "Restrictions", ajouter :
```
- Aliments indesirables (preferences) : ${dietPreferences?.disliked_foods || "aucun"}
```
Instruction complementaire dans le prompt : "Si des ingredients de la liste ingredients_list correspondent aux aliments indesirables, les lister dans allergen_alerts avec la mention '(preference)'."

IMPORTANT : ne pas supprimer ni modifier les lignes existantes sur dog.allergies. Ajouter uniquement en complement.
  </action>
  <verify>grep -n "disliked_foods\|DietPreferences" "C:/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Scan.jsx"</verify>
  <done>
- Scan.jsx contient `DietPreferences.filter` dans loadData
- Scan.jsx contient `disliked_foods` dans les 2 prompts (analyzeFood + analyzeLabel)
- dog.allergies est toujours present dans les 2 prompts
  </done>
</task>

<task type="auto">
  <name>Task 2: FoodComparator.jsx — recevoir dietPreferences en prop et injecter disliked_foods</name>
  <files>pawcoach/src/components/nutrition/FoodComparator.jsx, pawcoach/src/pages/Nutri.jsx</files>
  <action>
Lire FoodComparator.jsx en entier et Nutri.jsx autour du rendu de FoodComparator avant de modifier.

**FoodComparator.jsx :**

1. Modifier la signature du composant :
```javascript
export default function FoodComparator({ dog, user, dietPreferences }) {
```

2. Extraire disliked_foods comme variable locale :
```javascript
const dislikedFoods = dietPreferences?.disliked_foods || "aucun";
```

3. Dans analyzeProduct() (prompt ligne ~165), apres `allergies : ${dog?.allergies || "aucune"}`, ajouter :
```
, aliments indesirables : ${dislikedFoods}
```
Ajouter apres le profil chien : "Si un ingredient correspond aux aliments indesirables, le signaler dans l'analyse."

4. Dans compare() (prompt ligne ~219), apres `allergies : ${dog?.allergies || "aucune"})`, ajouter :
```
, aliments indesirables : ${dislikedFoods}
```

**Nutri.jsx :**

Chercher le rendu de FoodComparator dans Nutri.jsx. Nutri charge deja `dietPreferences` en state (depuis le fetch parallele ligne 246). Passer la prop :
```javascript
<FoodComparator dog={dog} user={user} dietPreferences={dietPreferences[0] || null} />
```
Verifier que le state `dietPreferences` est bien accessible a l'endroit ou FoodComparator est rendu (il est dans un tab de Nutri). Si Nutri stocke le resultat du fetch DietPreferences dans une variable locale, utiliser cette variable.

IMPORTANT : ne pas supprimer ni modifier les lignes existantes sur dog.allergies. Ajouter uniquement en complement.
  </action>
  <verify>grep -n "disliked_foods\|dietPreferences" "C:/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/nutrition/FoodComparator.jsx" && grep -n "dietPreferences" "C:/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Nutri.jsx" | grep -i "foodcomparator\|dietprefs"</verify>
  <done>
- FoodComparator accepte dietPreferences en prop
- FoodComparator contient disliked_foods dans les 2 prompts (analyzeProduct + compare)
- Nutri.jsx passe dietPreferences a FoodComparator
- dog.allergies reste present dans les prompts
  </done>
</task>

</tasks>

<verification>
Verification globale DATA-01 :
1. Dans Scan.jsx : `grep -c "disliked_foods" pawcoach/src/pages/Scan.jsx` doit retourner >= 2 (un par prompt)
2. Dans FoodComparator.jsx : `grep -c "disliked_foods" pawcoach/src/components/nutrition/FoodComparator.jsx` doit retourner >= 2
3. `grep -c "allergies" pawcoach/src/pages/Scan.jsx` doit retourner >= 2 (allergies toujours presentes)
4. Aucun fichier utilitaire partage (healthStatus.js, streakHelper.jsx) n'a ete modifie
</verification>

<success_criteria>
- Scanner aliment : le prompt inclut `aliments indesirables : [valeur disliked_foods]` en plus des allergies
- Scanner etiquette : le prompt inclut les aliments indesirables, allergen_alerts peut les capturer
- Comparateur analyse produit : le prompt inclut les aliments indesirables
- Comparateur comparaison finale : le prompt inclut les aliments indesirables
- Aucune regression sur les allergies existantes
- Aucun Build prompt utilise (0 credit)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-data-coherence/01-01-SUMMARY.md`
</output>
