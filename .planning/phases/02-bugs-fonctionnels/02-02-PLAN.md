---
phase: 02-bugs-fonctionnels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/src/components/sante/DiagnosisContent.jsx
  - pawcoach/src/pages/Onboarding.jsx
autonomous: true
requirements:
  - BUG-02
  - BUG-06

must_haves:
  truths:
    - "Les rapports de diagnostic passes sont affichables depuis DiagnosisContent via un accordeon ou liste"
    - "Recharger le navigateur en plein onboarding restaure l'etape exacte et toutes les reponses deja saisies"
    - "Le sessionStorage est nettoye apres la creation reussie du chien (pas de donnees residuelles)"
  artifacts:
    - path: "pawcoach/src/components/sante/DiagnosisContent.jsx"
      provides: "Section historique diagnostics chargeant DiagnosisReport.filter({dog_id})"
      contains: "DiagnosisReport.filter"
    - path: "pawcoach/src/pages/Onboarding.jsx"
      provides: "Persistence sessionStorage de step et answers[], restauration au montage"
      contains: "sessionStorage.setItem"
  key_links:
    - from: "DiagnosisContent.jsx"
      to: "DiagnosisReport entity"
      via: "DiagnosisReport.filter({ dog_id: dog?.id }, '-report_date', 10)"
      pattern: "DiagnosisReport.filter"
    - from: "Onboarding.jsx useEffect"
      to: "sessionStorage"
      via: "sessionStorage.setItem('onboarding_step', step) + setItem('onboarding_answers', JSON.stringify(answers))"
      pattern: "sessionStorage.setItem.*onboarding"
---

<objective>
Ajouter l'historique des diagnostics dans DiagnosisContent et la persistence sessionStorage dans l'onboarding.

Purpose: BUG-02 — les rapports existent en base mais restent invisibles. BUG-06 — 10 etapes d'onboarding perdues au moindre rechargement accidentel.
Output: DiagnosisContent charge et affiche les rapports passes. Onboarding lit/ecrit sessionStorage pour ne jamais perdre la progression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Contrats existants que l'executeur doit utiliser directement -->

Depuis src/api/entities.js :
```javascript
export const DiagnosisReport = wrapEntity(base44.entities.DiagnosisReport, "DiagnosisReport");
// Usage : DiagnosisReport.filter({ dog_id: dog.id }, "-report_date", 10)
```

Champs d'un DiagnosisReport (ecrits dans AIDiagnosisModal.jsx:163) :
```javascript
{
  dog_id: string,
  owner_email: string,
  dog_name: string,
  dog_breed: string,
  dog_weight: number,
  symptoms: string,
  duration: string,
  additional_info: string,
  diagnosis_text: string,  // JSON.stringify(diagnosisData) — parse pour affichage
  urgency_level: "low" | "medium" | "high" | "emergency",
  report_date: string,     // YYYY-MM-DD
}
```

DiagnosisReportView est un composant existant reutilisable :
```javascript
// src/components/vet/DiagnosisReportView.jsx
// Props: report (objet diagnosisData parse), dogName (string), reportDate (string)
// Affiche urgency badge + observations + possible_causes + immediate_advice + important_note
import DiagnosisReportView from "@/components/vet/DiagnosisReportView";
```

Onboarding state actuel (lignes 112-114 de Onboarding.jsx) :
```javascript
const [step, setStep] = useState(0);
const [answers, setAnswers] = useState(Array(INTERVIEW_STEPS.length).fill(""));
// INTERVIEW_STEPS.length = 10
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter la section historique diagnostics dans DiagnosisContent (BUG-02)</name>
  <files>pawcoach/src/components/sante/DiagnosisContent.jsx</files>
  <read_first>
    - pawcoach/src/components/sante/DiagnosisContent.jsx (lire en entier)
    - pawcoach/src/components/vet/DiagnosisReportView.jsx (lire en entier — composant reutilise)
  </read_first>
  <action>
Modifier DiagnosisContent.jsx pour ajouter :

1. **Nouveaux imports en haut** :
```javascript
import { useState, useEffect } from "react";
import { DiagnosisReport } from "@/api/entities";
import DiagnosisReportView from "@/components/vet/DiagnosisReportView";
import { History, ChevronDown, ChevronUp } from "lucide-react";
```
   Remplacer `import { useState } from "react";` par `import { useState, useEffect } from "react";`

2. **Nouveau state dans le composant** apres les useState existants :
```javascript
const [reports, setReports] = useState([]);
const [loadingReports, setLoadingReports] = useState(false);
const [expandedReport, setExpandedReport] = useState(null);
```

3. **useEffect pour charger les rapports** apres les useState :
```javascript
useEffect(() => {
  if (!dog?.id) return;
  setLoadingReports(true);
  DiagnosisReport.filter({ dog_id: dog.id }, "-report_date", 10)
    .then(data => setReports(data || []))
    .catch(() => setReports([]))
    .finally(() => setLoadingReports(false));
}, [dog?.id]);
```

4. **Section historique** a ajouter dans le JSX, APRES le bouton CTA "Preparer mon bilan" et AVANT la section "En cas d'urgence" :
```jsx
{/* Diagnosis history */}
{(loadingReports || reports.length > 0) && (
  <div className="bg-white rounded-2xl border border-border p-4 space-y-3">
    <div className="flex items-center gap-2">
      <History className="w-4 h-4 text-muted-foreground" />
      <p className="font-bold text-sm text-foreground">Bilans precedents</p>
      <span className="ml-auto text-xs text-muted-foreground">{reports.length} rapport{reports.length > 1 ? 's' : ''}</span>
    </div>
    {loadingReports && (
      <p className="text-xs text-muted-foreground text-center py-2">Chargement...</p>
    )}
    {!loadingReports && reports.map((r) => {
      const parsedReport = (() => {
        try { return typeof r.diagnosis_text === 'string' ? JSON.parse(r.diagnosis_text) : r.diagnosis_text; }
        catch { return null; }
      })();
      const isExpanded = expandedReport === r.id;
      const urgencyColors = {
        low: 'text-emerald-600 bg-emerald-50 border-emerald-200',
        medium: 'text-amber-600 bg-amber-50 border-amber-200',
        high: 'text-orange-600 bg-orange-50 border-orange-200',
        emergency: 'text-red-600 bg-red-50 border-red-200',
      };
      const colorClass = urgencyColors[r.urgency_level] || urgencyColors.medium;
      return (
        <div key={r.id} className="border border-border rounded-xl overflow-hidden">
          <button
            className="w-full flex items-center justify-between p-3 text-left hover:bg-muted/30 transition-colors"
            onClick={() => setExpandedReport(isExpanded ? null : r.id)}
          >
            <div className="flex items-center gap-2 min-w-0">
              <span className={`text-[11px] font-semibold px-2 py-0.5 rounded-full border ${colorClass} flex-shrink-0`}>
                {r.urgency_level === 'emergency' ? 'Urgence' : r.urgency_level === 'high' ? 'Eleve' : r.urgency_level === 'low' ? 'Faible' : 'Modere'}
              </span>
              <span className="text-xs text-muted-foreground truncate">{r.symptoms?.substring(0, 40)}{r.symptoms?.length > 40 ? '...' : ''}</span>
            </div>
            <div className="flex items-center gap-2 flex-shrink-0 ml-2">
              <span className="text-[11px] text-muted-foreground">{r.report_date}</span>
              {isExpanded ? <ChevronUp className="w-3.5 h-3.5 text-muted-foreground" /> : <ChevronDown className="w-3.5 h-3.5 text-muted-foreground" />}
            </div>
          </button>
          {isExpanded && parsedReport && (
            <div className="px-3 pb-3 border-t border-border bg-muted/10">
              <div className="pt-3">
                <DiagnosisReportView report={parsedReport} dogName={r.dog_name} reportDate={r.report_date} />
              </div>
            </div>
          )}
          {isExpanded && !parsedReport && (
            <div className="px-3 pb-3 border-t border-border">
              <p className="text-xs text-muted-foreground pt-3">Rapport non disponible.</p>
            </div>
          )}
        </div>
      );
    })}
  </div>
)}
```

**Contrainte importante** : `dog` est deja un prop de DiagnosisContent. Ne pas changer la signature `export default function DiagnosisContent({ dog })`.
  </action>
  <verify>
    <automated>grep -n "DiagnosisReport.filter" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/sante/DiagnosisContent.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "DiagnosisReport.filter" dans DiagnosisContent.jsx retourne 1 occurrence
    - grep "DiagnosisReportView" dans DiagnosisContent.jsx retourne au moins 2 occurrences (import + usage)
    - grep "Bilans precedents" dans DiagnosisContent.jsx retourne 1 occurrence
    - grep "expandedReport" dans DiagnosisContent.jsx retourne au moins 3 occurrences (useState, setter, comparaison)
  </acceptance_criteria>
  <done>DiagnosisContent charge les rapports passes via DiagnosisReport.filter et les affiche dans un accordeon. L'historique est visible sans rechargement de page.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Persistence sessionStorage dans Onboarding (BUG-06)</name>
  <files>pawcoach/src/pages/Onboarding.jsx</files>
  <read_first>
    - pawcoach/src/pages/Onboarding.jsx (lire en entier — toutes les lignes)
  </read_first>
  <action>
Modifier Onboarding.jsx pour persister `step` et `answers` dans sessionStorage.

**Cle sessionStorage** : `onboarding_state` (un seul objet JSON pour les deux valeurs).

**3 modifications a faire :**

**Modification 1 — Initialisation du state avec restauration sessionStorage** (remplacer les lignes 112-113) :

Remplacer :
```javascript
const [step, setStep] = useState(0);
const [answers, setAnswers] = useState(Array(INTERVIEW_STEPS.length).fill(""));
```

Par :
```javascript
const [step, setStep] = useState(() => {
  try {
    const saved = sessionStorage.getItem('onboarding_state');
    if (saved) { const parsed = JSON.parse(saved); return typeof parsed.step === 'number' ? parsed.step : 0; }
  } catch {}
  return 0;
});
const [answers, setAnswers] = useState(() => {
  try {
    const saved = sessionStorage.getItem('onboarding_state');
    if (saved) {
      const parsed = JSON.parse(saved);
      if (Array.isArray(parsed.answers) && parsed.answers.length === INTERVIEW_STEPS.length) return parsed.answers;
    }
  } catch {}
  return Array(INTERVIEW_STEPS.length).fill("");
});
```

**Modification 2 — useEffect de sauvegarde** a ajouter apres les useState existants (avant la constante OPTIONAL_STEPS) :

```javascript
// Persist onboarding progress so page reload restores the user's position
useEffect(() => {
  try {
    sessionStorage.setItem('onboarding_state', JSON.stringify({ step, answers }));
  } catch {}
}, [step, answers]);
```

**Modification 3 — Nettoyage apres succes** dans la fonction `handleNext`, apres la ligne `setDone(true)` (dans le bloc try, apres `setSaveError(false); setDone(true);`) :

Remplacer `setSaveError(false); setDone(true);` par :
```javascript
setSaveError(false);
try { sessionStorage.removeItem('onboarding_state'); } catch {}
setDone(true);
```

**Contraintes importantes** :
- Ne pas toucher `isAddDog` ni `started` — ils ne font pas partie de l'etat a persister (c'est OK de repartir du splash si on recharge avant de cliquer "Commencer")
- Le `started` state n'est pas persiste intentionnellement : si l'user recharge avant de cliquer "Commencer", il revoit le splash (comportement acceptable)
- Tous les try/catch autour de sessionStorage protegent la navigation privee (NoAccessError)
  </action>
  <verify>
    <automated>grep -n "sessionStorage.setItem" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Onboarding.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "sessionStorage.setItem" dans Onboarding.jsx retourne au moins 1 occurrence (le useEffect de sauvegarde)
    - grep "sessionStorage.getItem" dans Onboarding.jsx retourne au moins 2 occurrences (une pour step, une pour answers dans leur initializer)
    - grep "sessionStorage.removeItem" dans Onboarding.jsx retourne 1 occurrence (nettoyage post-succes)
    - grep "onboarding_state" dans Onboarding.jsx retourne au moins 3 occurrences
  </acceptance_criteria>
  <done>Recharger la page en cours d'onboarding restaure l'etape et toutes les reponses. Apres creation reussie du chien, la cle sessionStorage est supprimee.</done>
</task>

</tasks>

<verification>
Commandes de verification post-execution :

```bash
# BUG-02 : historique diagnostics
grep -c "DiagnosisReport.filter" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/sante/DiagnosisContent.jsx
# Attendu : 1

# BUG-06 : sessionStorage persistence
grep -c "onboarding_state" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Onboarding.jsx
# Attendu : >= 3
```
</verification>

<success_criteria>
- DiagnosisContent affiche les rapports passes via DiagnosisReport.filter({dog_id}) dans un accordeon
- Le JSON diagnosis_text est parse et rendu via DiagnosisReportView (composant existant reutilise)
- Onboarding.jsx ecrit step+answers dans sessionStorage a chaque changement
- Onboarding.jsx relit sessionStorage au montage (lazy initializer)
- Onboarding.jsx supprime la cle apres Dog.create reussi
- Tous les acces sessionStorage sont dans try/catch (navigation privee)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-bugs-fonctionnels/02-02-SUMMARY.md`
</output>
