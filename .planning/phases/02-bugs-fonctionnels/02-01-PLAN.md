---
phase: 02-bugs-fonctionnels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/base44/functions/finalDiagnosis/entry.ts
  - pawcoach/src/utils/recommendations.js
  - pawcoach/base44/functions/trialExpiryReminder/entry.ts
  - pawcoach/src/components/UserNotRegisteredError.jsx
autonomous: true
requirements:
  - BUG-01
  - BUG-03
  - BUG-04
  - BUG-05

must_haves:
  truths:
    - "Un diagnostic complet (preDiagnosis + finalDiagnosis) consomme 1 seul credit, pas 2"
    - "Cliquer sur une recommandation 'Trouver un veto' navigue vers la page Sante (tab findvet), pas vers un 404"
    - "L'email trial J-1 annonce 10 messages/jour et non 5"
    - "L'ecran UserNotRegisteredError s'affiche entierement en francais avec un bouton de deconnexion fonctionnel"
  artifacts:
    - path: "pawcoach/base44/functions/finalDiagnosis/entry.ts"
      provides: "Backend finalDiagnosis sans decrement actions_remaining"
      contains: "# No credit decrement"
    - path: "pawcoach/src/utils/recommendations.js"
      provides: "Recommandation findvet pointant vers page Sante existante"
      contains: "page: \"Sante\""
    - path: "pawcoach/base44/functions/trialExpiryReminder/entry.ts"
      provides: "Email trial avec limite correcte 10 messages/jour"
      contains: "10 messages/jour"
    - path: "pawcoach/src/components/UserNotRegisteredError.jsx"
      provides: "Ecran d'erreur en francais avec bouton logout"
      contains: "base44.auth.logout"
  key_links:
    - from: "preDiagnosis/entry.ts"
      to: "User.actions_remaining"
      via: "decrement dans preDiagnosis uniquement"
      pattern: "actions_remaining.*remaining - 1"
    - from: "recommendations.js"
      to: "createPageUrl('Sante')"
      via: "page: 'Sante', tab: 'findvet'"
      pattern: "page.*Sante"
---

<objective>
Corriger 4 bugs chirurgicaux : double-decrement credit diagnostic, lien 404 FindVet, texte email incorrect, ecran erreur en anglais.

Purpose: Ces 4 bugs cassent des flows visibles par l'utilisateur — credits consommes en double, 404 au clic, email menteur, interface bloquante sans solution.
Output: 4 fichiers modifies, chacun avec 1 a 5 lignes changees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retirer le decrement credit dans finalDiagnosis (BUG-01)</name>
  <files>pawcoach/base44/functions/finalDiagnosis/entry.ts</files>
  <read_first>
    - pawcoach/base44/functions/finalDiagnosis/entry.ts (lire en entier)
    - pawcoach/base44/functions/preDiagnosis/entry.ts (lignes 1-40 pour confirmer le decrement reste la)
  </read_first>
  <action>
Dans finalDiagnosis/entry.ts, supprimer le bloc entier du decrement server-side (lignes 14-35 environ) :

```
// Server-side quota check — prevents client-side bypass
const isPremium = user.is_premium || (user.trial_expires_at && new Date(user.trial_expires_at) > new Date());
if (!isPremium) {
  const ACTION_DAILY_LIMIT = 3;
  const today = new Date().toISOString().split("T")[0];
  let remaining = user.actions_remaining ?? ACTION_DAILY_LIMIT;
  const lastReset = user.actions_daily_reset;

  // Daily reset: restore full quota if date has changed
  if (lastReset !== today) {
    remaining = ACTION_DAILY_LIMIT;
  }

  if (remaining <= 0) {
    return Response.json({ error: "daily_limit_reached", message: "Tu as atteint la limite du jour. Réessaie demain ou passe en Premium." }, { status: 429 });
  }

  // Decrement server-side
  await base44.asServiceRole.entities.User.update(user.id, {
    actions_remaining: remaining - 1,
    actions_daily_reset: today,
  });
}
```

Remplacer par un commentaire court indiquant pourquoi il n'y a pas de decrement ici :

```
// No credit decrement here — preDiagnosis already decremented for this diagnostic flow.
// finalDiagnosis is always called as step 2 of the same user-initiated action.
```

Le fichier doit rester fonctionnel : le bloc `const { symptoms, ... } = await req.json();` et tout le reste (sanitize, prompt, InvokeLLM, DiagnosisReport.create si present) doit etre preserve intact.
  </action>
  <verify>
    <automated>grep -n "actions_remaining" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/finalDiagnosis/entry.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "actions_remaining" dans finalDiagnosis/entry.ts retourne 0 occurrences
    - grep "No credit decrement" dans finalDiagnosis/entry.ts retourne 1 occurrence
    - preDiagnosis/entry.ts contient toujours "actions_remaining: remaining - 1"
  </acceptance_criteria>
  <done>finalDiagnosis ne decrement plus actions_remaining. Un diagnostic complet = 1 credit (depuis preDiagnosis).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Corriger le lien FindVet vers Sante dans recommendations.js (BUG-03)</name>
  <files>pawcoach/src/utils/recommendations.js</files>
  <read_first>
    - pawcoach/src/utils/recommendations.js (lignes 185-202)
  </read_first>
  <action>
Dans src/utils/recommendations.js, a la ligne ~197, changer le champ `page` de la recommandation `diagnosis_followup` :

Avant :
```javascript
page: "FindVet",
cta: "Trouver un veto",
```

Apres :
```javascript
page: "Sante",
tab: "findvet",
cta: "Trouver un veto",
```

Le reste de l'objet (id, priority, icon, iconBg, iconColor, label, sub, accent) est inchange.

Verification que "FindVet" n'existe pas : pages.config.js liste exactement Activite, Chat, Dashboard, DogProfile, DogPublicProfile, Home, Library, Nutri, Onboarding, Premium, Profile, Sante, Scan, Training, VetDogView, VetPortal. Pas de FindVet.
  </action>
  <verify>
    <automated>grep -n "FindVet" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/utils/recommendations.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "FindVet" dans recommendations.js retourne 0 occurrences
    - grep 'page: "Sante"' dans recommendations.js retourne au moins 1 occurrence dans le bloc diagnosis_followup
    - grep 'tab: "findvet"' dans recommendations.js retourne 1 occurrence
  </acceptance_criteria>
  <done>La recommandation "Trouver un veto" navigue vers Sante?tab=findvet au lieu d'un 404.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Corriger l'email trial J-1 (5 → 10 messages/jour) et traduire UserNotRegisteredError (BUG-05 + BUG-04)</name>
  <files>
    pawcoach/base44/functions/trialExpiryReminder/entry.ts
    pawcoach/src/components/UserNotRegisteredError.jsx
  </files>
  <read_first>
    - pawcoach/base44/functions/trialExpiryReminder/entry.ts (lignes 65-90)
    - pawcoach/src/components/UserNotRegisteredError.jsx (lire en entier)
  </read_first>
  <action>
**Fichier 1 : trialExpiryReminder/entry.ts (BUG-05)**

A la ligne ~77, dans le corps du mail J-1, changer :
```
`• Le chat IA illimité (retour à 5 messages/jour)`,
```
En :
```
`• Le chat IA illimité (retour à 10 messages/jour)`,
```

Un seul caractere change. Le reste du fichier est inchange.

---

**Fichier 2 : UserNotRegisteredError.jsx (BUG-04)**

Remplacement complet du composant. Conserver :
- Meme structure div/flex/rounded-lg/shadow-lg
- Meme icone SVG amber (triangle d'avertissement)
- Meme classes Tailwind generales

Remplacer le contenu texte et ajouter un bouton logout :

```jsx
import React from 'react';
import { base44 } from '@/api/base44Client';

const UserNotRegisteredError = () => {
  const handleLogout = async () => {
    try {
      await base44.auth.logout();
    } catch (e) {
      console.error('Logout error:', e);
    }
  };

  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-gradient-to-b from-white to-muted">
      <div className="max-w-md w-full p-8 bg-white rounded-lg shadow-lg border border-border">
        <div className="text-center">
          <div className="inline-flex items-center justify-center w-16 h-16 mb-6 rounded-full bg-amber-100">
            <svg className="w-8 h-8 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
          </div>
          <h1 className="text-3xl font-bold text-foreground mb-4">Acces non autorise</h1>
          <p className="text-muted-foreground mb-8">
            Ton compte n'est pas inscrit sur cette application. Contacte l'administrateur pour obtenir un acces.
          </p>
          <div className="p-4 bg-muted rounded-md text-sm text-muted-foreground mb-6">
            <p>Si tu penses que c'est une erreur, tu peux :</p>
            <ul className="list-disc list-inside mt-2 space-y-1">
              <li>Verifier que tu es connecte avec le bon compte</li>
              <li>Contacter l'administrateur de l'application</li>
              <li>Te deconnecter puis te reconnecter</li>
            </ul>
          </div>
          <button
            onClick={handleLogout}
            className="w-full h-12 rounded-xl bg-foreground text-background font-semibold text-sm hover:opacity-90 transition-opacity"
          >
            Se deconnecter
          </button>
        </div>
      </div>
    </div>
  );
};

export default UserNotRegisteredError;
```
  </action>
  <verify>
    <automated>grep -n "5 messages/jour" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/trialExpiryReminder/entry.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "5 messages/jour" dans trialExpiryReminder/entry.ts retourne 0 occurrences
    - grep "10 messages/jour" dans trialExpiryReminder/entry.ts retourne 1 occurrence
    - grep "base44.auth.logout" dans UserNotRegisteredError.jsx retourne 1 occurrence
    - grep "Access Restricted" dans UserNotRegisteredError.jsx retourne 0 occurrences (texte anglais supprime)
    - grep "Acces non autorise" dans UserNotRegisteredError.jsx retourne 1 occurrence
  </acceptance_criteria>
  <done>Email trial J-1 annonce 10 messages/jour. UserNotRegisteredError est en francais avec un bouton "Se deconnecter" fonctionnel.</done>
</task>

</tasks>

<verification>
Commandes de verification post-execution :

```bash
# BUG-01 : plus de decrement dans finalDiagnosis
grep -c "actions_remaining" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/finalDiagnosis/entry.ts
# Attendu : 0

# BUG-03 : FindVet disparu
grep -c "FindVet" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/utils/recommendations.js
# Attendu : 0

# BUG-05 : bonne limite
grep "messages/jour" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/base44/functions/trialExpiryReminder/entry.ts
# Attendu : "10 messages/jour"

# BUG-04 : francais + logout
grep "logout" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/UserNotRegisteredError.jsx
# Attendu : 1 occurrence
```
</verification>

<success_criteria>
- Un diagnostic complet consomme exactement 1 credit (preDiagnosis decremente, finalDiagnosis ne decremente pas)
- recommendations.js ne contient plus aucune reference a "FindVet"
- L'email trial J-1 contient "10 messages/jour" a la place de "5 messages/jour"
- UserNotRegisteredError est en francais avec base44.auth.logout() branche sur le bouton
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-bugs-fonctionnels/02-01-SUMMARY.md`
</output>
