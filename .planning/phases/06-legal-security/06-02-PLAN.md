---
phase: 06-legal-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Onboarding.jsx
  - src/utils/analytics.js
autonomous: true
requirements:
  - FIX-03
  - FIX-04
  - FIX-08

must_haves:
  truths:
    - "L'onboarding affiche une checkbox RGPD + âge sur la dernière étape avant Dog.create()"
    - "Si la checkbox n'est pas cochée, Dog.create() n'est pas appelé et un message d'erreur s'affiche"
    - "trackEvent() ne s'exécute que si le consentement analytics est accordé (localStorage gate)"
    - "L'utilisateur peut utiliser l'app sans analytics (refus respecté)"
  artifacts:
    - path: "src/pages/Onboarding.jsx"
      provides: "Consentement RGPD + âge bloquant avant création chien"
      contains: "gdprConsent"
    - path: "src/utils/analytics.js"
      provides: "Gate de consentement analytics"
      contains: "CONSENT_KEY"
  key_links:
    - from: "src/pages/Onboarding.jsx"
      to: "Dog.create()"
      via: "gdprConsent === true check avant appel"
      pattern: "gdprConsent"
    - from: "src/utils/analytics.js"
      to: "localStorage"
      via: "consentement vérifié avant trackEvent"
      pattern: "CONSENT_KEY"
---

<objective>
Ajouter le consentement RGPD + vérification d'âge dans l'onboarding (bloquant avant Dog.create()), et implémenter un gate de consentement analytics dans analytics.js.

Purpose: Sans consentement explicite, le stockage de données personnelles et le tracking analytics violent le RGPD (Art. 6, 8) et la directive ePrivacy. Bloquant P0.
Output: Checkbox consentement dans Onboarding, gate analytics dans utils/analytics.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

Onboarding flow actuel : OnboardingWelcome (splash) → 10 étapes d'interview → handleFinish() → Dog.create() → WelcomeScreen.
La checkbox doit apparaître sur la dernière étape (step === INTERVIEW_STEPS.length - 1) OU comme étape finale dédiée avant handleFinish.
Analytics : trackEvent() dans analytics.js stocke dans localStorage sans consentement. Solution : vérifier localStorage.getItem("pawcoach_analytics_consent") === "true" avant de stocker. Si absent → ne pas tracker (opt-in).
Le consentement analytics sera accordé quand l'utilisateur coche la checkbox RGPD dans l'onboarding.
NE PAS modifier src/components/ui/.
</context>

<interfaces>
<!-- Contexte code existant Onboarding.jsx -->
From src/pages/Onboarding.jsx:
```jsx
// Lignes 195-215 — états principaux
const [step, setStep] = useState(0);
const [answers, setAnswers] = useState([]);
const [saving, setSaving] = useState(false);
// handleFinish est appelé quand step === INTERVIEW_STEPS.length (après la dernière étape)

// Ligne 242 — Dog.create() call (c'est ICI qu'on doit bloquer si non consenti)
const dog = await Dog.create({ ... });

// Ligne 253 — trackEvent après création réussie
trackEvent("onboarding_complete", { ... });
```

From src/utils/analytics.js:
```js
// Ligne 20-45 — trackEvent actuelle sans gate
export function trackEvent(eventName, properties = {}) {
  // ... stocke dans localStorage sans vérifier consentement
}
```

Pattern checkbox Tailwind (à utiliser) :
```jsx
<label className="flex items-start gap-3 cursor-pointer">
  <input
    type="checkbox"
    checked={gdprConsent}
    onChange={e => setGdprConsent(e.target.checked)}
    className="mt-0.5 w-4 h-4 accent-emerald-600 flex-shrink-0"
  />
  <span className="text-xs text-muted-foreground leading-relaxed">
    J'accepte la politique de confidentialité...
  </span>
</label>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Gate analytics dans analytics.js</name>
  <files>src/utils/analytics.js</files>

  <read_first>
    - src/utils/analytics.js (lignes 1-64) — code complet à modifier
  </read_first>

  <action>
Modifier `src/utils/analytics.js` :

1. Ajouter une constante en haut du fichier (après STORAGE_KEY) :
```js
const CONSENT_KEY = "pawcoach_analytics_consent";
```

2. Ajouter une fonction exportée pour enregistrer le consentement :
```js
export function setAnalyticsConsent(granted) {
  try {
    if (granted) {
      localStorage.setItem(CONSENT_KEY, "true");
    } else {
      localStorage.removeItem(CONSENT_KEY);
    }
  } catch {
    // localStorage unavailable
  }
}

export function hasAnalyticsConsent() {
  try {
    return localStorage.getItem(CONSENT_KEY) === "true";
  } catch {
    return false;
  }
}
```

3. Dans `trackEvent()`, ajouter un guard au tout début de la fonction (avant la ligne `console.debug`) :
```js
if (!hasAnalyticsConsent()) return;
```

Cela signifie : sans consentement explicite (opt-in), aucun événement n'est stocké ni loggué. Les appels existants à `trackEvent()` dans Onboarding.jsx, Premium.jsx, Scan.jsx, ai-credits.js continueront de fonctionner mais ne feront rien si le consentement n'a pas été accordé.
  </action>

  <verify>
    <automated>grep -n "CONSENT_KEY\|hasAnalyticsConsent\|setAnalyticsConsent" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/utils/analytics.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CONSENT_KEY" src/utils/analytics.js` retourne 2+ lignes (déclaration + utilisation)
    - `grep "hasAnalyticsConsent" src/utils/analytics.js` retourne 2+ lignes (déclaration + appel dans trackEvent)
    - `grep "setAnalyticsConsent" src/utils/analytics.js` retourne 1+ ligne (export)
    - `grep "if.*hasAnalyticsConsent" src/utils/analytics.js` retourne 1 ligne (guard dans trackEvent)
  </acceptance_criteria>

  <done>trackEvent() ne stocke rien si localStorage("pawcoach_analytics_consent") !== "true".</done>
</task>

<task type="auto">
  <name>Task 2: Checkbox RGPD + âge dans Onboarding avant Dog.create()</name>
  <files>src/pages/Onboarding.jsx</files>

  <read_first>
    - src/pages/Onboarding.jsx (lignes 100-160) — états, gestion step, sessionStorage
    - src/pages/Onboarding.jsx (lignes 190-300) — handleFinish, Dog.create(), flow complet
    - src/pages/Onboarding.jsx (lignes 300-450) — rendu des étapes (pour trouver où insérer la checkbox)
  </read_first>

  <action>
Stratégie : ajouter la checkbox RGPD comme étape finale dédiée (une nouvelle étape CONSENT après les 10 étapes d'interview). C'est la plus propre : l'utilisateur a déjà tout rempli et voit clairement ce qu'il accepte.

**1. Importer setAnalyticsConsent depuis analytics.js** (remplacer l'import existant ligne 3) :
```jsx
import { trackEvent, setAnalyticsConsent } from "@/utils/analytics";
```

**2. Ajouter un état gdprConsent** (après les états existants, ex. après `const [saving, setSaving]`) :
```jsx
const [gdprConsent, setGdprConsent] = useState(false);
const [consentError, setConsentError] = useState(false);
```

**3. Dans `handleFinish()`** (avant `setSaving(true)`), ajouter le guard :
```jsx
if (!gdprConsent) {
  setConsentError(true);
  return;
}
setConsentError(false);
// puis appeler setAnalyticsConsent(true) juste avant Dog.create()
setAnalyticsConsent(true);
```

Placer `setAnalyticsConsent(true)` juste avant la ligne `const dog = await Dog.create({...})`.

**4. Ajouter l'étape CONSENT dans le rendu** — dans la logique de rendu des étapes (après la dernière étape d'interview, avant le bouton "Terminer"), afficher un écran de consentement quand `step === INTERVIEW_STEPS.length` :

Dans le composant principal (function OnboardingForm ou la zone de rendu), quand `step >= INTERVIEW_STEPS.length`, afficher :

```jsx
{/* Consentement RGPD — étape finale */}
<div className="space-y-5 px-5 pb-8">
  <div className="text-center">
    <div className="w-14 h-14 rounded-full bg-emerald-50 flex items-center justify-center mx-auto mb-4">
      <ShieldCheck className="w-7 h-7 text-emerald-600" />
    </div>
    <h2 className="text-xl font-black text-foreground">Presque là !</h2>
    <p className="text-sm text-muted-foreground mt-2">
      Quelques confirmations avant de créer le profil de ton chien.
    </p>
  </div>

  <div className="bg-white rounded-2xl border border-border p-4 space-y-4">
    {/* Consentement RGPD + CGU */}
    <label className="flex items-start gap-3 cursor-pointer">
      <input
        type="checkbox"
        checked={gdprConsent}
        onChange={e => { setGdprConsent(e.target.checked); setConsentError(false); }}
        className="mt-0.5 w-4 h-4 accent-emerald-600 flex-shrink-0"
      />
      <span className="text-xs text-muted-foreground leading-relaxed">
        J'ai 16 ans ou plus (ou j'ai l'autorisation parentale) et j'accepte la{" "}
        <button
          type="button"
          onClick={() => navigate(createPageUrl("Privacy"))}
          className="text-primary underline"
        >
          politique de confidentialité
        </button>{" "}
        et les{" "}
        <button
          type="button"
          onClick={() => navigate(createPageUrl("Terms"))}
          className="text-primary underline"
        >
          conditions d'utilisation
        </button>
        .
      </span>
    </label>

    {consentError && (
      <p className="text-xs text-destructive font-medium">
        Tu dois accepter pour créer un profil.
      </p>
    )}
  </div>

  <Button
    onClick={handleFinish}
    disabled={saving || !gdprConsent}
    className="w-full h-14 rounded-2xl gradient-primary border-0 text-white font-bold text-base gap-2 shadow-lg"
  >
    {saving ? (
      <><Loader2 className="w-4 h-4 animate-spin" /> Création en cours...</>
    ) : (
      <>"Créer le profil de {currentAnswer || "mon chien"}" <ArrowRight className="w-5 h-5" /></>
    )}
  </Button>
</div>
```

Note : `ShieldCheck` doit être importé (vérifier si déjà présent dans les imports Onboarding.jsx). Si absent, l'ajouter aux imports lucide-react existants. `ArrowRight` est déjà importé (ligne 10).

Vérifier que le bouton "Suivant" de l'étape 9 (dernière interview) avance bien `step` à `INTERVIEW_STEPS.length` pour déclencher l'affichage de l'écran de consentement. La logique actuelle de navigation entre étapes doit naturellement gérer cela.
  </action>

  <verify>
    <automated>grep -n "gdprConsent\|consentError\|setAnalyticsConsent\|CONSENT_KEY" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Onboarding.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "gdprConsent" src/pages/Onboarding.jsx` retourne 4+ lignes (état, label, guard, button disabled)
    - `grep "consentError" src/pages/Onboarding.jsx` retourne 2+ lignes (état + affichage conditionnel)
    - `grep "setAnalyticsConsent" src/pages/Onboarding.jsx` retourne 1 ligne (appel avant Dog.create)
    - `grep "politique de confidentialité\|conditions d'utilisation" src/pages/Onboarding.jsx` retourne 2 lignes (texte checkbox)
    - `grep "16 ans" src/pages/Onboarding.jsx` retourne 1 ligne (vérification âge RGPD Art. 8)
  </acceptance_criteria>

  <done>Checkbox RGPD + âge affichée comme étape finale. Dog.create() bloqué si non cochée. Analytics activées au consentement.</done>
</task>

</tasks>

<verification>
```bash
# Gate analytics en place
grep "CONSENT_KEY\|hasAnalyticsConsent\|if.*hasAnalyticsConsent" src/utils/analytics.js

# Consentement dans Onboarding
grep "gdprConsent\|16 ans\|politique de confidentialité" src/pages/Onboarding.jsx

# Guard avant Dog.create
grep -A2 "gdprConsent.*false\|!gdprConsent" src/pages/Onboarding.jsx

# setAnalyticsConsent appelé
grep "setAnalyticsConsent" src/pages/Onboarding.jsx
```
</verification>

<success_criteria>
- analytics.js : trackEvent() no-op si pas de consentement (CONSENT_KEY absent)
- Onboarding : étape finale avec checkbox "J'ai 16 ans + j'accepte PdC + CGU"
- Dog.create() impossible sans gdprConsent === true (button disabled + guard dans handleFinish)
- setAnalyticsConsent(true) appelé avant Dog.create() quand consentement accordé
- Liens vers /Privacy et /Terms dans la checkbox (dépend du plan 06-01)
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-legal-security/06-02-SUMMARY.md`
</output>
