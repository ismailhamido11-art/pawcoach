---
phase: 03-scalabilite-premium
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
  - base44/functions/weeklyInsightGenerate/entry.ts
autonomous: true
requirements:
  - SCALE-02
  - SCALE-03
  - PREM-01
must_haves:
  truths:
    - "Un fetch qui echoue dans Promise.all de fetchDogData ne bloque plus le reste du chargement Home"
    - "Si OPENROUTER_API_KEY est absente, weeklyInsightGenerate retourne sans creer de WeeklyInsight vide"
    - "Apres redirect /?premium=success, l'app poll base44.auth.me() jusqu'a is_premium=true et recharge les donnees"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "fetchDogData avec .catch sur toutes les requetes + polling premium"
      contains: "catch(() => [])"
    - path: "base44/functions/weeklyInsightGenerate/entry.ts"
      provides: "Guard early-return si apiKey absent"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "src/pages/Home.jsx"
      to: "fetchDogData Promise.all"
      via: ".catch(() => []) sur chaque requete non-protegee"
      pattern: "catch.*\[\]"
    - from: "src/pages/Home.jsx"
      to: "handlePremiumSuccess → polling base44.auth.me()"
      via: "setInterval toutes les 2s, max 10s, arret sur is_premium=true"
      pattern: "setInterval|premium.*poll"
    - from: "base44/functions/weeklyInsightGenerate/entry.ts"
      to: "OPENROUTER_API_KEY check"
      via: "early return si !apiKey avant boucle dogs"
      pattern: "if.*!apiKey.*return"
---

<objective>
Trois fixes dans Home.jsx et weeklyInsightGenerate : (1) protéger les 8 requêtes non-protégées de fetchDogData avec .catch, (2) ajouter un polling post-paiement Stripe pour activer premium en <10s, (3) empêcher weeklyInsightGenerate de créer des insights vides sans API key.

Purpose: Stabilité chargement Home + expérience paiement fluide + comportement correct sans API key.
Output: Home.jsx et weeklyInsightGenerate/entry.ts modifiés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Extrait de src/pages/Home.jsx — contexte executeur -->

Fonction fetchDogData (lignes 45-61) :
```javascript
async function fetchDogData(dogId) {
  const today = getTodayString();
  const [checkins, streaks, recent, recs, exs, scs, logs, diags, plans, tBks, bBks] = await Promise.all([
    DailyCheckin.filter({ dog_id: dogId, date: today }),         // pas de .catch
    Streak.filter({ dog_id: dogId }),                             // pas de .catch
    DailyCheckin.filter({ dog_id: dogId }, "-date", 30),         // pas de .catch
    HealthRecord.filter({ dog_id: dogId }),                      // pas de .catch
    UserProgress.filter({ dog_id: dogId }),                      // pas de .catch
    FoodScan.filter({ dog_id: dogId }),                          // pas de .catch
    DailyLog.filter({ dog_id: dogId }, "-date", 30),             // pas de .catch
    DiagnosisReport.filter({ dog_id: dogId }, "-report_date", 5).catch(() => []),  // déjà protégé
    NutritionPlan.filter({ dog_id: dogId }, "-generated_at", 3).catch(() => []),  // déjà protégé
    Bookmark.filter({ dog_id: dogId, source: "training" }, "-created_at", 10).catch(() => []),   // déjà protégé
    Bookmark.filter({ dog_id: dogId, source: "behavior_program" }, "-created_at", 10).catch(() => []), // déjà protégé
  ]);
  return { checkins, streaks, recent, recs, exs, scs, logs, diags, plans, tBks, bBks };
}
```

handlePremiumSuccess (lignes 215-227) :
```javascript
const handlePremiumSuccess = () => {
  if (premiumSuccessHandledRef.current) return;
  const param = new URLSearchParams(window.location.search).get("premium");
  if (param === "success") {
    premiumSuccessHandledRef.current = true;
    window.history.replaceState({}, "", "/");
    toast.success("Bienvenue en Premium ! Profite de toutes les fonctionnalités.");
    confetti({ ... });
  }
};
```

weeklyInsightGenerate (ligne 25) :
```typescript
const apiKey = Deno.env.get("OPENROUTER_API_KEY");
// ... 250 lignes plus loin, utilisé dans if (apiKey) { ... }
// Mais la WeeklyInsight est créée de toute façon (ligne 287), même si summary/highlights/recommendations sont vides
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: fetchDogData — ajouter .catch(() => []) sur les 7 requêtes non protégées</name>
  <files>src/pages/Home.jsx</files>
  <read_first>src/pages/Home.jsx lignes 45-61 — vérifier la liste exacte des requêtes sans .catch</read_first>
  <action>
Dans la fonction `fetchDogData` (ligne 45), ajouter `.catch(() => [])` sur les 7 premières requêtes du Promise.all qui n'ont pas encore de catch :

```javascript
async function fetchDogData(dogId) {
  const today = getTodayString();
  const [checkins, streaks, recent, recs, exs, scs, logs, diags, plans, tBks, bBks] = await Promise.all([
    DailyCheckin.filter({ dog_id: dogId, date: today }).catch(() => []),
    Streak.filter({ dog_id: dogId }).catch(() => []),
    DailyCheckin.filter({ dog_id: dogId }, "-date", 30).catch(() => []),
    HealthRecord.filter({ dog_id: dogId }).catch(() => []),
    UserProgress.filter({ dog_id: dogId }).catch(() => []),
    FoodScan.filter({ dog_id: dogId }).catch(() => []),
    DailyLog.filter({ dog_id: dogId }, "-date", 30).catch(() => []),
    DiagnosisReport.filter({ dog_id: dogId }, "-report_date", 5).catch(() => []),
    NutritionPlan.filter({ dog_id: dogId }, "-generated_at", 3).catch(() => []),
    Bookmark.filter({ dog_id: dogId, source: "training" }, "-created_at", 10).catch(() => []),
    Bookmark.filter({ dog_id: dogId, source: "behavior_program" }, "-created_at", 10).catch(() => []),
  ]);
  return { checkins, streaks, recent, recs, exs, scs, logs, diags, plans, tBks, bBks };
}
```

Ne toucher que cette fonction. Ne pas modifier le reste de Home.jsx dans cette tâche.
  </action>
  <verify>
    <automated>grep -c "catch(() => \[\])" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
- La commande ci-dessus retourne au moins 11 (11 requêtes avec .catch dans fetchDogData + possibles autres dans le fichier)
- Vérification manuelle : toutes les lignes du Promise.all dans fetchDogData se terminent par `.catch(() => [])`
  </acceptance_criteria>
  <done>Les 11 requêtes du Promise.all fetchDogData ont un .catch(() => []) — un fetch échoué retourne [] et ne bloque pas les autres</done>
</task>

<task type="auto">
  <name>Task 2: handlePremiumSuccess — ajouter polling base44.auth.me() toutes les 2s pendant 10s</name>
  <files>src/pages/Home.jsx</files>
  <read_first>src/pages/Home.jsx lignes 215-230 — lire handlePremiumSuccess et le contexte useEffect</read_first>
  <action>
Remplacer la fonction `handlePremiumSuccess` (lignes 215-227) par une version asynchrone qui poll `base44.auth.me()` toutes les 2s pendant 10s max jusqu'à ce que `is_premium` soit true.

Remplacer :
```javascript
const handlePremiumSuccess = () => {
  if (premiumSuccessHandledRef.current) return;
  const param = new URLSearchParams(window.location.search).get("premium");
  if (param === "success") {
    premiumSuccessHandledRef.current = true;
    window.history.replaceState({}, "", "/");
    toast.success("Bienvenue en Premium ! Profite de toutes les fonctionnalités.");
    confetti({ particleCount: 120, spread: 90, origin: { x: 0.5, y: 0.5 }, colors: ["#1A4D3E", "#2D9F82", "#10b981", "#34d399", "#f59e0b"] });
  }
};
```

Par :
```javascript
const handlePremiumSuccess = () => {
  if (premiumSuccessHandledRef.current) return;
  const param = new URLSearchParams(window.location.search).get("premium");
  if (param !== "success") return;

  premiumSuccessHandledRef.current = true;
  window.history.replaceState({}, "", "/");
  confetti({ particleCount: 120, spread: 90, origin: { x: 0.5, y: 0.5 }, colors: ["#1A4D3E", "#2D9F82", "#10b981", "#34d399", "#f59e0b"] });

  // Poll base44.auth.me() every 2s for up to 10s — webhook may not have fired yet
  let attempts = 0;
  const maxAttempts = 5; // 5 x 2s = 10s
  const interval = setInterval(async () => {
    attempts++;
    try {
      const freshUser = await base44.auth.me();
      if (freshUser?.is_premium) {
        clearInterval(interval);
        setUser(freshUser);
        toast.success("Bienvenue en Premium ! Profite de toutes les fonctionnalités.");
        return;
      }
    } catch {
      // ignore — retry next tick
    }
    if (attempts >= maxAttempts) {
      clearInterval(interval);
      // Webhook may be slow — show success anyway and let user refresh if needed
      toast.success("Paiement reçu ! Active Premium visible dans quelques secondes.");
    }
  }, 2000);
};
```

IMPORTANT : `base44` est importé en haut du fichier via `@/api/base44Client`. `setUser` est le state setter React disponible dans le composant. `toast` est importé via `sonner`. Ne pas modifier les imports.

La ligne `loadData().then(handlePremiumSuccess)` (ligne 227) reste inchangée — `handlePremiumSuccess` ne retourne pas de Promise critique.
  </action>
  <verify>
    <automated>grep -n "setInterval\|poll\|maxAttempts\|auth\.me" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
- `setInterval` apparaît dans handlePremiumSuccess
- `base44.auth.me()` apparaît dans le polling
- `is_premium` est vérifié dans la boucle
- `clearInterval` est appelé quand is_premium=true ET après maxAttempts
  </acceptance_criteria>
  <done>Après redirect /?premium=success, l'app poll base44.auth.me() toutes les 2s pendant 10s — premium s'active dans l'UI sans rechargement</done>
</task>

<task type="auto">
  <name>Task 3: weeklyInsightGenerate — early return si OPENROUTER_API_KEY absent</name>
  <files>base44/functions/weeklyInsightGenerate/entry.ts</files>
  <read_first>base44/functions/weeklyInsightGenerate/entry.ts lignes 20-32 — lire la zone après Dog.list() et avant la boucle</read_first>
  <action>
Le problème : `apiKey` est lu ligne 25, mais la boucle `for (const dog of dogs)` s'exécute même si `apiKey` est null. Dans ce cas, `summary/highlights/recommendations` restent des chaînes vides mais `WeeklyInsight.create(...)` est quand même appelé (ligne 287) — créant des insights vides.

Fix : ajouter un early return juste après la lecture de `apiKey` (ligne 25) :

```typescript
const apiKey = Deno.env.get("OPENROUTER_API_KEY");
if (!apiKey) {
  console.warn("weeklyInsightGenerate: OPENROUTER_API_KEY not set — skipping insight generation");
  return Response.json({ ok: true, generated: 0, reason: "no_api_key" });
}
```

Placer ce bloc immédiatement après la ligne `const apiKey = Deno.env.get("OPENROUTER_API_KEY");` (ligne 25), avant le fetch des users.

ATTENTION : Vérifier le numéro de ligne exact en relisant le fichier avant de modifier — la ligne 25 peut avoir bougé. Chercher le pattern `Deno.env.get("OPENROUTER_API_KEY")`.
  </action>
  <verify>
    <automated>grep -n "OPENROUTER_API_KEY\|no_api_key\|apiKey" base44/functions/weeklyInsightGenerate/entry.ts</automated>
  </verify>
  <acceptance_criteria>
- Un bloc `if (!apiKey)` avec `return Response.json(...)` apparaît juste après la lecture de `OPENROUTER_API_KEY`
- La ligne `return Response.json({ ok: true, generated: 0, reason: "no_api_key" })` est présente
  </acceptance_criteria>
  <done>weeklyInsightGenerate retourne immédiatement avec { ok: true, generated: 0 } si OPENROUTER_API_KEY est absent — aucun WeeklyInsight vide créé</done>
</task>

</tasks>

<verification>
```bash
# SCALE-02 : toutes les requêtes fetchDogData protégées
grep -A 15 "async function fetchDogData" src/pages/Home.jsx | grep -c "catch(() => \[\])"

# PREM-01 : polling présent
grep -c "setInterval\|maxAttempts" src/pages/Home.jsx

# SCALE-03 : early return présent
grep -n "no_api_key" base44/functions/weeklyInsightGenerate/entry.ts
```
</verification>

<success_criteria>
1. fetchDogData : toutes les requêtes ont .catch(() => []) — grep retourne 11
2. handlePremiumSuccess : setInterval + base44.auth.me() + is_premium check présents
3. weeklyInsightGenerate : early return si !apiKey avant la boucle dogs
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-scalabilite-premium/03-02-SUMMARY.md`
</output>
