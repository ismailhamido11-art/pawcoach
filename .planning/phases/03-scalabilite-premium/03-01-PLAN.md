---
phase: 03-scalabilite-premium
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - base44/functions/walkReminder/entry.ts
  - base44/functions/monthlySummary/entry.ts
  - base44/functions/trialExpiryReminder/entry.ts
autonomous: true
requirements:
  - SCALE-01
must_haves:
  truths:
    - "walkReminder ne charge plus Dog.list() sans filtre — seuls les chiens des users eligibles sont charges"
    - "monthlySummary ne charge plus HealthRecord.list() et DailyCheckin.list() sans filtre — filtre par date du mois precedent"
    - "trialExpiryReminder ne charge plus Dog.list() sans filtre — seuls les chiens des users avec trial actif sont charges"
  artifacts:
    - path: "base44/functions/walkReminder/entry.ts"
      provides: "Dog filtered by owner emails from matched users"
      contains: "Dog.filter"
    - path: "base44/functions/monthlySummary/entry.ts"
      provides: "HealthRecord and DailyCheckin filtered by date range"
      contains: "startsWith(monthStr)"
    - path: "base44/functions/trialExpiryReminder/entry.ts"
      provides: "Dog filtered by owner emails from trial users"
      contains: "Dog.filter"
  key_links:
    - from: "walkReminder/entry.ts"
      to: "Dog entity"
      via: "Dog.filter({ owner: email }) per matched user, not Dog.list()"
      pattern: "Dog\\.filter"
    - from: "monthlySummary/entry.ts"
      to: "HealthRecord/DailyCheckin entities"
      via: "filter by date prefix in-memory after loading with filter"
      pattern: "filter.*monthStr"
    - from: "trialExpiryReminder/entry.ts"
      to: "Dog entity"
      via: "Dog.filter({ owner: email }) per trial user, not Dog.list()"
      pattern: "Dog\\.filter"
---

<objective>
Eliminer les charges non filtrées sur les entités Dog, HealthRecord et DailyCheckin dans les trois fonctions backend schedulées. Ces fonctions tournent automatiquement et chargeront toute la base quand l'app aura des milliers d'utilisateurs.

Purpose: Scalabilité backend — chaque fonction doit charger uniquement les données dont elle a besoin.
Output: 3 fonctions backend modifiées avec des requêtes filtrées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: walkReminder — remplacer Dog.list() par Dog.filter() par owner</name>
  <files>base44/functions/walkReminder/entry.ts</files>
  <read_first>base44/functions/walkReminder/entry.ts — lire entier avant toute modification</read_first>
  <action>
Ligne 29 : `const dogs = await base44.asServiceRole.entities.Dog.list();`

Remplacer par une boucle qui charge uniquement le chien de chaque user déjà filtré. Les `users` (ligne 20-23) contiennent déjà uniquement les users avec reminder actif à cette heure. Il suffit de remplacer le Dog.list() global par un Dog.filter par owner :

```typescript
// Build owner email set from matched users
const ownerEmails = (users || []).map(u => u.email).filter(Boolean);

// Load only dogs owned by these users (one query per user via filter)
const dogsByOwner: Record<string, any> = {};
await Promise.all(
  ownerEmails.map(async (email) => {
    const userDogs = await base44.asServiceRole.entities.Dog.filter({ owner: email });
    if (userDogs?.length > 0) dogsByOwner[email] = userDogs[0];
  })
);
```

La suite du code utilise `dogsByOwner[user.email]` — ne pas changer cette partie.

IMPORTANT: Si `users` est vide, la fonction retourne déjà en ligne 25-27 — le nouveau code ne s'exécute que si users.length > 0.
  </action>
  <verify>
    <automated>grep -n "Dog\.filter\|Dog\.list" base44/functions/walkReminder/entry.ts</automated>
  </verify>
  <acceptance_criteria>
- `Dog.list()` n'apparaît plus dans ce fichier
- `Dog.filter({ owner: email })` apparaît dans la boucle
- La logique `dogsByOwner[user.email]` est intacte
  </acceptance_criteria>
  <done>walkReminder charge uniquement les chiens des users avec reminder actif à cette heure</done>
</task>

<task type="auto">
  <name>Task 2: trialExpiryReminder — remplacer Dog.list() par Dog.filter() par owner</name>
  <files>base44/functions/trialExpiryReminder/entry.ts</files>
  <read_first>base44/functions/trialExpiryReminder/entry.ts — lire entier avant modification</read_first>
  <action>
Ligne 21 : `const dogs = await base44.asServiceRole.entities.Dog.list();`

Remplacer par un chargement filtré basé sur les emails des users avec trial actif. Le tableau `users` (ligne 12-14) est déjà filtré sur `is_premium: false`. Construire `dogsByOwner` en filtrant par owner email :

```typescript
// Build owner email set from trial users
const ownerEmails = (users || []).map(u => u.email).filter(Boolean);

// Load only dogs owned by these trial users
const dogsByOwner: Record<string, { name: string }> = {};
await Promise.all(
  ownerEmails.map(async (email) => {
    const userDogs = await base44.asServiceRole.entities.Dog.filter({ owner: email });
    if (userDogs?.length > 0 && !dogsByOwner[email]) {
      dogsByOwner[email] = userDogs[0];
    }
  })
);
```

Le reste du code utilise `dogsByOwner[user.email]` (ligne 43) — ne pas toucher.
  </action>
  <verify>
    <automated>grep -n "Dog\.filter\|Dog\.list" base44/functions/trialExpiryReminder/entry.ts</automated>
  </verify>
  <acceptance_criteria>
- `Dog.list()` n'apparaît plus dans ce fichier
- `Dog.filter({ owner: email })` apparaît dans la boucle Promise.all
- La logique `dogsByOwner[user.email]` ligne 43 est intacte
  </acceptance_criteria>
  <done>trialExpiryReminder charge uniquement les chiens des users avec trial actif</done>
</task>

<task type="auto">
  <name>Task 3: monthlySummary — filtrer HealthRecord et DailyCheckin par date</name>
  <files>base44/functions/monthlySummary/entry.ts</files>
  <read_first>base44/functions/monthlySummary/entry.ts — lire entier avant modification</read_first>
  <action>
Le problème est différent ici : la logique actuelle charge `HealthRecord.list()` et `DailyCheckin.list()` (lignes 16-17) pour TOUTE la base, puis filtre en mémoire. Le SDK Base44 supporte des filtres de type `filter({ field: value })` mais pas de filtres de plage de dates nativement.

La stratégie correcte : garder le filtre en mémoire pour les records (logique existante déjà correcte) mais ajouter un filtre sur `dog_id` au lieu de charger tous les records globaux. La variable `dogs` contient déjà les chiens à traiter.

Remplacer les charges globales par des charges par-chien à l'intérieur de la boucle `for (const dog of (dogs || []))` :

1. Supprimer les lignes 16-17 :
```typescript
const allRecords = await base44.asServiceRole.entities.HealthRecord.list();
const allCheckins = await base44.asServiceRole.entities.DailyCheckin.list().catch(() => []);
```

2. À l'intérieur de la boucle `for (const dog of ...)`, après la vérification premium (ligne 26), ajouter :
```typescript
// Load records filtered per dog — avoids loading entire DB
const [dogHealthRecords, dogCheckins] = await Promise.all([
  base44.asServiceRole.entities.HealthRecord.filter({ dog_id: dog.id }),
  base44.asServiceRole.entities.DailyCheckin.filter({ dog_id: dog.id }).catch(() => []),
]);
const allRecords = dogHealthRecords || [];
const allCheckins = dogCheckins || [];
```

3. Mettre à jour les deux références locales dans la boucle : les variables `dogRecords`, `lastMonthRecords`, `weightRecords`, `monthCheckins` utilisent déjà `(allRecords || [])` et `(allCheckins || [])` — pas besoin de les changer. Elles fonctionneront avec les nouvelles variables locales.

ATTENTION : Supprimer aussi la ligne `const userMap = new Map(...)` qui utilise `allUsers` si `allUsers` n'est plus défini. Mais `allUsers` (ligne 15) est nécessaire pour `userMap.get(dog.owner)`. Garder `allUsers` et `userMap`. Seuls `allRecords` et `allCheckins` globaux sont supprimés.
  </action>
  <verify>
    <automated>grep -n "HealthRecord\.list\|DailyCheckin\.list\|HealthRecord\.filter\|DailyCheckin\.filter" base44/functions/monthlySummary/entry.ts</automated>
  </verify>
  <acceptance_criteria>
- `HealthRecord.list()` n'apparaît plus en dehors de la boucle for
- `DailyCheckin.list()` n'apparaît plus en dehors de la boucle for
- `HealthRecord.filter({ dog_id: dog.id })` et `DailyCheckin.filter({ dog_id: dog.id })` apparaissent dans la boucle
- `Dog.list()` et `User.list()` (lignes 14-15) restent en dehors de la boucle — charges globales nécessaires pour itérer
  </acceptance_criteria>
  <done>monthlySummary charge HealthRecord et DailyCheckin par dog_id dans la boucle, pas en global</done>
</task>

</tasks>

<verification>
```bash
grep -n "Dog\.list\(\)\|HealthRecord\.list\(\)\|DailyCheckin\.list\(\)" \
  base44/functions/walkReminder/entry.ts \
  base44/functions/monthlySummary/entry.ts \
  base44/functions/trialExpiryReminder/entry.ts
```
Résultat attendu : aucune ligne.
</verification>

<success_criteria>
1. walkReminder : `Dog.list()` remplacé par `Dog.filter({ owner: email })` par user — vérifié par grep
2. trialExpiryReminder : `Dog.list()` remplacé par `Dog.filter({ owner: email })` par user — vérifié par grep
3. monthlySummary : `HealthRecord.list()` et `DailyCheckin.list()` globaux remplacés par `.filter({ dog_id })` dans la boucle — vérifié par grep
4. Aucune régression syntaxique (TypeScript valide)
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-scalabilite-premium/03-01-SUMMARY.md`
</output>
