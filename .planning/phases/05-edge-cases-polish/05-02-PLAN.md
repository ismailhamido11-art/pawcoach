---
phase: 05-edge-cases-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/VetDogView.jsx
  - src/components/tracker/WalkMode.jsx
  - src/components/vet/AIDiagnosisModal.jsx
  - src/pages/Home.jsx
  - src/components/premium/PremiumNudgeSheet.jsx
autonomous: true
requirements:
  - EDGE-04
  - EDGE-05
  - EDGE-06
  - EDGE-07

must_haves:
  truths:
    - "VetDogView n'affiche pas les records de poids en double quand SectionPoids est visible"
    - "Finir une balade ne cree pas de DailyLog si user.email est absent"
    - "AIDiagnosisModal detecte une reponse JSON d'erreur avant de tenter de creer un PDF blob"
    - "Le dead code walkStreak dans Home.jsx et la prop context inutile dans PremiumNudgeSheet sont supprimes"
  artifacts:
    - path: "src/pages/VetDogView.jsx"
      provides: "Liste chronologique filtre les records weight quand SectionPoids est visible"
    - path: "src/components/tracker/WalkMode.jsx"
      provides: "Guard user?.email avant DailyLog.create dans le useEffect de recovery"
    - path: "src/components/vet/AIDiagnosisModal.jsx"
      provides: "Detection d'une erreur JSON avant la creation du Blob PDF"
    - path: "src/pages/Home.jsx"
      provides: "walkStreak useMemo supprime"
    - path: "src/components/premium/PremiumNudgeSheet.jsx"
      provides: "Param context retire de la signature"
  key_links:
    - from: "src/pages/VetDogView.jsx"
      to: "SectionPoids"
      via: "filtre records weight de la liste chronologique quand sharedSections.includes('weight')"
      pattern: "filter.*type.*weight"
    - from: "src/components/tracker/WalkMode.jsx"
      to: "DailyLog.create"
      via: "guard user?.email present avant l'appel"
      pattern: "user\\?\\.email"
---

<objective>
Corriger quatre bugs distincts : double affichage poids dans VetDogView, creation de DailyLog orphelin sans owner dans WalkMode, PDF corrompu quand generateDiagnosisPDF retourne une erreur JSON, et suppression du dead code walkStreak/context prop.

Purpose: Ces bugs produisent des donnees corrompues (DailyLog sans owner), des PDFs invalides silencieux, une confusion visuelle (poids en double), et du code mort qui alourdit la maintenance.
Output: VetDogView, WalkMode, AIDiagnosisModal, Home.jsx et PremiumNudgeSheet corriges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-04 — Filtrer les records weight de la liste chrono dans VetDogView</name>
  <files>src/pages/VetDogView.jsx</files>

  <read_first>
    - Lire src/pages/VetDogView.jsx lignes 122-153 : TabsContent "records" complet
    - Confirmer la logique : SectionPoids s'affiche si sharedSections.includes("weight") ET records.filter(weight).length > 0
    - La liste chronologique .map ligne 135 itere sur records sans filtre
  </read_first>

  <action>
    Dans src/pages/VetDogView.jsx, modifier la liste chronologique des records (lignes ~135-153) pour exclure les records de type "weight" quand SectionPoids est deja affichee.

    Localiser la liste `.sort((a, b) => ...).map((r, i) => (` qui rend tous les records sans filtre.

    Modifier pour filtrer les weight records quand SectionPoids est visible :

    AVANT :
    ```jsx
    [...records].sort((a, b) => new Date(b.date) - new Date(a.date)).map((r, i) => (
    ```

    APRES :
    ```jsx
    [...records]
      .filter(r => !(sharedSections.includes("weight") && r.type === "weight"))
      .sort((a, b) => new Date(b.date) - new Date(a.date))
      .map((r, i) => (
    ```

    Ne rien changer d'autre. sharedSections est deja destructure ligne 74.
  </action>

  <verify>
    <automated>grep -n "filter.*sharedSections.*weight\|sharedSections.*weight.*filter" src/pages/VetDogView.jsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "filter(r => !(sharedSections.includes" src/pages/VetDogView.jsx retourne 1 resultat
    - La ligne de filtre est bien AVANT .sort dans la chaine
    - SectionPoids garde ses propres records (son prop records n'est pas modifie)
  </acceptance_criteria>

  <done>Quand SectionPoids est visible dans VetDogView, les records de type "weight" n'apparaissent plus dans la liste chronologique en dessous — zero doublon.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-05 — Guard user.email dans le useEffect recovery de WalkMode</name>
  <files>src/components/tracker/WalkMode.jsx</files>

  <read_first>
    - Lire src/components/tracker/WalkMode.jsx lignes 103-142 : useEffect "Recover interrupted walk from localStorage"
    - Identifier l'appel DailyLog.create ligne ~126 : owner: user?.email est utilise, mais le useEffect fire meme si user est undefined (dependances: [dog?.id])
    - Lire la signature du composant pour confirmer que user est un prop ou context value
  </read_first>

  <action>
    Dans src/components/tracker/WalkMode.jsx, le useEffect de recovery (lignes ~104-142) doit etre guarde sur user?.email.

    Deux changements :

    1. Ajouter un guard au debut de la IIFE async, juste apres le `try {` :
       ```js
       if (!user?.email) return; // Pas de recovery sans owner identifie
       ```

    2. Ajouter user?.email dans le tableau de dependances du useEffect :
       AVANT : `}, [dog?.id]);`
       APRES  : `}, [dog?.id, user?.email]);`

    Ces deux changements garantissent :
    - Le useEffect ne s'execute pas avant que l'user soit connu
    - Si l'email arrive plus tard (chargement async), le useEffect re-execute
  </action>

  <verify>
    <automated>grep -n "user?.email" src/components/tracker/WalkMode.jsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "if (!user?.email) return" src/components/tracker/WalkMode.jsx retourne 1 resultat dans le useEffect de recovery
    - grep "dog?.id, user?.email" src/components/tracker/WalkMode.jsx retourne 1 resultat (tableau de dependances)
  </acceptance_criteria>

  <done>Le useEffect de recovery de WalkMode ne cree plus de DailyLog si user.email est absent. Aucun DailyLog orphelin sans owner ne peut etre cree lors d'une recovery.</done>
</task>

<task type="auto">
  <name>Task 3: EDGE-06 — Detecter erreur JSON avant Blob PDF dans AIDiagnosisModal</name>
  <files>src/components/vet/AIDiagnosisModal.jsx</files>

  <read_first>
    - Lire src/components/vet/AIDiagnosisModal.jsx lignes 185-214 : fonction handleDownloadPDF complete
    - L'appel base44.functions.invoke("generateDiagnosisPDF") peut retourner un JSON d'erreur au lieu d'un PDF binaire
    - Ligne 200 : new Blob([res.data], ...) est appele sans verifier si res.data est un PDF ou un JSON d'erreur
  </read_first>

  <action>
    Dans src/components/vet/AIDiagnosisModal.jsx, modifier la fonction handleDownloadPDF pour detecter un JSON d'erreur avant de creer le Blob.

    Apres l'appel a base44.functions.invoke (ligne ~199), ajouter une detection d'erreur avant la creation du Blob :

    AVANT :
    ```js
    const blob = new Blob([res.data], { type: "application/pdf" });
    ```

    APRES :
    ```js
    // Detect JSON error response before creating PDF blob
    if (res.data && typeof res.data === "object" && res.data.error) {
      toast.error(res.data.message || "Erreur lors de la generation du PDF.");
      setDownloading(false);
      return;
    }
    // Guard against non-binary response (string containing JSON)
    if (typeof res.data === "string" && res.data.startsWith("{")) {
      try {
        const parsed = JSON.parse(res.data);
        if (parsed.error) {
          toast.error(parsed.message || "Erreur lors de la generation du PDF.");
          setDownloading(false);
          return;
        }
      } catch {}
    }
    const blob = new Blob([res.data], { type: "application/pdf" });
    ```

    Ne pas modifier le try/catch existant autour (il reste utile pour les erreurs reseau).
  </action>

  <verify>
    <automated>grep -n "res.data.error\|startsWith.*{" src/components/vet/AIDiagnosisModal.jsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "res.data.error" src/components/vet/AIDiagnosisModal.jsx retourne au moins 1 resultat
    - grep "startsWith.*{" src/components/vet/AIDiagnosisModal.jsx retourne 1 resultat
    - Les deux guards sont positionnes AVANT la ligne "new Blob"
    - grep "new Blob" src/components/vet/AIDiagnosisModal.jsx retourne toujours 1 resultat (le Blob est conserve, pas supprime)
  </acceptance_criteria>

  <done>AIDiagnosisModal detecte les reponses JSON d'erreur du backend avant de tenter de creer un Blob PDF. Un toast clair s'affiche au lieu d'un fichier PDF corrompu telecharge silencieusement.</done>
</task>

<task type="auto">
  <name>Task 4: EDGE-07 — Supprimer le dead code walkStreak et la prop context</name>
  <files>src/pages/Home.jsx, src/components/premium/PremiumNudgeSheet.jsx</files>

  <read_first>
    - Lire src/pages/Home.jsx lignes 321-342 : bloc useMemo walkStreak complet
    - Chercher toute utilisation de walkStreak dans Home.jsx apres la ligne 342 (grep walkStreak)
    - Lire src/components/premium/PremiumNudgeSheet.jsx ligne 38 : signature avec context: _context = "default"
    - Chercher tous les sites d'appel de PremiumNudgeSheet dans le codebase pour verifier qu'aucun ne passe context
  </read_first>

  <action>
    **Dans src/pages/Home.jsx :**
    Supprimer le bloc useMemo walkStreak entier (lignes ~321-342) :
    ```js
    // Walk streak — calculated from dailyLogs (same logic as TrackerHistory)
    const walkStreak = useMemo(() => {
      ...
    }, [dailyLogs]);
    ```
    Ce calcul n'est jamais rendu dans le JSX (confirme par grep). Supprimer aussi toute reference residuelle a walkStreak si elle existe apres le bloc.

    **Dans src/components/premium/PremiumNudgeSheet.jsx :**
    Modifier la signature du composant ligne 38 :
    AVANT : `export default function PremiumNudgeSheet({ visible, onClose, dogName, ownerGoal, context: _context = "default" }) {`
    APRES  : `export default function PremiumNudgeSheet({ visible, onClose, dogName, ownerGoal }) {`

    Verification prealable : confirmer que _context n'est jamais utilise dans le corps du composant (grep "_context" dans le fichier). Si oui, supprimer en toute securite.

    Note : Les appelants (Home.jsx, WelcomeScreen.jsx) ne passent pas la prop context donc aucun site d'appel n'est a modifier.
  </action>

  <verify>
    <automated>grep -n "walkStreak\|_context" src/pages/Home.jsx src/components/premium/PremiumNudgeSheet.jsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "walkStreak" src/pages/Home.jsx retourne 0 resultats
    - grep "_context\|context: _context" src/components/premium/PremiumNudgeSheet.jsx retourne 0 resultats
    - La signature de PremiumNudgeSheet n'a plus le parametre context
  </acceptance_criteria>

  <done>Le dead code walkStreak est supprime de Home.jsx (calcul never-rendered). La prop context non utilisee est retiree de PremiumNudgeSheet. Le codebase est plus clair et les appels existants restent compatibles.</done>
</task>

</tasks>

<verification>
Apres les 4 taches :
1. grep "filter.*sharedSections.*weight" src/pages/VetDogView.jsx — 1 resultat
2. grep "if (!user?.email) return" src/components/tracker/WalkMode.jsx — 1 resultat
3. grep "res.data.error" src/components/vet/AIDiagnosisModal.jsx — 1 resultat avant "new Blob"
4. grep "walkStreak" src/pages/Home.jsx — 0 resultats
5. grep "_context" src/components/premium/PremiumNudgeSheet.jsx — 0 resultats
</verification>

<success_criteria>
1. VetDogView : zero doublon poids quand SectionPoids est visible
2. WalkMode : DailyLog.create ne s'execute jamais sans user.email
3. AIDiagnosisModal : erreur JSON backend = toast propre, pas PDF corrompu
4. Home.jsx : walkStreak supprime, PremiumNudgeSheet : prop context supprimee
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05-edge-cases-polish/05-02-SUMMARY.md`
</output>
