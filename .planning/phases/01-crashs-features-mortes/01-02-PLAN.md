---
phase: 01-crashs-features-mortes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Scan.jsx
autonomous: true
requirements:
  - CRASH-02

must_haves:
  truths:
    - "La page Scanner se charge sans ReferenceError au rendu initial"
    - "Le mode switcher (Food / Etiquette Nutri) s'affiche correctement quand result=null"
    - "LabelScanMode peut communiquer son etat labelResult vers Scan.jsx via callback"
  artifacts:
    - path: "src/pages/Scan.jsx"
      provides: "state labelResult declare dans le composant Scan"
      contains: "const [labelResult, setLabelResult] = useState(null)"
  key_links:
    - from: "Scan.jsx (JSX lignes 351, 356, 409)"
      to: "labelResult variable"
      via: "useState declare dans Scan — plus de ReferenceError"
      pattern: "const \\[labelResult"
---

<objective>
Corriger CRASH-02 : Scan.jsx reference labelResult aux lignes 351, 356 et 409 mais cette variable n'est jamais declaree dans Scan.jsx — elle est interne a LabelScanMode (composant enfant). Cela provoque un ReferenceError a chaque rendu quand result=null et scanLimitReached=false, rendant la page Scanner inutilisable au chargement.

Purpose: La page Scanner est la deuxieme feature la plus utilisee apres le check-in. Un crash au chargement bloque 100% des utilisateurs.
Output: Scan.jsx avec `const [labelResult, setLabelResult] = useState(null)` et un callback `onLabelResult` passe a LabelScanMode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/audit/SFA-HOME-NAV.md

<interfaces>
<!-- Extrait de src/pages/Scan.jsx — lire ce fichier avant de modifier -->

Scan.jsx:114-137 — declarations de state actuelles (labelResult ABSENT) :
```jsx
export default function Scan() {
  const navigate = useNavigate();
  const [user, setUser] = useState(null);
  const [dog, setDog] = useState(null);
  const [loading, setLoading] = useState(true);
  const [mode, setMode] = useState("food"); // "food" | "label"
  const [preview, setPreview] = useState(null);
  const [file, setFile] = useState(null);
  const [scanning, setScanning] = useState(false);
  const [result, setResult] = useState(null);
  const [saved, setSaved] = useState(false);
  const [showDetails, setShowDetails] = useState(false);
  const [history, setHistory] = useState([]);
  const [showHistory, setShowHistory] = useState(false);
  const [historyFilter, setHistoryFilter] = useState("all");
  const [showShare, setShowShare] = useState(false);
  const [dogAteIt, setDogAteIt] = useState(false);
  const [scanLimitReached, setScanLimitReached] = useState(false);
  const [error, setError] = useState(null);
  const fileRef = useRef();
  const [dietPreferences, setDietPreferences] = useState(null);
```

Scan.jsx:351, 356, 409 — utilisations de labelResult (causes du crash) :
```jsx
{!result && !scanLimitReached && !labelResult && (   // ligne 351
{!result && !scanLimitReached && !labelResult && (   // ligne 356
{!result && !scanLimitReached && !isUserPremium(user) && !isInTrial && !labelResult && (  // ligne 409
```

LabelScanMode.jsx — composant enfant qui gere son propre labelResult en interne.
Il faudra lui passer un prop `onLabelResult` pour remonter l'etat.

CGC evidence :
- "labelResult" trouve dans LabelScanMode.jsx:45 (definition interne) et Scan.jsx:114 (fonction Scan mais pas comme state — c'est le nom du composant)
- Aucune declaration useState pour labelResult dans Scan.jsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declarer labelResult dans Scan.jsx et passer callback a LabelScanMode</name>
  <files>src/pages/Scan.jsx</files>
  <read_first>
    src/pages/Scan.jsx complet (lire les lignes 114-160 pour les states, 340-420 pour les utilisations, et chercher ou LabelScanMode est rendu pour y ajouter le prop)
  </read_first>
  <action>
Dans src/pages/Scan.jsx :

**Etape 1 — Ajouter la declaration de state** apres les declarations existantes (apres `const [dietPreferences, setDietPreferences] = useState(null);`) :

```jsx
// CRASH-02: labelResult leve depuis LabelScanMode pour controler le mode switcher
const [labelResult, setLabelResult] = useState(null);
```

**Etape 2 — Passer le callback a LabelScanMode** : chercher dans le JSX ou `<LabelScanMode` est rendu et ajouter le prop `onLabelResult={setLabelResult}`.

Exemple (la ligne exacte peut varier — lire le fichier pour confirmer) :
```jsx
<LabelScanMode
  dog={dog}
  user={user}
  ...props existants...
  onLabelResult={setLabelResult}
/>
```

**Etape 3 — Dans LabelScanMode.jsx**, verifier si `onLabelResult` est deja gere. S'il ne l'est pas, ajouter dans LabelScanMode un appel `onLabelResult?.(result)` quand `labelResult` change (dans l'analyse ou le reset). Ne pas casser l'API existante du composant.

Note strategique : Si LabelScanMode ne supporte pas encore onLabelResult, la solution minimale et safe est de ne PAS modifier LabelScanMode — il suffit que labelResult soit declare dans Scan.jsx pour eliminer le ReferenceError. Le state restera a null mais la page ne crashera plus. Les conditionnelles `!labelResult` seront toujours true (null est falsy) ce qui est le comportement correct par defaut.
  </action>
  <verify>
    <automated>grep -n "const \[labelResult" src/pages/Scan.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "const \[labelResult" src/pages/Scan.jsx retourne une ligne avec useState(null)
    - Les 3 occurrences de !labelResult aux lignes ~351, ~356, ~409 sont toujours presentes (elles sont maintenant valides)
    - La page Scan.jsx compile sans erreur de variable non declaree
    - LabelScanMode recoit onLabelResult si le composant peut l'utiliser, sinon la declaration seule suffit
  </acceptance_criteria>
  <done>labelResult est declare comme state dans Scan.jsx — plus de ReferenceError au rendu. La page Scanner se charge sans crash.</done>
</task>

</tasks>

<verification>
```bash
# Verifier la declaration
grep -n "const \[labelResult" src/pages/Scan.jsx

# Verifier que les 3 usages sont toujours presents (maintenant valides)
grep -n "labelResult" src/pages/Scan.jsx

# Verifier qu'aucune import n'a ete casse
grep -n "^import" src/pages/Scan.jsx | head -15
```
</verification>

<success_criteria>
- `const [labelResult, setLabelResult] = useState(null)` est present dans Scan.jsx
- Les 3 conditionnelles `!labelResult` restent presentes et sont maintenant syntaxiquement valides
- Scan.jsx s'ouvre sans ReferenceError
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-crashs-features-mortes/01-02-SUMMARY.md`
</output>
