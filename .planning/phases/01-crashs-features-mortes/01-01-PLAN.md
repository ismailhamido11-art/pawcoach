---
phase: 01-crashs-features-mortes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
autonomous: true
requirements:
  - CRASH-01

must_haves:
  truths:
    - "Taper une humeur dans DailyBriefing soumet bien un check-in au backend (pas de return silencieux)"
    - "Le toast 'Check-in enregistré !' apparait apres un tap humeur"
    - "Le state todayCheckin est mis a jour apres un quick check-in"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "handleQuickCheckin avec defaults energy et appetite"
      contains: "energy ?? 2"
  key_links:
    - from: "DailyBriefing.jsx:107 (onQuickCheckin({ mood }))"
      to: "Home.jsx:handleCheckin"
      via: "handleQuickCheckin avec energy=2, appetite=2 par defaut"
      pattern: "energy.*??.*2"
---

<objective>
Corriger CRASH-01 : le quick check-in (tap humeur dans DailyBriefing) ne soumet rien au backend parce que handleQuickCheckin passe energy et appetite comme undefined, ce qui declenche le guard `!energy || !appetite` dans handleCheckin.

Purpose: C'est l'action principale de la Home — chaque visite commence par un check-in humeur. Le fait que ca ne fasse rien du tout (silencieusement) casse l'experience de base.
Output: Home.jsx avec handleQuickCheckin qui passe des valeurs par defaut (energy=2, appetite=2, notes="", symptoms=[], behaviorNotes="").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/audit/SFA-HOME-NAV.md

<interfaces>
<!-- Extrait de src/pages/Home.jsx — lire ce fichier avant de modifier -->

Home.jsx:271-272 — le guard bloquant :
```jsx
const handleCheckin = async ({ mood, energy, appetite, notes, symptoms, behaviorNotes }) => {
  if (!mood || !energy || !appetite || submitting) return;
```

Home.jsx:446-449 — handleQuickCheckin actuel (CASSÉ) :
```jsx
const handleQuickCheckin = async ({ mood, energy, appetite }) => {
  if (submitting) return;
  handleCheckin({ mood, energy, appetite, notes: "", symptoms: [], behaviorNotes: "" });
};
```

DailyBriefing.jsx:107 — appel du parent (ne peut pas etre modifie ici) :
```jsx
onQuickCheckin({ mood })   // energy et appetite sont undefined
```

Valeur attendue pour un quick check-in : energy=2 (milieu de l'echelle 1-5), appetite=2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger handleQuickCheckin — defaults energy et appetite</name>
  <files>src/pages/Home.jsx</files>
  <read_first>src/pages/Home.jsx lignes 446-449</read_first>
  <action>
Dans src/pages/Home.jsx, modifier handleQuickCheckin (actuellement ligne 446) pour appliquer des valeurs par defaut si energy ou appetite sont undefined.

Remplacer :
```jsx
const handleQuickCheckin = async ({ mood, energy, appetite }) => {
  if (submitting) return;
  handleCheckin({ mood, energy, appetite, notes: "", symptoms: [], behaviorNotes: "" });
};
```

Par :
```jsx
const handleQuickCheckin = async ({ mood, energy, appetite }) => {
  if (submitting) return;
  // CRASH-01: DailyBriefing ne passe que mood — on applique des valeurs neutres par defaut
  handleCheckin({ mood, energy: energy ?? 2, appetite: appetite ?? 2, notes: "", symptoms: [], behaviorNotes: "" });
};
```

Aucun autre changement dans ce fichier. Le guard dans handleCheckin (ligne 272) reste intact — avec energy=2 et appetite=2, il passera correctement.
  </action>
  <verify>
    <automated>grep -n "energy ?? 2" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "energy ?? 2" src/pages/Home.jsx retourne la ligne dans handleQuickCheckin
    - grep "appetite ?? 2" src/pages/Home.jsx retourne la ligne dans handleQuickCheckin
    - handleCheckin garde son guard intact (grep "!mood || !energy || !appetite" retourne toujours la ligne 272)
    - Aucune autre modification dans Home.jsx
  </acceptance_criteria>
  <done>handleQuickCheckin passe energy=2 et appetite=2 quand DailyBriefing n'envoie que mood — le check-in atteint le backend.</done>
</task>

</tasks>

<verification>
```bash
# Verifier le fix
grep -n "energy ?? 2" src/pages/Home.jsx
grep -n "appetite ?? 2" src/pages/Home.jsx

# Verifier que le guard est intact
grep -n "!mood || !energy || !appetite" src/pages/Home.jsx

# Verifier qu'aucune regression import n'a ete introduite
grep -n "^import" src/pages/Home.jsx | head -20
```
</verification>

<success_criteria>
- handleQuickCheckin contient `energy ?? 2` et `appetite ?? 2`
- Le guard dans handleCheckin est inchange (ligne ~272)
- Un tap humeur dans DailyBriefing peut maintenant atteindre `base44.functions.invoke("dailyCheckinProcess", ...)`
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-crashs-features-mortes/01-01-SUMMARY.md`
</output>
