---
phase: 01-crashs-features-mortes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
autonomous: true
requirements:
  - CRASH-04

must_haves:
  truths:
    - "Le bouton FAB (+) est visible sur la page Home"
    - "Ouvrir le FAB permet de logger poids, eau, balade ou une note"
    - "Apres un log, le cache Home est invalide (DailyProgress se rafraichit au prochain mount)"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "CombinedFAB monte avec dog, user, et onLogSaved"
      contains: "CombinedFAB"
  key_links:
    - from: "Home.jsx (render)"
      to: "CombinedFAB"
      via: "import + rendu avec onLogSaved={invalidateHome}"
      pattern: "import CombinedFAB|<CombinedFAB"
---

<objective>
Corriger CRASH-04 : CombinedFAB.jsx est defini mais jamais importe ni monte dans aucune page (CGC confirme : 2 occurrences seulement, toutes dans sa propre definition). Le FAB de log rapide (poids, eau, balade) est completement invisible pour l'utilisateur.

Purpose: CombinedFAB est la seule facon de logger rapidement poids/eau/balade depuis la Home. Sans lui, ces donnees ne peuvent etre entrees que depuis des pages specifiques — friction inutile pour l'action quotidienne.
Output: Home.jsx avec CombinedFAB importe et rendu. Le callback onLogSaved appelle invalidateHome() pour que DailyProgress se rafraichisse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/audit/SFA-ACTIVITE.md

<interfaces>
<!-- Extrait de src/pages/Home.jsx — lire ce fichier avant de modifier -->

Home.jsx:1-14 — imports actuels (CombinedFAB ABSENT) :
```jsx
import { useEffect, useState, useMemo, useRef } from "react";
import { useNavigate } from "react-router-dom";
import { createPageUrl, getActiveDog } from "@/utils";
import { base44 } from "@/api/base44Client";
import { Dog, DailyCheckin, Streak, HealthRecord, UserProgress, FoodScan, DailyLog, DiagnosisReport, NutritionPlan, Bookmark, WeeklyInsight } from "@/api/entities";
import { isUserPremium } from "@/utils/premium";
import { useHomeCache } from "@/lib/HomeCacheContext";
import { useAuth } from "@/lib/AuthContext";
import BottomNav from "../components/BottomNav";
import PullToRefresh from "../components/PullToRefresh";
...
import ChatFAB from "../components/ChatFAB";
```

Home.jsx:671-693 — fin du render (position pour inserer CombinedFAB) :
```jsx
        <ChatFAB offsetBottom={4.5} />
      </PullToRefresh>
      <BottomNav currentPage="Home" />

      <PremiumNudgeSheet ... />
      <PostTrialSheet ... />
    </div>
  );
}
```

CombinedFAB.jsx:17 — signature du composant :
```jsx
export default function CombinedFAB({ dog, user, onLogSaved })
```

useHomeCache depuis HomeCacheContext expose `invalidateHome` — deja importe et destructure dans Home.jsx :
```jsx
const { getCachedHome, setCachedHome, invalidateHome } = useHomeCache();
```

ChatFAB.jsx est l'exemple de reference pour placer un FAB dans le meme render — CombinedFAB doit etre place AVANT ChatFAB (ChatFAB prend la position basse, CombinedFAB prend une position intermediaire pour ne pas se superposer).

CGC evidence : CombinedFAB existe dans src/components/CombinedFAB.jsx — jamais importe ailleurs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Importer et monter CombinedFAB dans Home.jsx</name>
  <files>src/pages/Home.jsx</files>
  <read_first>
    src/pages/Home.jsx — lire : lignes 1-30 (imports), lignes 670-693 (fin du render pour localiser ChatFAB et BottomNav), et verifier que `invalidateHome` est bien destructure depuis useHomeCache
  </read_first>
  <action>
**Etape 1 — Ajouter l'import** : dans le bloc des imports de composants (pres de ChatFAB), ajouter :
```jsx
import CombinedFAB from "../components/CombinedFAB";
```

**Etape 2 — Monter le composant** : dans le return de Home, ajouter `<CombinedFAB>` juste avant `<ChatFAB>`. ChatFAB est a `offsetBottom={4.5}` (rem) — CombinedFAB doit etre place a une position qui ne se superpose pas.

Position exacte dans le JSX (apres le bloc PullToRefresh, avant BottomNav) :
```jsx
        <ChatFAB offsetBottom={4.5} />
      </PullToRefresh>
      <BottomNav currentPage="Home" />

      {/* CRASH-04: Log rapide FAB — poids, eau, balade */}
      <CombinedFAB
        dog={dog}
        user={user}
        onLogSaved={invalidateHome}
      />
```

Placer CombinedFAB apres `</PullToRefresh>` et avant les sheets (`<PremiumNudgeSheet>`, `<PostTrialSheet>`). CombinedFAB gere son propre positionnement en fixed — pas besoin de wrapper.

**Verifier** que `invalidateHome` est bien destructure dans le composant (chercher `const { getCachedHome, setCachedHome, invalidateHome } = useHomeCache();`). Si oui, le passer directement. Si non, l'ajouter a la destructuration.

**Note** : `dog` et `user` sont les states locaux de Home.jsx — ils sont disponibles au moment du rendu.
  </action>
  <verify>
    <automated>grep -n "CombinedFAB" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "CombinedFAB" src/pages/Home.jsx retourne au moins 2 lignes (import + usage JSX)
    - grep "onLogSaved={invalidateHome}" src/pages/Home.jsx retourne une ligne
    - grep "import CombinedFAB" src/pages/Home.jsx retourne une ligne avec le bon chemin
    - ChatFAB est toujours present (pas supprime par erreur)
  </acceptance_criteria>
  <done>CombinedFAB est monte sur Home — le bouton FAB (+) est visible. Apres un log, invalidateHome() est appele.</done>
</task>

</tasks>

<verification>
```bash
# Verifier import et usage
grep -n "CombinedFAB" src/pages/Home.jsx

# Verifier le callback
grep -n "onLogSaved" src/pages/Home.jsx

# Verifier que ChatFAB est toujours la
grep -n "ChatFAB" src/pages/Home.jsx

# Verifier que invalidateHome est disponible
grep -n "invalidateHome" src/pages/Home.jsx
```
</verification>

<success_criteria>
- `import CombinedFAB` present dans Home.jsx
- `<CombinedFAB dog={dog} user={user} onLogSaved={invalidateHome}` present dans le render
- ChatFAB toujours present et non modifie
- invalidateHome passe bien comme callback (cache invalide apres chaque log)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-crashs-features-mortes/01-04-SUMMARY.md`
</output>
