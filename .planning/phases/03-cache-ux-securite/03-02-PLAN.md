---
phase: 03-cache-ux-securite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Profile.jsx
  - src/pages/DogPublicProfile.jsx
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "La card Passe a Premium est invisible pour un utilisateur deja abonne"
    - "L'email du proprietaire n'est pas visible sur DogPublicProfile"
    - "Une erreur lors de la sauvegarde du profil utilisateur affiche un message d'erreur"
  artifacts:
    - path: "src/pages/Profile.jsx"
      provides: "Premium card conditionally hidden + handleSaveUser try/catch"
      contains: "isUserPremium"
    - path: "src/pages/DogPublicProfile.jsx"
      provides: "Email removed from public profile"
  key_links:
    - from: "src/pages/Profile.jsx"
      to: "src/utils/premium.js"
      via: "isUserPremium import (already present)"
      pattern: "isUserPremium"
---

<objective>
Corriger 3 failles UX/securite dans Profile et DogPublicProfile.

Purpose: Un utilisateur premium ne doit pas voir la card upsell. L'email du proprietaire ne doit pas etre expose publiquement. Les erreurs de sauvegarde doivent etre communiquees.
Output: Profile.jsx avec guard premium + try/catch handleSaveUser. DogPublicProfile.jsx sans email visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- isUserPremium — already imported in Profile.jsx (line 5) -->
From src/utils/premium.js:
```javascript
export function isUserPremium(user) { /* returns boolean */ }
```

<!-- toast — already imported in Profile.jsx -->
From sonner:
```javascript
import { toast } from "sonner";
// toast.error("message") — shows error toast
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile.jsx — masquer card Premium pour abonnes + try/catch handleSaveUser (UX-01 + UX-03)</name>
  <files>src/pages/Profile.jsx</files>
  <read_first>src/pages/Profile.jsx, src/utils/premium.js</read_first>
  <action>
**UX-01 — Masquer card "Passe a Premium" pour utilisateurs premium:**

Dans Profile.jsx, vers lignes 153-162, il y a un bloc :
```jsx
{/* Premium card */}
<motion.div custom={1} variants={sectionVariants} initial="hidden" animate="visible">
  <div className="bg-gradient-to-r from-amber-50 ..." onClick={() => navigate(...)}>
    ...
    <p className="text-[14px] font-bold text-foreground">Passe a Premium</p>
    ...
  </div>
</motion.div>
```

Entourer ce bloc COMPLET (motion.div + son contenu) avec une condition :
```jsx
{!isUserPremium(user) && (
  <motion.div custom={1} variants={sectionVariants} initial="hidden" animate="visible">
    <div className="bg-gradient-to-r from-amber-50 to-yellow-50/50 rounded-3xl p-4 border border-amber-100/50 shadow-sm flex items-center gap-4 cursor-pointer active:scale-[0.98] transition-transform" onClick={() => navigate(createPageUrl("Premium") + "?from=profile")}>
      <StorysetIllustration name="premium" className="w-24 h-24 flex-shrink-0" />
      <div className="flex-1 min-w-0">
        <p className="text-[14px] font-bold text-foreground">Passe a Premium</p>
        <p className="text-[11px] text-muted-foreground mt-1 leading-relaxed">Acces illimite a toutes les fonctionnalites</p>
      </div>
    </div>
  </motion.div>
)}
```

`isUserPremium` est deja importe ligne 5 : `import { isUserPremium } from "@/utils/premium";`. Pas besoin d'ajouter d'import.

**UX-03 — handleSaveUser try/catch avec feedback erreur:**

Dans Profile.jsx, vers ligne 100-103, le code actuel est :
```javascript
const handleSaveUser = async (updates) => {
  await base44.auth.updateMe(updates);
  setUser(prev => ({ ...prev, ...updates }));
};
```

Remplacer par :
```javascript
const handleSaveUser = async (updates) => {
  try {
    await base44.auth.updateMe(updates);
    setUser(prev => ({ ...prev, ...updates }));
  } catch (e) {
    console.error("Profile handleSaveUser error:", e);
    toast.error("Impossible de sauvegarder. Verifie ta connexion et reessaie.");
  }
};
```

`toast` est deja importe ligne 11 : `import { toast } from "sonner";`. Pas besoin d'ajouter d'import.
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "isUserPremium" src/pages/Profile.jsx && grep -n "try" src/pages/Profile.jsx && grep -n "toast.error" src/pages/Profile.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "!isUserPremium(user)" src/pages/Profile.jsx retourne au moins 1 ligne (le guard sur la card premium)
    - grep "try {" dans handleSaveUser existe (autour de la ligne 100-106)
    - grep "toast.error" src/pages/Profile.jsx retourne au moins 1 ligne dans handleSaveUser
    - Le catch dans handleSaveUser contient console.error + toast.error
    - La card "Passe a Premium" est INSIDE le block {!isUserPremium(user) && (...)}
  </acceptance_criteria>
  <done>Un utilisateur premium ne voit plus la card "Passe a Premium" dans Profile. Une erreur de sauvegarde affiche un toast d'erreur au lieu d'echouer silencieusement.</done>
</task>

<task type="auto">
  <name>Task 2: DogPublicProfile.jsx — retirer l'email du proprietaire (UX-02)</name>
  <files>src/pages/DogPublicProfile.jsx</files>
  <read_first>src/pages/DogPublicProfile.jsx</read_first>
  <action>
**UX-02 — Email proprietaire non expose sur DogPublicProfile:**

Dans DogPublicProfile.jsx, vers lignes 237-249, il y a un bloc qui affiche l'email du proprietaire :
```jsx
{/* Contacter le proprietaire */}
{dog.owner && (
  <div className="bg-emerald-50 border border-emerald-200 rounded-2xl p-4">
    <h2 className="text-xs font-black text-emerald-700 uppercase tracking-widest mb-2">Contacter le proprietaire</h2>
    <a
      href={`mailto:${dog.owner}`}
      className="flex items-center gap-2 text-emerald-700 hover:text-emerald-800 transition-colors"
    >
      <Mail className="w-4 h-4 flex-shrink-0" />
      <span className="text-sm font-semibold">Envoyer un message au proprietaire</span>
    </a>
  </div>
)}
```

Ce bloc expose `dog.owner` (l'email) dans un lien `mailto:`. C'est une faille de securite sur une page PUBLIQUE (pas de login requis).

**Option retenue:** Supprimer ENTIEREMENT le bloc "Contacter le proprietaire" (lignes 237-249). Un profil public de chien ne doit pas exposer l'email du proprietaire.

Supprimer aussi l'import `Mail` de lucide-react s'il n'est plus utilise ailleurs dans le fichier. Verifier avec grep dans DogPublicProfile.jsx : `Mail` est utilise UNIQUEMENT dans ce bloc (lignes 5 et 245). Retirer `Mail` de la destructuration d'import lucide-react (ligne 5).

L'import lucide-react (ligne 5) passe de :
```javascript
import { Syringe, Weight, AlertTriangle, Calendar, MapPin, PawPrint, ShieldCheck, FileText, Mail, Fingerprint, Stethoscope, Pill } from "lucide-react";
```
a :
```javascript
import { Syringe, Weight, AlertTriangle, Calendar, MapPin, PawPrint, ShieldCheck, FileText, Fingerprint, Stethoscope, Pill } from "lucide-react";
```
(Mail retire)
  </action>
  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "mailto\|dog\.owner\|Mail" src/pages/DogPublicProfile.jsx; echo "--- Expected: 0 lines ---"</automated>
  </verify>
  <acceptance_criteria>
    - grep "mailto" src/pages/DogPublicProfile.jsx retourne 0 lignes
    - grep "dog.owner" src/pages/DogPublicProfile.jsx retourne 0 lignes
    - grep "Mail" src/pages/DogPublicProfile.jsx retourne 0 lignes
    - Le bloc "Contacter le proprietaire" est entierement supprime
    - L'import Mail est retire de lucide-react
  </acceptance_criteria>
  <done>L'email du proprietaire n'est plus visible ni accessible sur la page publique DogPublicProfile. Zero fuite de donnees personnelles.</done>
</task>

</tasks>

<verification>
1. `grep -c "isUserPremium" src/pages/Profile.jsx` — doit etre >= 4 (import + handleAddDog x2 + premium card guard)
2. `grep -c "toast.error" src/pages/Profile.jsx` — doit etre >= 1 (handleSaveUser)
3. `grep -c "mailto\|dog\.owner" src/pages/DogPublicProfile.jsx` — doit etre 0
4. `grep -c "Mail" src/pages/DogPublicProfile.jsx` — doit etre 0
</verification>

<success_criteria>
- UX-01: La card "Passe a Premium" est conditionnee par `!isUserPremium(user)` dans Profile.jsx
- UX-02: Le bloc email proprietaire est entierement supprime de DogPublicProfile.jsx
- UX-03: handleSaveUser est entoure d'un try/catch avec toast.error
</success_criteria>

<output>
After completion, create `.planning/phases/03-cache-ux-securite/03-02-SUMMARY.md`
</output>
