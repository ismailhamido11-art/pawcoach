---
phase: 08-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/training/VideoCoaching.jsx
  - src/components/activite/AITrainingProgram.jsx
  - src/components/home/ActiveProgramCards.jsx
autonomous: true
requirements:
  - CONS-01

must_haves:
  truths:
    - "Tous les boutons CTA primaires affichent le dégradé vert PawCoach (gradient-primary), pas du violet ni du bleu"
    - "Aucun bouton d'action principale n'utilise bg-purple-6xx, bg-blue-5xx, bg-blue-6xx, ou bg-blue-7xx"
    - "Le comportement fonctionnel des boutons est inchangé (même onClick, même disabled, même texte)"
  artifacts:
    - path: "src/components/training/VideoCoaching.jsx"
      provides: "Boutons CTA VideoCoaching harmonisés"
      contains: "gradient-primary"
    - path: "src/components/activite/AITrainingProgram.jsx"
      provides: "Boutons CTA AITrainingProgram harmonisés"
      contains: "gradient-primary"
    - path: "src/components/home/ActiveProgramCards.jsx"
      provides: "Bouton CTA ActiveProgramCards harmonisé"
      contains: "gradient-primary"
  key_links:
    - from: "src/components/training/VideoCoaching.jsx"
      to: "gradient-primary CSS class"
      via: "className prop"
      pattern: "gradient-primary"
    - from: "src/components/activite/AITrainingProgram.jsx"
      to: "gradient-primary CSS class"
      via: "className prop"
      pattern: "gradient-primary"
---

<objective>
Harmoniser les boutons CTA primaires dans 3 composants qui utilisent des couleurs non-standard (purple, blue) au lieu du pattern `gradient-primary` de PawCoach.

Purpose: CONS-01 — Un utilisateur qui navigue entre pages ne doit pas voir des boutons principaux violets sur Training/Video, bleus sur AITraining, et verts sur les autres pages. Incohérence visuelle qui donne l'impression d'une app non finie.
Output: 3 composants modifiés, tous les boutons CTA primaires en gradient-primary.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Pattern cible:** `gradient-primary` est défini dans `src/index.css` ligne 180 — c'est le dégradé vert PawCoach. Les boutons secondaires (annuler, outline, saved-state) gardent leur style actuel.

**Règle de décision:** Un bouton est "primaire" si c'est l'action principale de la section (lancer une vidéo, enregistrer un bilan, démarrer un programme). Les toggles, options sélectionnables, et badges de catégorie ne sont PAS des boutons primaires.

**Ne pas modifier:**
- Les checkbox items et toggles sélectionnables dans AITrainingProgram (bg-blue-600 sur checked state = sélecteur, pas CTA)
- Les stat boxes colorées (bg-amber-50, bg-blue-50 = catégories contextuelles)
- Les états "saved" confirmés (bg-green-50 = feedback de confirmation)
- Les éléments shadcn/ui dans src/components/ui/ (ne jamais toucher)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmoniser VideoCoaching.jsx — boutons CTA violet</name>
  <files>src/components/training/VideoCoaching.jsx</files>
  <action>
Lire VideoCoaching.jsx en entier. Identifier les 3 occurrences de bg-purple-600/hover:bg-purple-700 sur des boutons CTA primaires :
- Ligne ~108 : bouton "Lancer la vidéo" (h-14 rounded-2xl) — remplacer `bg-purple-600 hover:bg-purple-700 text-white` par `gradient-primary border-0 text-white`
- Ligne ~129 : bouton secondaire dans le viewer — même remplacement
- Ligne ~167 : bouton saved-state conditionnel — le côté `bg-purple-600 hover:bg-purple-700 text-white` (quand !saved) doit devenir `gradient-primary border-0 text-white`

Le state saved (bg-green-50 text-green-700 border border-green-200) reste inchangé — c'est un feedback de confirmation, pas un CTA.

Vérifier après modification : grep "bg-purple" VideoCoaching.jsx doit retourner 0 résultat.
  </action>
  <verify>
    <automated>grep -n "bg-purple-" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/training/VideoCoaching.jsx | wc -l</automated>
  </verify>
  <done>0 occurrence de bg-purple- dans VideoCoaching.jsx, boutons CTA affichent gradient-primary</done>
</task>

<task type="auto">
  <name>Task 2: Harmoniser AITrainingProgram.jsx — boutons CTA bleu/violet</name>
  <files>src/components/activite/AITrainingProgram.jsx</files>
  <action>
Lire AITrainingProgram.jsx en entier. Ce fichier est long (~900 lignes). Identifier précisément les boutons CTA primaires à modifier (pas les sélecteurs) :

**CTA primaires à modifier :**
- Ligne ~360 : `<Button onClick={onSaveBilan} className="w-full bg-blue-600 hover:bg-blue-700 text-white" size="sm">` — remplacer par `className="w-full gradient-primary border-0 text-white"`

**Sélecteurs/toggles à NE PAS modifier** (ce sont des options que l'utilisateur sélectionne, leur couleur active a du sens) :
- Ligne ~288 : `checked ? "bg-blue-600 border-blue-600" : "border-border/60"` — c'est une checkbox item, garder
- Ligne ~348 : `selected ? "bg-blue-600 text-white" : ...` — c'est un sélecteur d'option, garder
- Ligne ~819 : `selected ? "bg-purple-600 text-white" : ...` — sélecteur de difficulté, garder

**Stat boxes à NE PAS modifier** (couleurs catégorielles) :
- Les `bg-blue-50 rounded-xl`, `bg-purple-50 rounded-xl` etc. dans les zones de résumé — garder

Vérifier : seule la ligne onSaveBilan est modifiée.
  </action>
  <verify>
    <automated>grep -n "bg-blue-600\|bg-blue-700" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/activite/AITrainingProgram.jsx</automated>
  </verify>
  <done>La ligne onSaveBilan utilise gradient-primary. Les sélecteurs bg-blue-600 (checked state) sont intacts. Résultat grep = seulement les lignes sélecteur (not bg-blue-600 hover:bg-blue-700 seul).</done>
</task>

<task type="auto">
  <name>Task 3: Harmoniser ActiveProgramCards.jsx — bouton CTA bleu</name>
  <files>src/components/home/ActiveProgramCards.jsx</files>
  <action>
Lire ActiveProgramCards.jsx. Identifier la ligne ~514 :
`className="w-full mt-2 py-2 rounded-xl bg-blue-500 text-white text-xs font-bold hover:bg-blue-600 disabled:opacity-50 transition-colors"`

Remplacer `bg-blue-500 ... hover:bg-blue-600` par `gradient-primary border-0`. Conserver `disabled:opacity-50 transition-colors` et les autres classes.

Vérifier : grep "bg-blue-500\|bg-blue-600" dans ActiveProgramCards.jsx doit retourner 0 résultat.
  </action>
  <verify>
    <automated>grep -n "bg-blue-500\|bg-blue-600" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/components/home/ActiveProgramCards.jsx | wc -l</automated>
  </verify>
  <done>0 occurrence de bg-blue-500/blue-600 dans ActiveProgramCards.jsx</done>
</task>

</tasks>

<verification>
Après les 3 tâches :
1. grep -rn "bg-purple-6\|bg-blue-5\|bg-blue-6\|bg-blue-7" src/components/training/VideoCoaching.jsx src/components/activite/AITrainingProgram.jsx src/components/home/ActiveProgramCards.jsx — doit retourner uniquement les sélecteurs/checked states (pas de boutons CTA solitaires)
2. Vite build passe sans erreur : `cd /c/Users/smalt/Desktop/app-chien-ia/pawcoach && npm run build 2>&1 | tail -5`
</verification>

<success_criteria>
- Tous les boutons CTA principaux dans VideoCoaching, AITrainingProgram, ActiveProgramCards affichent gradient-primary
- Les sélecteurs interactifs (checked state, selected state) conservent leur couleur catégorielle
- Build Vite passe sans erreur
- CONS-01 satisfait
</success_criteria>

<output>
Après completion, créer `.planning/phases/08-consistency/08-01-SUMMARY.md`
</output>
