---
phase: 08-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Premium.jsx
  - src/pages/VetDogView.jsx
  - src/pages/Sante.jsx
  - src/pages/Scan.jsx
  - src/pages/Training.jsx
  - src/pages/Profile.jsx
autonomous: true
requirements:
  - CONS-02
  - CONS-03

must_haves:
  truths:
    - "Les content cards autonomes (conteneurs de section) utilisent rounded-2xl partout"
    - "Les list items incrustés dans des sections gardent rounded-xl (hiérarchie visuelle intentionnelle)"
    - "Les marges latérales de page suivent le pattern par page : px-4 ou px-5 selon densité, sans mélange incohérent dans une même section"
    - "Les gaps entre sections au même niveau sont uniformes (space-y-4 entre sections, space-y-3 dans une section)"
  artifacts:
    - path: "src/pages/Premium.jsx"
      provides: "Warning banner avec rounded-2xl"
      contains: "rounded-2xl"
    - path: "src/pages/VetDogView.jsx"
      provides: "List items vétérinaires cohérents avec le reste de l'app"
  key_links:
    - from: "Toutes les pages auditées"
      to: "Pattern rounded-2xl pour content cards"
      via: "grep -rn"
      pattern: "rounded-2xl"
---

<objective>
Auditer les 18 pages et les composants principaux pour identifier les vraies incohérences de cards et d'espacements, puis corriger les cas réels.

Purpose: CONS-02 + CONS-03 — Harmoniser les rares cas où une content card autonome utilise rounded-xl au lieu de rounded-2xl, et s'assurer que les espacements entre sections majeures sont uniformes.
Output: Pages auditées, inconsistances corrigées, pattern documenté dans le SUMMARY.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Règle card (déjà établie dans le codebase) :**
- Content card autonome (conteneur de section principal) = `rounded-2xl` + `border border-border` + `p-4` ou `p-5`
- List item dans une section = `rounded-xl` (intentionnel — hiérarchie visuelle)
- Icon wrapper / badge / chip = `rounded-xl` ou `rounded-lg` (correct)
- Input / textarea = `rounded-xl` (correct — shadcn standard)

**Règle espacement (déjà établie) :**
- Entre sections majeures d'une page = `space-y-4` ou `gap-4`
- Dans une section dense = `space-y-3` ou `gap-3`
- Marge latérale page header = `px-5`
- Marge latérale body = `px-4` ou `px-5` selon densité (cohérent par page)

**Fichiers à NE PAS toucher :** `src/components/ui/` (shadcn), et tout fichier non listé dans files_modified.

**Cas confirmé à corriger :**
- `Premium.jsx:422` : `bg-amber-50 border border-amber-300 rounded-xl` → `rounded-2xl` (c'est une notification banner autonome, pas un list item)
- `VetDogView.jsx:102,123,152` : `p-3 rounded-xl bg-white border border-border` → audit si ces items ressemblent aux items similaires dans Dashboard.jsx (rounded-2xl) ou DiagnosisContent.jsx (rounded-xl). Dashboard utilise rounded-2xl pour ses items de liste → uniformiser VetDogView en rounded-2xl pour les record/checkin/scan items (ils ont la même taille et importance)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit systématique et identification des incohérences</name>
  <files>src/pages/Premium.jsx, src/pages/VetDogView.jsx</files>
  <action>
**Étape 1 — Audit rapide de toutes les pages :**
Lire les fichiers suivants et noter tout `rounded-xl` sur un conteneur avec `bg-white` ou `bg-*-50` de taille > 100px (p-4 ou plus) qui n'est pas un list item, icon wrapper, ou input :
- Scan les 18 pages : `grep -rn "rounded-xl" src/pages/ --include="*.jsx" | grep "bg-white\|bg-amber-50\|bg-red-50\|bg-emerald-50\|bg-blue-50"` (exclure les lignes avec `w-[0-9]` ou `h-[0-9]`)

**Étape 2 — Corriger Premium.jsx:422 :**
Lire Premium.jsx. Ligne ~422 : `<div className="bg-amber-50 border border-amber-300 rounded-xl px-4 py-2.5 text-center">`
Remplacer `rounded-xl` par `rounded-2xl`.

**Étape 3 — Corriger VetDogView.jsx :102, 123, 152 :**
Lire VetDogView.jsx. Les items record/checkin/scan sont des cards de taille moyenne (p-3) avec du contenu significatif. Comparer avec Dashboard.jsx qui utilise `rounded-2xl` pour des items similaires.
- Si les items ont plus de 2 lignes de contenu = content card → `rounded-2xl`
- Si les items sont de simples one-liners = list item → garder `rounded-xl`
Lire les 3 sections (records, checkins, scans) et appliquer le bon rounding selon le contenu réel.

**Étape 4 — Audit espacements :**
Pour chaque page listée, vérifier que les sections principales ont des `space-y-4` et pas de `space-y-6` isolé dans le body (section trop espacée par rapport au reste). Si trouvé, corriger.
  </action>
  <verify>
    <automated>grep -rn "rounded-xl" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/ --include="*.jsx" | grep -E "bg-(white|amber-50|red-50|emerald-50|blue-50)" | grep -v "w-[0-9]|h-[0-9]|rounded-full"</automated>
  </verify>
  <done>Premium.jsx:422 utilise rounded-2xl. VetDogView list items corrigés. Grep retourne uniquement des list items légitimes ou inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Audit et corrections espacements dans Sante, Scan, Training, Profile</name>
  <files>src/pages/Sante.jsx, src/pages/Scan.jsx, src/pages/Training.jsx, src/pages/Profile.jsx</files>
  <action>
Lire chaque page. Pour chaque page, vérifier :

**Pattern attendu dans le body (après le header gradient-primary) :**
- Le conteneur body principal doit avoir `px-4` ou `px-5` (cohérent dans toute la page)
- Les sections majeures : `space-y-4` entre elles
- Les sous-items dans une section : `space-y-3`
- Pas de `space-y-6` ou `space-y-8` dans le body content (trop espacé)
- Pas de `gap-6` ou `gap-8` entre cards au même niveau (trop espacé)

Pour chaque fichier :
1. Lire le fichier
2. Identifier les sections du body avec leurs espacements actuels
3. Si un espacement sort du pattern (`space-y-5` ou plus dans une liste de cards) → corriger vers `space-y-4`
4. Si px-4 et px-5 se mélangent DANS LA MÊME section (pas entre header et body) → uniformiser

Si tout est déjà conforme pour une page, noter "conforme" et passer à la suivante.
  </action>
  <verify>
    <automated>grep -n "space-y-6\|space-y-8\|gap-6\|gap-8" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Sante.jsx /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Scan.jsx /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Training.jsx /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Profile.jsx 2>/dev/null</automated>
  </verify>
  <done>Les pages Sante, Scan, Training, Profile n'ont pas de space-y-6/8 ou gap-6/8 aberrant dans leur body content. Toute incohérence trouvée est corrigée.</done>
</task>

</tasks>

<verification>
Après les 2 tâches :
1. `grep -n "rounded-xl" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/Premium.jsx` — doit ne plus montrer la ligne 422 avec rounded-xl pour la notification banner
2. `npm run build 2>&1 | tail -5` depuis le répertoire pawcoach — doit passer sans erreur
3. `grep -rn "space-y-6\|space-y-8" /c/Users/smalt/Desktop/app-chien-ia/pawcoach/src/pages/ --include="*.jsx" | grep -v "header\|hero\|gradient-primary"` — doit retourner peu ou pas de résultats dans le body content
</verification>

<success_criteria>
- Premium.jsx warning banner utilise rounded-2xl (CONS-02 satisfait)
- VetDogView items corrigés selon leur contenu réel
- Espacements dans Sante, Scan, Training, Profile conformes (space-y-3/4 dans body, pas de space-y-6/8 aberrant)
- Build Vite passe sans erreur
- CONS-02 + CONS-03 satisfaits
</success_criteria>

<output>
Après completion, créer `.planning/phases/08-consistency/08-02-SUMMARY.md`
</output>
