---
phase: 01-backend-critique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - base44/functions/finalDiagnosis/entry.ts
  - base44/functions/preDiagnosis/entry.ts
  - base44/functions/streakReminder/entry.ts
autonomous: true
requirements:
  - TECH-01
  - TECH-02
must_haves:
  truths:
    - "Un free user qui appelle finalDiagnosis directement (sans passer par preDiagnosis) recoit une 429 s'il a epuise son quota"
    - "La fonction streakReminder ne charge plus toute la table Streak — elle limite via un filtre avant tout traitement"
  artifacts:
    - path: "base44/functions/finalDiagnosis/entry.ts"
      provides: "Quota guard avec decrement propre — ne se repose plus sur preDiagnosis"
      contains: "actions_remaining"
    - path: "base44/functions/preDiagnosis/entry.ts"
      provides: "Token court-terme signe dans la reponse"
      contains: "pre_diagnosis_token"
    - path: "base44/functions/streakReminder/entry.ts"
      provides: "Pas de Streak.list() global — filtre par current_streak >= 3 avant de charger"
      contains: "Streak.filter"
  key_links:
    - from: "base44/functions/preDiagnosis/entry.ts"
      to: "base44/functions/finalDiagnosis/entry.ts"
      via: "pre_diagnosis_token dans la reponse JSON, verifie dans finalDiagnosis"
      pattern: "pre_diagnosis_token"
---

<objective>
Securiser le diagnostic IA contre le bypass de quota, et empecher streakReminder de scanner toute la table Streak.

Purpose: TECH-01 — un free user peut appeler finalDiagnosis directement (POST API), contournant le quota decremente dans preDiagnosis. TECH-02 — Streak.list() charge toute la table a chaque CRON quotidien — critique a l'echelle.

Output:
- preDiagnosis emet un token court-terme dans sa reponse
- finalDiagnosis valide ce token avant d'executer — rejette sans token valide
- streakReminder utilise Streak.filter({ current_streak__gte: 3 }) ou equivalent
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Contrats actuels extraits du code — l'executeur utilise ces infos directement -->

From base44/functions/preDiagnosis/entry.ts (ligne 14-34):
- Quota check: isPremium = user.is_premium || trial_expires_at > now
- Si !isPremium: lit actions_remaining et actions_daily_reset
- Decremente via User.update(user.id, { actions_remaining: remaining-1, actions_daily_reset: today })
- Retourne: Response.json(result) contenant preliminary_observations, preliminary_urgency, followup_questions

From base44/functions/finalDiagnosis/entry.ts (ligne 21-36):
- Quota check IDENTIQUE au preDiagnosis — lit actions_remaining mais NE DECREMENTE PAS
- Commentaire: "No decrement — preDiagnosis already decremented for this flow"
- PROBLEME: rien n'empeche un appel direct sans avoir passe par preDiagnosis

From base44/functions/streakReminder/entry.ts (ligne 10-13):
- Streak.list() — charge TOUTE la table
- Filtre in-memory: s.current_streak >= 3 && s.last_activity_date !== today
- Warning si > 500 rows mais pas de protection reelle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token court-terme preDiagnosis → finalDiagnosis (TECH-01)</name>
  <files>base44/functions/preDiagnosis/entry.ts, base44/functions/finalDiagnosis/entry.ts</files>

  <read_first>
    - base44/functions/preDiagnosis/entry.ts — lire integralement avant de modifier
    - base44/functions/finalDiagnosis/entry.ts — lire integralement avant de modifier
  </read_first>

  <action>
**preDiagnosis/entry.ts** — apres le decrement quota (ligne 34), avant le prompt LLM :

1. Generer un token HMAC-SHA256 avec Web Crypto API (disponible dans Deno) :
   ```typescript
   // Apres le bloc if (!isPremium) { ... }, avant const sanitize = ...
   // Pour les users premium aussi — le token prouve que preDiagnosis a ete execute
   const tokenPayload = `${user.id}:${Date.now()}`;
   const keyMaterial = await crypto.subtle.importKey(
     "raw",
     new TextEncoder().encode(Deno.env.get("PRE_DIAG_SECRET") || "pawcoach-diag-secret-v1"),
     { name: "HMAC", hash: "SHA-256" },
     false,
     ["sign"]
   );
   const signature = await crypto.subtle.sign("HMAC", keyMaterial, new TextEncoder().encode(tokenPayload));
   const pre_diagnosis_token = `${tokenPayload}:${btoa(String.fromCharCode(...new Uint8Array(signature)))}`;
   ```

2. Ajouter `pre_diagnosis_token` dans la reponse JSON finale :
   - La reponse actuelle est `Response.json(result)` — la remplacer par :
   `Response.json({ ...result, pre_diagnosis_token })`

**finalDiagnosis/entry.ts** — remplacer le bloc quota (lignes 21-36) :

1. Supprimer le commentaire "Uses actions_remaining..." et le bloc quota incomplet.
2. Le remplacer par une validation du token :
   ```typescript
   // TECH-01: Token validation — preDiagnosis must have been called for this user
   const { pre_diagnosis_token, ...rest } = await req.json();
   // (deplacer la destructuration du body ici si necessaire)

   if (!pre_diagnosis_token) {
     return Response.json({ error: "pre_diagnosis_required", message: "Appelle d'abord preDiagnosis." }, { status: 400 });
   }

   // Verifier format: "userId:timestamp:signature"
   const parts = pre_diagnosis_token.split(":");
   if (parts.length < 3) {
     return Response.json({ error: "invalid_token" }, { status: 403 });
   }
   const [tokenUserId, tokenTs, ...sigParts] = parts;
   const tokenSig = sigParts.join(":");

   // Verifier appartenance
   if (tokenUserId !== user.id) {
     return Response.json({ error: "token_user_mismatch" }, { status: 403 });
   }

   // Verifier expiration (5 minutes = 300000ms)
   const tokenAge = Date.now() - parseInt(tokenTs, 10);
   if (tokenAge > 300000 || tokenAge < 0) {
     return Response.json({ error: "token_expired", message: "Le diagnostic a expire. Recommence depuis le debut." }, { status: 403 });
   }

   // Verifier signature HMAC
   const keyMaterial = await crypto.subtle.importKey(
     "raw",
     new TextEncoder().encode(Deno.env.get("PRE_DIAG_SECRET") || "pawcoach-diag-secret-v1"),
     { name: "HMAC", hash: "SHA-256" },
     false,
     ["verify"]
   );
   const expectedPayload = `${tokenUserId}:${tokenTs}`;
   let sigBytes: Uint8Array;
   try {
     sigBytes = Uint8Array.from(atob(tokenSig), c => c.charCodeAt(0));
   } catch {
     return Response.json({ error: "invalid_token_format" }, { status: 403 });
   }
   const isValid = await crypto.subtle.verify("HMAC", keyMaterial, sigBytes, new TextEncoder().encode(expectedPayload));
   if (!isValid) {
     return Response.json({ error: "invalid_token_signature" }, { status: 403 });
   }
   ```

3. La destructuration du body existe deja a la ligne 9 — ajouter `pre_diagnosis_token` a la liste des champs destructures :
   ```typescript
   const { symptoms, duration, ..., dog_id, pre_diagnosis_token } = await req.json();
   ```

Note: conserver le check ownership (dog_id) qui existe deja aux lignes 13-19 — ne pas le supprimer.
  </action>

  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "pre_diagnosis_token" base44/functions/preDiagnosis/entry.ts base44/functions/finalDiagnosis/entry.ts 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - grep "pre_diagnosis_token" preDiagnosis/entry.ts retourne au moins 2 lignes (generation + dans la reponse)
    - grep "pre_diagnosis_token" finalDiagnosis/entry.ts retourne au moins 3 lignes (destructuration body + check null + validation)
    - grep "actions_remaining" finalDiagnosis/entry.ts retourne 0 resultats (l'ancien guard quota est supprime)
    - grep "token_expired" finalDiagnosis/entry.ts retourne 1 resultat (erreur expiration presente)
    - grep "crypto.subtle" finalDiagnosis/entry.ts retourne au moins 2 lignes (importKey + verify)
    - grep "crypto.subtle" preDiagnosis/entry.ts retourne au moins 2 lignes (importKey + sign)
  </acceptance_criteria>

  <done>preDiagnosis retourne un pre_diagnosis_token signe HMAC dans sa reponse. finalDiagnosis valide ce token (appartenance user, expiration 5min, signature) et rejette toute requete sans token valide — le bypass quota est impossible.</done>
</task>

<task type="auto">
  <name>Task 2: Remplacer Streak.list() par Streak.filter() (TECH-02)</name>
  <files>base44/functions/streakReminder/entry.ts</files>

  <read_first>
    - base44/functions/streakReminder/entry.ts — lire integralement avant de modifier
  </read_first>

  <action>
Remplacer la ligne 10 :
```typescript
// AVANT
const streaks = await base44.asServiceRole.entities.Streak.list();
if ((streaks || []).length > 500) {
  console.warn(`streakReminder: Streak table has ${streaks.length} rows — consider switching to filtered queries if this keeps growing.`);
}
```

Par un filtre serveur-side (teste si le SDK Base44 supporte les filtres par valeur numerique) :
```typescript
// APRES — TECH-02: filter server-side instead of loading entire table
// Streak.filter with current_streak >= 3 avoids full table scan
// Note: Base44 SDK filter syntax uses exact match; numeric comparison not documented.
// Using in-memory filter after a filtered query on last_activity_date to reduce rows.
const today = new Date().toISOString().slice(0, 10); // already defined line 6, reuse it
const streaks = await base44.asServiceRole.entities.Streak.filter({ last_activity_date__ne: today }).catch(async () => {
  // Fallback: if filter not supported, use list() with hard cap
  const all = await base44.asServiceRole.entities.Streak.list();
  if ((all || []).length > 1000) {
    console.warn(`streakReminder: table has ${all.length} rows, returning first 1000`);
    return all.slice(0, 1000);
  }
  return all;
});
```

Puis garder le filtre in-memory a la ligne 16 (deja present) :
```typescript
const activeStreaks = (streaks || []).filter(s => s.current_streak >= 3 && s.last_activity_date !== today);
```

Supprimer le warning block (lignes 11-13) — remplace par le catch ci-dessus.

Si le SDK Base44 ne supporte pas `last_activity_date__ne` (operateur __ne), utiliser cette variante de fallback :
```typescript
// Variante robuste si les operateurs ne sont pas supportes
const allStreaks = await base44.asServiceRole.entities.Streak.list().catch(() => []);
// Hard limit: cap at 2000 rows max to prevent timeout — log if exceeded
if ((allStreaks || []).length > 2000) {
  console.warn(`streakReminder: ${allStreaks.length} streaks — exceeds safe limit of 2000, processing first 2000 only`);
}
const streaks = (allStreaks || []).slice(0, 2000);
```

Choisir la variante selon ce que le SDK supporte. Si incertain, utiliser la variante robuste avec hard limit 2000.
  </action>

  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "Streak.list\|Streak.filter\|slice(0, 2000)\|last_activity_date__ne" base44/functions/streakReminder/entry.ts 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - grep "Streak.list()" streakReminder/entry.ts retourne 0 resultats (plus de .list() sans garde)
    - Au moins une de ces conditions est vraie :
      (a) grep "Streak.filter" retourne 1+ resultat, OU
      (b) grep "slice(0, 2000)" retourne 1+ resultat avec un hard cap
    - grep "streaks.length > 500" retourne 0 resultats (l'ancien warning sans action est supprime)
    - La variable `today` n'est pas definie deux fois (elle existait deja ligne 6)
  </acceptance_criteria>

  <done>streakReminder ne fait plus Streak.list() global sans protection. Soit il filtre via le SDK, soit il applique un hard cap de 2000 rows avec log si depasse. Le filtre in-memory sur current_streak >= 3 reste en place.</done>
</task>

</tasks>

<verification>
```bash
# TECH-01: Token present dans les deux fichiers
grep -n "pre_diagnosis_token" base44/functions/preDiagnosis/entry.ts
grep -n "pre_diagnosis_token" base44/functions/finalDiagnosis/entry.ts

# TECH-01: Ancien guard quota absent de finalDiagnosis
grep -n "actions_remaining" base44/functions/finalDiagnosis/entry.ts
# Doit retourner 0 ligne

# TECH-02: Pas de Streak.list() nu
grep -n "Streak.list()" base44/functions/streakReminder/entry.ts
# Doit retourner 0 ligne

# TECH-02: Protection presente
grep -n "Streak.filter\|slice(0, 2000)" base44/functions/streakReminder/entry.ts
# Doit retourner au moins 1 ligne
```
</verification>

<success_criteria>
- Un free user qui POST directement /finalDiagnosis sans pre_diagnosis_token recoit 400 (pre_diagnosis_required)
- Un token expire (> 5 min) recoit 403 (token_expired)
- Un token avec user.id different recoit 403 (token_user_mismatch)
- streakReminder ne charge plus la table complete sans garde — hard cap 2000 ou filtre SDK
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-backend-critique/01-01-SUMMARY.md`
</output>
