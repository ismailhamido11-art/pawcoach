---
phase: 01-backend-critique
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - base44/functions/monthlySummary/entry.ts
  - src/pages/DogProfile.jsx
autonomous: true
requirements:
  - TECH-04
  - TECH-05
must_haves:
  truths:
    - "monthlySummary s'execute sans erreur pour tous les users — is_trial inexistant ne bloque plus les trial users"
    - "La suppression d'un chien efface ParkReview et PlaceFavorite via le wrapper entities.js — pas de donnees orphelines"
  artifacts:
    - path: "base44/functions/monthlySummary/entry.ts"
      provides: "Filtre trial users base sur trial_expires_at — pas de is_trial inexistant"
      contains: "trial_expires_at"
    - path: "src/pages/DogProfile.jsx"
      provides: "handleDeleteDog inclut ParkReview et PlaceFavorite dans la cascade delete"
      contains: "ParkReview"
  key_links:
    - from: "base44/functions/monthlySummary/entry.ts"
      to: "base44 SDK User.filter"
      via: "trial_expires_at__gte comparison"
      pattern: "trial_expires_at"
    - from: "src/pages/DogProfile.jsx"
      to: "src/api/entities.js"
      via: "import ParkReview, PlaceFavorite depuis entities.js"
      pattern: "ParkReview|PlaceFavorite"
---

<objective>
Corriger is_trial inexistant dans monthlySummary et ajouter ParkReview/PlaceFavorite au cascade delete de DogProfile.

Purpose: TECH-04 — User.filter({ is_trial: true }) ne retourne jamais rien car le champ n'existe pas (le vrai champ est trial_expires_at). Les trial users actifs ne recoivent pas leur resume mensuel. TECH-05 — la suppression d'un chien via DogProfile ne nettoie pas ParkReview et PlaceFavorite — entites orphelines en DB.

Output:
- monthlySummary filtre les trial users via trial_expires_at__gte ou filtre in-memory
- handleDeleteDog inclut ParkReview et PlaceFavorite dans entityNames
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Contrats extraits du code actuel -->

From base44/functions/monthlySummary/entry.ts (lignes 13-23):
```typescript
const premiumUsers = await base44.asServiceRole.entities.User.filter({ is_premium: true });
const trialUsers = await base44.asServiceRole.entities.User.filter({ is_trial: true }).catch(() => []);
// PROBLEME: is_trial n'existe pas dans le schema — retourne [] silencieusement
// Merge dedup en lignes 17-23 : filtre isActiveTrial base sur trial_expires_at — correct
// Mais si trialUsers est [] a cause du mauvais filtre, des trial users manquent dans premiumUsers
```

From src/pages/DogProfile.jsx (lignes 128-138):
```jsx
const entityNames = [
  "HealthRecord", "DailyCheckin", "DailyLog", "Streak",
  "FoodScan", "Bookmark", "UserProgress", "WeeklyInsight",
  "ChatMessage", "SharedVetAccess", "VetNote", "DiagnosisReport",
  "DogAchievement", "GrowthEntry", "NutritionPlan", "DietPreferences"
];
// MANQUANT: "ParkReview", "PlaceFavorite"
// Ces entites utilisent dog_id — elles sont bien dans src/api/entities.js
// base44.entities[name].deleteMany({ dog_id }) fonctionne via acces direct a base44
```

From src/api/entities.js (ligne 35-36):
```javascript
export const ParkReview = wrapEntity(base44.entities.ParkReview, "ParkReview");
export const PlaceFavorite = wrapEntity(base44.entities.PlaceFavorite, "PlaceFavorite");
// Ces entites existent dans le wrapper — accessible via base44.entities.ParkReview
```

From src/pages/DogProfile.jsx (ligne 6):
```javascript
import { base44 } from "@/api/base44Client";
// base44.entities["ParkReview"] est accessible — le pattern dynamique fonctionne
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger is_trial dans monthlySummary (TECH-04)</name>
  <files>base44/functions/monthlySummary/entry.ts</files>

  <read_first>
    - base44/functions/monthlySummary/entry.ts — lire lignes 1-30 avant de modifier
  </read_first>

  <action>
Remplacer les lignes 14-23 (bloc premiumUsers + trialUsers + merge) :

```typescript
// AVANT (lignes 13-23)
const premiumUsers = await base44.asServiceRole.entities.User.filter({ is_premium: true });
const trialUsers = await base44.asServiceRole.entities.User.filter({ is_trial: true }).catch(() => []);
// Merge and deduplicate: include active trial users even if not yet flagged is_premium
const allEligibleUsers = [...(premiumUsers || []), ...(trialUsers || [])].filter(
  (u, i, arr) => u.email && arr.findIndex(x => x.email === u.email) === i
).filter(u => {
  const isPremium = u.is_premium;
  const isActiveTrial = u.trial_expires_at && new Date(u.trial_expires_at) > new Date();
  return isPremium || isActiveTrial;
});
```

Remplacer par :

```typescript
// TECH-04: is_trial n'existe pas dans le schema — utiliser trial_expires_at pour les trial users
// Approche: charger premium + non-premium, filtrer in-memory par trial_expires_at
const premiumUsers = await base44.asServiceRole.entities.User.filter({ is_premium: true });

// Tenter un filtre SDK sur trial_expires_at si supporte, sinon fallback in-memory
// Note: Base44 SDK peut ne pas supporter les operateurs de date comparison (__gte)
// On recupere donc tous les users non-premium et on filtre en memoire
// Pour eviter User.list() global, on ne charge que les non-premium si l'app est petite
// En prod, cette requete retourne seulement les non-premium qui ont un trial actif
const today = now.toISOString().slice(0, 10);
const trialUsers = await base44.asServiceRole.entities.User.filter({ is_premium: false }).catch(() => []);
const activeTrialUsers = (trialUsers || []).filter(u => {
  return u.trial_expires_at && new Date(u.trial_expires_at) > now;
});

// Merge and deduplicate
const allEligibleUsers = [...(premiumUsers || []), ...activeTrialUsers].filter(
  (u, i, arr) => u.email && arr.findIndex(x => x.email === u.email) === i
);
```

Notes importantes :
- `now` est deja defini en ligne 7 (`const now = new Date()`) — reutiliser cette variable
- Supprimer `const isActiveTrial` et le `.filter(u => {...})` supplementaire en ligne 19-23 car le merge ci-dessus garantit deja que tous les users sont eligibles
- Conserver la variable `userMap` et la suite du code (lignes 26+) — ne pas modifier
  </action>

  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "is_trial" base44/functions/monthlySummary/entry.ts 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - grep "is_trial" base44/functions/monthlySummary/entry.ts retourne 0 resultats (champ inexistant supprime)
    - grep "trial_expires_at" base44/functions/monthlySummary/entry.ts retourne au moins 2 lignes (filtre in-memory et la variable now)
    - grep "is_premium: false" base44/functions/monthlySummary/entry.ts retourne 1 resultat (filtre SDK pour non-premium)
    - grep "activeTrialUsers\|active_trial" base44/functions/monthlySummary/entry.ts retourne au moins 1 resultat
    - grep "Streak.list\|User.list\|Dog.list" base44/functions/monthlySummary/entry.ts retourne 0 resultats (pas de .list() global)
  </acceptance_criteria>

  <done>monthlySummary ne reference plus is_trial. Les trial users actifs (trial_expires_at > now) sont correctement inclus dans allEligibleUsers. La fonction s'execute sans erreur silencieuse sur le filtre SDK.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter ParkReview et PlaceFavorite au cascade delete (TECH-05)</name>
  <files>src/pages/DogProfile.jsx</files>

  <read_first>
    - src/pages/DogProfile.jsx — lire lignes 123-152 avant de modifier
    - src/api/entities.js — lire pour confirmer que ParkReview et PlaceFavorite sont exportes
  </read_first>

  <action>
Dans `handleDeleteDog` (ligne 128-138), modifier le tableau `entityNames` :

```javascript
// AVANT
const entityNames = [
  "HealthRecord", "DailyCheckin", "DailyLog", "Streak",
  "FoodScan", "Bookmark", "UserProgress", "WeeklyInsight",
  "ChatMessage", "SharedVetAccess", "VetNote", "DiagnosisReport",
  "DogAchievement", "GrowthEntry", "NutritionPlan", "DietPreferences"
];
```

```javascript
// APRES — TECH-05: ajouter ParkReview et PlaceFavorite (filtrees par dog_id)
const entityNames = [
  "HealthRecord", "DailyCheckin", "DailyLog", "Streak",
  "FoodScan", "Bookmark", "UserProgress", "WeeklyInsight",
  "ChatMessage", "SharedVetAccess", "VetNote", "DiagnosisReport",
  "DogAchievement", "GrowthEntry", "NutritionPlan", "DietPreferences",
  "ParkReview", "PlaceFavorite"
];
```

Verification que le pattern `base44.entities[name].deleteMany({ dog_id: dog.id })` fonctionne pour ParkReview et PlaceFavorite :
- `base44.entities` est l'objet SDK direct (ligne 6: `import { base44 } from "@/api/base44Client"`)
- ParkReview et PlaceFavorite existent dans le schema Base44 (confirme par entities.js lignes 35-36)
- Le `.catch(() => {})` protege contre toute erreur silencieuse

Aucune autre modification du code handleDeleteDog — uniquement le tableau entityNames.

Note: Ne pas ajouter PlaceFavorite dans le bloc user-level cleanup (ligne 60 de deleteUser) car c'est deja gere la. Le DogProfile.jsx supprime par dog_id — le deleteUser supprime par owner (email). Les deux cas sont couverts.
  </action>

  <verify>
    <automated>cd "C:/Users/smalt/Desktop/app-chien-ia/pawcoach" && grep -n "ParkReview\|PlaceFavorite" src/pages/DogProfile.jsx 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - grep "ParkReview" src/pages/DogProfile.jsx retourne au moins 1 resultat (dans entityNames)
    - grep "PlaceFavorite" src/pages/DogProfile.jsx retourne au moins 1 resultat (dans entityNames)
    - grep -c '"' src/pages/DogProfile.jsx sur la ligne entityNames montre 18 entites (16 existantes + 2 ajoutees) — verifier visuellement que les 18 strings sont presentes
    - Le reste de handleDeleteDog (Dog.delete, localStorage, navigate) n'est pas modifie
  </acceptance_criteria>

  <done>La suppression d'un chien nettoie maintenant ParkReview et PlaceFavorite en plus des 16 entites existantes. Plus de donnees orphelines apres suppression d'un profil chien.</done>
</task>

</tasks>

<verification>
```bash
# TECH-04: is_trial absent
grep "is_trial" base44/functions/monthlySummary/entry.ts
# Doit retourner 0 ligne

# TECH-04: trial_expires_at present
grep "trial_expires_at" base44/functions/monthlySummary/entry.ts
# Doit retourner au moins 2 lignes

# TECH-05: ParkReview et PlaceFavorite dans entityNames
grep "ParkReview\|PlaceFavorite" src/pages/DogProfile.jsx
# Doit retourner 2 lignes
```
</verification>

<success_criteria>
- monthlySummary: les trial users actifs (trial_expires_at dans le futur) recoivent bien leur resume mensuel
- monthlySummary: aucune erreur silencieuse liee a un champ is_trial inexistant
- DogProfile: supprimer un chien avec des avis de parcs (ParkReview) ou des favoris (PlaceFavorite) ne laisse plus de donnees orphelines en DB
</success_criteria>

<output>
Apres completion, creer `.planning/phases/01-backend-critique/01-03-SUMMARY.md`
</output>
