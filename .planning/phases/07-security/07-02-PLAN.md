---
phase: 07-security
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - pawcoach/functions/finalDiagnosis.ts
  - pawcoach/functions/preDiagnosis.ts
  - pawcoach/functions/processHealthInput.ts
  - pawcoach/functions/analyzeGrowthPhoto.ts
autonomous: true
requirements:
  - SEC-03
  - SEC-04

must_haves:
  truths:
    - "Les image_url fournies par l'utilisateur dans finalDiagnosis et preDiagnosis sont validees contre une allowlist avant utilisation"
    - "Les imageUrl dans processHealthInput et analyzeGrowthPhoto sont validees contre la meme allowlist"
    - "Aucun rendu HTML brut depuis du contenu utilisateur nexiste dans le repo (hors shadcn/ui chart exception documentee)"
  artifacts:
    - path: "pawcoach/functions/finalDiagnosis.ts"
      provides: "validation URL allowlist pour image_url"
      contains: "allowedHosts"
    - path: "pawcoach/functions/preDiagnosis.ts"
      provides: "validation URL allowlist pour image_url"
      contains: "allowedHosts"
    - path: "pawcoach/functions/processHealthInput.ts"
      provides: "validation URL allowlist pour imageUrl"
      contains: "allowedHosts"
    - path: "pawcoach/functions/analyzeGrowthPhoto.ts"
      provides: "validation URL allowlist pour photoUrl"
      contains: "allowedHosts"
  key_links:
    - from: "req.json() image_url (user-supplied)"
      to: "base44.callAI(fileUrls)"
      via: "validation URL allowlist avant push dans fileUrls"
      pattern: "allowedHosts.*some.*endsWith"
---

<objective>
Ajouter la validation URL (prevention SSRF) sur les 4 fonctions backend qui acceptent des image URLs utilisateur sans validation, et confirmer l'absence de rendu HTML brut depuis du contenu utilisateur (SEC-04).

Purpose: SEC-03 est le risque SSRF le plus concret — finalDiagnosis, preDiagnosis, processHealthInput et analyzeGrowthPhoto acceptent des URLs fournies par l'utilisateur et les passent directement a l'API IA sans valider le domaine. Un attaquant pourrait fournir une URL interne. pawcoachChat a deja ce pattern correctement implemente — le copier dans les 4 fonctions manquantes.
Output: 4 fonctions backend avec validation URL, SEC-04 confirme satisfait.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Pattern de reference : validation URL deja implementee dans pawcoachChat.ts (lignes 13-22) -->

Pattern source (pawcoachChat.ts) :
```typescript
if (imageUrl) {
  try {
    const parsed = new URL(imageUrl);
    const allowedHosts = ['base44.app', 'amazonaws.com', 's3.amazonaws.com'];
    if (!allowedHosts.some(h => parsed.hostname.endsWith(h))) {
      return Response.json({ error: 'Invalid image URL' }, { status: 400 });
    }
  } catch {
    return Response.json({ error: 'Invalid image URL' }, { status: 400 });
  }
}
```

Localisation des URLs a valider par fichier :
- finalDiagnosis.ts : `image_url` (destructure ligne 9), utilise ligne 78 dans fileUrls
- preDiagnosis.ts : `image_url` (destructure ligne 9), utilise ligne 59 dans fileUrls
- processHealthInput.ts : `imageUrl` (destructure ligne 9) et lastMsg image_url (ligne 231)
- analyzeGrowthPhoto.ts : `photoUrl` (destructure ligne 9), utilise ligne 41 dans file_urls
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit SEC-04 — confirmer absence de rendu HTML brut non-protege</name>
  <files>pawcoach/src/pages/Chat.jsx, pawcoach/src/pages/Sante.jsx, pawcoach/src/pages/Dashboard.jsx</files>
  <action>
Audit SEC-04 : verifier qu'aucun composant hors shadcn/ui ne rend du HTML brut depuis du contenu utilisateur.

1. Lancer : `grep -rn "dangerously\|innerHTML\|eval\b" pawcoach/src/ --include="*.jsx" --include="*.js" | grep -v "ui/" | grep -v "node_modules"` — si 0 resultats, SEC-04 est satisfait.

2. Note : le seul usage de rendu HTML direct se trouve dans `pawcoach/src/components/ui/chart.jsx` — il contient uniquement des styles CSS systeme (pas de contenu utilisateur). C'est du code shadcn/ui, ne pas toucher.

Si un rendu HTML depuis du contenu utilisateur est trouve en dehors de shadcn/ui : STOP, noter dans SUMMARY, ne pas corriger dans ce plan — alert pour Ismail.

Ce task ne modifie aucun fichier — c'est un audit de confirmation.
  </action>
  <verify>
    <automated>grep -rn "dangerously\|innerHTML" pawcoach/src/ --include="*.jsx" --include="*.js" 2>/dev/null | grep -v "ui/" | grep -v "node_modules" | wc -l</automated>
  </verify>
  <done>La commande retourne 0. SEC-04 documente comme satisfait dans le SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Corriger SEC-03 — validation URL dans finalDiagnosis, preDiagnosis, processHealthInput, analyzeGrowthPhoto</name>
  <files>pawcoach/functions/finalDiagnosis.ts, pawcoach/functions/preDiagnosis.ts, pawcoach/functions/processHealthInput.ts, pawcoach/functions/analyzeGrowthPhoto.ts</files>
  <action>
Copier le pattern de validation URL de pawcoachChat.ts dans les 4 fonctions manquantes.

Helper a ajouter dans chaque fichier apres la destructuration des params :
```typescript
const validateImageUrl = (url: string | undefined | null): string | null => {
  if (!url) return null;
  try {
    const parsed = new URL(url);
    const allowedHosts = ['base44.app', 'amazonaws.com', 's3.amazonaws.com'];
    if (!allowedHosts.some(h => parsed.hostname.endsWith(h))) return null;
    return url;
  } catch {
    return null;
  }
};
```

--- finalDiagnosis.ts ---
Lire le fichier entier avant de modifier.

Apres la destructuration des params, ajouter validateImageUrl, puis :
```typescript
const safeImageUrl = validateImageUrl(image_url);
if (image_url && !safeImageUrl) return Response.json({ error: 'Invalid image URL' }, { status: 400 });
```
Remplacer `const fileUrls = image_url ? [image_url] : undefined;` par `const fileUrls = safeImageUrl ? [safeImageUrl] : undefined;`
Remplacer `${image_url ?` par `${safeImageUrl ?` dans le prompt.

--- preDiagnosis.ts ---
Meme modification exacte que finalDiagnosis.ts (meme structure, meme var `image_url`).

--- processHealthInput.ts ---
Lire le fichier entier avant de modifier.

Ajouter validateImageUrl. Pour imageUrl (param direct) :
```typescript
const safeImageUrl = validateImageUrl(imageUrl);
```
Pour lastMsg.image_url (ligne ~231) :
```typescript
const safeLastImageUrl = validateImageUrl(lastMsg?.image_url);
```
Remplacer `if (imageUrl) fileUrls.push(imageUrl)` par `if (safeImageUrl) fileUrls.push(safeImageUrl)`.
Remplacer `if (lastMsg?.image_url) fileUrls.push(lastMsg.image_url)` par `if (safeLastImageUrl) fileUrls.push(safeLastImageUrl)`.
Ne pas retourner 400 ici — l'image est optionnelle, ignorer silencieusement si URL invalide.

--- analyzeGrowthPhoto.ts ---
Lire le fichier entier avant de modifier.

Apres la verification `if (!dogId || !photoUrl) return 400` existante, ajouter :
```typescript
const safePhotoUrl = validateImageUrl(photoUrl);
if (!safePhotoUrl) return Response.json({ error: 'Invalid photo URL' }, { status: 400 });
```
Remplacer `file_urls: [photoUrl]` par `file_urls: [safePhotoUrl]`.

IMPORTANT : Ne modifier que les lignes de validation URL. Ne pas toucher a la logique de prompt, DB, ni aux autres params.
  </action>
  <verify>
    <automated>grep -n "allowedHosts\|validateImageUrl\|safeImageUrl\|safePhotoUrl" pawcoach/functions/finalDiagnosis.ts pawcoach/functions/preDiagnosis.ts pawcoach/functions/processHealthInput.ts pawcoach/functions/analyzeGrowthPhoto.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>La commande retourne au moins 4 (une occurrence par fichier). Verification secondaire : aucun usage brut de image_url dans fileUrls — confirme par lecture des lignes modifiees.</done>
</task>

</tasks>

<verification>
Apres les deux tasks :

1. SEC-03 : lire les 4 fonctions et confirmer que fileUrls utilisent safeImageUrl/safePhotoUrl, pas les variables brutes

2. SEC-04 : `grep -rn "dangerously" pawcoach/src/ --include="*.jsx" | grep -v "ui/"` retourne 0

3. Build Vite passe : `cd pawcoach && npm run build 2>&1 | tail -5` — aucune erreur TypeScript/syntaxe
</verification>

<success_criteria>
- SEC-03 satisfait : les 4 fonctions backend valident les URLs image contre allowlist avant utilisation
- SEC-04 satisfait : aucun rendu HTML brut hors shadcn/ui, confirme par grep
- Build Vite passe sans erreur
- Aucune regression fonctionnelle
</success_criteria>

<output>
Apres completion, creer `.planning/phases/07-security/07-security-02-SUMMARY.md` avec :
- SEC-03 : 4 fonctions corrigees, pattern validateImageUrl applique
- SEC-04 : statut confirme (satisfait ou probleme decouvert)
- Resultat du build
</output>
