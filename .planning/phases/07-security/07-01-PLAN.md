---
phase: 07-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/functions/pawcoachChat.ts
  - pawcoach/functions/weeklyInsightGenerate.ts
  - pawcoach/functions/generateTrainingProgram.ts
  - pawcoach/functions/analyzeGrowthPhoto.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "Aucun secret/token/cle API n'est present en clair dans le code source (frontend ou backend)"
    - "Tout champ utilisateur injecte dans un prompt IA passe par sanitize() avec replace(/[<>]/g, '') et substring(max)"
    - "Les fonctions pawcoachChat, weeklyInsightGenerate, generateTrainingProgram, analyzeGrowthPhoto sanitisent les inputs DB avant injection dans les prompts"
  artifacts:
    - path: "pawcoach/functions/pawcoachChat.ts"
      provides: "dog.allergies et walk notes sanitises avant injection prompt"
      contains: "replace(/[<>]/g"
    - path: "pawcoach/functions/weeklyInsightGenerate.ts"
      provides: "notes context sanitise avant injection prompt"
      contains: "replace(/[<>]/g"
    - path: "pawcoach/functions/generateTrainingProgram.ts"
      provides: "owner_goal et allergies sanitises avant injection prompt"
      contains: "replace(/[<>]/g"
    - path: "pawcoach/functions/analyzeGrowthPhoto.ts"
      provides: "dogBreed sanitise avant injection prompt"
      contains: "replace(/[<>]/g"
  key_links:
    - from: "DB (Dog.allergies)"
      to: "prompt IA (pawcoachChat systemPrompt)"
      via: "sanitize inline — String(dog.allergies).substring(0,100).replace(/[<>]/g,'')"
      pattern: "dog\\.allergies.*replace"
    - from: "DB (DailyCheckin.notes)"
      to: "prompt IA (weeklyInsightGenerate systemPrompt)"
      via: "sanitize inline — String(c.notes).substring(0,100).replace(/[<>]/g,'')"
      pattern: "c\\.notes.*replace"
---

<objective>
Eliminer toute injection de donnees utilisateur non sanitisees dans les prompts IA, et confirmer l'absence de secrets en clair dans le repo.

Purpose: SEC-02 est le risque le plus concret — des champs DB comme dog.allergies, walk notes et dogBreed sont injectes directement dans des prompts sans replace(/[<>]/g, ''). Une injection de prompt est possible. SEC-01 est deja satisfait structurellement (Deno.env, VITE_*) mais doit etre verifie formellement.
Output: 4 fichiers backend corriges avec sanitize coherente sur tous les champs utilisateur injectes dans les prompts IA.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit SEC-01 — verifier absence de secrets en clair</name>
  <files>pawcoach/functions/pawcoachChat.ts, pawcoach/functions/weeklyInsightGenerate.ts, pawcoach/functions/generateTrainingProgram.ts, pawcoach/functions/analyzeGrowthPhoto.ts, pawcoach/src/lib/app-params.js, pawcoach/.gitignore</files>
  <action>
Verifier les 4 conditions de SEC-01 :

1. Lire pawcoach/.gitignore — confirmer que `.env` et `.env.*` sont presentes (elles le sont deja, verification formelle).

2. Grep les 4 fichiers backend pour detecter tout pattern de secret hardcode : `sk-`, `pk_live`, `pk_test`, `whsec_`, `Bearer [a-zA-Z0-9]{20,}` (valeur hardcodee, pas une variable). Si rien trouve, SEC-01 est satisfait — noter dans le SUMMARY.

3. Grep le frontend (src/) pour detecter toute cle en clair hors `import.meta.env.*` ou `getAppParamValue()`. Si rien trouve, OK.

4. Confirmer que toutes les cles dans les fonctions backend utilisent exclusivement `Deno.env.get("NOM_CLE")` — c'est le cas pour OPENROUTER_API_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, BASE44_APP_ID.

Si un secret en clair est decouvert : STOP, le noter dans le SUMMARY et NE PAS le committer — alert pour Ismail.

SEC-01 ne necessite pas de modification de code si l'audit est propre.
  </action>
  <verify>
    <automated>grep -rn "sk-\|pk_live\|pk_test\|whsec_\|rk_live" pawcoach/src/ pawcoach/functions/ 2>/dev/null | grep -v "Deno.env\|import.meta.env\|getAppParamValue\|node_modules" | wc -l</automated>
  </verify>
  <done>La commande retourne 0 (aucun secret hardcode). SEC-01 documente comme satisfait dans le SUMMARY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Corriger SEC-02 — sanitize dans pawcoachChat, weeklyInsightGenerate, generateTrainingProgram, analyzeGrowthPhoto</name>
  <files>pawcoach/functions/pawcoachChat.ts, pawcoach/functions/weeklyInsightGenerate.ts, pawcoach/functions/generateTrainingProgram.ts, pawcoach/functions/analyzeGrowthPhoto.ts</files>
  <action>
Le pattern de sanitize correct (deja etabli dans dailyCheckinProcess.ts, finalDiagnosis.ts, preDiagnosis.ts) :
```typescript
const sanitize = (s: any, max = 500) => String(s || '').substring(0, max).replace(/[<>]/g, '');
```

Appliquer dans chaque fichier :

--- pawcoachChat.ts ---
Lire le fichier. La fonction `sanitize` n'existe pas dans ce fichier.

1. Ajouter en haut du handler (apres la destructuration des params) :
```typescript
const sanitize = (s: any, max = 500) => String(s || '').substring(0, max).replace(/[<>]/g, '');
```

2. Ligne ~440 — `dog.allergies` est injecte sans sanitize :
```typescript
// AVANT
dog.allergies ? `- Allergies : ${dog.allergies}` : null,
// APRES
dog.allergies ? `- Allergies : ${sanitize(dog.allergies, 200)}` : null,
```

3. Ligne ~474 — `dog.owner_goal` dans le bloc OBJECTIF PRINCIPAL — deja entoure de String().substring(0,150), mais ajouter .replace(/[<>]/g, '') :
Remplacer `String(dog.owner_goal).substring(0, 150)` par `sanitize(dog.owner_goal, 150)` dans les deux occurrences (lignes ~445 et ~474).

4. Ligne ~131 — `latestNote` utilise String().substring() sans replace :
Remplacer `String(latestNote).substring(0, 200)` par `sanitize(latestNote, 200)`.

5. Ligne ~133 — meme correction pour `latestBehaviorNote`.

6. Ligne ~214 — `dietPref.notes` : remplacer `String(dietPref.notes).substring(0, 100)` par `sanitize(dietPref.notes, 100)`.

--- weeklyInsightGenerate.ts ---
Lire le fichier. La fonction `sanitize` n'existe pas dans ce fichier.

1. Ajouter la fonction sanitize en haut du handler.

2. Lignes ~148-149 — notes et behavior_notes des check-ins : deja String().substring() mais sans replace :
```typescript
// AVANT
if (c.notes && String(c.notes).trim()) weekNotes.push(String(c.notes).substring(0, 100));
if (c.behavior_notes && String(c.behavior_notes).trim()) weekNotes.push(String(c.behavior_notes).substring(0, 100));
// APRES
if (c.notes && String(c.notes).trim()) weekNotes.push(sanitize(c.notes, 100));
if (c.behavior_notes && String(c.behavior_notes).trim()) weekNotes.push(sanitize(c.behavior_notes, 100));
```

3. Ligne ~205 — `dog.allergies` : deja substring mais sans replace :
`String(dog.allergies).substring(0, 100)` → `sanitize(dog.allergies, 100)`

4. Ligne ~182 — `dog.behavior_summary` : `String(dog.behavior_summary).substring(0, 300)` → `sanitize(dog.behavior_summary, 300)`

--- generateTrainingProgram.ts ---
Lire le fichier.

1. Ajouter la fonction sanitize en haut du handler (apres destructuration).

2. Ligne ~76 — `dog.owner_goal` : `String(dog.owner_goal).substring(0, 200)` → `sanitize(dog.owner_goal, 200)`

3. Ligne ~80 — `dog.allergies` : `String(dog.allergies).substring(0, 200)` → `sanitize(dog.allergies, 200)`

4. Note : `safeGoals` a deja `.replace(/[<>]/g, '')` — OK, ne pas changer.

--- analyzeGrowthPhoto.ts ---
Lire le fichier.

1. Ajouter la fonction sanitize en haut du handler.

2. Dans le prompt — `dogBreed` est injecte sans sanitize :
```typescript
// AVANT
`...race: ${dogBreed || 'inconnue'}...`
// APRES
`...race: ${sanitize(dogBreed, 50) || 'inconnue'}...`
```

3. Meme chose pour `currentWeight` (nombre, mais valider que c'est bien un nombre) :
Ajouter avant le prompt : `const safeWeight = typeof currentWeight === 'number' ? currentWeight : null;`
Et utiliser `safeWeight` dans le prompt au lieu de `currentWeight`.

IMPORTANT : Ne pas toucher aux parties du code qui ne concernent pas la sanitization. Ne pas modifier la logique metier. Faire uniquement des substitutions de pattern String().substring() → sanitize() et ajouter dog.allergies → sanitize(dog.allergies).
  </action>
  <verify>
    <automated>grep -n "dog\.allergies\b" pawcoach/functions/pawcoachChat.ts | grep -v "sanitize\|replace\|substring" | wc -l && grep -rn "replace.*\[<>\]" pawcoach/functions/pawcoachChat.ts pawcoach/functions/weeklyInsightGenerate.ts pawcoach/functions/generateTrainingProgram.ts pawcoach/functions/analyzeGrowthPhoto.ts | wc -l</automated>
  </verify>
  <done>La commande retourne 0 (dog.allergies n'est plus injecte brut). Verification secondaire : grep -rn "replace.*\[<>\]" pawcoach/functions/pawcoachChat.ts pawcoach/functions/weeklyInsightGenerate.ts pawcoach/functions/generateTrainingProgram.ts pawcoach/functions/analyzeGrowthPhoto.ts retourne au moins 1 ligne par fichier.</done>
</task>

</tasks>

<verification>
Apres les deux tasks :

1. SEC-01 : `grep -rn "sk-\|pk_live\|pk_test\|whsec_" pawcoach/src/ pawcoach/functions/ | grep -v "Deno.env\|import.meta.env" | wc -l` → retourne 0

2. SEC-02 : `grep -n "dog\.allergies" pawcoach/functions/pawcoachChat.ts` → toutes les occurrences montrent `sanitize(dog.allergies` et non `${dog.allergies}`

3. Build Vite passe : `cd pawcoach && npm run build 2>&1 | tail -5` — aucune erreur TypeScript/syntaxe
</verification>

<success_criteria>
- SEC-01 satisfait : 0 secrets hardcodes dans le repo, confirme par grep
- SEC-02 satisfait : tous les champs utilisateur injectes dans les 4 fonctions backend utilisent sanitize() avec replace(/[<>]/g, '')
- Build Vite passe sans erreur
- Aucune regression fonctionnelle (la logique metier est inchangee)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/07-security/07-security-01-SUMMARY.md` avec :
- SEC-01 : statut (verifie propre / secret trouve)
- Fichiers modifies et pattern applique pour SEC-02
- Liste des champs sanitises ajoutes
- Resultat du build
</output>
