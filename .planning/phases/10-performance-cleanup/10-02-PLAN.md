---
phase: 10-performance-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/DogContext.jsx
  - src/lib/HomeCacheContext.jsx
  - src/pages/Home.jsx
autonomous: true
requirements: [FIX-62, FIX-63]
must_haves:
  truths:
    - "Un DogContext expose le chien actif a toute l'app sans appel Dog.filter() individuel"
    - "Home.jsx utilise un hook useHomeData() au lieu d'avoir fetchDogData inline"
    - "Les pages qui consommaient Dog.filter() directement peuvent importer useDog() a la place"
  artifacts:
    - path: "src/lib/DogContext.jsx"
      provides: "DogProvider + useDog hook"
      exports: ["DogProvider", "useDog"]
    - path: "src/pages/Home.jsx"
      provides: "Home refactorisee avec useHomeData"
      contains: "useHomeData"
  key_links:
    - from: "src/App.jsx"
      to: "DogProvider"
      via: "wrapping de l'arbre de composants"
      pattern: "DogProvider"
    - from: "src/pages/Home.jsx"
      to: "useHomeData"
      via: "import et appel du hook"
      pattern: "useHomeData"
---

<objective>
Reduire le couplage et la complexite de Home.jsx par extraction du data-fetching et creation d'un contexte Dog partage.

Purpose: Home.jsx est un God Page de 742 lignes avec fetchDogData inline, cache logic, et state de rendu melange. Extraire useHomeData() reduit la charge cognitive et facilite les tests. DogContext permet aux pages qui font Dog.filter() independamment (Nutri, Training, etc.) de partager un seul appel.

Output:
- src/lib/DogContext.jsx : nouveau contexte avec useDog() hook
- src/pages/Home.jsx : fetchDogData + cache logic extraits dans useHomeData() (dans le meme fichier ou fichier adjacent)
- src/App.jsx : DogProvider ajout dans l'arbre
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- HomeCacheContext existant dans src/lib/HomeCacheContext.jsx -->
<!-- Exports: useHomeCache() -> { getCachedHome, setCachedHome, invalidateHome } -->
<!-- Ne pas modifier HomeCacheContext — DogContext est un contexte distinct -->

<!-- AuthContext existant dans src/lib/AuthContext.jsx -->
<!-- Exports: useAuth() -> { user, isLoadingAuth, checkAppState } -->

<!-- getActiveDog() dans src/utils/index.js -->
<!-- Retourne l'ID du chien actif depuis localStorage -->

<!-- Dog entity dans src/api/entities.js -->
<!-- Dog.filter({ owner: user.email }) -> Promise<Dog[]> -->

<!-- Home.jsx actuel -->
<!-- fetchDogData(dogId) est une fonction standalone (lignes 47-63) -->
<!-- fetchAndCache() est une methode dans useEffect (a extraire aussi) -->
<!-- dogData state groupe : { todayCheckin, streak, recentCheckins, records, exercises, scans, dailyLogs, diagnosisReports, nutritionPlans, trainingBookmarks, behaviorBookmarks } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer DogContext avec useDog hook</name>
  <files>src/lib/DogContext.jsx, src/App.jsx</files>
  <read_first>
    src/lib/HomeCacheContext.jsx
    src/lib/AuthContext.jsx
    src/App.jsx
    src/utils/index.js (pattern getActiveDog)
  </read_first>
  <action>
Creer src/lib/DogContext.jsx avec le pattern suivant :

```jsx
import { createContext, useContext, useState, useEffect, useCallback } from "react";
import { Dog } from "@/api/entities";
import { getActiveDog } from "@/utils";
import { useAuth } from "@/lib/AuthContext";

const DogContext = createContext(null);

export function DogProvider({ children }) {
  const { user, isLoadingAuth } = useAuth();
  const [dog, setDog] = useState(null);
  const [dogs, setDogs] = useState([]);
  const [loadingDog, setLoadingDog] = useState(true);

  const refreshDogs = useCallback(async () => {
    if (!user) return;
    try {
      const list = await Dog.filter({ owner: user.email });
      setDogs(list || []);
      const activeDogId = getActiveDog();
      const active = activeDogId
        ? (list || []).find(d => d.id === activeDogId) || (list || [])[0]
        : (list || [])[0];
      setDog(active || null);
    } catch (e) {
      console.warn("DogContext: Dog.filter failed:", e);
    } finally {
      setLoadingDog(false);
    }
  }, [user]);

  useEffect(() => {
    if (!isLoadingAuth && user) {
      refreshDogs();
    } else if (!isLoadingAuth && !user) {
      setLoadingDog(false);
    }
  }, [isLoadingAuth, user, refreshDogs]);

  return (
    <DogContext.Provider value={{ dog, dogs, setDog, setDogs, loadingDog, refreshDogs }}>
      {children}
    </DogContext.Provider>
  );
}

export function useDog() {
  const ctx = useContext(DogContext);
  if (!ctx) throw new Error("useDog must be used inside DogProvider");
  return ctx;
}
```

Dans src/App.jsx :
1. Lire le fichier entier
2. Importer DogProvider : `import { DogProvider } from "@/lib/DogContext";`
3. Wrapper l'arbre existant avec DogProvider, apres AuthProvider et HomeCacheProvider (DogContext dependo de AuthContext)
4. L'ordre doit etre : AuthProvider > DogProvider > HomeCacheProvider > Router... (ou HomeCacheProvider > DogProvider si HomeCacheProvider ne depend pas de Dog)

IMPORTANT : Ne modifier aucun composant existant dans cette tache — juste creer le contexte et l'ajouter a App.jsx. Les pages continueront a faire leurs propres Dog.filter() pour l'instant. DogContext est opt-in.
  </action>
  <verify>
    <automated>grep -n "DogProvider" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/DogContext.jsx existe avec exports DogProvider et useDog
    - grep "DogProvider" src/App.jsx retourne au moins 2 lignes (import + usage)
    - DogProvider wraps les routes dans App.jsx
    - useDog() lance une Error si appele hors DogProvider (throw present dans le code)
  </acceptance_criteria>
  <done>DogContext cree et monte dans App.jsx — useDog() disponible pour toutes les pages</done>
</task>

<task type="auto">
  <name>Task 2: Extraire useHomeData hook de Home.jsx</name>
  <files>src/pages/Home.jsx</files>
  <read_first>src/pages/Home.jsx</read_first>
  <action>
Dans src/pages/Home.jsx, extraire la logique de data fetching en un hook `useHomeData` defini DANS le meme fichier (pas dans un fichier separe — pas necessaire pour cette iteration).

Extraire dans useHomeData() :
1. La fonction `fetchDogData(dogId)` (lignes 47-63) — deja standalone, juste la deplacer dans le hook
2. La logique `fetchAndCache()` de useEffect (construction du cache, appel fetchDogData, applyDogData, loadInsights)
3. Les states `dogData`, `insights`, `isDataStale` et leurs setters
4. Les fonctions `applyDogData`, `applyInsights`, `loadInsights`

Le hook useHomeData retourne :
```javascript
{
  dogData,       // { todayCheckin, streak, recentCheckins, records, ... }
  insights,      // { weeklyInsight, previousInsight, pastInsights }
  isDataStale,
  refreshHome,   // equivalent de fetchAndCache()
  applyDogData,
  applyInsights,
}
```

Structure du hook :
```javascript
function useHomeData(user, dog) {
  const { getCachedHome, setCachedHome } = useHomeCache();
  const [dogData, setDogData] = useState({ ... }); // meme valeur initiale
  const [insights, setInsights] = useState({ ... }); // meme valeur initiale
  const [isDataStale, setIsDataStale] = useState(false);

  const applyDogData = useCallback(({ checkins, ... }) => { ... }, []);
  const applyInsights = useCallback((insightsData) => { ... }, []);
  const loadInsights = useCallback(async (u, dogId) => { ... }, []);
  const fetchAndCache = useCallback(async (forceRefresh = false) => { ... }, [dog, user, ...]);

  return { dogData, insights, isDataStale, refreshHome: fetchAndCache, applyDogData, applyInsights };
}
```

Dans le composant Home, remplacer :
- Les declarations des states extraits par : `const { dogData, insights, isDataStale, refreshHome, applyDogData, applyInsights } = useHomeData(user, dog);`
- Les appels a fetchAndCache() dans le composant par refreshHome()

IMPORTANT :
- Ne pas modifier la logique de fetchDogData, applyDogData, ou loadInsights
- Conserver le cache HomeCacheContext tel quel (getCachedHome/setCachedHome)
- Le milestone detection logic (MILESTONES array, streak checks) reste dans le composant Home — il concerne le rendu UI, pas le fetching
- Si useCallback introduit des problemes de dep (eslint warnings), utiliser useRef pour les fonctions stables comme applyDogData
- Ne pas toucher a la partie render de Home.jsx (JSX, event handlers UI)
  </action>
  <verify>
    <automated>grep -n "function useHomeData\|useHomeData(" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "function useHomeData" src/pages/Home.jsx retourne 1 ligne
    - grep "useHomeData(" src/pages/Home.jsx retourne au moins 2 lignes (definition + appel dans Home)
    - grep "fetchDogData\|applyDogData\|loadInsights" src/pages/Home.jsx — toutes les occurrences sont DANS useHomeData ou ses appels
    - Home.jsx compile sans erreur (pas de reference a variable non definie)
  </acceptance_criteria>
  <done>useHomeData() extrait dans Home.jsx, composant Home reduit en logique de rendu</done>
</task>

</tasks>

<verification>
Depuis C:/Users/smalt/Desktop/app-chien-ia/pawcoach/src :

1. ls lib/DogContext.jsx
   Attendu : fichier present

2. grep "DogProvider" App.jsx | wc -l
   Attendu : >= 2

3. grep "function useHomeData" pages/Home.jsx
   Attendu : 1 ligne

4. grep "export.*DogProvider\|export.*useDog" lib/DogContext.jsx
   Attendu : 2 lignes
</verification>

<success_criteria>
- DogContext.jsx cree avec DogProvider + useDog() hook
- App.jsx monte DogProvider dans l'arbre
- Home.jsx a useHomeData() comme hook interne
- Aucune regression : Home charge toujours les donnees du chien actif
</success_criteria>

<output>
Apres completion, creer `.planning/phases/10-performance-cleanup/10-02-SUMMARY.md`
</output>
