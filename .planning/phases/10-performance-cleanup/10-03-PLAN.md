---
phase: 10-performance-cleanup
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - src/pages/Nutri.jsx
autonomous: true
requirements: [FIX-64]
must_haves:
  truths:
    - "Nutri.jsx n'a plus de shorthand setters (18 fonctions boilerplate supprimees)"
    - "coachState est gere par useReducer avec des actions typees"
    - "dogDataState peut etre migre vers useReducer si le pattern est coherent"
  artifacts:
    - path: "src/pages/Nutri.jsx"
      provides: "Nutri avec useReducer pour coachState"
      contains: "useReducer"
  key_links:
    - from: "Nutri.jsx coachReducer"
      to: "setMessages, setLoading, setInput..."
      via: "dispatch({ type: 'SET_MESSAGES', payload: ... })"
      pattern: "dispatch\\("
---

<objective>
Supprimer les 18 shorthand setters boilerplate dans Nutri.jsx et migrer coachState vers useReducer.

Purpose: Nutri.jsx a 10 shorthand setters pour dogDataState et 10 pour coachState (lignes 71-104), soit ~50 lignes de boilerplate. Ce pattern cree une friction pour chaque ajout de champ. useReducer pour coachState (la conversation IA) est particulierement adapte car ses transitions sont liees a des actions metier claires (SEND_MESSAGE, RECEIVE_MESSAGE, SET_LOADING, etc.).

Output:
- Nutri.jsx : coachState migre vers useReducer, shorthand setters coachState supprimes
- dogDataState : shorthand setters remplaces par setDogDataState(p => ({...p, field: v})) inline aux callsites
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- coachState actuel dans Nutri.jsx (lignes 83-93) -->
<!--
{
  messages: [],
  input: "",
  loading: false,
  messagesRemaining: null,
  bookmarked: {},
  isStreaming: false,
  streamingText: "",
  showScrollBtn: false,
  lastFailedInput: null,
}
-->

<!-- dogDataState actuel dans Nutri.jsx (lignes 57-68) -->
<!--
{
  dog: null, user: null, recentScans: [], dietPrefs: null,
  checkins: [], healthRecords: [], dailyLogs: [],
  activePlan: null, monthlyPlanCount: 0, allPlans: [],
}
-->

<!-- Shorthand setters coachState a supprimer (lignes 96-104) -->
<!-- setMessages, setInput, setLoading, setMessagesRemaining, setBookmarked -->
<!-- setIsStreaming, setStreamingText, setShowScrollBtn, setLastFailedInput -->

<!-- DEPENDS ON 10-02 : verifier que Home.jsx a ete refactorise avant cette tache -->
<!-- car le pattern useReducer/hook peut s'inspirer de useHomeData() si cree -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrer coachState vers useReducer</name>
  <files>src/pages/Nutri.jsx</files>
  <read_first>src/pages/Nutri.jsx</read_first>
  <action>
Lire Nutri.jsx entier avant de modifier. Identifier tous les callsites des shorthand setters coachState.

Etape 1 — Creer le reducer coachState AVANT le composant Nutri() :

```javascript
const COACH_INITIAL_STATE = {
  messages: [],
  input: "",
  loading: false,
  messagesRemaining: null,
  bookmarked: {},
  isStreaming: false,
  streamingText: "",
  showScrollBtn: false,
  lastFailedInput: null,
};

function coachReducer(state, action) {
  switch (action.type) {
    case "SET_MESSAGES": return { ...state, messages: typeof action.payload === "function" ? action.payload(state.messages) : action.payload };
    case "SET_INPUT": return { ...state, input: action.payload };
    case "SET_LOADING": return { ...state, loading: action.payload };
    case "SET_MESSAGES_REMAINING": return { ...state, messagesRemaining: action.payload };
    case "SET_BOOKMARKED": return { ...state, bookmarked: typeof action.payload === "function" ? action.payload(state.bookmarked) : action.payload };
    case "SET_STREAMING": return { ...state, isStreaming: action.payload };
    case "SET_STREAMING_TEXT": return { ...state, streamingText: action.payload };
    case "SET_SHOW_SCROLL_BTN": return { ...state, showScrollBtn: action.payload };
    case "SET_LAST_FAILED_INPUT": return { ...state, lastFailedInput: action.payload };
    case "RESET_COACH": return { ...COACH_INITIAL_STATE };
    default: return state;
  }
}
```

Etape 2 — Dans le composant Nutri(), remplacer :
```javascript
const [coachState, setCoachState] = useState({ ... });
```
par :
```javascript
const [coachState, dispatchCoach] = useReducer(coachReducer, COACH_INITIAL_STATE);
```

Etape 3 — Supprimer les 9 shorthand setters coachState (lignes 96-104) : setMessages, setInput, setLoading, setMessagesRemaining, setBookmarked, setIsStreaming, setStreamingText, setShowScrollBtn, setLastFailedInput.

Etape 4 — Remplacer tous les callsites dans Nutri.jsx :
- `setMessages(v)` → `dispatchCoach({ type: "SET_MESSAGES", payload: v })`
- `setInput(v)` → `dispatchCoach({ type: "SET_INPUT", payload: v })`
- `setLoading(v)` → `dispatchCoach({ type: "SET_LOADING", payload: v })`
- `setMessagesRemaining(v)` → `dispatchCoach({ type: "SET_MESSAGES_REMAINING", payload: v })`
- `setBookmarked(v)` → `dispatchCoach({ type: "SET_BOOKMARKED", payload: v })`
- `setIsStreaming(v)` → `dispatchCoach({ type: "SET_STREAMING", payload: v })`
- `setStreamingText(v)` → `dispatchCoach({ type: "SET_STREAMING_TEXT", payload: v })`
- `setShowScrollBtn(v)` → `dispatchCoach({ type: "SET_SHOW_SCROLL_BTN", payload: v })`
- `setLastFailedInput(v)` → `dispatchCoach({ type: "SET_LAST_FAILED_INPUT", payload: v })`

ATTENTION : les shorthand setters supportaient les functional updates (ex: `setMessages(prev => [...prev, msg])`). Le reducer gere ce cas avec `typeof action.payload === "function" ? action.payload(state.messages) : action.payload` pour SET_MESSAGES et SET_BOOKMARKED. Pour les autres, verifier si des functional updates existent dans les callsites — si oui, ajouter le meme pattern dans le case correspondant.

Etape 5 — Pour dogDataState, NE PAS migrer vers useReducer (trop de changements pour ce plan). A la place, remplacer les shorthand setters par des setters inline directs aux callsites :
- `setDog(v)` → `setDogDataState(p => ({ ...p, dog: typeof v === "function" ? v(p.dog) : v }))`
- Idem pour setUser, setRecentScans, setDietPrefs, setCheckins, setHealthRecords, setDailyLogs, setActivePlan, setMonthlyPlanCount, setAllPlans

Chercher chaque callsite de ces setters dans le fichier et remplacer inline.
Apres remplacement, supprimer les definitions des shorthand setters dogDataState (lignes 71-80).

IMPORTANT : Ne modifier aucune logique metier. Ne pas toucher au JSX de rendu. Ne pas changer l'ordre de deconstruction `const { dog, user, ... } = dogDataState;`.
  </action>
  <verify>
    <automated>grep -c "useReducer" src/pages/Nutri.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "useReducer" src/pages/Nutri.jsx retourne exactement 1 (import compte pas, c'est l'appel)
    - grep "const setMessages\|const setInput\|const setLoading\|const setIsStreaming\|const setStreamingText\|const setMessagesRemaining\|const setBookmarked\|const setShowScrollBtn\|const setLastFailedInput" src/pages/Nutri.jsx retourne 0
    - grep "const setDog \|const setUser \|const setRecentScans\|const setDietPrefs\|const setCheckins\|const setHealthRecords\|const setDailyLogs\|const setActivePlan\|const setMonthlyPlanCount\|const setAllPlans" src/pages/Nutri.jsx retourne 0
    - grep "dispatchCoach" src/pages/Nutri.jsx retourne >= 9 lignes (9 types d'actions au minimum)
    - grep "coachReducer" src/pages/Nutri.jsx retourne 2 (definition + usage dans useReducer)
  </acceptance_criteria>
  <done>Nutri.jsx sans shorthand setters, coachState gere par useReducer avec actions typees</done>
</task>

</tasks>

<verification>
Depuis C:/Users/smalt/Desktop/app-chien-ia/pawcoach/src :

1. grep -c "const set" pages/Nutri.jsx
   Attendu : significativement moins qu'avant (etait ~20, cible < 5)

2. grep "coachReducer\|dispatchCoach" pages/Nutri.jsx | head -5
   Attendu : lignes presentes

3. git diff --stat HEAD -- src/pages/Nutri.jsx
   Attendu : ~80-100 lignes modifiees (suppressions + remplacements)
</verification>

<success_criteria>
- 18 shorthand setters supprimes de Nutri.jsx
- coachState gere par useReducer avec 9 action types
- dogDataState setters inline aux callsites (pas de fonctions intermediaires)
- Le composant Nutri fonctionne identiquement (meme UX, meme logique)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/10-performance-cleanup/10-03-SUMMARY.md`
</output>
