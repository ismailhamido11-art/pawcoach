---
phase: 10-performance-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - base44/functions/vetAccess/entry.ts
  - src/pages/VetPortal.jsx
  - src/utils/analytics.js
  - src/utils/ai-credits.js
  - src/pages/Premium.jsx
  - src/pages/Scan.jsx
  - src/pages/Onboarding.jsx
autonomous: true
requirements: [FIX-65, FIX-66, FIX-67, FIX-68]
must_haves:
  truths:
    - "VetPortal charge tous les chiens patients en 1 roundtrip backend au lieu de N"
    - "verdictFr dans pawcoachChat est verifie — supprime si inutilise globalement, documente si utilise localement"
    - "Les helpers sanitize/validateImageUrl dans les backends sont documentes comme intentionnellement locaux"
    - "trackEvent() et analytics.js supprimes — zero reference dans src/"
  artifacts:
    - path: "base44/functions/vetAccess/entry.ts"
      provides: "action listMyPatients (batch dog fetch)"
      contains: "listMyPatients"
    - path: "src/pages/VetPortal.jsx"
      provides: "VetPortal avec appel batch listMyPatients"
      contains: "listMyPatients"
  key_links:
    - from: "VetPortal.loadAccesses"
      to: "vetAccess listMyPatients"
      via: "base44.functions.invoke('vetAccess', { action: 'listMyPatients' })"
      pattern: "listMyPatients"
    - from: "src/pages/Premium.jsx"
      to: "analytics.js"
      via: "import supprime"
      pattern: "trackEvent"
---

<objective>
Quatre fixes de nettoyage independants : batch query VetPortal, verification dead code verdictFr/sanitize, et suppression du systeme analytics localStorage inutile.

Purpose:
- FIX-65 : VetPortal fait N appels getDogData sequentiels (un par patient). Un 'listMyPatients' backend retourne accesses + dogs en une seule requete.
- FIX-66 : verdictFr dans pawcoachChat est flaggue dead par CGC mais utilise dans le scope local (nutritionMemory). Verifier et documenter.
- FIX-67 : sanitize et validateImageUrl dans 9+ backends sont intentionnellement locaux (contrainte Deno). Documenter pour eviter de futurs faux positifs CGC.
- FIX-68 : analytics.js est un placeholder localStorage qui ne genere aucun insight. La suppression est plus propre qu'une integration prematured'un vrai service.

Output:
- vetAccess : action 'listMyPatients' ajoutee
- VetPortal.jsx : loadAccesses migre vers listMyPatients
- pawcoachChat : verdictFr annote ou supprime
- backends : commentaire sync sur sanitize/validateImageUrl
- analytics.js supprime + 4 fichiers importateurs nettoyes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- VetPortal.loadAccesses ACTUEL (N+1 pattern) -->
<!--
const loadAccesses = async () => {
  const res = await base44.functions.invoke("vetAccess", { action: "listMyAccess" });
  const accessList = res.data.accesses || [];
  setAccesses(accessList);
  const dogPromises = accessList.map(async (a) => {
    const dogRes = await base44.functions.invoke("vetAccess", { action: "getDogData", dogId: a.dog_id });
    return { ...dogRes.data.dog, _accessId: a.id };
  });
  const dogResults = await Promise.all(dogPromises);
  setDogs(dogResults.filter(Boolean));
};
-->

<!-- vetAccess backend — action 'listMyAccess' existante (ligne 212) -->
<!--
if (action === 'listMyAccess') {
  const accesses = await base44.asServiceRole.entities.SharedVetAccess.filter({ vet_email: user.email, status: 'active' });
  return Response.json({ accesses: accesses || [] });
}
-->

<!-- trackEvent usage dans 4 fichiers -->
<!-- ai-credits.js: trackEvent("daily_limit_reached", ...) aux lignes 63, 80 -->
<!-- Premium.jsx: trackEvent("premium_page_viewed", ...) et trackEvent("premium_checkout_clicked", ...) -->
<!-- Scan.jsx: trackEvent("scan_completed", ...) -->
<!-- Onboarding.jsx: trackEvent("onboarding_complete", ...) + setAnalyticsConsent() -->

<!-- analytics.js exports: trackEvent, getEvents, setAnalyticsConsent, hasAnalyticsConsent -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter listMyPatients dans vetAccess + migrer VetPortal</name>
  <files>base44/functions/vetAccess/entry.ts, src/pages/VetPortal.jsx</files>
  <read_first>
    base44/functions/vetAccess/entry.ts (offset 210, limit 30)
    src/pages/VetPortal.jsx
  </read_first>
  <action>
Dans base44/functions/vetAccess/entry.ts, ajouter une nouvelle action 'listMyPatients' apres l'action 'listMyAccess' existante (ligne ~212) :

```typescript
// --- VET: List dogs shared with me (batch — replaces N+1 getDogData calls) ---
if (action === 'listMyPatients') {
  const accesses = await base44.asServiceRole.entities.SharedVetAccess.filter({ vet_email: user.email, status: 'active' });
  if (!accesses || accesses.length === 0) return Response.json({ accesses: [], dogs: [] });

  // Fetch all dogs in parallel (one query per dog, but parallel not sequential)
  const dogResults = await Promise.all(
    accesses.map(async (a) => {
      try {
        const dogs = await base44.asServiceRole.entities.Dog.filter({ id: a.dog_id });
        if (!dogs || dogs.length === 0) return null;
        return { ...dogs[0], _accessId: a.id };
      } catch {
        return null;
      }
    })
  );

  return Response.json({
    accesses,
    dogs: dogResults.filter(Boolean),
  });
}
```

Cette action retourne `{ accesses: SharedVetAccess[], dogs: Dog[] }` en un seul appel backend.

Dans src/pages/VetPortal.jsx, remplacer la fonction loadAccesses pour utiliser listMyPatients :

```javascript
const loadAccesses = async () => {
  try {
    const res = await base44.functions.invoke("vetAccess", { action: "listMyPatients" });
    const { accesses: accessList, dogs: dogList } = res.data;
    setAccesses(accessList || []);
    setDogs((dogList || []).filter(Boolean));
  } catch (e) {
    console.error("loadAccesses error:", e);
    toast.error("Impossible de charger tes patients. Vérifie ta connexion.");
  }
};
```

NE PAS supprimer l'action 'listMyAccess' du backend — elle peut etre utilisee ailleurs. Juste ajouter 'listMyPatients' en complement.
  </action>
  <verify>
    <automated>grep -n "listMyPatients" base44/functions/vetAccess/entry.ts src/pages/VetPortal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "listMyPatients" vetAccess/entry.ts retourne 1 ligne (definition du case)
    - grep "listMyPatients" src/pages/VetPortal.jsx retourne 1 ligne (appel dans loadAccesses)
    - VetPortal.jsx n'a plus de Promise.all sur getDogData (grep "getDogData" src/pages/VetPortal.jsx retourne 0)
    - L'action listMyAccess est toujours presente dans vetAccess/entry.ts (grep "listMyAccess" retourne >= 2)
  </acceptance_criteria>
  <done>VetPortal charge tous ses patients en 1 roundtrip backend au lieu de N</done>
</task>

<task type="auto">
  <name>Task 2: Verifier verdictFr et annoter sanitize/validateImageUrl</name>
  <files>base44/functions/pawcoachChat/entry.ts</files>
  <read_first>
    base44/functions/pawcoachChat/entry.ts (offset 218, limit 15)
  </read_first>
  <action>
VERIFICATION verdictFr (FIX-66) :

Dans pawcoachChat/entry.ts, localiser verdictFr (ligne ~222). C'est une arrow function inline :
```typescript
const verdictFr = (v: string) => v === "safe" ? "sur" : v === "caution" ? "a surveiller" : "TOXIQUE";
```

Verifier si elle est appelee dans le meme fichier. Elle EST utilisee : `nutritionMemory += ... recentScans.map(s => ... verdictFr(s.verdict) ...) ...` — CGC la declare dead car elle n'est pas exportee et pas detectee comme "caller" de l'exterieur, mais elle est utilisee localement.

Ajouter un commentaire au-dessus de la definition :
```typescript
// verdictFr: utilisee localement dans nutritionMemory — CGC false positive (scope local)
```

ANNOTATION sanitize/validateImageUrl (FIX-67) :

Dans les backends suivants, localiser la definition de sanitize() et ajouter un commentaire au-dessus :
- base44/functions/pawcoachChat/entry.ts (ligne ~24)
- base44/functions/preDiagnosis/entry.ts
- base44/functions/processHealthInput/entry.ts

Le commentaire type a ajouter :
```typescript
// sanitize v1 — injection guard — copie locale intentionnelle (Deno: pas d'import cross-function)
// Logique: substring + strip <> — si modifie, mettre a jour toutes les copies
```

Pour validateImageUrl dans preDiagnosis/entry.ts et processHealthInput/entry.ts, ajouter :
```typescript
// validateImageUrl v1 — SSRF guard — copie locale intentionnelle (Deno: pas d'import cross-function)
// Allowlist: ['base44.app', 'amazonaws.com', 's3.amazonaws.com'] — si modifie, mettre a jour toutes les copies
```

IMPORTANT : Ne modifier que les commentaires. Ne toucher a aucune logique.
  </action>
  <verify>
    <automated>grep -n "CGC false positive\|copie locale intentionnelle" base44/functions/pawcoachChat/entry.ts base44/functions/preDiagnosis/entry.ts base44/functions/processHealthInput/entry.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "CGC false positive" pawcoachChat/entry.ts retourne 1 ligne
    - grep "copie locale intentionnelle" preDiagnosis/entry.ts retourne >= 1 ligne
    - grep "copie locale intentionnelle" processHealthInput/entry.ts retourne >= 1 ligne
    - verdictFr est toujours presente dans pawcoachChat/entry.ts (pas supprimee)
  </acceptance_criteria>
  <done>verdictFr documente comme false positive CGC, sanitize/validateImageUrl annotes comme copies intentionnelles</done>
</task>

<task type="auto">
  <name>Task 3: Supprimer le systeme analytics localStorage</name>
  <files>src/utils/analytics.js, src/utils/ai-credits.js, src/pages/Premium.jsx, src/pages/Scan.jsx, src/pages/Onboarding.jsx</files>
  <read_first>
    src/utils/ai-credits.js
    src/pages/Premium.jsx (offset 1, limit 30)
    src/pages/Scan.jsx (offset 1, limit 10)
    src/pages/Onboarding.jsx (offset 1, limit 10)
  </read_first>
  <action>
Supprimer src/utils/analytics.js entier (delete le fichier).

Dans chaque fichier qui importe de analytics.js :

**src/utils/ai-credits.js** :
- Supprimer la ligne d'import : `import { trackEvent } from "@/utils/analytics";`
- Supprimer les 2 appels trackEvent :
  - Ligne ~63 : `if (newRemaining === 0) trackEvent("daily_limit_reached", { type: "message" });`
  - Ligne ~80 : `if (newRemaining === 0) trackEvent("daily_limit_reached", { type: "action" });`

**src/pages/Premium.jsx** :
- Supprimer la ligne d'import trackEvent
- Supprimer l'appel `trackEvent("premium_page_viewed", ...)` dans useEffect
- Supprimer l'appel `trackEvent("premium_checkout_clicked", ...)` dans handleCheckout

**src/pages/Scan.jsx** :
- Supprimer la ligne d'import trackEvent
- Supprimer l'appel `trackEvent("scan_completed", ...)`

**src/pages/Onboarding.jsx** :
- Supprimer la ligne d'import `import { trackEvent, setAnalyticsConsent } from "@/utils/analytics";`
- Supprimer l'appel `trackEvent("onboarding_complete", ...)`
- Supprimer l'appel `setAnalyticsConsent(true)` si present dans le flux onboarding

ATTENTION pour Onboarding.jsx :
- Lire la section complete autour de setAnalyticsConsent avant de supprimer
- Si setAnalyticsConsent est appelee dans le flow RGPD consent (checkbox), supprimer le call mais NE PAS supprimer la checkbox RGPD elle-meme
- La checkbox consent RGPD (FIX-04) doit rester — elle ne doit juste plus appeler setAnalyticsConsent

ATTENTION pour ai-credits.js :
- La logique de decrement des credits doit rester intacte
- Seule la ligne trackEvent dans le if doit etre supprimee
- Ne pas toucher au reste de la logique

Apres suppression, verifier avec grep :
```bash
grep -r "analytics" src/ --include="*.js" --include="*.jsx"
```
Attendu : 0 resultats.
  </action>
  <verify>
    <automated>grep -r "trackEvent\|analytics.js\|from.*analytics" src/ --include="*.js" --include="*.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep "trackEvent" src/ retourne 0 resultats
    - grep "from.*analytics" src/ retourne 0 resultats
    - src/utils/analytics.js n'existe plus (ls retourne erreur)
    - src/utils/ai-credits.js compile sans erreur (import supprime, logique credits intacte)
    - La checkbox RGPD consent dans Onboarding.jsx est toujours presente (grep "consentement\|politique de confidentialite" Onboarding.jsx retourne >= 1)
  </acceptance_criteria>
  <done>analytics.js supprime, zero reference trackEvent dans src/ — code base propre</done>
</task>

</tasks>

<verification>
Depuis C:/Users/smalt/Desktop/app-chien-ia/pawcoach :

1. grep "listMyPatients" base44/functions/vetAccess/entry.ts src/pages/VetPortal.jsx
   Attendu : 2 lignes

2. grep -r "trackEvent" src/
   Attendu : 0 resultats

3. ls src/utils/analytics.js 2>/dev/null || echo "deleted"
   Attendu : "deleted"

4. grep "CGC false positive" base44/functions/pawcoachChat/entry.ts
   Attendu : 1 ligne
</verification>

<success_criteria>
- VetPortal : 1 roundtrip backend au lieu de N (listMyPatients)
- verdictFr documente, sanitize/validateImageUrl annotes
- analytics.js supprime et zero reference dans src/
- Aucune regression UX : Onboarding garde la checkbox RGPD, ai-credits garde la logique de decrement
</success_criteria>

<output>
Apres completion, creer `.planning/phases/10-performance-cleanup/10-04-SUMMARY.md`
</output>
