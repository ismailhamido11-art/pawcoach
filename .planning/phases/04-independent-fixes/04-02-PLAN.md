---
phase: 04-independent-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nutrition/NutritionMealPlan.jsx
autonomous: true
requirements: [NUTRI-01]

must_haves:
  truths:
    - "Quand l'utilisateur genere un plan 3 repas/jour, le JSON retourne bien morning + noon + evening pour chaque jour"
    - "NutritionMealPlan affiche la carte midi dans l'UI expanded quand todayMeal.noon existe"
    - "ActiveProgramCards affiche le repas midi quand todayData.noon existe"
  artifacts:
    - path: "src/components/nutrition/NutritionMealPlan.jsx"
      provides: "Prompt avec noon + affichage UI midi"
      contains: "noon"
  key_links:
    - from: "src/components/nutrition/NutritionMealPlan.jsx"
      to: "prompt LLM"
      via: "template JSON avec noon entre morning et evening"
      pattern: "noon"
---

<objective>
Corriger la generation de plan nutritionnel 3 repas/jour : le template JSON dans le prompt ne contient que morning/evening, donc le LLM ne genere jamais de repas midi. Ajouter noon au template et a l'affichage UI.

Purpose: Quand DietPreferences.portions_per_day = 3, ou quand l'utilisateur demande 3 repas, le plan genere une carte midi visible.
Output: NutritionMealPlan.jsx avec noon dans le prompt et dans le rendu UI.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/nutrition/NutritionMealPlan.jsx
@src/components/home/ActiveProgramCards.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter noon au prompt LLM et au rendu UI de NutritionMealPlan</name>
  <files>src/components/nutrition/NutritionMealPlan.jsx</files>
  <action>
**Partie 1 — Prompt LLM (vers ligne 260-263)**

Dans le template JSON du prompt (la structure "Structure exacte"), modifier l'exemple de jour pour inclure `noon` entre `morning` et `evening` :

```json
{
  "day": "Lundi",
  "morning": { "food": "Croquettes + huile de saumon", "quantity": "145g + 1 c.c." },
  "noon": { "food": "Crostille ou friandise faible en calories", "quantity": "10g" },
  "evening": { "food": "Croquettes + courgette cuite", "quantity": "95g + 30g" }
}
```

Dans les REGLES du prompt, ajouter apres "Le tableau 'days' doit contenir exactement 7 jours" :
```
- Si le chien mange 3 fois par jour (portions_per_day >= 3 ou précisé dans les préférences), inclure un champ "noon" pour le repas de midi. Sinon, omets le champ "noon".
```

La variable `portions` est deja disponible dans le scope (ligne ~180 : `const portions = dietPrefs.portions_per_day || 2`). Utiliser cette information dans le prompt via `prefsContext` deja construit — la valeur "Repas par jour: X" est deja injectee dans le prompt. Aucune logique supplementaire a coder : le LLM lit la preference et adapte.

**Partie 2 — Affichage UI dans NutritionMealPlan (todayMeal section, vers ligne 360-380)**

Dans la section "today's meals expanded" du plan actif, entre le bloc `{todayMeal.morning && ...}` et `{todayMeal.evening && ...}`, ajouter :

```jsx
{todayMeal.noon && (
  <div className="flex items-start gap-3">
    <span className="text-lg leading-none">☀️</span>
    <div>
      <p className="text-xs font-bold text-amber-600 mb-0.5">Midi</p>
      <p className="text-sm font-medium text-foreground">{todayMeal.noon.food}</p>
      <p className="text-xs text-muted-foreground">{todayMeal.noon.quantity}</p>
    </div>
  </div>
)}
```

**Partie 3 — Affichage dans la vue semaine complete (vers ligne 407-425)**

Dans la boucle `allPlans` qui affiche tous les jours (`planData.days.map(...)`), entre `{d.morning && ...}` et `{d.evening && ...}`, ajouter :

```jsx
{d.noon && (
  <div className="flex items-center gap-1.5 mt-0.5">
    <span className="text-[10px] font-bold text-amber-600 bg-amber-50 px-1.5 py-0.5 rounded flex-shrink-0">Midi</span>
    <div>
      <p className="text-xs text-foreground">{d.noon.food}</p>
      <p className="text-[10px] text-muted-foreground">{d.noon.quantity}</p>
    </div>
  </div>
)}
```

Faire de meme dans la section "historique" (vers ligne 640-660) si elle a le meme pattern d'affichage morning/evening.

**Partie 4 — Compatibilite : dans previousPlanContext (vers ligne 236)**

La ligne qui construit `prevFoods` concatene seulement `d.morning?.food` et `d.evening?.food`. Ajouter noon pour eviter la repetition :
```js
const prevFoods = prevData.days.map(d =>
  [d.morning?.food, d.noon?.food, d.evening?.food].filter(Boolean).join(", ")
).join("; ");
```

NOTE : tous ces changements sont conditionnes par `todayMeal.noon` / `d.noon` — si le plan a ete genere en 2 repas, noon n'existe pas dans le JSON et ces blocs ne s'affichent pas. Pas de regression pour les plans existants.
  </action>
  <verify>grep -n "noon" src/components/nutrition/NutritionMealPlan.jsx | head -20</verify>
  <done>NutritionMealPlan.jsx contient "noon" dans le template prompt et dans au moins 2 blocs d'affichage UI. Les plans 2 repas existants ne sont pas affectes (noon est optionnel).</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter l'affichage noon dans ActiveProgramCards (NutritionPlanCard)</name>
  <files>src/components/home/ActiveProgramCards.jsx</files>
  <action>
Dans `NutritionPlanCard` (fonction vers ligne 170), dans la section expanded (`open && todayData`), entre le bloc `{todayData.morning && ...}` (ligne ~303) et `{todayData.evening && ...}` (ligne ~312), ajouter :

```jsx
{todayData.noon && (
  <div className="flex items-start gap-2 ml-7">
    <span className="text-[10px] font-bold text-amber-600 bg-amber-50 px-1.5 py-0.5 rounded mt-0.5 flex-shrink-0">Midi</span>
    <div>
      <p className="text-[12px] text-foreground/80">{todayData.noon.food}</p>
      <p className="text-[10px] text-muted-foreground">{todayData.noon.quantity}</p>
    </div>
  </div>
)}
```

Ce bloc est conditionnel — si le plan n'a pas de repas midi, il n'affiche rien. Les plans existants (2 repas) ne sont pas touches.
  </action>
  <verify>grep -n "noon" src/components/home/ActiveProgramCards.jsx</verify>
  <done>ActiveProgramCards.jsx affiche le repas midi quand todayData.noon existe, avec le meme style que morning/evening.</done>
</task>

</tasks>

<verification>
```bash
grep -c "noon" src/components/nutrition/NutritionMealPlan.jsx
grep -c "noon" src/components/home/ActiveProgramCards.jsx
```
Chaque commande doit retourner >= 2.
</verification>

<success_criteria>
- Le prompt LLM contient "noon" dans le template JSON et dans les regles
- L'UI de NutritionMealPlan affiche "Midi" quand todayMeal.noon existe
- L'UI de ActiveProgramCards affiche "Midi" quand todayData.noon existe
- Aucun affichage "Midi" pour les plans generes en 2 repas (noon est absent du JSON)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/04-independent-fixes/04-02-SUMMARY.md`
</output>
