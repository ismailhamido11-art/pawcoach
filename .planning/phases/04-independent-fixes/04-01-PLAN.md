---
phase: 04-independent-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/SmartAlerts.jsx
  - src/components/tracker/WalkMode.jsx
autonomous: true
requirements: [DASH-01, ACT-01]

must_haves:
  truths:
    - "SmartAlerts affiche une alerte warning ou critical quand l'appetit moyen des 7 derniers jours baisse vs les 7 precedents"
    - "Une balade terminee dans WalkMode met a jour le streak (appelle updateStreakSilently)"
  artifacts:
    - path: "src/components/dashboard/SmartAlerts.jsx"
      provides: "Alerte tendance appetit dans computeAlerts"
      contains: "appetite_drop"
    - path: "src/components/tracker/WalkMode.jsx"
      provides: "Appel updateStreakSilently apres save DailyLog"
      contains: "updateStreakSilently"
  key_links:
    - from: "src/components/dashboard/SmartAlerts.jsx"
      to: "computeAlerts section 1"
      via: "calcul avgAppetiteLast + avgAppetitePrev"
      pattern: "avgAppetiteLast"
    - from: "src/components/tracker/WalkMode.jsx"
      to: "streakHelper.updateStreakSilently"
      via: "import + appel apres checkWalkBadges"
      pattern: "updateStreakSilently"
---

<objective>
Corriger deux comportements independants : (1) les alertes intelligentes ignorent l'appetit alors qu'elles analysent mood et energy ; (2) les balades enregistrees via WalkMode ne maintiennent pas le streak alors que les check-ins le font.

Purpose: L'utilisateur qui balade son chien mais oublie le check-in ne perd pas son streak. L'utilisateur voit une alerte quand l'appetit de son chien decline.
Output: SmartAlerts.jsx avec detection appetite, WalkMode.jsx avec appel streak.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/dashboard/SmartAlerts.jsx
@src/components/tracker/WalkMode.jsx
@src/components/streakHelper.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter la detection de tendance appetit dans SmartAlerts</name>
  <files>src/components/dashboard/SmartAlerts.jsx</files>
  <action>
Dans la fonction `computeAlerts`, apres le bloc "1. TENDANCE VITALITE" (lignes 30-88), ajouter un bloc "1b. TENDANCE APPETIT" distinct.

L'appetit dans DailyCheckin est une string : "normal", "increased", "decreased", "none".
Mapper ces valeurs en score numerique : `{ normal: 2, increased: 3, decreased: 1, none: 0 }` (valeur par defaut : 2).

Calculer `avgAppetiteLast` sur les 7 derniers check-ins et `avgAppetitePrev` sur les 7 precedents (en utilisant `last7` et `prev7` deja calcules dans le bloc 1).

Seuils (meme logique que mood/energy) :
- `appetiteDrop >= 1.0` → alerte `critical`, id `appetite_drop_critical`, titre "Appétit en forte baisse", desc mentionnant le nombre de pts de chute, CTA "Faire un check-in" vers Home
- `appetiteDrop >= 0.5` → alerte `warning`, id `appetite_drop_warning`, titre "Baisse d'appétit détectée", desc avec nom du chien, CTA "Check-in" vers Home
- N'ajouter une alerte ok "Bon appétit" que si `avgAppetiteLast >= 2.5` ET aucune alerte vitality_drop n'est deja critique (eviter le spam d'alertes ok).

Placer ce bloc apres le bloc vitality (avant le bloc vaccins). Importer `UtensilsCrossed` de lucide-react (icone appetite).

ATTENTION : la variable `sorted`, `last7`, `prev7` sont deja declarees dans le bloc 1 — les reutiliser directement, ne pas les redeclarer.

Condition de garde identique au bloc 1 : `if (last7.length >= 3 && prev7.length >= 3)`.
  </action>
  <verify>grep -n "appetite_drop\|avgAppetiteLast\|UtensilsCrossed" src/components/dashboard/SmartAlerts.jsx</verify>
  <done>SmartAlerts.jsx contient les deux alertes appetite (critical + warning) dans computeAlerts, avec les bons ids et seuils. Le bloc est place entre "TENDANCE VITALITE" et "PREDICTION VACCINS".</done>
</task>

<task type="auto">
  <name>Task 2: Appeler updateStreakSilently apres save d'une balade dans WalkMode</name>
  <files>src/components/tracker/WalkMode.jsx</files>
  <action>
1. Ajouter l'import en tete de fichier :
   ```
   import { updateStreakSilently } from "@/components/streakHelper";
   ```

2. Dans la fonction `handleStop` (vers ligne 304), apres l'appel `checkWalkBadges` (ligne ~349) et avant `onLogged?.()`, ajouter :
   ```
   updateStreakSilently(dog.id, user?.email).catch(() => {});
   ```

La logique `updateStreakSilently` inclut deja un guard "Already updated today" (dedup sur `last_activity_date === today`) — si le check-in a deja mis a jour le streak aujourd'hui, l'appel sera un no-op. Pas de double-comptage possible.

Le pattern exact depuis Training.jsx (qui fait deja la meme chose) :
```js
updateStreakSilently(dog.id, user.email).catch(() => {});
```

Verifier que `dog.id` et `user?.email` sont disponibles dans le scope (ils le sont : ligne 304 du code original verifie deja `!dog?.id || !user?.email`).

Ne pas toucher la logique de sauvegarde DailyLog existante ni les appels checkWalkBadges.
  </action>
  <verify>grep -n "updateStreakSilently" src/components/tracker/WalkMode.jsx</verify>
  <done>WalkMode.jsx importe et appelle updateStreakSilently apres checkWalkBadges dans handleStop. L'import est present en tete de fichier.</done>
</task>

</tasks>

<verification>
```bash
grep -n "appetite_drop_critical\|appetite_drop_warning\|avgAppetiteLast" src/components/dashboard/SmartAlerts.jsx
grep -n "updateStreakSilently" src/components/tracker/WalkMode.jsx
```
Les deux commandes doivent retourner au moins une ligne chacune.
</verification>

<success_criteria>
- SmartAlerts.jsx : `computeAlerts` contient le bloc appetit avec les ids `appetite_drop_critical` et `appetite_drop_warning`, seuils 1.0 et 0.5, icone UtensilsCrossed
- WalkMode.jsx : `import { updateStreakSilently }` present + appel apres checkWalkBadges dans handleStop
- Aucune regression sur les alertes mood/energy existantes (blocs 1-5 inchanges)
- Aucune regression sur la sauvegarde DailyLog de WalkMode
</success_criteria>

<output>
Apres completion, creer `.planning/phases/04-independent-fixes/04-01-SUMMARY.md`
</output>
