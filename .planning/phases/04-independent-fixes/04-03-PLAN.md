---
phase: 04-independent-fixes
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - src/components/home/ActiveProgramCards.jsx
autonomous: true
requirements: [ACT-02]

must_haves:
  truths:
    - "BehaviorProgramCard affiche quels jours sont completes (comme TrainingCard avec completed_days)"
    - "L'utilisateur peut marquer le jour courant comme fait depuis BehaviorProgramCard"
    - "La barre de progression behavior reflete les jours reellement completes, pas juste le jour ecoule"
    - "Quand l'utilisateur clique Marquer comme fait, le jour passe immediatement en vert sans attendre un refresh de page"
  artifacts:
    - path: "src/components/home/ActiveProgramCards.jsx"
      provides: "BehaviorProgramCard avec completion tracking par jour"
      contains: "completedDays"
  key_links:
    - from: "src/components/home/ActiveProgramCards.jsx"
      to: "Bookmark entity"
      via: "base44.entities.Bookmark.update avec completed_days"
      pattern: "Bookmark.update"
---

<objective>
BehaviorProgramCard calcule sa progression sur les jours ecoules (time-based) mais ne tracke pas si les exercices ont ete reellement faits — contrairement a TrainingCard qui utilise `completed_days`. Ajouter le meme systeme de completion jour par jour aux programmes comportement.

Purpose: L'utilisateur voit quels jours de son programme comportement sont reellement completes (check vert) et peut marquer le jour courant comme fait. Le changement est visible immediatement (optimistic update), sans attendre un refresh.
Output: BehaviorProgramCard avec completed_days, bouton "Marquer comme fait", mise a jour optimiste locale, barre de progression sur jours completes.
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/home/ActiveProgramCards.jsx
@.planning/phases/04-independent-fixes/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Pattern de reference : TrainingCard (dans le meme fichier) utilise deja completed_days -->
<!-- Structure Bookmark.content (BehaviorProgram) :
{
  "start_date": "2026-03-10",
  "days": [ { "day_name": "Jour 1", "theme": "...", "exercises": [...] } ],
  "problem_label": "Aboiements",
  "problem_id": "barking",
  "summary": "..."
}
-->
<!-- completed_days est un array de strings ex: ["d0", "d1", "d3"] — meme convention que TrainingCard -->
<!-- Bookmark entity fields utilises : id, content, completed_days -->
<!-- base44.entities.Bookmark.update(id, { completed_days: [...] }) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter le tracking completed_days a BehaviorProgramCard</name>
  <files>src/components/home/ActiveProgramCards.jsx</files>
  <action>
Dans la fonction `BehaviorProgramCard({ program })`, le composant recoit `program` qui est le `data` parse du `bk.content`. Le probleme est que `program` ne contient pas `id` (Bookmark.id) — il faut aussi passer le `bookmarkId`.

**Etape 1 : Modifier ActiveProgramCards pour passer bookmarkId**

Dans `ActiveProgramCards` (fonction vers ligne 504), la variable `activeBehavior` est construite depuis `behaviorBookmarks`. Modifier pour stocker aussi l'id :

```js
const activeBehavior = useMemo(() => {
  for (const bk of behaviorBookmarks) {
    try {
      const data = JSON.parse(bk.content);
      if (data.start_date && data.days) {
        const elapsed = getElapsedDays(data.start_date);
        if (elapsed >= 0 && elapsed < 7) return { ...data, _bookmarkId: bk.id, _completedDays: bk.completed_days || [] };
      }
    } catch {}
  }
  return null;
}, [behaviorBookmarks]);
```

**Etape 2 : Modifier BehaviorProgramCard pour utiliser completed_days avec mise a jour optimiste**

Remplacer la barre de progression basee sur `elapsed` par une basee sur les jours completes, avec un `useState` local pour la mise a jour optimiste. L'etat local `localCompleted` est initialise a `null` (sentinel) : quand il est `null`, on lit `program._completedDays` ; apres un clic, il contient la valeur mise a jour immediatement, sans attendre un refresh :

```js
function BehaviorProgramCard({ program }) {
  const [open, setOpen] = useState(false);
  const [marking, setMarking] = useState(false);
  const [localCompleted, setLocalCompleted] = useState(null); // null = pas encore modifie localement
  const elapsed = getElapsedDays(program.start_date);

  if (elapsed < 0 || elapsed >= 7) return null;
  const dayIndex = Math.min(elapsed, 6);
  const day = program.days?.[dayIndex];
  if (!day) return null;

  // Optimistic: utiliser localCompleted si disponible, sinon la valeur du serveur
  const completedDays = localCompleted ?? (program._completedDays || []);
  const completedCount = completedDays.length;
  const progress = Math.round((completedCount / 7) * 100);
  const todayDone = completedDays.includes(`d${elapsed}`);

  const handleMarkDone = async () => {
    if (todayDone || !program._bookmarkId) return;
    setMarking(true);
    const newCompleted = [...completedDays, `d${elapsed}`];
    setLocalCompleted(newCompleted); // Optimistic update immediat
    try {
      await base44.entities.Bookmark.update(program._bookmarkId, { completed_days: newCompleted });
    } catch (e) {
      setLocalCompleted(null); // Rollback si erreur
      console.warn("Mark done failed:", e?.message);
    }
    setMarking(false);
  };
```

Ajouter l'import `base44` si pas encore present (verifier en tete de fichier). Si absent : `import { base44 } from "@/api/base44Client";`

**Etape 3 : Modifier l'affichage de la carte BehaviorProgramCard**

Dans la partie header (ferme), remplacer la progression basee sur `elapsed` par `completedCount` :
```
{progress}%  →  {completedCount}/7
```

Dans la partie expanded, apres les exercices et avant la progress bar finale, ajouter un bouton "Marquer comme fait" (comme TrainingCard le signale visuellement) :

```jsx
{!todayDone ? (
  <button
    onClick={handleMarkDone}
    disabled={marking}
    className="w-full mt-2 py-2 rounded-xl bg-blue-500 text-white text-xs font-bold hover:bg-blue-600 disabled:opacity-50 transition-colors"
  >
    {marking ? "Enregistrement..." : `Marquer Jour ${elapsed + 1} comme fait`}
  </button>
) : (
  <p className="text-[10px] text-blue-600 font-bold mt-1 flex items-center gap-1">
    <CheckCircle2 className="w-3 h-3" /> Jour {elapsed + 1} completé !
  </p>
)}
```

Importer `CheckCircle2` depuis lucide-react si pas deja importe (verifier les imports existants en tete du fichier).

Dans la progress bar fermee (compact view), remplacer `progress` par `completedCount/7` :
```
<span className="text-[10px] font-bold text-blue-600">{completedCount}/7</span>
```

**IMPORTANT** : Ne pas modifier TrainingCard, NutritionPlanCard, ni la structure des autres composants. Seule BehaviorProgramCard change. Le fichier a ete modifie par le plan 04-02 (ajout noon dans NutritionPlanCard) — lire le fichier actuel avant d'editer pour ne pas perdre ces changements.
  </action>
  <verify>grep -n "completedDays\|_bookmarkId\|_completedDays\|handleMarkDone\|localCompleted" src/components/home/ActiveProgramCards.jsx | head -20</verify>
  <done>BehaviorProgramCard utilise completed_days pour la progression, affiche un bouton "Marquer comme fait", et le jour courant passe immediatement en vert via mise a jour optimiste locale (useState localCompleted). Pas de refresh de page necessaire. La barre de progression reflete les jours reellement completes.</done>
</task>

</tasks>

<verification>
```bash
grep -n "completedDays\|_bookmarkId\|handleMarkDone\|CheckCircle2\|localCompleted" src/components/home/ActiveProgramCards.jsx
```
Doit retourner au moins 5 lignes.
</verification>

<success_criteria>
- BehaviorProgramCard affiche X/7 jours completes (pas X% base sur le temps)
- Le bouton "Marquer Jour X comme fait" est visible dans la vue expanded
- Quand marque, le jour passe IMMEDIATEMENT en "Completé !" avec icone verte (optimistic update via localCompleted useState)
- Si l'appel Bookmark.update echoue, localCompleted est remis a null (rollback)
- TrainingCard et NutritionPlanCard ne sont pas modifiees
- L'import base44 est present si necessaire
</success_criteria>

<output>
Apres completion, creer `.planning/phases/04-independent-fixes/04-03-SUMMARY.md`
</output>
