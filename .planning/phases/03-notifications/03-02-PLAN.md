---
phase: 03-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pawcoach/functions/vetVisitReminders.ts
autonomous: true
requirements:
  - NOTIF-02

must_haves:
  truths:
    - "Une visite vet avec next_date J+3 declenche un email de rappel au proprietaire"
  artifacts:
    - path: "pawcoach/functions/vetVisitReminders.ts"
      provides: "Rappels visites veterinaire CRON"
      exports: ["Deno.serve handler"]
  key_links:
    - from: "vetVisitReminders.ts"
      to: "base44.asServiceRole.entities.HealthRecord"
      via: "filter({ type: 'vet_visit' })"
      pattern: "type.*vet_visit"
    - from: "vetVisitReminders.ts"
      to: "base44.asServiceRole.integrations.Core.SendEmail"
      via: "email loop"
      pattern: "SendEmail"
---

<objective>
Creer les rappels email pour les visites veterinaires planifiees.

Purpose: NOTIF-02 etend le systeme de rappels aux visites vet (HealthRecord type "vet_visit") avec la meme logique que vaccins et medicaments.
Output: vetVisitReminders.ts cree (nouvelle fonction CRON).
</objective>

<execution_context>
@C:/Users/smalt/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smalt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Pattern exact a reproduire — identique a vaccineReminders.ts et medicationReminders.ts -->

```typescript
// Structure complete de la fonction CRON reminder
import { createClientFromRequest } from 'npm:@base44/sdk@0.8.20';

Deno.serve(async (req) => {
  try {
    const base44 = createClientFromRequest(req);
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    const records = await base44.asServiceRole.entities.HealthRecord.filter({ type: "vet_visit" });
    const allDogs = await base44.asServiceRole.entities.Dog.list();
    const allUsers = await base44.asServiceRole.entities.User.list();

    const dogMap = new Map((allDogs || []).map(d => [d.id, d]));
    const userMap = new Map((allUsers || []).map(u => [u.email, u]));

    const REMINDER_DAYS = [14, 7, 3, 1, 0];
    const todayStr = today.toISOString().slice(0, 10);

    const upcoming = (records || []).filter(r => {
      if (!r.next_date) return false;
      const due = new Date(r.next_date);
      due.setHours(0, 0, 0, 0);
      const diffDays = Math.round((due.getTime() - today.getTime()) / (1000 * 60 * 60 * 24));
      return diffDays >= 0 && REMINDER_DAYS.includes(diffDays);
    });

    let sent = 0;
    for (const record of upcoming) {
      if (record.reminder_sent_date === todayStr) continue;
      const dog = dogMap.get(record.dog_id);
      if (!dog) continue;
      const user = userMap.get(dog.owner);
      if (!user) continue;

      // ... SendEmail
      // ... update reminder_sent_date avec try/catch
      sent++;
    }
    return Response.json({ ok: true, checked: upcoming.length, sent });
  } catch (error) {
    console.error("vetVisitReminders error:", error);
    return Response.json({ error: error?.message || String(error) }, { status: 500 });
  }
});
```

// Champ HealthRecord pertinent pour les visites vet :
// - type: "vet_visit"
// - title: description de la visite (ex: "Bilan annuel", "Controle post-op")
// - next_date: date de la prochaine visite
// - reminder_sent_date: dedup (meme champ que vaccins et medicaments)
// - dog_id: lien vers le chien
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer vetVisitReminders.ts (NOTIF-02)</name>
  <files>pawcoach/functions/vetVisitReminders.ts</files>
  <action>
Creer le fichier `pawcoach/functions/vetVisitReminders.ts` en reproduisant exactement le pattern de vaccineReminders.ts, adapte aux visites veterinaires :

- Fetch `HealthRecord.filter({ type: "vet_visit" })`
- Meme REMINDER_DAYS = [14, 7, 3, 1, 0]
- Meme logique dedup via `reminder_sent_date`
- PAS de filtre premium — tous les utilisateurs recoivent les rappels visites vet
- Subject email : `Rappel visite vétérinaire pour ${dog.name}`
- Body email :
  ```
  Bonjour !

  Rappel visite chez le vétérinaire pour ${dog.name} : "${record.title || "Consultation"}" est prévue le ${formattedDate} (dans ${diffDays} jour${diffDays > 1 ? "s" : ""}).

  Pense à confirmer ton rendez-vous. 🏥

  — PawCoach
  ```
- `record.title || "Consultation"` car le title peut etre absent sur certaines visites vet
- Apres envoi, update `reminder_sent_date` sur le HealthRecord avec try/catch :
  ```typescript
  try {
    await base44.asServiceRole.entities.HealthRecord.update(record.id, { reminder_sent_date: todayStr });
  } catch (updateErr) {
    console.warn(`vetVisitReminders: failed to update reminder_sent_date for record ${record.id}:`, updateErr?.message || String(updateErr));
  }
  ```
- Utiliser `.getTime()` dans le calcul de diffDays pour eviter l'erreur TypeScript
- Wrapper le tout dans try/catch global avec `console.error("vetVisitReminders error:", error)` et status 500
- Retourner `Response.json({ ok: true, checked: upcoming.length, sent })`

La fonction doit avoir exactement la meme structure que medicationReminders.ts (cree en plan 03-01), avec uniquement le type, les textes email et le nom de la console.error qui changent.
  </action>
  <verify>
    <automated>grep -c "SendEmail\|reminder_sent_date\|vet_visit" C:/Users/smalt/Desktop/app-chien-ia/pawcoach/functions/vetVisitReminders.ts</automated>
  </verify>
  <done>Le fichier vetVisitReminders.ts existe, contient les trois elements cles (SendEmail, reminder_sent_date, filter vet_visit), et suit exactement le pattern de vaccineReminders.ts. Pas de filtre premium.</done>
</task>

</tasks>

<verification>
Apres la tache :
1. Le fichier `pawcoach/functions/vetVisitReminders.ts` existe
2. `grep -c "vet_visit" pawcoach/functions/vetVisitReminders.ts` retourne >= 1
3. `grep "isPremium" pawcoach/functions/vetVisitReminders.ts` retourne 0 resultats (pas de filtre premium)
4. Structure identique a vaccineReminders.ts : createClientFromRequest, asServiceRole, REMINDER_DAYS, reminder_sent_date dedup, try/catch global
</verification>

<success_criteria>
- vetVisitReminders.ts cree, pattern identique a vaccineReminders.ts, filtre `type: "vet_visit"`, pas de filtre premium
- Une visite vet avec next_date dans REMINDER_DAYS genere un email au proprietaire
</success_criteria>

<output>
After completion, create `.planning/phases/03-notifications/03-02-SUMMARY.md`
</output>
