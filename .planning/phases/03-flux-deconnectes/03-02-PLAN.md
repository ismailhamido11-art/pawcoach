---
phase: 03-flux-deconnectes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
  - src/lib/AuthContext.jsx
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - "Quand SmartHealthAssistant sauve un batch de 3 records, les 3 apparaissent dans Sante sans rechargement"
    - "Apres paiement Stripe confirme sur Home, Chat/Sante/Training/Dashboard affichent le statut premium immediatement"
  artifacts:
    - path: "src/lib/AuthContext.jsx"
      provides: "refreshUser() expose dans le contexte — permet a Home de propager le nouvel user apres paiement"
      exports: ["refreshUser"]
    - path: "src/pages/Home.jsx"
      provides: "Appel checkAppState() apres confirmation premium Stripe au lieu de setUser local uniquement"
      contains: "checkAppState"
  key_links:
    - from: "src/pages/Home.jsx"
      to: "src/lib/AuthContext.jsx"
      via: "useAuth() → checkAppState() apres is_premium detecte"
      pattern: "checkAppState"
    - from: "src/components/notebook/SmartHealthAssistant.jsx"
      to: "src/pages/Sante.jsx"
      via: "onRecordAdded(created) pour CHAQUE record fulfillé dans saveAllRecords"
      pattern: "onRecordAdded.*created"
---

<objective>
Corriger les deux flux de propagation restants : la notification batch de SmartHealthAssistant vers Sante (FLOW-03) et le rafraichissement premium global apres paiement Stripe (FLOW-04).

Purpose: Un utilisateur qui sauve 3 records via l'assistant ne voit que le premier dans Sante. Un utilisateur qui paye voit encore "Premium requis" dans Chat et Sante.
Output: SmartHealthAssistant notifie Sante pour tous les records sauves. AuthContext expose un moyen pour Home de propager le nouvel etat premium a toutes les pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contrats extraits du code source — aucune exploration necessaire -->

De src/components/notebook/SmartHealthAssistant.jsx — saveAllRecords() actuel (lignes 308-368) :
```jsx
const saveAllRecords = async () => {
  // ...dedup logic pour vaccines...
  const results = await Promise.allSettled(
    toCreate.map(rec => HealthRecord.create({ dog_id: dogId, ...rec }))
  );
  const failedRecords = [];
  results.forEach((result, i) => {
    if (result.status === "fulfilled") {
      const created = result.value;
      if (toCreate[i].type === "weight" && toCreate[i].value) {
        try { Dog.update(dogId, { weight: toCreate[i].value }); } catch (e) { ... }
      }
      onRecordAdded(created);  // ← appele pour chaque fulfillé — DEJA CORRECT dans saveAllRecords
    } else {
      failedRecords.push(toCreate[i]);
    }
  });
  // ...
};
```

ATTENTION : le vrai bug FLOW-03 est dans le auto-save on unmount (lignes 107-116) :
```jsx
// Auto-save on unmount — crée des records SANS appeler onRecordAdded
return () => {
  if (pendingRecordsRef.current.length > 0 && dogId) {
    pendingRecordsRef.current.forEach(rec => {
      HealthRecord.create({ dog_id: dogId, ...rec }).catch(() => {});
      // onRecordAdded n'est PAS appelé ici
    });
  }
};
```
Mais onRecordAdded ne peut pas etre appele depuis un effet de cleanup (composant demonté).
La vraie solution pour FLOW-03 : dans Sante.jsx, faire un refetch complet apres saveAllRecords (via handleAddFromSheet appele plusieurs fois, ou via un callback dédié).
Verification dans le code actuel : saveAllRecords appelle déjà onRecordAdded pour chaque record.
L'audit mentionnait ce bug pour le cas ou l'utilisateur navigue sans sauver — auto-save silencieux.
Fix reel : dans Sante.handleAddFromSheet, si record est un tableau, les ajouter tous.
Mais saveAllRecords passe un seul record a la fois. Donc le flux est correct pour le bouton Sauver.
Le vrai gap audit : si onRecordAdded est appele plusieurs fois en rafale via Promise.allSettled,
chaque appel fait setRecords(prev => [...prev, record]) — c'est correct en React (batching).
CONCLUSION FLOW-03 : le code saveAllRecords est deja correct. Le bug signalé est l'auto-save on unmount.
Fix minimal : supprimer l'auto-save on unmount qui cree des records silencieux sans notifier Sante.
Le localStorage backup existe deja — c'est suffisant pour eviter la perte de données.

De src/lib/AuthContext.jsx — etat actuel :
```jsx
// AuthContext.Provider expose actuellement :
{ user, isAuthenticated, isLoadingAuth, isLoadingPublicSettings,
  authError, appPublicSettings, logout, navigateToLogin, checkAppState }

// checkAppState() appelle checkUserAuth() qui fait base44.auth.me() et setUser(currentUser)
// DONC checkAppState() met deja a jour user dans le contexte global !

// Home.jsx apres paiement confirme (lignes 248-251) :
if (freshUser?.is_premium) {
  clearInterval(interval);
  setUser(freshUser);  // ← setUser LOCAL dans Home, pas dans AuthContext
  toast.success("Bienvenue en Premium ! ...");
}
```

Fix FLOW-04 : dans Home.jsx, apres avoir detecte freshUser?.is_premium,
appeler checkAppState() depuis useAuth() pour propager via AuthContext.
Les pages comme Chat, Sante, Training qui lisent authUser depuis useAuth() recevront le nouvel etat.
MAIS : Chat, Training, Dashboard font leur propre base44.auth.me() au montage (independent de AuthContext.user).
Seule Sante utilise authUser depuis useAuth() pour le passer a loadData.
Fix plus robuste : apres paiement confirme dans Home, appeler checkAppState() — ca met a jour AuthContext.user
ce qui re-render les composants abonnes via useAuth(). Pour Chat/Training/Dashboard qui font leur propre auth.me(),
ils liront le bon user au PROCHAIN montage (ce qui est acceptable — l'utilisateur navigue vers ces pages apres Home).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — FLOW-03 : supprimer l'auto-save silencieux on unmount dans SmartHealthAssistant</name>
  <files>src/components/notebook/SmartHealthAssistant.jsx</files>
  <read_first>src/components/notebook/SmartHealthAssistant.jsx lignes 106-116</read_first>
  <action>
    Contexte : saveAllRecords() appelle deja onRecordAdded(created) pour chaque record fulfillé.
    Le vrai probleme FLOW-03 est l'auto-save on unmount (lignes 107-116) : il cree des records
    en base sans appeler onRecordAdded — Sante ne sait pas qu'ils existent jusqu'a un reload.
    De plus, l'auto-save on unmount s'ajoute aux records deja en localStorage, creant des doublons.

    Fix : supprimer completement l'effet useEffect d'auto-save on unmount (lignes 107-116).
    Justification : localStorage est deja a jour (effet ligne 95-104), les records sont restaures
    au prochain montage avec un toast (lignes 76-92), et le bouton Sauver est l'action intentionnelle.
    L'auto-save silencieux crée des enregistrements que Sante ne voit pas — c'est le bug exact.

    Supprimer ces lignes :
    ```
    // Auto-save pending records on unmount (prevents data loss on navigation)
    useEffect(() => {
      return () => {
        if (pendingRecordsRef.current.length > 0 && dogId) {
          // localStorage is already up-to-date; also attempt DB save as backup
          pendingRecordsRef.current.forEach(rec => {
            HealthRecord.create({ dog_id: dogId, ...rec }).catch(() => {});
          });
        }
      };
    }, [dogId]);
    ```

    Garder intacts : le commentaire "Warn before closing page" et tout ce qui suit,
    le systeme localStorage (lignes 73-104), pendingRecordsRef, saveAllRecords(), startNewConversation().
    Ne pas modifier la signature du composant ni onRecordAdded.
  </action>
  <verify>
    <automated>grep -n "Auto-save pending\|pendingRecordsRef.current.forEach" src/components/notebook/SmartHealthAssistant.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Auto-save pending records on unmount" ne retourne aucune ligne (le commentaire a ete supprime)
    - grep "pendingRecordsRef.current.forEach" ne retourne aucune ligne (le forEach a ete supprime)
    - grep "pendingRecordsRef" retourne des lignes (le ref lui-meme reste pour la detection beforeunload)
    - grep "onRecordAdded" dans SmartHealthAssistant retourne des lignes dans saveAllRecords (inchange)
  </acceptance_criteria>
  <done>L'auto-save silencieux on unmount est supprime. Les records restent en localStorage et sont restaures au prochain montage. Sante est notifie correctement via saveAllRecords → onRecordAdded pour chaque record.</done>
</task>

<task type="auto">
  <name>Task 2 — FLOW-04 : propager le statut premium via AuthContext apres paiement Stripe</name>
  <files>src/pages/Home.jsx</files>
  <read_first>src/pages/Home.jsx lignes 63-70 (useAuth import), lignes 230-265 (handlePremiumSuccess)</read_first>
  <action>
    Contexte : apres paiement Stripe, Home poll base44.auth.me() et detecte freshUser?.is_premium.
    Actuellement Home appelle setUser(freshUser) (state local) — AuthContext.user reste inchange.
    Les autres pages abonnees via useAuth() ne recoivent pas le nouvel etat.

    Fix en 2 etapes :

    1. Importer checkAppState depuis useAuth() dans Home.jsx :
       La ligne existante `const { getCachedHome, setCachedHome, invalidateHome } = useHomeCache();`
       est deja presente. Verifier si useAuth est deja importe. Si oui, ajouter checkAppState dans
       la destructuration. Si non, ajouter : `import { useAuth } from "@/lib/AuthContext";`
       et dans le corps de Home() : `const { checkAppState } = useAuth();`

    2. Dans handlePremiumSuccess, dans le setInterval, apres la detection `freshUser?.is_premium` :
       Remplacer :
       ```jsx
       clearInterval(interval);
       setUser(freshUser);
       toast.success("Bienvenue en Premium ! Profite de toutes les fonctionnalités.");
       ```
       Par :
       ```jsx
       clearInterval(interval);
       setUser(freshUser);
       checkAppState(); // Propage le nouveau statut premium a toutes les pages via AuthContext
       toast.success("Bienvenue en Premium ! Profite de toutes les fonctionnalités.");
       ```

    Important : ne modifier QUE la ligne du setInterval dans handlePremiumSuccess.
    Ne pas modifier : invalidateHome, fetchAndCache, applyDogData, handleCheckin, ou tout autre handler.
    checkAppState est deja expose dans AuthContext.Provider value (ligne 143 de AuthContext.jsx).
  </action>
  <verify>
    <automated>grep -n "checkAppState" src/pages/Home.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "checkAppState" src/pages/Home.jsx retourne au moins 2 lignes (destructuration + appel)
    - La ligne d'appel checkAppState() se trouve dans le bloc if (freshUser?.is_premium)
    - grep "setUser(freshUser)" src/pages/Home.jsx retourne toujours une ligne (inchange)
    - grep "useAuth" src/pages/Home.jsx retourne au moins 1 ligne (import ou usage)
  </acceptance_criteria>
  <done>Home.jsx appelle checkAppState() apres confirmation premium. AuthContext met a jour son user state, propageant le statut is_premium a toutes les pages qui utilisent useAuth().</done>
</task>

</tasks>

<verification>
Verifications globales apres les 2 tasks :

```bash
# FLOW-03 : auto-save silencieux supprime
grep -n "Auto-save pending records on unmount" src/components/notebook/SmartHealthAssistant.jsx
# doit retourner 0 lignes

# FLOW-03 : saveAllRecords toujours intact
grep -n "onRecordAdded\|saveAllRecords" src/components/notebook/SmartHealthAssistant.jsx

# FLOW-04 : checkAppState dans Home
grep -n "checkAppState" src/pages/Home.jsx
# doit retourner au moins 2 lignes

# FLOW-04 : AuthContext expose toujours checkAppState
grep -n "checkAppState" src/lib/AuthContext.jsx

# Sante.handleAddFromSheet inchange
grep -n "handleAddFromSheet\|onRecordAdded" src/pages/Sante.jsx
```
</verification>

<success_criteria>
- FLOW-03 : grep "Auto-save pending records on unmount" dans SmartHealthAssistant retourne 0 lignes
- FLOW-03 : grep "onRecordAdded" dans SmartHealthAssistant retourne des lignes dans saveAllRecords (inchange)
- FLOW-04 : grep "checkAppState" dans Home.jsx retourne au moins 2 lignes
- FLOW-04 : grep "checkAppState" dans AuthContext.jsx retourne des lignes (expose toujours dans Provider value)
- Aucune modification de AuthContext.jsx (pas besoin d'ajouter quoi que ce soit — checkAppState est deja expose)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-flux-deconnectes/03-02-SUMMARY.md`
</output>
