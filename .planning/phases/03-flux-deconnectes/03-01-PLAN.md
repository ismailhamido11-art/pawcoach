---
phase: 03-flux-deconnectes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Activite.jsx
  - src/pages/Profile.jsx
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
must_haves:
  truths:
    - "Apres une balade terminee, DailyProgress dans Home affiche les vraies stats sans rechargement manuel"
    - "Apres un switch de chien dans Profile, toutes les donnees Home correspondent au nouveau chien immediatement"
  artifacts:
    - path: "src/pages/Activite.jsx"
      provides: "Appel invalidateHome() apres refreshLogs() — cache Home invalide apres balade"
      contains: "invalidateHome"
    - path: "src/pages/Profile.jsx"
      provides: "Appel invalidateHome() dans handleSwitchDog — cache Home invalide apres switch"
      contains: "invalidateHome"
  key_links:
    - from: "src/components/tracker/WalkMode.jsx"
      to: "src/pages/Activite.jsx"
      via: "onLogged prop → refreshLogs() callback"
      pattern: "onLogged.*refreshLogs"
    - from: "src/pages/Activite.jsx"
      to: "src/lib/HomeCacheContext.jsx"
      via: "useHomeCache() → invalidateHome()"
      pattern: "invalidateHome"
    - from: "src/pages/Profile.jsx"
      to: "src/lib/HomeCacheContext.jsx"
      via: "useHomeCache() → invalidateHome() dans handleSwitchDog"
      pattern: "invalidateHome"
---

<objective>
Reconnecter les deux flux de cache Home les plus impactants : la fin de balade (FLOW-01) et le switch de chien (FLOW-02).

Purpose: L'utilisateur voit 0 min de balade sur Home apres avoir marche (TTL 2 min), et l'ancien chien apres un switch — deux frictions visibles a chaque usage.
Output: Activite.jsx et Profile.jsx appellent invalidateHome() aux bons moments. Aucune modification de HomeCacheContext (la fonction existe deja).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contrats extraits du code source — aucune exploration necessaire -->

De src/lib/HomeCacheContext.jsx :
```jsx
// HomeCacheProvider exporte ces 3 fonctions via useHomeCache()
export function useHomeCache() {
  const ctx = useContext(HomeCacheContext);
  if (!ctx) throw new Error("useHomeCache must be used within HomeCacheProvider");
  return ctx;
}
// invalidateHome() met cacheRef.current = null → prochain getCachedHome() retourne null → Home refetch
const invalidateHome = () => { cacheRef.current = null; };
// HomeCacheProvider entoure le routeur dans App.jsx ligne 98-100 → disponible dans Activite et Profile
```

De src/pages/Activite.jsx (etat actuel — ce qui MANQUE) :
```jsx
// refreshLogs() fait le refetch DailyLog mais n'invalide PAS le cache Home
const refreshLogs = async () => {
  if (!dog || !user) return;
  const l = await DailyLog.filter({ dog_id: dog.id }, "-date", 30);
  setLogs(l || []);
  checkWalkBadges(dog.id, user.email, l || []).catch(() => {});
  // MANQUE : invalidateHome() ici
};
// WalkMode recoit onLogged={refreshLogs} — ligne 209
```

De src/pages/Profile.jsx (etat actuel — ce qui MANQUE) :
```jsx
// handleSwitchDog fait localStorage mais n'invalide PAS le cache Home
const handleSwitchDog = (dogId) => {
  setActiveDogId(dogId);
  localStorage.setItem("activeDogId", dogId);
  // MANQUE : invalidateHome() ici
};
// Profile n'importe pas useHomeCache
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — FLOW-01 : invalidateHome apres balade dans Activite</name>
  <files>src/pages/Activite.jsx</files>
  <behavior>
    - Apres refreshLogs() : invalidateHome() est appele — le cache Home est null
    - Si dog ou user est absent : pas d'appel (guard existant preserve)
    - L'import useHomeCache est present en haut du fichier
    - refreshLogs() reste fonctionnel (DailyLog.filter + checkWalkBadges inchanges)
  </behavior>
  <read_first>src/pages/Activite.jsx lignes 1-95</read_first>
  <action>
    1. Ajouter l'import : `import { useHomeCache } from "@/lib/HomeCacheContext";` dans le bloc d'imports (apres les imports existants de "@/lib/").
    2. Dans le corps de la fonction Activite(), apres les useState existants, ajouter : `const { invalidateHome } = useHomeCache();`
    3. Dans refreshLogs(), apres `setLogs(l || [])` et `checkWalkBadges(...)`, ajouter une derniere ligne : `invalidateHome();`
    4. Ne pas modifier WalkMode.jsx ni HomeCacheContext.jsx — le pipe onLogged → refreshLogs existe deja.
    5. Ne pas modifier le comportement de checkWalkBadges ou la signature de refreshLogs.
  </action>
  <verify>
    <automated>grep -n "useHomeCache\|invalidateHome" src/pages/Activite.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "useHomeCache" src/pages/Activite.jsx retourne au moins 2 lignes (import + usage)
    - grep "invalidateHome" src/pages/Activite.jsx retourne au moins 1 ligne dans refreshLogs
    - grep "invalidateHome" src/pages/Activite.jsx ne retourne PAS de ligne dans les imports seulement
  </acceptance_criteria>
  <done>Activite.jsx appelle invalidateHome() a la fin de refreshLogs(). Le cache Home est invalide apres chaque log de balade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — FLOW-02 : invalidateHome apres switch de chien dans Profile</name>
  <files>src/pages/Profile.jsx</files>
  <behavior>
    - handleSwitchDog(dogId) appelle invalidateHome() apres localStorage.setItem
    - L'import useHomeCache est present
    - setActiveDogId et localStorage.setItem restent inchanges (comportement Profile conserve)
  </behavior>
  <read_first>src/pages/Profile.jsx lignes 1-95</read_first>
  <action>
    1. Ajouter l'import : `import { useHomeCache } from "@/lib/HomeCacheContext";` dans le bloc d'imports (apres les imports de "@/lib/" existants).
    2. Dans le corps de la fonction Profile(), ajouter : `const { invalidateHome } = useHomeCache();` apres les useState existants (par exemple apres `const [loading, setLoading] = useState(true);`).
    3. Dans handleSwitchDog(), apres `localStorage.setItem("activeDogId", dogId)`, ajouter : `invalidateHome();`
    4. Ne pas modifier la logique existante de handleSwitchDog (setActiveDogId, localStorage conserves).
  </action>
  <verify>
    <automated>grep -n "useHomeCache\|invalidateHome" src/pages/Profile.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "useHomeCache" src/pages/Profile.jsx retourne au moins 2 lignes (import + usage)
    - grep "invalidateHome" src/pages/Profile.jsx retourne une ligne dans handleSwitchDog
    - La ligne invalidateHome() se trouve APRES localStorage.setItem dans handleSwitchDog
  </acceptance_criteria>
  <done>Profile.jsx appelle invalidateHome() dans handleSwitchDog(). Le cache Home est invalide immediatement apres un switch de chien.</done>
</task>

</tasks>

<verification>
Verifications globales apres les 2 tasks :

```bash
# FLOW-01 : invalidateHome dans Activite
grep -n "invalidateHome\|useHomeCache" src/pages/Activite.jsx

# FLOW-02 : invalidateHome dans Profile
grep -n "invalidateHome\|useHomeCache" src/pages/Profile.jsx

# Pas de regression sur WalkMode (onLogged inchange)
grep -n "onLogged" src/components/tracker/WalkMode.jsx
grep -n "onLogged" src/pages/Activite.jsx

# HomeCacheContext non modifie
grep -c "invalidateHome" src/lib/HomeCacheContext.jsx
# doit retourner 2 (definition + export dans Provider value)
```
</verification>

<success_criteria>
- FLOW-01 : `grep "invalidateHome" src/pages/Activite.jsx` retourne au moins 1 ligne dans refreshLogs
- FLOW-02 : `grep "invalidateHome" src/pages/Profile.jsx` retourne au moins 1 ligne dans handleSwitchDog
- Aucune modification de WalkMode.jsx, HomeCacheContext.jsx, ou d'autres fichiers
- Build sans erreur : `cd src && node --input-type=module --eval "import './pages/Activite.jsx'" 2>&1` (verification syntaxe)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-flux-deconnectes/03-01-SUMMARY.md`
</output>
