---
phase: 07-flow-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/vet/VetNoteForm.jsx
  - src/pages/Library.jsx
autonomous: true
requirements:
  - FIX-15
  - FIX-19
must_haves:
  truths:
    - "L'utilisateur peut soumettre une note vétérinaire sans TypeError"
    - "Activer un programme depuis Library ne plante jamais (JSON valide ou non)"
  artifacts:
    - path: "src/components/vet/VetNoteForm.jsx"
      provides: "Soumission note via base44.functions.invoke('vetAccess', {...})"
      contains: "invoke(\"vetAccess\""
    - path: "src/pages/Library.jsx"
      provides: "handleActivateTraining protégé try/catch sur JSON.parse"
      contains: "toast.error(\"Programme corrompu\""
  key_links:
    - from: "src/components/vet/VetNoteForm.jsx"
      to: "base44.functions.invoke"
      via: "remplacement appel incorrect"
      pattern: "invoke\\(\"vetAccess\""
    - from: "src/pages/Library.jsx"
      to: "handleActivateTraining"
      via: "try/catch JSON.parse"
      pattern: "Programme corrompu"
---

<objective>
Corriger deux ruptures P0 indépendantes dans des fichiers sans intersection.

FIX-15 : VetNoteForm appelle `base44.functions.vetAccess({...})` (TypeError — cette syntaxe n'existe pas). Fix : `base44.functions.invoke("vetAccess", {...})`. Les notes vétérinaires ne soumettent jamais en l'état.

FIX-19 : Library.handleActivateTraining — le JSON.parse de bk.content est déjà dans un try/catch (lignes 97-110), mais le catch actuel affiche un message générique "Impossible d'activer ce programme". La vérification du code confirme que Library a DÉJÀ un try/catch opérationnel. Ce plan vérifie l'état réel et s'assure que le message est informatif.

Purpose: Zéro TypeError sur les deux flows les plus critiques (notes vétérinaires, activation programme).
Output: VetNoteForm.jsx et Library.jsx corrigés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix VetNoteForm — remplacer l'appel API incorrect</name>
  <files>src/components/vet/VetNoteForm.jsx</files>
  <read_first>
    Lire src/components/vet/VetNoteForm.jsx en entier avant de modifier.
    La ligne 30 contient : `const { note } = await base44.functions.vetAccess({`
    Cette syntaxe est invalide — base44.functions n'expose pas les fonctions comme propriétés directes.
  </read_first>
  <action>
    Dans VetNoteForm.jsx ligne ~30, remplacer :
    ```
    const { note } = await base44.functions.vetAccess({
    ```
    par :
    ```
    const response = await base44.functions.invoke("vetAccess", {
    ```
    et adapter la destructuring sur la ligne suivante si nécessaire :
    - Si le code utilisait `note` depuis la réponse destructurée, vérifier comment l'objet réponse est utilisé dans la suite du handler et adapter (`response.data?.note` ou `response.note` selon ce que vetAccess retourne).
    - Conserver la structure du try/catch existante autour de l'appel si elle existe.
    - NE PAS modifier la logique métier, les props, ni le JSX du formulaire.
    - NE PAS modifier src/components/ui/* .
  </action>
  <verify>
    <automated>grep -n "invoke(\"vetAccess\"" src/components/vet/VetNoteForm.jsx</automated>
  </verify>
  <done>La ligne avec `base44.functions.vetAccess(` n'existe plus. `invoke("vetAccess"` est présent. Le fichier compile (pas de syntaxe cassée).</done>
</task>

<task type="auto">
  <name>Task 2: Vérifier et améliorer Library handleActivateTraining</name>
  <files>src/pages/Library.jsx</files>
  <read_first>
    Lire src/pages/Library.jsx lignes 97-112 pour voir l'état exact de handleActivateTraining.
    Code actuel confirmé :
    ```
    const handleActivateTraining = async (bk) => {
      try {
        const data = JSON.parse(bk.content);
        data.start_date = new Date().toISOString().split("T")[0];
        await Bookmark.update(bk.id, { content: JSON.stringify(data) });
        setBookmarks(prev => prev.map(b => b.id === bk.id ? { ...b, content: JSON.stringify(data) } : b));
        toast.success("Programme activé ! Retrouve-le sur ton accueil.");
      } catch {
        toast.error("Impossible d'activer ce programme. Réessaie.");
      }
    };
    ```
    Le try/catch EXISTE déjà. La seule amélioration nécessaire : distinguer les erreurs JSON (contenu corrompu) des erreurs réseau (Bookmark.update échoue) pour un message plus précis.
  </read_first>
  <action>
    Améliorer handleActivateTraining pour distinguer les types d'erreur :

    ```javascript
    const handleActivateTraining = async (bk) => {
      let data;
      try {
        data = JSON.parse(bk.content);
      } catch {
        toast.error("Programme corrompu — impossible de l'activer.");
        return;
      }
      try {
        data.start_date = new Date().toISOString().split("T")[0];
        await Bookmark.update(bk.id, { content: JSON.stringify(data) });
        setBookmarks(prev => prev.map(b => b.id === bk.id ? { ...b, content: JSON.stringify(data) } : b));
        toast.success("Programme activé ! Retrouve-le sur ton accueil.");
      } catch {
        toast.error("Impossible d'activer ce programme. Réessaie.");
      }
    };
    ```

    Cette séparation permet : (1) message "Programme corrompu" si JSON invalide, (2) message "Réessaie" si l'API échoue.
    NE PAS modifier les autres fonctions de Library.jsx.
  </action>
  <acceptance_criteria>
    - grep "Programme corrompu" src/pages/Library.jsx retourne 1 résultat
    - grep "handleActivateTraining" src/pages/Library.jsx retourne la fonction avec 2 blocs try/catch distincts
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Programme corrompu\|handleActivateTraining" src/pages/Library.jsx</automated>
  </verify>
  <done>handleActivateTraining a deux try/catch séparés. "Programme corrompu" apparaît dans le code. Le message d'erreur réseau reste "Réessaie".</done>
</task>

</tasks>

<verification>
```bash
# FIX-15 : plus d'appel direct vetAccess
grep "base44.functions.vetAccess" src/components/vet/VetNoteForm.jsx && echo "FAIL - old pattern still present" || echo "OK - pattern removed"

# FIX-15 : invoke correct
grep "invoke(\"vetAccess\"" src/components/vet/VetNoteForm.jsx && echo "OK" || echo "FAIL - invoke not found"

# FIX-19 : message corrompu présent
grep "Programme corrompu" src/pages/Library.jsx && echo "OK" || echo "FAIL"
```
</verification>

<success_criteria>
- `base44.functions.vetAccess(` : 0 occurrences dans le codebase
- `invoke("vetAccess"` : présent dans VetNoteForm.jsx
- `Programme corrompu` : présent dans Library.jsx
- Aucune autre fonction de Library.jsx modifiée
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-flow-fixes/07-01-SUMMARY.md` avec :
- Fichiers modifiés (VetNoteForm.jsx, Library.jsx)
- Ligne exacte modifiée dans VetNoteForm (avant/après)
- Confirmation que le try/catch Library existait déjà
- Résultats des commandes de vérification
</output>
