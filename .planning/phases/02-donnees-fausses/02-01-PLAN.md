---
phase: 02-donnees-fausses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
  - src/components/home/DailyProgress.jsx
  - src/components/home/CalendarStrip.jsx
  - src/components/home/DailyBriefing.jsx
autonomous: true
requirements: [DATA-01, DATA-02, DATA-06]
must_haves:
  truths:
    - "Apres un check-in mood=1 ou mood=2, le hero Home affiche le nom du chien suivi de 'n est pas au top...' — jamais 'est en forme'"
    - "La carte Repas dans DailyProgress affiche le nombre de repas reels (meals_count), pas les bols d'eau (water_bowls)"
    - "CalendarStrip : un point vert n'apparait que si walk_minutes > 0, pas si seulement de l'eau a ete bue"
    - "Les textes DailyBriefing affichent 'debut' avec accent (debut) et toutes les phrases sans caracteres manquants"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "Hero message conditionnel sur todayCheckin.mood"
      contains: "todayCheckin.mood >= 4"
    - path: "src/components/home/DailyProgress.jsx"
      provides: "Carte Repas lisant meals_count"
      contains: "meals_count"
    - path: "src/components/home/CalendarStrip.jsx"
      provides: "hasActivity base sur walk_minutes uniquement"
      contains: "walk_minutes > 0"
    - path: "src/components/home/DailyBriefing.jsx"
      provides: "Textes avec accents corrects"
      contains: "début"
  key_links:
    - from: "Home.jsx"
      to: "hero text"
      via: "todayCheckin.mood ternaire"
      pattern: "todayCheckin\\.mood >= 4"
    - from: "DailyProgress.jsx"
      to: "DailyLog.meals_count"
      via: "todayLog?.meals_count"
      pattern: "meals_count"
---

<objective>
Corriger trois sources de donnees incorrectes visibles immediatement par l'utilisateur : le message hero qui dit "en forme" independamment du mood, la carte Repas qui affiche les bols d'eau, et les accents manquants dans DailyBriefing.

Purpose: L'utilisateur qui vient de signaler que son chien va mal ne doit pas lire "Rex est en forme !". C'est le bug le plus signale par Ismail et le plus visible.
Output: Home.jsx, DailyProgress.jsx, CalendarStrip.jsx, DailyBriefing.jsx corriges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/audit/DATA-COHERENCE.md
@.planning/audit/USER-JOURNEY.md

<interfaces>
<!-- Contrats extraits du code source — a utiliser directement -->

From src/pages/Home.jsx (lignes 493-504 — le bug exact) :
```jsx
{todayCheckin
  ? `${dog?.name || "Ton chien"} est en forme !`
  : `${dog?.name || "Ton chien"} attend son check-in`
}
```
`todayCheckin` a la forme `{ mood: number (1-5), energy: number, appetite: number, ... }`.

From src/components/home/DailyProgress.jsx (ligne 11 — le bug repas) :
```js
const meals = todayLog?.water_bowls || 0;  // BUG: lit les bols d'eau
```
Le champ correct dans DailyLog est `meals_count` (verifier dans CombinedFAB.jsx ou le schema Base44 — utiliser `meals_count` ou `meal_count`).
IMPORTANT: Verifier le nom exact du champ avant de l'utiliser : lire CombinedFAB.jsx pour voir quel champ est ecrit lors d'un log repas.

From src/components/home/CalendarStrip.jsx (ligne 19) :
```js
const hasActivity = logDates.has(dateStr) || (dailyLogs || []).some(l => l.date === dateStr && (l.walk_minutes > 0 || l.water_bowls > 0));
```
Fix : retirer `|| l.water_bowls > 0` — seul `walk_minutes > 0` compte comme activite physique.

From src/components/home/DailyBriefing.jsx (lignes 38 et 76) :
```js
message = `Bienvenue ! C'est le debut de l'aventure avec ${name}.`;  // ligne 38 — accent manquant
message += ` ${streakDays} jours de suite — le debut d'une habitude.`;  // ligne 76 — accent manquant
```
Fix : remplacer "debut" par "début" dans ces deux lignes.

Also ligne 44 : `message = \`${name} etait en forme hier.\`` — "etait" manque accent (etait → était).
Ligne 55 : `message = \`${name} est en forme !\`` — correct quand mood >= 4, NE PAS toucher.
Ligne 56 : `sub: \`${name} a de l'energie a revendre\`` — "energie" sans accent, "a" sans accent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger le hero message Home selon le mood reel (DATA-01)</name>
  <read_first>src/pages/Home.jsx (lignes 483-510)</read_first>
  <files>src/pages/Home.jsx</files>
  <action>
Lire Home.jsx lignes 483-510 pour voir le bloc exact.

Remplacer le ternaire simple (lignes 493-496) par un ternaire conditionnel sur todayCheckin.mood :

```jsx
{todayCheckin
  ? (todayCheckin.mood >= 4
      ? `${dog?.name || "Ton chien"} est en forme !`
      : todayCheckin.mood <= 2
        ? `${dog?.name || "Ton chien"} n'est pas au top...`
        : `${dog?.name || "Ton chien"} a une journee tranquille`)
  : `${dog?.name || "Ton chien"} attend son check-in`
}
```

Et le sous-texte (lignes 499-502) conditionner aussi :
```jsx
{todayCheckin
  ? (todayCheckin.mood >= 4
      ? "Continue comme ca, chaque jour compte pour sa sante."
      : todayCheckin.mood <= 2
        ? "Garde un oeil sur lui et note tout changement."
        : "Une petite balade lui ferait du bien.")
  : "20 min de marche par jour renforcent son coeur et son moral."
}
```

Pattern-proactif : verifier qu'il n'existe plus d'autre `"est en forme !"` non conditionnel dans Home.jsx apres la modification.
  </action>
  <verify>
    <automated>grep -n "est en forme" "src/pages/Home.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep "est en forme" dans Home.jsx retourne uniquement la ligne dans le ternaire conditionne par `todayCheckin.mood >= 4`
    - aucune occurrence de `todayCheckin ? \`.*est en forme\`` sans condition sur mood
  </acceptance_criteria>
  <done>Le hero Home affiche "n'est pas au top..." quand mood est 1 ou 2, "a une journee tranquille" quand mood est 3, "est en forme !" quand mood est 4 ou 5</done>
</task>

<task type="auto">
  <name>Task 2: Corriger carte Repas (water_bowls → meals_count) et CalendarStrip hasActivity (DATA-02)</name>
  <read_first>
    src/components/home/DailyProgress.jsx,
    src/components/home/CalendarStrip.jsx,
    src/components/CombinedFAB.jsx (pour trouver le nom exact du champ repas dans DailyLog)
  </read_first>
  <files>src/components/home/DailyProgress.jsx, src/components/home/CalendarStrip.jsx</files>
  <action>
1. AVANT de modifier, lire CombinedFAB.jsx pour trouver le champ exact ecrit dans DailyLog pour les repas (chercher "meal" dans le fichier). Candidats : `meals_count`, `meal_count`, `meals`. Utiliser le nom exact du champ existant dans le schema.

2. Dans DailyProgress.jsx ligne 11 :
   Remplacer `const meals = todayLog?.water_bowls || 0;`
   par `const meals = todayLog?.meals_count || 0;`  (ou le nom exact trouve dans CombinedFAB)

3. Dans CalendarStrip.jsx ligne 19 :
   Remplacer :
   ```js
   const hasActivity = logDates.has(dateStr) || (dailyLogs || []).some(l => l.date === dateStr && (l.walk_minutes > 0 || l.water_bowls > 0));
   ```
   par :
   ```js
   const hasActivity = (dailyLogs || []).some(l => l.date === dateStr && (l.walk_minutes || 0) > 0);
   ```
   Note: simplifier aussi la condition — `logDates.has(dateStr)` inclut des jours ou seul water_bowls est non-nul, donc le remplacer par la condition stricte `walk_minutes > 0`.

4. Verifier : pattern-proactif — grep "water_bowls" dans tout le frontend pour s'assurer qu'il ne reste aucune instance qui compte les bols d'eau comme des repas ou comme une activite physique.
  </action>
  <verify>
    <automated>grep -rn "water_bowls" "src/components/home/" && grep -n "meals" "src/components/home/DailyProgress.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - DailyProgress.jsx ne contient plus `water_bowls` — utilise `meals_count` (ou nom confirme)
    - CalendarStrip.jsx ne contient plus `water_bowls` dans la condition `hasActivity`
    - CalendarStrip.jsx `hasActivity` verifie uniquement `walk_minutes > 0`
  </acceptance_criteria>
  <done>La carte Repas affiche les vrais repas (meals_count), le calendrier marque un jour actif uniquement si une balade a ete faite (walk_minutes > 0)</done>
</task>

<task type="auto">
  <name>Task 3: Corriger les accents manquants dans DailyBriefing (DATA-06)</name>
  <read_first>src/components/home/DailyBriefing.jsx (lignes 22-82)</read_first>
  <files>src/components/home/DailyBriefing.jsx</files>
  <action>
Lire DailyBriefing.jsx lignes 22-82 (fonction generateBriefing) et corriger TOUS les mots sans accent :

Corrections confirmees par lecture du code :
- Ligne 38 : "debut" → "début"  (dans "C'est le debut de l'aventure")
- Ligne 44 : "etait" → "était" (dans "etait en forme hier")
- Ligne 56 : "energie" → "énergie" (dans "a de l'energie a revendre")
- Ligne 76 : "debut" → "début" (dans "le debut d'une habitude")

Scanner AUSSI lignes 84-115 (reste du composant) pour d'autres mots sans accent oublies. Corrections communes : "regularite" → "régularité", "hydrate" → "hydraté/hydrate" (selon contexte), "verifie" → "vérifie", "recommandees" → "recommandées".

NE PAS modifier :
- Les termes anglais (labels, config keys)
- Les chaines dans MISSION_CONFIG (ces valeurs sont des labels UI deja corrects)
- Les mots intentionnellement sans accent (noms propres de chiens, etc.)

Pattern-proactif : grep "debut\b" et "etait\b" et "energie\b" dans DailyBriefing.jsx apres correction pour confirmer 0 instances non-accentuees restantes.
  </action>
  <verify>
    <automated>grep -n "debut\b\|etait\b\|energie\b\|regularite\b" "src/components/home/DailyBriefing.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep retourne 0 resultats pour les mots sans accent listes
    - "début" apparait avec accent aux lignes concernees
    - "était" apparait avec accent
    - Le fichier est syntaxiquement valide JSX (pas de caracteres brises)
  </acceptance_criteria>
  <done>DailyBriefing affiche tous les textes avec accents corrects — aucun mot sans accent dans les messages generes</done>
</task>

</tasks>

<verification>
Apres les 3 taches :

1. grep -n "todayCheckin.mood" src/pages/Home.jsx — doit retourner au moins 1 ligne montrant le ternaire conditionnel
2. grep -n "water_bowls" src/components/home/DailyProgress.jsx — doit retourner 0 resultats
3. grep -n "water_bowls" src/components/home/CalendarStrip.jsx — doit retourner 0 resultats
4. grep -n "debut\b" src/components/home/DailyBriefing.jsx — doit retourner 0 resultats (seulement "début" avec accent)
5. Aucun fichier modifie ne doit avoir d'erreur de syntaxe JSX
</verification>

<success_criteria>
- Hero Home : mood 1-2 → "n'est pas au top", mood 3 → "journee tranquille", mood 4-5 → "en forme"
- Carte Repas : valeur issue de meals_count (repas reels), pas water_bowls
- CalendarStrip : point vert seulement si walk_minutes > 0
- DailyBriefing : tous les mots affiches portent leurs accents corrects
- 0 instance de water_bowls dans les composants home comme source de repas ou d'activite
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-donnees-fausses/02-01-SUMMARY.md`
</output>
