---
phase: 02-donnees-fausses
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sante/GrowthTrackerContent.jsx
  - src/components/home/DailyBriefing.jsx
autonomous: true
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "Apres l'enregistrement d'une mesure via GrowthTracker (photo IA ou saisie manuelle), Dog.weight est mis a jour — le badge poids dans le header Home affiche la nouvelle valeur"
    - "Le check-in rapide (handleMoodTap) ne forge plus energy et appetite — ces champs sont absents du payload envoyé si l'utilisateur ne les a pas saisis"
  artifacts:
    - path: "src/components/sante/GrowthTrackerContent.jsx"
      provides: "Dog.update apres GrowthEntry.create dans saveAnalysis et saveManual"
      contains: "Dog.update"
    - path: "src/components/home/DailyBriefing.jsx"
      provides: "handleMoodTap sans fabrication energy/appetite"
      contains: "onQuickCheckin({ mood })"
  key_links:
    - from: "GrowthTrackerContent.jsx saveAnalysis"
      to: "Dog.update"
      via: "Dog.update(dog.id, { weight: entry.weight_kg })"
      pattern: "Dog\\.update"
    - from: "GrowthTrackerContent.jsx saveManual"
      to: "Dog.update"
      via: "Dog.update(dog.id, { weight: parsedWeight })"
      pattern: "Dog\\.update"
    - from: "DailyBriefing.jsx handleMoodTap"
      to: "onQuickCheckin"
      via: "onQuickCheckin({ mood }) sans energy/appetite"
      pattern: "onQuickCheckin\\(\\{ mood \\}\\)"
---

<objective>
Corriger deux sources de donnees fabricuees : GrowthTracker qui cree une GrowthEntry sans synchroniser Dog.weight, et le quick check-in qui forge des valeurs energy/appetite non saisies par l'utilisateur.

Purpose: Dog.weight desynchronise impacte les calculs IA nutrition. Les valeurs energy/appetite fabricuees faussent le score wellness et les recommandations.
Output: GrowthTrackerContent.jsx et DailyBriefing.jsx corriges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/audit/DATA-COHERENCE.md

<interfaces>
<!-- Contrats extraits du code source -->

From src/components/sante/GrowthTrackerContent.jsx :

Import en haut du fichier (ligne ~49) : GrowthEntry est importee depuis "@/api/entities".
Dog doit etre importe aussi — verifier si Dog est deja dans les imports, sinon l'ajouter.

saveAnalysis() (lignes 108-135) — structure actuelle :
```js
async function saveAnalysis() {
  if (!analysisResult) return;
  try {
    const entry = await GrowthEntry.create({
      dog_id: dog.id,
      owner_email: user.email,
      date: format(new Date(), "yyyy-MM-dd"),
      weight_kg: analysisResult.weight_kg,
      // ...autres champs...
    });
    if (onGrowthAdded) onGrowthAdded(entry);
    setSavedAnalysis(true);
    // ...toast + timeout...
  } catch {
    toast.error("...");
  }
}
```

saveManual() (lignes 137-172) — structure actuelle :
```js
async function saveManual() {
  // ...validation...
  setSavingManual(true);
  try {
    const entry = await GrowthEntry.create({
      dog_id: dog.id,
      weight_kg: parsedWeight,
      // ...
    });
    if (onGrowthAdded) onGrowthAdded(entry);
    toast.success("Mesure ajoutee !");
    // ...reset...
  } catch {
    toast.error("...");
  } finally {
    setSavingManual(false);
  }
}
```

Pattern existant dans WeightCard.jsx / SectionPoids.jsx (reference pour le fix) :
```js
await Dog.update(dogId, { weight: w });
```

From src/components/home/DailyBriefing.jsx (ligne 105-108) — le bug :
```js
const handleMoodTap = (mood) => {
  setMoodPicked(true);
  onQuickCheckin({ mood, energy: mood >= 4 ? 4 : 3, appetite: mood >= 4 ? 4 : 3 });
};
```

Props disponibles sur DailyBriefing : dog, user, recentCheckins, dailyLogs, streak, todayCheckin, onQuickCheckin, submitting, recommendations.
Le parent Home.jsx passe onQuickCheckin = handleQuickCheckin qui accepte { mood, energy, appetite }.
Le backend dailyCheckinProcess accepte des champs null/undefined — ils seront ignores.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchroniser Dog.weight apres GrowthEntry.create (DATA-03)</name>
  <read_first>
    src/components/sante/GrowthTrackerContent.jsx (lignes 1-60 pour les imports, puis 108-172 pour saveAnalysis et saveManual)
  </read_first>
  <files>src/components/sante/GrowthTrackerContent.jsx</files>
  <action>
1. Verifier les imports en haut du fichier. Dog doit etre importe. Si la ligne d'import contient uniquement GrowthEntry, ajouter Dog :
   ```js
   import { GrowthEntry, Dog } from "@/api/entities";
   ```
   Si Dog est deja importe, ne pas modifier la ligne.

2. Dans saveAnalysis(), apres `const entry = await GrowthEntry.create({...})` et AVANT `if (onGrowthAdded)`, ajouter :
   ```js
   // Sync Dog.weight so header and AI nutrition use the latest value
   if (entry.weight_kg) {
     try { await Dog.update(dog.id, { weight: entry.weight_kg }); } catch (e) { console.warn("Dog.weight sync failed:", e); }
   }
   ```

3. Dans saveManual(), apres `const entry = await GrowthEntry.create({...})` et AVANT `if (onGrowthAdded)`, ajouter :
   ```js
   // Sync Dog.weight so header and AI nutrition use the latest value
   try { await Dog.update(dog.id, { weight: parsedWeight }); } catch (e) { console.warn("Dog.weight sync failed:", e); }
   ```

4. Pattern-proactif : grep "GrowthEntry.create" dans tout le codebase pour verifier qu'il n'existe pas d'autre endroit qui cree une GrowthEntry sans syncer Dog.weight.
  </action>
  <verify>
    <automated>grep -n "Dog.update" "src/components/sante/GrowthTrackerContent.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep retourne au moins 2 lignes (une pour saveAnalysis, une pour saveManual)
    - Le mot "Dog" apparait dans les imports du fichier
    - Les appels Dog.update sont wrappés dans try/catch (ne bloquent pas si la sync echoue)
    - La logique de sauvegarde (toast, reset) est inchangee
  </acceptance_criteria>
  <done>Apres saveAnalysis() ou saveManual(), Dog.weight est mis a jour. CoachHomeHeader affichera le nouveau poids au prochain rendu. Les calculs nutrition IA seront bases sur le poids reel.</done>
</task>

<task type="auto">
  <name>Task 2: Supprimer la fabrication energy/appetite dans handleMoodTap (DATA-04)</name>
  <read_first>src/components/home/DailyBriefing.jsx (lignes 100-115)</read_first>
  <files>src/components/home/DailyBriefing.jsx</files>
  <action>
Lire DailyBriefing.jsx lignes 100-115.

Modifier handleMoodTap pour ne transmettre que le mood :

```js
const handleMoodTap = (mood) => {
  setMoodPicked(true);
  onQuickCheckin({ mood });
};
```

Supprimer `energy: mood >= 4 ? 4 : 3, appetite: mood >= 4 ? 4 : 3` du payload.

Le backend dailyCheckinProcess recevra energy=undefined et appetite=undefined — ces champs seront null dans la DB, ce qui est correct : l'utilisateur ne les a pas renseignes.

Verifier que Home.jsx handleQuickCheckin accepte un payload partiel (il passe tout a handleCheckin qui lui passe au backend — les champs undefined seront ignores cote Base44).

Pattern-proactif : grep "energy:" et "appetite:" dans DailyBriefing.jsx pour confirmer 0 instances de fabrication restantes.
  </action>
  <verify>
    <automated>grep -n "energy:" "src/components/home/DailyBriefing.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep "energy:" dans DailyBriefing.jsx retourne 0 resultats (ou uniquement dans des commentaires)
    - grep "appetite:" dans DailyBriefing.jsx retourne 0 resultats (ou uniquement dans des commentaires)
    - handleMoodTap appelle onQuickCheckin avec uniquement { mood }
    - Le reste du composant (statusBadge, mission, rendu) est inchange
  </acceptance_criteria>
  <done>Le check-in rapide enregistre uniquement le mood observe par l'utilisateur. Les champs energy et appetite restent null si non renseignes — les calculs wellness utilisent uniquement des donnees reelles.</done>
</task>

</tasks>

<verification>
Apres les 2 taches :

1. grep -n "Dog.update" src/components/sante/GrowthTrackerContent.jsx — doit retourner 2 lignes
2. grep -n "energy:" src/components/home/DailyBriefing.jsx — doit retourner 0 (ou commentaires uniquement)
3. grep -n "appetite:" src/components/home/DailyBriefing.jsx — doit retourner 0 (ou commentaires uniquement)
4. grep -rn "GrowthEntry.create" src/ — verifier qu'aucune autre instance existe sans Dog.update apres
</verification>

<success_criteria>
- GrowthTracker photo IA + saisie manuelle : apres sauvegarde, Dog.weight est mis a jour en DB
- Quick check-in : le payload ne contient que { mood } — energy et appetite ne sont pas forges
- Aucun effet de bord sur le reste du flux de sauvegarde (toast, reset, onGrowthAdded)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-donnees-fausses/02-02-SUMMARY.md`
</output>
