---
phase: 02-donnees-fausses
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.jsx
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "Dashboard et Sante affichent le meme score wellness pour le meme chien au meme moment — la divergence due aux sources differentes (dailyLogs vs growthEntries) est eliminee"
  artifacts:
    - path: "src/pages/Dashboard.jsx"
      provides: "computeHealthScore appele avec growthEntries + dailyLogs comme extraWeightSources unifies"
      contains: "GrowthEntry"
    - path: "src/pages/Dashboard.jsx"
      provides: "GrowthEntry charge dans le useEffect initial"
      contains: "GrowthEntry.filter"
  key_links:
    - from: "Dashboard.jsx useEffect"
      to: "GrowthEntry.filter"
      via: "Promise.all([..., GrowthEntry.filter({ dog_id: d.id })])"
      pattern: "GrowthEntry\\.filter"
    - from: "Dashboard.jsx computeHealthScore"
      to: "extraWeightSources = [...growthEntries, ...dailyLogs]"
      via: "merge deduplique des deux sources"
      pattern: "growthEntries"
---

<objective>
Unifier la source du score wellness entre Dashboard et Sante. Dashboard utilise actuellement dailyLogs comme extraWeightSources, Sante utilise growthEntries. Resultat : les deux scores peuvent diverger. Fix : Dashboard charge aussi GrowthEntries et passe les deux sources mergees a computeHealthScore, exactement comme le fait NotebookContent.

Purpose: Un utilisateur qui voit son score dans Dashboard et dans Sante doit voir le meme nombre. La divergence cree de la confusion et un manque de confiance dans les donnees.
Output: Dashboard.jsx charge GrowthEntry et unifie les sources du score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/audit/DATA-COHERENCE.md

<interfaces>
<!-- Contrats extraits du code source -->

From src/pages/Dashboard.jsx (etat actuel) :

Imports ligne 4 :
```js
import { Dog, HealthRecord, DailyCheckin, Streak, UserProgress, DailyLog, FoodScan } from "@/api/entities";
```
GrowthEntry n'est PAS dans cet import. Il faut l'ajouter.

State ligne 57-58 :
```js
const [dailyLogs, setDailyLogs] = useState([]);
const [scans, setScans] = useState([]);
```
Ajouter : `const [growthEntries, setGrowthEntries] = useState([]);`

Promise.all dans useEffect (lignes 74-81) :
```js
const [recs, cks, stk, prog, logs, foodScans] = await Promise.all([
  HealthRecord.filter({ dog_id: d.id }, "-date", 100),
  DailyCheckin.filter({ dog_id: d.id }, "-date", 90),
  Streak.filter({ dog_id: d.id }),
  UserProgress.filter({ dog_id: d.id }),
  DailyLog.filter({ dog_id: d.id }, "-date", 90),
  FoodScan.filter({ dog_id: d.id }, "-timestamp", 20).catch(() => []),
]);
```
Ajouter GrowthEntry.filter dans le Promise.all.

Appel computeHealthScore (ligne 170) — etat actuel :
```js
const score = computeHealthScore(records, dog, dailyLogs);
```
NotebookContent passe growthEntries (via computeNotebookSummary). Pour unifier :
computeHealthScore accepte `extraWeightSources` — chaque element doit avoir { weight_kg, date }.
GrowthEntry a weight_kg et date. DailyLog a weight_kg et date.
Il faut passer les deux : `[...growthEntries, ...dailyLogs]`.
computeHealthScore deduplique par date (HealthRecord wins, puis extraWeightSources).

From src/utils/healthStatus.js (signature de computeHealthScore, ligne 293) :
```js
export function computeHealthScore(records, dog, extraWeightSources = []) {
  // extraWeightSources: Array of { weight_kg, date, id? }
  // Deduplication: si meme date dans HealthRecord, extraWeightSource ignoree
```

From NotebookContent.jsx (ligne 191) — reference : comment Sante calcule le score :
```js
const summary = useMemo(
  () => computeNotebookSummary(allRecords, dog, growthEntries),
  [allRecords, dog, growthEntries]
);
// computeNotebookSummary appelle computeHealthScore(recs, dog, growthEntries)
```
Sante passe uniquement growthEntries (pas dailyLogs). Pour avoir les deux alignes :
- soit Dashboard passe uniquement growthEntries (meme chose que Sante)
- soit on accepte que Dashboard soit plus precis (dailyLogs + growthEntries)

Decision : passer `[...growthEntries, ...dailyLogs]` dans Dashboard. C'est plus complet ET il faut aligner les deux ensembles. Pour Sante, growthEntries suffit car allRecords inclut deja les DailyLog weights via la merge dans NotebookContent. Le score sera proche, pas forcement identique au centime pres — l'objectif est de supprimer la divergence majeure (GrowthEntry absentes du Dashboard).

IMPORTANT : verifier que l'appel `computeHealthScore(records, dog, dailyLogs)` dans Dashboard.jsx est bien a la ligne 170 ou trouver la ligne exacte avant de modifier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Charger GrowthEntry dans Dashboard et unifier extraWeightSources (DATA-05)</name>
  <read_first>
    src/pages/Dashboard.jsx (lignes 1-10 pour imports, 57-60 pour state, 74-95 pour useEffect, 97-175 pour useMemo computeHealthScore)
  </read_first>
  <files>src/pages/Dashboard.jsx</files>
  <action>
Modifier Dashboard.jsx en 4 etapes atomiques :

**Etape 1 — Import GrowthEntry :**
Ligne 4, ajouter GrowthEntry dans l'import des entities :
```js
import { Dog, HealthRecord, DailyCheckin, Streak, UserProgress, DailyLog, FoodScan, GrowthEntry } from "@/api/entities";
```

**Etape 2 — Ajouter state growthEntries :**
Apres `const [dailyLogs, setDailyLogs] = useState([]);`, ajouter :
```js
const [growthEntries, setGrowthEntries] = useState([]);
```

**Etape 3 — Charger GrowthEntry dans Promise.all :**
Dans le Promise.all (useEffect), ajouter GrowthEntry.filter en derniere position :
```js
const [recs, cks, stk, prog, logs, foodScans, growthData] = await Promise.all([
  HealthRecord.filter({ dog_id: d.id }, "-date", 100),
  DailyCheckin.filter({ dog_id: d.id }, "-date", 90),
  Streak.filter({ dog_id: d.id }),
  UserProgress.filter({ dog_id: d.id }),
  DailyLog.filter({ dog_id: d.id }, "-date", 90),
  FoodScan.filter({ dog_id: d.id }, "-timestamp", 20).catch(() => []),
  GrowthEntry.filter({ dog_id: d.id }, "-date", 50).catch(() => []),
]);
```
Puis ajouter apres le bloc setRecords/setCheckins/etc. :
```js
setGrowthEntries(growthData || []);
```

**Etape 4 — Unifier extraWeightSources dans computeHealthScore :**
Trouver la ligne `const score = computeHealthScore(records, dog, dailyLogs);` dans le useMemo.
Remplacer par :
```js
const score = computeHealthScore(records, dog, [...growthEntries, ...dailyLogs]);
```
Attention : `growthEntries` doit etre dans les dependances du useMemo. Verifier la ligne `}, [records, dailyLogs, checkins, dog]);` et ajouter `growthEntries` si absent :
```js
}, [records, dailyLogs, checkins, dog, growthEntries]);
```

Pattern-proactif : verifier qu'il n'existe pas d'autre appel a computeHealthScore dans Dashboard.jsx qui n'utiliserait pas les growthEntries.
  </action>
  <verify>
    <automated>grep -n "GrowthEntry\|growthEntries\|computeHealthScore" "src/pages/Dashboard.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep retourne GrowthEntry dans la ligne d'import
    - grep retourne `const [growthEntries` dans le state
    - grep retourne `GrowthEntry.filter` dans le useEffect
    - grep retourne `computeHealthScore(records, dog, \[...growthEntries` (les deux sources mergees)
    - grep retourne `growthEntries` dans les dependances du useMemo
  </acceptance_criteria>
  <done>Dashboard charge les GrowthEntries et les inclut dans le calcul du score wellness — la divergence majeure avec le score de Sante est eliminee</done>
</task>

</tasks>

<verification>
Apres la tache :

1. grep -n "GrowthEntry" src/pages/Dashboard.jsx — doit retourner 3 lignes (import, filter, state setter)
2. grep -n "computeHealthScore" src/pages/Dashboard.jsx — doit montrer `[...growthEntries, ...dailyLogs]`
3. grep -n "growthEntries" src/pages/Dashboard.jsx — doit apparaitre dans state, useEffect, useMemo et dependances
4. Aucune erreur de syntaxe (fichier JSX valide)
</verification>

<success_criteria>
- Dashboard.jsx charge GrowthEntry au montage (filtre par dog_id, max 50 entrees)
- computeHealthScore dans Dashboard recoit les deux sources : growthEntries + dailyLogs
- Le score Dashboard et le score Sante (NotebookContent) sont calcules depuis les memes types de sources — la divergence visible est eliminee
- Le chargement supplementaire (GrowthEntry) est dans le meme Promise.all — pas de ralentissement additionnel
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-donnees-fausses/02-03-SUMMARY.md`
</output>
