---
phase: 04-ux-trompeuse
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.jsx
autonomous: true
requirements:
  - UX-01
  - UX-04

must_haves:
  truths:
    - "Quand l'utilisateur valide un check-in avec au moins un symptome coché, l'app réagit différemment de 'Check-in enregistré !' — elle propose un lien ou un bouton vers Sante"
    - "Depuis la Home, l'utilisateur peut accéder au Dashboard (SmartAlerts) sans passer par Profile — un bouton ou lien explicite est visible"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "handleCheckin avec réaction symptomes + section Dashboard accessible"
      contains: "symptoms.length"
    - path: "src/pages/Home.jsx"
      provides: "Lien Dashboard visible"
      contains: "createPageUrl(\"Dashboard\")"
  key_links:
    - from: "src/pages/Home.jsx handleCheckin"
      to: "symptomes détectés après succès"
      via: "symptoms.length > 0 check après toast.success"
      pattern: "symptoms.*length.*0"
    - from: "src/pages/Home.jsx rendu"
      to: "page Dashboard"
      via: "bouton ou lien explicite avec createPageUrl(Dashboard)"
      pattern: "createPageUrl.*Dashboard"
---

<objective>
Corriger deux patterns UX trompeuses dans Home.jsx : les symptomes cochés dans le check-in qui n'enclenchent aucune action visible, et le Dashboard (SmartAlerts vaccins/poids) qui est introuvable car absent de la navigation principale.

Purpose: Un propriétaire qui coche "Vomissements" et reçoit juste "Check-in enregistré !" pense que l'app n'a pas compris la gravité. Un propriétaire dont le chien a un vaccin en retard ne le verra jamais si le Dashboard est caché derrière Profile > Stats.

Output: Home.jsx modifié — réaction symptomes après check-in + entrée Dashboard visible sur la Home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contexte extrait du code pour l'exécuteur -->

Home.jsx — handleCheckin (ligne 271-315) — zone de modification UX-01 :
```jsx
const handleCheckin = async ({ mood, energy, appetite, notes, symptoms, behaviorNotes }) => {
  // ... optimistic update ...
  try {
    const response = await base44.functions.invoke("dailyCheckinProcess", {...});
    const result = response.data || {};
    const newCheckin = result.checkin || {...};
    setDogData(prev => ({...}));
    // streak milestone logic ...
    toast.success("Check-in enregistré !");           // ← AJOUTER la réaction symptoms ICI
    checkStreakBadges(dog.id, user.email).catch(() => {});
  } catch (err) { ... }
};
```

Home.jsx — imports actuels (ligne 24) :
```jsx
import { Flame, ScanLine, Footprints, Stethoscope, BookOpen, Lock } from "lucide-react";
```
Ajouter `AlertTriangle` ou `Heart` depuis lucide-react pour l'alerte symptomes.

Home.jsx — rendu après DailyProgress (ligne 524+) :
```jsx
{/* Calendar Strip */}
<CalendarStrip dailyLogs={dailyLogs} />

{/* Daily Progress — 3 mini cards */}
<DailyProgress
  dailyLogs={dailyLogs}
  todayCheckin={todayCheckin}
  dog={dog}
/>
```

DogRadarHero.jsx — a déjà un lien Dashboard (petit cercle score, ligne 147) :
```jsx
<Link to={createPageUrl("Dashboard")} className="flex-shrink-0 w-11 h-11 relative" aria-label="Voir le tableau de bord">
```
Ce lien existe mais est discret (cercle 44px dans le header). UX-04 demande un accès visible, pas discret.

Home.jsx — position dans le rendu pour la carte Dashboard :
Ajouter après CalendarStrip et DailyProgress, avant le bloc "Nutrition tip card" (ligne 550). La carte doit être conditionnelle : s'affiche seulement quand il y a des alertes critiques/warning, ou toujours avec un message d'invitation.

SmartAlerts — computeAlerts est dans src/components/dashboard/SmartAlerts.jsx.
Pour ne pas dupliquer la logique, la carte Dashboard sur Home doit être un simple lien vers Dashboard, pas recalculer les alertes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Réaction symptomes après check-in (UX-01)</name>
  <files>src/pages/Home.jsx</files>
  <read_first>
    - src/pages/Home.jsx (lignes 271-315 — handleCheckin)
    - src/pages/Home.jsx (lignes 1-35 — imports)
  </read_first>
  <action>
    Dans `handleCheckin` (ligne 271), après `toast.success("Check-in enregistré !")`, ajouter une réaction conditionnelle si des symptomes ont été cochés :

    ```jsx
    toast.success("Check-in enregistré !");
    // UX-01: Réagir si symptomes détectés
    if (symptoms && symptoms.length > 0) {
      setTimeout(() => {
        toast("Des symptomes signalés — consulte l'onglet Santé.", {
          duration: 6000,
          action: {
            label: "Voir Santé",
            onClick: () => navigate(createPageUrl("Sante")),
          },
        });
      }, 800);
    }
    checkStreakBadges(dog.id, user.email).catch(() => {});
    ```

    Le délai de 800ms permet au premier toast "Check-in enregistré !" d'apparaître et disparaître partiellement avant que le deuxième arrive — les deux ne se chevauchent pas visuellement.

    Note : `navigate` est déjà disponible dans le scope de `handleCheckin` (importé ligne 65 via `useNavigate()`). `createPageUrl` est importé ligne 3. Aucun nouvel import nécessaire pour cette task.

    Note sur le toast Sonner : la syntaxe `toast("message", { action: { label, onClick } })` est la syntaxe standard de sonner. Ne pas utiliser `toast.message()` — utiliser `toast()` directement.
  </action>
  <verify>
    grep -n "symptoms.*length\|symptoms\.length" src/pages/Home.jsx
    → Doit trouver la condition après toast.success.

    grep -n "Voir Santé\|Voir Sant" src/pages/Home.jsx
    → Doit trouver le label du toast action.
  </verify>
  <done>Dans handleCheckin, après succès API, si symptoms.length > 0 un deuxième toast apparaît 800ms après avec un bouton "Voir Santé" qui navigue vers la page Sante. Les symptomes ne sont plus ignorés silencieusement.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Carte Dashboard accessible depuis Home (UX-04)</name>
  <files>src/pages/Home.jsx</files>
  <read_first>
    - src/pages/Home.jsx (lignes 520-565 — CalendarStrip, DailyProgress, Nutrition tip card)
    - src/pages/Home.jsx (lignes 1-35 — imports)
    - src/pages/Home.jsx (lignes 586-600 — Health card existante)
  </read_first>
  <action>
    Ajouter une carte "Tableau de bord" visible sur la Home, après le bloc DailyProgress (ligne ~524) et avant le bloc "Nutrition tip card" (ligne ~550).

    La carte doit :
    1. Etre visuellement cohérente avec les autres cartes (bg-gradient-to-r, rounded-3xl, p-4, border, shadow-sm, flex items-center gap-4)
    2. Cliquer vers Dashboard via `navigate(createPageUrl("Dashboard"))`
    3. Afficher une icône et un message d'invitation — utiliser `BarChart3` de lucide-react ou `Activity`
    4. Mentionner les SmartAlerts (vaccins, poids) pour que l'utilisateur comprenne ce qu'il y trouve

    Code à insérer après `<DailyProgress ... />` et avant le commentaire `{/* Nutrition tip card */}` :

    ```jsx
    {/* Dashboard access — SmartAlerts */}
    <button
      onClick={() => navigate(createPageUrl("Dashboard"))}
      className="w-full bg-gradient-to-r from-blue-50 to-indigo-50/50 rounded-3xl p-4 border border-blue-100/50 shadow-sm flex items-center gap-4 text-left active:scale-[0.98] transition-transform"
      aria-label="Voir le tableau de bord et les alertes santé"
    >
      <StorysetIllustration name="vet-checkup" className="w-20 h-20 flex-shrink-0" />
      <div className="flex-1 min-w-0">
        <p className="text-[14px] font-bold text-foreground">Tableau de bord</p>
        <p className="text-[11px] text-muted-foreground mt-1 leading-relaxed">Vaccins, alertes poids, tendances humeur — tout en un coup d'oeil.</p>
        <span className="inline-block mt-2 text-[12px] font-bold text-blue-700 bg-blue-100 px-3 py-1.5 rounded-full">
          Voir les alertes
        </span>
      </div>
    </button>
    ```

    Note : `StorysetIllustration` est déjà importé (ligne 34). `navigate` et `createPageUrl` sont déjà disponibles. Aucun nouvel import nécessaire.

    Ne pas supprimer ni modifier le lien Dashboard existant dans DogRadarHero (mini cercle score) — les deux coexistent sans problème.
  </action>
  <verify>
    grep -n "Tableau de bord\|Voir les alertes\|Dashboard.*button\|button.*Dashboard" src/pages/Home.jsx
    → Doit trouver la nouvelle carte Dashboard.

    grep -c "createPageUrl(\"Dashboard\")" src/pages/Home.jsx
    → Doit retourner au moins 1 (la nouvelle carte ; DogRadarHero est un composant séparé).
  </verify>
  <done>Une carte "Tableau de bord" est visible sur la Home scroll sans avoir à deviner l'existence du Dashboard. Elle mentionne vaccins et alertes poids. Elle navigue vers Dashboard au clic.</done>
</task>

</tasks>

<verification>
Vérification globale Plan 04-02 :

```bash
# UX-01 : réaction symptomes présente dans handleCheckin
grep -n "symptoms.*length\|Voir Sant\|Voir Santé" src/pages/Home.jsx

# UX-04 : carte Dashboard présente dans le rendu
grep -n "Tableau de bord\|Voir les alertes" src/pages/Home.jsx

# Aucune régression : imports toujours valides
grep -n "^import" src/pages/Home.jsx | head -20
```

Résultats attendus :
- Commande 1 → au moins 2 lignes (condition + label toast)
- Commande 2 → au moins 2 lignes (titre + CTA)
- Commande 3 → imports inchangés ou étendus, pas de suppression accidentelle
</verification>

<success_criteria>
- UX-01 : Après un check-in avec symptome coché, un second toast apparaît avec bouton "Voir Santé". L'utilisateur comprend que l'app a noté la préoccupation et lui propose une action.
- UX-04 : La Home affiche une carte "Tableau de bord" visible au scroll normal, sans avoir besoin de passer par Profile. Les SmartAlerts vaccins et poids sont donc découvrables par l'utilisateur moyen.
</success_criteria>

<output>
Après completion, créer `.planning/phases/04-ux-trompeuse/04-02-SUMMARY.md`
</output>
