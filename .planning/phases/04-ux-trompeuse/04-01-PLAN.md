---
phase: 04-ux-trompeuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/premium/PostTrialSheet.jsx
  - src/components/profile/SubscriptionSection.jsx
  - src/components/training/ExerciseDetail.jsx
  - src/pages/Scan.jsx
  - src/components/onboarding/WelcomeScreen.jsx
autonomous: true
requirements:
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Aucun CTA dans l'app n'affiche '5 €/mois' ou 'dès 5 €/mois' sans mention que c'est le tarif annuel"
    - "Le vrai prix mensuel (7,99 €/mois) est visible ou le prix annuel est clairement labellisé '(abonnement annuel)'"
    - "Le champ code parrain n'est plus visible dans WelcomeScreen — l'utilisateur ne peut pas saisir un code qui sera ignoré"
  artifacts:
    - path: "src/components/premium/PostTrialSheet.jsx"
      provides: "CTA S'abonner avec prix honnête"
      contains: "7,99"
    - path: "src/components/profile/SubscriptionSection.jsx"
      provides: "Boutons abonnement dans Profile"
      contains: "7,99"
    - path: "src/components/training/ExerciseDetail.jsx"
      provides: "CTA Premium dans fiche exercice"
      contains: "7,99"
    - path: "src/pages/Scan.jsx"
      provides: "CTA Premium dans scan limite"
      contains: "7,99"
    - path: "src/components/onboarding/WelcomeScreen.jsx"
      provides: "Ecran post-onboarding sans champ parrain"
  key_links:
    - from: "src/components/premium/PostTrialSheet.jsx"
      to: "prix affiché à l'utilisateur"
      via: "texte du bouton CTA"
      pattern: "S'abonner.*7,99"
    - from: "src/components/onboarding/WelcomeScreen.jsx"
      to: "champ code parrain"
      via: "absence de showReferral state et Gift button"
      pattern: "parrain"
---

<objective>
Corriger deux patterns UX trompeurs dans l'app : le prix "dès 5 €/mois" qui induit en erreur (le vrai mensuel est 7,99 €), et le champ code parrain dans WelcomeScreen qui n'a aucun backend de validation.

Purpose: Un utilisateur qui voit "5 €/mois" et se retrouve facturé 7,99 € perd confiance immédiatement. Un utilisateur qui saisit un code parrain et ne reçoit rien perd confiance aussi. Les deux sont des dark patterns involontaires à corriger avant tout utilisateur réel.

Output: 5 fichiers modifiés — CTAs premium avec prix honnête, WelcomeScreen sans champ parrain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contexte extrait du code pour l'exécuteur — pas besoin d'explorer le codebase -->

PostTrialSheet.jsx:93 — CTA actuel :
```jsx
S'abonner — à partir de 5 EUR/mois
```

SubscriptionSection.jsx:71 — bouton trial expiré :
```jsx
S'abonner · dès 5 €/mois
```

SubscriptionSection.jsx:91 — bouton plan gratuit :
```jsx
Passer Premium · dès 5 €/mois
```

ExerciseDetail.jsx:79 — CTA exercice premium :
```jsx
Passer Premium · dès 5 €/mois
```

Scan.jsx:399 — CTA scan quota atteint :
```jsx
Passer Premium · dès 5 €/mois
```

WelcomeScreen.jsx:66-102 — bloc complet à supprimer :
```jsx
{!showReferral ? (
  <button onClick={() => setShowReferral(true)} ...>
    <Gift className="w-3.5 h-3.5" />
    Tu as un code parrain ?
  </button>
) : (
  <div className="w-full max-w-xs mb-8">
    {referralSaved ? (...) : (
      <div className="flex gap-2">
        <input type="text" value={referralCode} ... placeholder="CODE PARRAIN" ... />
        <button onClick={handleReferralSubmit} ...>OK</button>
      </div>
    )}
  </div>
)}
```

WelcomeScreen.jsx states à supprimer (lignes 11-13) :
```jsx
const [referralCode, setReferralCode] = useState("");
const [referralSaved, setReferralSaved] = useState(false);
const [showReferral, setShowReferral] = useState(false);
```

WelcomeScreen.jsx fonction à supprimer (lignes 15-25) :
```jsx
const handleReferralSubmit = async () => { ... };
```

WelcomeScreen.jsx imports à nettoyer : retirer `Gift` et `Check` de lucide-react si plus utilisés après suppression.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Corriger les CTAs prix trompeurs dans les 4 fichiers premium</name>
  <files>
    src/components/premium/PostTrialSheet.jsx,
    src/components/profile/SubscriptionSection.jsx,
    src/components/training/ExerciseDetail.jsx,
    src/pages/Scan.jsx
  </files>
  <read_first>
    - src/components/premium/PostTrialSheet.jsx (ligne 93)
    - src/components/profile/SubscriptionSection.jsx (lignes 66-97)
    - src/components/training/ExerciseDetail.jsx (lignes 73-80)
    - src/pages/Scan.jsx (lignes 392-404)
  </read_first>
  <action>
    Remplacer les textes trompeurs dans les 4 fichiers :

    **PostTrialSheet.jsx ligne 93 :**
    Remplacer : `S'abonner — à partir de 5 EUR/mois`
    Par : `S'abonner — 7,99 €/mois ou 59,99 €/an`

    **SubscriptionSection.jsx ligne 71 :**
    Remplacer : `S'abonner · dès 5 €/mois`
    Par : `S'abonner · 7,99 €/mois`

    **SubscriptionSection.jsx ligne 91 :**
    Remplacer : `Passer Premium · dès 5 €/mois`
    Par : `Passer Premium · à partir de 7,99 €/mois`

    **ExerciseDetail.jsx ligne 79 :**
    Remplacer : `Passer Premium · dès 5 €/mois`
    Par : `Passer Premium · à partir de 7,99 €/mois`

    **Scan.jsx ligne 399 :**
    Remplacer : `Passer Premium · dès 5 €/mois`
    Par : `Passer Premium · à partir de 7,99 €/mois`

    Note : La page Premium.jsx affiche déjà les deux prix correctement (mensuel 7,99 €/mois + annuel 59,99 €/an · 5 €/mois). Les CTAs dans les autres pages doivent juste être honnêtes sur le prix minimum réel = 7,99 €/mois, sans cacher le fait qu'il y a un plan annuel. Ne pas modifier Premium.jsx (ligne 281, 427, 504) car le prix 5 €/mois y est correctement contextualisé avec le plan annuel sélectionné.
  </action>
  <verify>
    grep -n "dès 5\|à partir de 5\|5 EUR/mois\|5 €/mois" src/components/premium/PostTrialSheet.jsx src/components/profile/SubscriptionSection.jsx src/components/training/ExerciseDetail.jsx src/pages/Scan.jsx
    → Doit retourner 0 résultat (toutes les occurrences trompeuses supprimées).

    grep -n "7,99" src/components/premium/PostTrialSheet.jsx src/components/profile/SubscriptionSection.jsx src/components/training/ExerciseDetail.jsx src/pages/Scan.jsx
    → Doit retourner 4 occurrences (une par fichier).
  </verify>
  <done>Aucune occurrence de "dès 5 €/mois" ou "5 EUR/mois" dans les 4 fichiers CTA. Chaque fichier affiche 7,99 €/mois comme prix de référence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Supprimer le champ code parrain de WelcomeScreen</name>
  <files>src/components/onboarding/WelcomeScreen.jsx</files>
  <read_first>
    - src/components/onboarding/WelcomeScreen.jsx (fichier entier — 131 lignes)
  </read_first>
  <action>
    Supprimer le code parrain de WelcomeScreen.jsx sans casser le reste :

    1. Retirer les 3 states (lignes 11-13) :
       ```jsx
       const [referralCode, setReferralCode] = useState("");
       const [referralSaved, setReferralSaved] = useState(false);
       const [showReferral, setShowReferral] = useState(false);
       ```

    2. Retirer la fonction `handleReferralSubmit` (lignes 15-25 entières).

    3. Retirer le bloc JSX du code parrain (lignes 66-102) — le bloc conditionnel `{!showReferral ? ... : ...}` avec l'input et le bouton OK. Ce bloc est positionné entre le trial badge et le bouton CTA "Découvrir PawCoach".

    4. Nettoyer les imports en ligne 1 : retirer `Gift` et `Check` de l'import lucide-react si ces icônes ne sont plus utilisées ailleurs dans le fichier. Vérifier d'abord que `Gift` et `Check` ne sont utilisées qu'aux lignes 71 et 79.

    5. Le résultat final : WelcomeScreen affiche photo du chien, titre, description, badge trial 7j, puis directement le bouton CTA "Découvrir PawCoach". Aucune mention de code parrain.

    Note : ReferralSection.jsx (`src/components/profile/ReferralSection.jsx`) retourne déjà `null` — ce fichier n'a pas besoin d'être modifié.
  </action>
  <verify>
    grep -n "parrain\|referralCode\|referralSaved\|showReferral\|handleReferralSubmit\|Gift\|Check" src/components/onboarding/WelcomeScreen.jsx
    → Doit retourner 0 résultat (tout le code parrain supprimé, imports nettoyés).
  </verify>
  <done>WelcomeScreen.jsx sans aucune trace du code parrain. Le composant compile sans erreur (aucun import manquant). L'utilisateur post-onboarding voit directement le CTA "Découvrir PawCoach" après le badge trial.</done>
</task>

</tasks>

<verification>
Vérification globale Plan 04-01 :

```bash
# UX-02 : aucun CTA trompeur restant dans les 4 fichiers
grep -rn "dès 5 €\|5 EUR/mois\|à partir de 5" src/components/premium/PostTrialSheet.jsx src/components/profile/SubscriptionSection.jsx src/components/training/ExerciseDetail.jsx src/pages/Scan.jsx

# UX-02 : 7,99 présent dans les 4 fichiers modifiés
grep -c "7,99" src/components/premium/PostTrialSheet.jsx src/components/profile/SubscriptionSection.jsx src/components/training/ExerciseDetail.jsx src/pages/Scan.jsx

# UX-03 : plus aucune trace de parrain dans WelcomeScreen
grep -n "parrain\|referral\|Gift\|handleReferralSubmit" src/components/onboarding/WelcomeScreen.jsx
```

Résultats attendus :
- Commande 1 → 0 ligne
- Commande 2 → chaque fichier : 1
- Commande 3 → 0 ligne
</verification>

<success_criteria>
- UX-02 : Les 4 CTAs premium hors de la page Premium affichent "7,99 €/mois" comme prix de référence minimum. L'utilisateur ne découvre plus un prix 60% plus élevé qu'annoncé au checkout.
- UX-03 : WelcomeScreen ne propose plus de champ code parrain. Un utilisateur qui a reçu un code d'ami ne peut plus le saisir pour ne rien recevoir en retour.
</success_criteria>

<output>
Après completion, créer `.planning/phases/04-ux-trompeuse/04-01-SUMMARY.md`
</output>
